---
title: "Don't Let AI Agents YOLO Your Files: Shifting Information and Control to Filesystems for Agent Safety and Autonomy"
title_zh: 别让 AI 智能体随意操作你的文件：将信息与控制权移交给文件系统以保障智能体的安全性与自主性
authors: "Shawn, Zhong, Junxuan Liao, Jing Liu, Mai Zheng, Andrea C. Arpaci-Dusseau, Remzi H. Arpaci-Dusseau"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13536v1"
tags: ["query:agsec"]
score: 8.0
evidence: 将控制权转移到文件系统以实现 Agent 安全和动作约束
tldr: 本研究针对AI编程智能体在操作文件系统时易导致数据损坏和隐私泄露的问题，提出了YoloFS。通过分析290份误操作报告，研究者发现现有系统缺乏有效的信息反馈与控制。YoloFS引入了暂存区隔离、快照自纠错及渐进式权限管理三大技术，在保障安全的同时提升了智能体的自主性。实验表明，YoloFS能显著减少用户干预并支持智能体自我修复错误。
selection_source: fresh_fetch
motivation: AI智能体在直接操作文件系统时常发生误删或泄密，且现有方案在安全性和自主性之间存在难以调和的矛盾。
method: 设计了智能体原生文件系统YoloFS，利用暂存区隔离修改、快照机制支持自纠错以及渐进式权限控制减少交互负担。
result: "在带有副作用的任务中，YoloFS助力智能体在72%的场景下实现自纠错，并在常规任务中以更低的用户交互频率保持了同等成功率。"
conclusion: 通过将信息反馈和控制权下沉至文件系统层，可以有效解决AI智能体文件操作的安全隐患并增强其独立工作能力。
---

## 摘要
AI 编程智能体直接在用户的文件系统上运行，经常会导致数据损坏、文件删除以及密钥泄露。目前的方法被迫在安全性与自主性之间进行权衡：不受限制的访问存在风险，而频繁的权限提示则会增加用户负担并阻碍智能体运行。为了深入理解这一问题，我们对智能体滥用文件系统的情况进行了首次系统性研究，分析了涵盖 13 个框架的 290 份公开报告。我们的分析表明，目前的智能体对其在文件系统上产生的影响了解有限，且缺乏足够的控制能力。因此，我们主张将这些信息和控制权移交给文件系统本身。基于这一原则，我们设计了 YoloFS，这是一种采用三种技术的智能体原生文件系统。暂存（Staging）在提交前隔离所有变更，为用户提供纠正性控制；快照（Snapshots）将这种控制权延伸至智能体，使其能够检测并纠正自身的错误；渐进式权限（Progressive permission）通过以最少的交互进行访问门控，为用户提供预防性控制。为了评估 YoloFS，我们引入了一种捕获用户-智能体-文件系统交互的新方法。在 11 个具有隐藏副作用的任务中，YoloFS 使智能体在其中 8 个任务中实现了自我纠正，同时保持所有影响处于暂存状态且可供审查。在 112 个常规任务中，YoloFS 在保持与基准相当的成功率的同时，减少了用户交互。

## Abstract
AI coding agents operate directly on users' filesystems, where they regularly corrupt data, delete files, and leak secrets. Current approaches force a tradeoff between safety and autonomy: unrestricted access risks harm, while frequent permission prompts burden users and block agents. To understand this problem, we conduct the first systematic study of agent filesystem misuse, analyzing 290 public reports across 13 frameworks. Our analysis reveals that today's agents have limited information about their filesystem effects and insufficient control over them. We therefore argue for shifting this information and control to the filesystem itself.   Based on this principle, we design YoloFS, an agent-native filesystem with three techniques. Staging isolates all mutations before commit, giving users corrective control. Snapshots extend this control to agents, letting them detect and correct their own mistakes. Progressive permission provides users with preventive control by gating access with minimal interaction. To evaluate YoloFS, we introduce a new methodology that captures user-agent-filesystem interactions. On 11 tasks with hidden side effects, YoloFS enables agent self-correction in 8 while keeping all effects staged and reviewable. On 112 routine tasks, YoloFS requires fewer user interactions while matching the baseline success rate.

---

## 论文详细总结（自动生成）

这是一份关于论文《Don't Let AI Agents YOLO Your Files: Shifting Information and Control to Filesystems for Agent Safety and Autonomy》的结构化深入总结：

### 1. 核心问题与整体含义
*   **背景：** 随着 Claude Code、Cursor 等 AI 编程智能体（Agents）的普及，它们获得了直接操作用户本地文件系统的权限。
*   **核心问题：** 智能体在执行任务时经常发生“误操作”，如误删整个硬盘、泄露 `.env` 密钥或损坏源码。现有的防御机制（如频繁的权限弹窗）会导致用户产生“审批疲劳”，迫使用户开启危险的“YOLO 模式”（全自动运行，不加审核），从而引发严重的安全风险。
*   **研究动机：** 现有的安全方案在“安全性”与“自主性”之间难以平衡。作者认为根源在于智能体与文件系统之间存在**信息鸿沟**（智能体不知道操作的具体后果）和**控制鸿沟**（缺乏有效的预防和撤销手段）。

### 2. 方法论：YoloFS 智能体原生文件系统
论文提出将信息和控制权从智能体层下沉到文件系统层，设计并实现了 **YoloFS**。其核心技术包括：
*   **暂存（Staging）：** 采用 Linux 内核堆栈式文件系统技术。所有文件修改（写、删、改名）都会被重定向到一个暂存层，而原始文件保持不变。只有在用户审查并确认后，修改才会批量提交（Commit）到基准文件系统。
*   **快照与非破坏性回溯（Snapshots & Travel）：** 允许智能体在操作过程中随时创建快照。如果智能体发现操作结果不符合预期（如测试失败或误删文件），可以执行“Travel”回溯到之前的状态。与传统回滚不同，YoloFS 采用追加式日志记录，保留所有分支历史，确保审计的完整性。
*   **渐进式权限（Progressive Permission）：** 引入基于路径的层级规则树。系统默认处于“询问（Ask）”模式，当智能体访问新路径时动态请求用户许可。用户可以设置“允许该目录及其子目录”，从而在保障安全的前提下，随着会话进行逐渐减少弹窗频率。

### 3. 实验设计
*   **数据集/场景：**
    1.  **误操作报告研究：** 收集并分析了 2024-2026 年间 13 个框架的 **290 份** 公开误操作报告，建立了诱因分类学。
    2.  **隐蔽副作用任务（11个）：** 设计了看似正常但带有破坏性副作用的命令（如一个会删除源码的 Linter）。
    3.  **常规文件任务（112个）：** 涵盖读、写、移动、软链接等基础操作，测试成功率与交互成本。
*   **Benchmark：** 开发了一套新型交互式基准测试框架，能够模拟并记录“用户-智能体-文件系统”的三方交互过程。
*   **对比方法：** Claude Code (基准)、Codex、GitHub Copilot CLI、Gemini CLI。

### 4. 资源与算力
*   **硬件环境：** 实验运行在配备 AMD EPYC 7302P (16核) 处理器、125GB DDR4 内存和 SATA SSD 的服务器上。
*   **模型使用：** 智能体端使用了 `claude-3.5-sonnet`、`gpt-4o` 等主流大模型。
*   **算力说明：** 论文未涉及模型训练，主要算力消耗在于运行智能体推理和文件系统性能测试。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 11 组深度副作用实验、112 组功能性实验，以及针对 I/O 吞吐量、元数据延迟、快照扩展性的多项性能实验。
*   **充分性：** 实验设计非常全面。不仅验证了安全功能的有效性（自纠错能力），还通过对比 OverlayFS 和 BranchFS 证明了 YoloFS 在高频快照场景下的性能优势。对比实验涵盖了当前市面上最主流的 AI 编程工具，具有很强的说服力和客观性。

### 6. 主要结论与发现
*   **自纠错能力：** 在 YoloFS 的支持下，智能体能自主检测并撤销 11 个破坏性任务中的 8 个；其余 3 个虽未被智能体撤销，但因处于暂存状态，用户可在提交前轻松拦截。
*   **交互优化：** 在常规任务中，YoloFS 显著减少了用户交互次数（平均每任务 0.4 次），同时保持了 99% 的任务成功率。
*   **性能表现：** YoloFS 的 I/O 开销极低（接近原生 Ext4），且在创建数百个快照后性能依然稳定，远优于传统的 OverlayFS。

### 7. 优点
*   **架构创新：** 首次系统性地提出“智能体原生文件系统”概念，将安全逻辑从不可靠的“提示词工程”转向可靠的“系统内核”。
*   **非破坏性设计：** “Travel”机制允许智能体探索不同路径而不丢失历史记录，非常契合 AI 的试错特性。
*   **实证驱动：** 基于大量真实世界案例分析，精准打击了当前 AI 智能体最痛点的安全问题。

### 8. 不足与局限
*   **平台依赖：** 目前仅实现了 Linux 内核模块，对于 Windows (原生) 和 macOS 的支持尚不完善。
*   **语义盲区：** 虽然能监控文件变化，但如果智能体引入了逻辑漏洞（代码能跑通但有 Bug），文件系统层面的监控可能无法完全替代代码审计。
*   **应用限制：** 主要针对本地运行的智能体，对于完全在云端沙盒运行的智能体，其部署模式需要进一步调整。

（完）
