---
title: "ClawLess: A Security Model of AI Agents"
title_zh: ClawLess：一种 AI 智能体的安全模型
authors: "Hongyi Lu, Nian Liu, Shuai Wang, Fengwei Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06284v1"
tags: ["query:agsec"]
score: 9.0
evidence: 针对AI智能体及其运行时行为实施验证策略的安全框架
tldr: 针对大语言模型驱动的自主智能体在信息检索和代码执行中的安全风险，本文提出 ClawLess 框架。该框架在假设智能体可能具有对抗性的最坏情况下，通过形式化验证的策略实施安全管控。它定义了细粒度的实体、信任范围和权限模型，并利用基于 BPF 的系统调用拦截技术在用户态内核中强制执行动态策略，为智能体提供了底层安全保障。
selection_source: fresh_fetch
motivation: 现有的智能体安全方法主要依赖训练或提示词工程，无法在智能体本身具有对抗性时提供根本性的安全保证。
method: 提出 ClawLess 框架，通过形式化安全模型定义动态策略，并结合 BPF 技术在系统调用层面进行强制拦截。
result: 成功将形式化安全模型与实际系统执行相结合，实现了对智能体运行时行为的细粒度、动态安全管控。
conclusion: ClawLess 为自主智能体提供了一种不依赖其内部设计的、具有形式化保证的底层安全架构。
---

## 摘要
由大语言模型驱动的自主 AI 智能体能够进行推理、规划并执行复杂任务，但其自主检索信息和运行代码的能力引入了显著的安全风险。现有方法试图通过训练或提示词来规范智能体行为，但这无法提供根本性的安全保障。我们提出了 ClawLess，这是一个在最坏情况威胁模型下对 AI 智能体强制执行形式化验证策略的安全框架，在该模型中，智能体本身可能是对抗性的。ClawLess 针对系统实体、信任范围和权限形式化了一个细粒度的安全模型，以表达能够适应智能体运行时行为的动态策略。这些策略被转化为具体的安全规则，并通过一个增强了基于 BPF 的系统调用拦截技术的用户空间内核来强制执行。这种方法将形式化安全模型与实际执行相结合，确保了无论智能体的内部设计如何，其安全性都能得到保障。

## Abstract
Autonomous AI agents powered by Large Language Models can reason, plan, and execute complex tasks, but their ability to autonomously retrieve information and run code introduces significant security risks. Existing approaches attempt to regulate agent behavior through training or prompting, which does not offer fundamental security guarantees. We present ClawLess, a security framework that enforces formally verified policies on AI agents under a worst-case threat model where the agent itself may be adversarial. ClawLess formalizes a fine-grained security model over system entities, trust scopes, and permissions to express dynamic policies that adapt to agents' runtime behavior. These policies are translated into concrete security rules and enforced through a user-space kernel augmented with BPF-based syscall interception. This approach bridges the formal security model with practical enforcement, ensuring security regardless of the agent's internal design.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ClawLess** 的安全框架，旨在为大语言模型（LLM）驱动的自主 AI 智能体提供底层的安全保障。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：自主 AI 智能体（如 OpenClaw, Claude Code）具备推理、规划和执行代码的能力，但这种高度自治也带来了巨大的安全风险。
*   **研究动机**：
    *   **信任边界模糊**：智能体从互联网检索海量非结构化数据，容易受到间接提示词注入（Indirect Prompt Injection）攻击。
    *   **权限与可用性的权衡**：智能体需要文件访问、网络连接等高权限才能工作，但传统“最小权限原则”难以应用于非确定性的 AI 行为。
    *   **现有防御失效**：传统的 Docker 隔离依赖内核，容易被逃逸；而通过训练或提示词（Prompting）约束智能体行为缺乏根本性的安全保证。
*   **整体含义**：论文提出在“最坏情况”下（即假设智能体本身可能已被恶意控制），通过形式化验证的策略和底层系统拦截，构建一个不依赖智能体内部设计的安全防线。

### 2. 论文提出的方法论
ClawLess 的核心思想是将高层形式化安全策略转化为底层的系统调用（Syscall）拦截规则。
*   **形式化安全模型**：
    *   **实体（Entities）**：定义系统中的文件、进程、套接字、设备等。
    *   **范围（Scopes）**：定义三个隔离层级：沙箱（Sandbox）、智能体（Agent）、监控器（Monitor）。
    *   **权限（Permissions）**：包括 Read, Write, NoExecute, Append, 以及创新的 **Visible**（允许引用凭据但不允许读取内容）。
*   **线性时序逻辑（LTL）**：引入时间维度来表达动态策略。例如：*“如果智能体读取了敏感文件，则在未来禁止其建立对外网络连接”*。
*   **关键技术细节**：
    *   **策略编译器**：将抽象的形式化模型映射到具体的 Linux 系统调用（如 `read`, `execve`, `clone` 等）。
    *   **BPF 系统调用拦截**：利用 Berkeley Packet Filter (BPF) 在内核钩子处拦截系统调用，根据策略实时决定放行或阻断。
    *   **用户态内核（User-space Kernel）**：基于 gVisor 等技术，在应用和宿主内核之间增加一个可信层，减少攻击面。

### 3. 实验设计
*   **场景与基准**：论文主要通过架构对比和形式化验证来展示其有效性。
*   **对比方法**：
    *   对比了四种安全容器技术：标准 Docker、用户态内核（gVisor）、虚拟化（Kata Container）和机密容器（CoCo）。
    *   分析了它们在硬件独立性、内核兼容性、互操作性、部署难度和安全性方面的优劣。
*   **验证方式**：使用 **SMT 求解器（z3）** 来验证安全策略的一致性。例如，验证“沙箱权限是否严格小于智能体权限”等层级策略。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：ClawLess 是一个系统安全框架，侧重于运行时监控和策略实施，而非模型训练，因此其核心开销在于系统调用拦截的延迟，而非大规模算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：论文主要侧重于理论框架的构建、形式化模型的定义以及系统架构的设计。
*   **充分性评价**：
    *   **理论充分**：形式化定义非常细致，涵盖了实体、权限和动态时序逻辑。
    *   **实证不足**：论文缺乏大规模的性能基准测试（Benchmark），例如 BPF 拦截带来的延迟开销、在复杂任务下对智能体成功率的影响等。
    *   **客观性**：对比分析较为客观，准确指出了现有容器技术的局限性。

### 6. 论文的主要结论与发现
*   **结论**：AI 智能体的安全不能依赖于模型自身的“自觉”或提示词约束，必须在系统底层实施强制性的、形式化验证的隔离策略。
*   **发现**：
    *   通过 BPF 和用户态内核的结合，可以在不修改智能体代码的情况下实现细粒度的安全管控。
    *   动态策略（基于 LTL）对于处理智能体这种非确定性行为至关重要。

### 7. 优点
*   **防御深度**：采用了“零信任”假设，即使智能体完全被黑客控制，底层框架仍能限制其破坏范围。
*   **形式化严谨性**：使用 SMT 验证策略，避免了人为配置错误导致的安全漏洞。
*   **Visible 权限设计**：巧妙解决了“智能体需要使用 API Key 但不应看到 Key 内容”的矛盾。

### 8. 不足与局限
*   **性能开销**：频繁的系统调用拦截和 BPF 处理可能会显著降低智能体执行复杂任务的速度，文中缺乏相关量化数据。
*   **语义鸿沟**：将高层语义（如“泄露敏感信息”）准确映射到低层系统调用参数上具有极高难度，可能存在漏报或误报。
*   **应用限制**：目前主要针对 Linux 环境和特定的用户态内核架构，对于跨平台或高度集成的 AI 硬件环境适配性待考证。

（完）
