---
title: "Seeing No Evil: Blinding Large Vision-Language Models to Safety Instructions via Adversarial Attention Hijacking"
title_zh: 视而不见：通过对抗性注意力劫持使大规模视觉语言模型对安全指令致盲
authors: "Jingru Li, Wei Ren, Tianqing Zhu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10299v1"
tags: ["query:agsec"]
score: 8.0
evidence: 通过操纵注意力模式来规避视觉语言模型的安全对齐
tldr: 本研究针对大型视觉语言模型（LVLM）提出了一种名为“注意力引导视觉越狱”的新型攻击方法。该方法通过操纵注意力机制，使模型在生成过程中“无视”安全指令，从而绕过对齐机制。通过抑制对安全前缀的注意力和锚定对抗性图像特征，该方法显著减少了梯度冲突，在提高攻击成功率的同时加快了收敛速度，揭示了模型因无法检索安全规则而导致的“安全盲区”现象。
selection_source: fresh_fetch
motivation: 现有视觉越狱攻击因对抗目标与模型安全检索机制之间的梯度冲突，导致收敛速度慢且攻击效率低下。
method: 提出通过操纵注意力模式来绕过安全对齐，具体包括抑制对安全相关标记的注意力和将生成过程锚定在对抗性图像特征上。
result: "在Qwen-VL上达到94.4%的攻击成功率，迭代次数减少40%，并在低扰动预算下保持了显著的性能优势。"
conclusion: 研究揭示了“安全盲区”失效模式，即攻击通过劫持注意力使模型无法检索安全规则，从而导致有害内容的生成。
---

## 摘要
大规模视觉语言模型（LVLMs）依赖基于注意力的安全指令检索，以在生成过程中保持对齐。现有的攻击通常通过优化图像扰动来最大化有害输出的可能性，但由于对抗目标与模型的安全检索机制之间存在梯度冲突，导致收敛速度较慢。我们提出了注意力引导的视觉越狱（Attention-Guided Visual Jailbreaking），通过直接操纵注意力模式来规避而非压制安全对齐。我们的方法引入了两个简单的辅助目标：（1）抑制对对齐相关前缀标记（prefix tokens）的注意力；（2）将生成锚定在对抗性图像特征上。这种简单而有效的“推拉”公式将梯度冲突减少了 45%，在 Qwen-VL 上实现了 94.4% 的攻击成功率（相比基线的 68.8%），且迭代次数减少了 40%。在更严格的扰动预算（$ε=8/255$）下，我们仍能保持 59.0% 的攻击成功率（ASR），而标准方法仅为 45.7%。机理性分析揭示了一种我们称之为“安全致盲”（safety blindness）的失效模式：成功的攻击将对系统提示词（system-prompt）的注意力抑制了 80%，导致模型生成有害内容的原因并非由于推翻了安全规则，而是因为未能检索到这些规则。

## Abstract
Large Vision-Language Models (LVLMs) rely on attention-based retrieval of safety instructions to maintain alignment during generation. Existing attacks typically optimize image perturbations to maximize harmful output likelihood, but suffer from slow convergence due to gradient conflict between adversarial objectives and the model's safety-retrieval mechanism. We propose Attention-Guided Visual Jailbreaking, which circumvents rather than overpowers safety alignment by directly manipulating attention patterns. Our method introduces two simple auxiliary objectives: (1) suppressing attention to alignment-relevant prefix tokens and (2) anchoring generation on adversarial image features. This simple yet effective push-pull formulation reduces gradient conflict by 45% and achieves 94.4% attack success rate on Qwen-VL (vs. 68.8% baseline) with 40% fewer iterations. At tighter perturbation budgets ($ε=8/255$), we maintain 59.0% ASR compared to 45.7% for standard methods. Mechanistic analysis reveals a failure mode we term safety blindness: successful attacks suppress system-prompt attention by 80%, causing models to generate harmful content not by overriding safety rules, but by failing to retrieve them.

---

## 论文详细总结（自动生成）

这篇论文对大规模视觉语言模型（LVLM）的安全性进行了深入探讨，提出了一种通过操纵注意力机制来实现“越狱”攻击的新方法。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的针对 LVLM 的视觉越狱攻击（通过修改图像诱导模型输出有害信息）通常只关注优化输出结果（Logits），而忽略了模型内部的安全对齐机制。
*   **核心挑战**：作者发现，标准攻击在优化过程中存在**梯度冲突（Gradient Conflict）**。模型在生成每一步都会通过注意力机制检索安全指令（如系统提示词），这产生的“对齐梯度”与攻击者的“有害梯度”方向相反，导致攻击收敛慢、效率低且需要较大的扰动预算。
*   **整体含义**：论文旨在证明，与其强行对抗安全对齐，不如通过操纵注意力机制让模型“看不见”安全指令，从而实现更高效、更隐蔽的越狱。

### 2. 论文提出的方法论：注意力引导视觉越狱
核心思想是采用一种**“推拉（Push-Pull）”注意力引导机制**，直接干预模型在生成过程中的注意力分配。
*   **核心公式**：总损失函数定义为 $L_{total} = L_{target} + \alpha \cdot L_{suppress} + \beta \cdot L_{anchor}$。
    *   **$L_{target}$（目标损失）**：标准的对抗损失，最大化目标有害响应的似然概率。
    *   **$L_{suppress}$（抑制损失 - “推”）**：通过最小化生成标记（Generated Tokens）对安全前缀标记（Prefix Tokens，如系统指令）的注意力权重，切断模型对安全规则的检索。
    *   **$L_{anchor}$（锚定损失 - “拉”）**：将原本分配给安全指令的注意力质量重定向到对抗性图像特征上，确保生成过程受图像控制。
*   **技术细节**：
    *   **层级选择**：主要针对模型最后 $K$ 层（默认 $K=6$）的注意力图进行干预，因为研究表明拒绝行为主要在深层触发。
    *   **优化算法**：使用投影梯度下降（PGD）在 $\ell_\infty$ 约束下优化图像扰动 $\delta$。
    *   **提示词通用性**：生成的对抗图像具有通用性，可配合不同的有害文本查询使用。

### 3. 实验设计
*   **目标模型**：Qwen-VL, LLaVA-1.5-7B, InternVL2-8B, MiniGPT-4。
*   **数据集/基准测试**：
    *   使用了 5 个主流安全基准：AdvBench, StrongREJECT, HarmBench, JailbreakBench, MM-SafetyBench。
    *   涵盖了非法活动、仇恨言论、恶意软件生成、隐私侵犯等 13 个安全场景。
*   **对比方法**：VAE-JB, BAP, JB-Pcs 等主流视觉越狱基线。
*   **评估指标**：攻击成功率（ASR），并使用 Detoxify、Llama Guard 3 和 Beaver-dam-7B 作为自动化评判器。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100 GPU** 上进行。
*   **软件实现**：为了获取内部注意力图，禁用了 FlashAttention-2，改用标准注意力实现。
*   **开销**：作者指出，该方法引入的计算开销极小（单次迭代增加不到 5% 的成本），但由于收敛速度快，总优化时间减少了约 40%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个开源模型上进行了大规模测试。
    *   进行了跨架构迁移实验（Qwen-VL 优化的图像攻击 GPT-4o, Claude-3.5, Gemini-1.5）。
    *   **消融实验**：详细测试了不同层数选择（K=1 到 8）、不同损失权重（$\alpha, \beta$）、不同扰动预算（$\epsilon=4$ 到 32）的影响。
    *   **鲁棒性测试**：测试了图像缩放、噪声、JPEG 压缩等常见变换下的攻击稳定性。
*   **充分性评价**：实验设计非常全面，不仅有定量的数据对比，还包含了深入的机理性分析（如梯度冲突分析、注意力热力图可视化、因果干预实验），论证过程客观且逻辑严密。

### 6. 主要结论与发现
*   **性能卓越**：在 Qwen-VL 上达到 94.4% 的 ASR，远超基线（68.8%），且在极小扰动（$\epsilon=8/255$）下依然有效。
*   **解决梯度冲突**：该方法将严重梯度冲突的比例从 20% 降低到 11%，使优化过程更加平滑。
*   **安全致盲（Safety Blindness）**：成功的攻击使模型对系统提示词的注意力平均下降了 80%。模型生成有害内容不是因为“违反”了规则，而是因为在生成时根本没有“检索”到规则。
*   **因果性确认**：通过在推理时人为强行恢复对系统提示词的注意力，可以显著降低攻击成功率，证明了注意力抑制是越狱成功的因果因素。

### 7. 优点
*   **机制新颖**：从机械解释性（Mechanistic Interpretability）角度出发，将注意力视为可干预的变量，而非仅仅是分析工具。
*   **效率高**：通过解决梯度冲突，大幅提升了对抗样本的生成速度和质量。
*   **隐蔽性强**：在较低的扰动预算下仍能保持高成功率，图像在视觉上几乎无损（SSIM 高达 0.99）。

### 8. 不足与局限
*   **白盒依赖**：该方法需要访问模型的梯度和内部注意力图，属于白盒攻击。虽然展示了一定的黑盒迁移性，但在完全封闭的 API 模型上直接实施较难。
*   **前缀依赖性**：攻击效果依赖于模型将安全对齐信息编码在特定的前缀标记中。如果未来模型将安全对齐完全融入参数权重而非依赖上下文检索，该方法的效果可能会打折扣。
*   **超参数敏感性**：虽然论文给出了通用配置，但不同架构的模型可能需要微调 $\alpha$ 和 $\beta$ 以达到最佳效果。

（完）
