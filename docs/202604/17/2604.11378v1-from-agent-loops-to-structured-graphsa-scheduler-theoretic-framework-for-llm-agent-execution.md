---
title: "From Agent Loops to Structured Graphs:A Scheduler-Theoretic Framework for LLM Agent Execution"
title_zh: 从智能体循环到结构化图：一种大语言模型智能体执行的调度理论框架
authors: Hu Wei
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11378v1"
tags: ["query:agsec"]
score: 8.0
evidence: 用于可检查智能体策略的基于图的执行引擎
tldr: 针对LLM智能体循环（Agent Loop）存在的隐式依赖、恢复循环无界及调试难等缺陷，本文提出基于调度理论的SGH框架。该框架将控制流转化为显式静态DAG，通过不可变计划、三层架构分离和严格升级协议，在牺牲部分表达能力的同时，显著提升了智能体执行的可控性、可验证性与可实现性。
selection_source: fresh_fetch
motivation: 传统的Agent Loop范式依赖不透明的LLM推理进行决策，导致执行过程难以调试、依赖关系模糊且容易陷入无尽的错误恢复循环。
method: 提出SGH框架，将执行逻辑转化为显式的静态DAG，并采用不可变计划、三层分离架构以及严格的错误升级协议。
result: 建立了一个统一的调度理论框架，对70个现有系统进行了权衡分析，并提供了具备终止性和完备性保证的形式化规范及实验方案。
conclusion: 本文论证了从隐式循环转向结构化图执行是提升LLM智能体可控性的关键路径，为未来智能体系统的设计提供了理论基础。
---

## 摘要
构建基于大语言模型（LLM）智能体的主流范式是“智能体循环”（Agent Loop），这是一种迭代循环，其中单个语言模型通过读取不断增长的上下文窗口来决定下一步的操作。该范式存在三个结构性缺陷：步骤之间的隐式依赖、无界的恢复循环，以及使调试复杂化的可变执行历史。我们将智能体循环刻画为一种“单就绪单元调度器”：在任何时刻，最多只有一个可执行单元处于活动状态，且激活哪个单元的选择源于不透明的 LLM 推理，而非可检查的策略。这一视角将智能体循环与基于图的执行引擎置于同一个语义连续体中。我们提出了结构化图框架（Structured Graph Harness, SGH），它将控制流从隐式上下文提升为显式的静态有向无环图（DAG）。SGH 做出三项承诺：执行计划在计划版本内是不可变的；计划执行与恢复被分离为三个层级；恢复遵循严格的升级协议。这些选择以牺牲部分表达能力为代价，换取了可控性、可验证性和可实现性。我们的贡献有四个方面：一个统一的调度框架，将经典调度理论应用于 LLM 智能体执行，并识别了由非确定性 LLM 节点引入的挑战；对 70 个调研系统在可控性、表达能力和可实现性方面的权衡分析；包含具有终止性和完备性保证的节点状态机的形式化规范；以及一个具有七组设计的可归因实验框架，用于未来的验证。这是一篇观点论文和设计提案。我们提供的是理论框架、设计分析和实验协议，而非生产实现或实证结果。

## Abstract
The dominant paradigm for building LLM based agents is the Agent Loop, an iterative cycle where a single language model decides what to do next by reading an ever growing context window. This paradigm has three structural weaknesses: implicit dependencies between steps, unbounded recovery loops, and mutable execution history that complicates debugging. We characterize the Agent Loop as a single ready unit scheduler: at any moment, at most one executable unit is active, and the choice of which unit to activate comes from opaque LLM inference rather than an inspectable policy. This perspective places Agent Loops and graph based execution engines on a single semantic continuum. We propose SGH, Structured Graph Harness, which lifts control flow from implicit context into an explicit static DAG. SGH makes three commitments: execution plans are immutable within a plan version, planning execution and recovery are separated into three layers, and recovery follows a strict escalation protocol. These choices trade some expressiveness for controllability, verifiability, and implementability. Our contributions are fourfold: a scheduler unified framework that applies classical scheduling theory to LLM agent execution and identifies challenges introduced by non deterministic LLM nodes; a trade off analysis of controllability, expressiveness, and implementability across 70 surveyed systems; a formal specification including a node state machine with termination and soundness guarantees; and an attributable experimental framework with a seven group design for future validation. This is a position paper and design proposal. We provide a theoretical framework, design analysis, and experimental protocol, not a production implementation or empirical results.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Agent Loops to Structured Graphs: A Scheduler-Theoretic Framework for LLM Agent Execution》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文指出当前构建大语言模型（LLM）智能体的主流范式是**智能体循环（Agent Loop）**（如 ReAct）。该范式存在三个根本性结构缺陷：
*   **隐式依赖**：步骤间的先后顺序仅存在于上下文窗口中，缺乏结构化约束。
*   **无界恢复循环**：当执行失败时，LLM 自主决定重试或重新规划，容易陷入无限循环。
*   **可变执行历史**：执行计划在运行中会被静默重写，导致审计和调试极其困难。

作者通过**调度理论（Scheduler-Theoretic）**视角，将 Agent Loop 定义为“单就绪单元调度器”（$|U| \le 1$），并提出了一种名为 **SGH（Structured Graph Harness，结构化图框架）** 的新架构，旨在将控制流从隐式的上下文提升为显式的静态有向无环图（DAG），以提升复杂工程任务的可控性和可验证性。

### 2. 方法论：核心思想与关键技术
论文提出了 SGH 框架，其核心由以下三个设计承诺和形式化规范组成：

*   **调度统一框架**：定义执行系统为五元组 $E = (S, U, P, O, \Delta)$。其中 $U$ 是就绪节点集，$P$ 是调度策略。SGH 将 $|U|$ 从 1 扩展到 $\ge 1$，实现并行执行。
*   **三层架构分离**：
    1.  **规划层（Planner）**：生成不可变的静态 DAG 计划。
    2.  **运行时层（Runtime）**：根据 DAG 拓扑和确定性策略执行节点。
    3.  **恢复层（Recovery）**：独立于执行逻辑，负责故障诊断。
*   **三级升级恢复协议**：强制执行“重试（Retry） $\rightarrow$ 局部补丁（Patch） $\rightarrow$ 请求重新规划（Replan）”的阶梯式升级，防止无界循环。
*   **节点状态机**：形式化定义了节点从 `pending` 到 `executed` 或 `failed` 的转换逻辑，并从理论上证明了在有限预算下的**终止性（Termination）**和**完备性（Soundness）**。
*   **上下文隔离**：区分执行上下文（$C_{exec}$）和诊断上下文（$C_{diag}$），防止失败历史污染后续推理。

### 3. 实验设计：场景与基准
**注意：这是一篇立场论文（Position Paper），作者并未提供实际的运行结果，而是设计了一个严谨的实验协议。**

*   **实验场景**：设计了涵盖代码修复、数据分析和运维操作的 50 个任务。
*   **任务分级**：分为简单（1-3步，线性）、中等（4-8步，有分支）、复杂（9步以上，深层依赖）三个等级。
*   **对比方法（七组设计）**：
    *   **G0 (SOTA Loop)**：最先进的提示词增强型循环（如 Claude Code）。
    *   **G1-G3**：从朴素循环到带规划、带脚手架约束的循环。
    *   **G4-G6 (SGH 变体)**：逐步引入并行调度、局部补丁和完整重新规划协议。
*   **衡量指标**：成功率、执行耗时、Token 成本、故障定位准确度，以及通过“增益分解”计算各组件（如 $G_{graph}, G_{replan}$）的贡献。

### 4. 资源与算力
*   论文**未明确说明**具体的算力消耗或 GPU 型号。
*   由于该研究目前处于理论框架和设计提案阶段，尚未进行大规模的生产环境实验，因此缺乏相关的硬件资源统计数据。

### 5. 实验数量与充分性
*   **理论验证充分**：作者对 70 个开源智能体项目进行了系统性调研（附录中有详细列表），分析了它们的调度模式和缺陷，这为设计原则提供了客观依据。
*   **形式化证明完备**：对状态机的终止性和一致性给出了数学证明。
*   **实证缺失**：论文明确指出“不提供生产实现或实证结果”，因此其实验充分性仅体现在**实验方案的设计逻辑**上，而非数据结果上。

### 6. 主要结论与发现
*   **调度连续体**：Agent Loop 和基于图的执行器可以统一在调度理论下，区别在于就绪集基数和策略显式度。
*   **可控性 vs 表达能力**：SGH 通过限制动态图修改和竞争并行（first-of），换取了极高的可审计性和确定性。
*   **结构化优势**：对于可验证的工程任务，显式的 DAG 结构在并行效率和错误隔离方面优于隐式的 LLM 循环。
*   **故障处理协议化**：将恢复逻辑从 LLM 的随机决策转变为确定性的升级协议，是解决“失败循环”病态行为的关键。

### 7. 优点
*   **理论高度**：首次将经典的 DAG 调度理论系统地引入 LLM 智能体执行领域。
*   **严谨的权衡分析**：清晰地界定了为了获得“可控性”而牺牲的“表达能力”边界。
*   **可归因的实验设计**：提出的七组对比实验能够精确量化每一个设计决策（如并行、恢复协议）带来的具体收益。
*   **调研深入**：对 70 个开源项目的分类调研为论文的观点提供了坚实的现实背景。

### 8. 不足与局限
*   **缺乏实证支持**：作为设计提案，其在真实复杂环境下的性能表现（如 Token 节省、耗时减少）尚待验证。
*   **静态 DAG 的局限**：对于高度探索性、任务结构在运行中不断演化的场景（如开放式研究），静态 DAG 可能过于僵化。
*   **规划器依赖**：系统的上限严重依赖于 Planner 生成高质量 DAG 的能力，若 Planner 失败，系统会频繁触发昂贵的重新规划。
*   **工程复杂度**：相比简单的 Loop，实现 SGH 需要复杂的并发调度、状态持久化和合同验证引擎。

（完）
