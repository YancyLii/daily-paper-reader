---
title: "Willful Disobedience: Automatically Detecting Failures in Agentic Traces"
title_zh: 蓄意违抗：自动检测智能体轨迹中的故障
authors: "Reshabh K Sharma, Shraddha Barke, Benjamin Zorn"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23806v1"
tags: ["query:agsec"]
score: 8.0
evidence: 检测智能体轨迹中的不安全工具使用和规则违规
tldr: 随着 AI 智能体在复杂软件系统中的应用，其多步执行轨迹的验证变得愈发困难。传统的仅关注结果的基准测试往往忽略了工作流路由错误、不安全工具调用或违反提示词规则等程序性失败。本文提出 AgentPex，这是一款利用 AI 自动检测智能体轨迹故障的工具。它能从系统指令中提取行为规则，并据此评估轨迹的合规性。实验表明，AgentPex 能有效识别出结果评分无法捕捉的违规行为，为开发者提供跨领域的细粒度分析，助力提升智能体的可靠性。
selection_source: fresh_fetch
motivation: 传统的仅关注结果的评估方法无法捕捉 AI 智能体在执行多步工作流过程中的程序性错误和规则违规。
method: 开发了 AgentPex 工具，通过从系统提示词中提取行为规则，并利用这些规范自动评估智能体执行轨迹的合规性。
result: 在多个领域的测试中，AgentPex 成功识别出仅靠结果评分无法发现的规范违规，并提供了细粒度的性能分析。
conclusion: AgentPex 为开发者提供了一种在大规模场景下理解和优化 AI 智能体行为一致性与安全性的有效手段。
---

## 摘要
AI 智能体正越来越多地嵌入到真实的软件系统中，通过多轮对话、工具调用和中间决策来执行多步工作流。这些被称为“智能体轨迹”（agentic traces）的长执行历史使得验证变得困难。仅关注结果的基准测试可能会遗漏关键的过程性故障，例如错误的工作流路由、不安全的工具使用或违反提示词指定的规则。本文介绍了 AgentPex，这是一款由 AI 驱动的工具，旨在系统地评估智能体轨迹。AgentPex 从智能体提示词和系统指令中提取行为规则，然后利用这些规范自动评估轨迹的合规性。我们在来自 τ2-bench 的 424 条轨迹上评估了 AgentPex，涵盖了电信、零售和航空公司客户服务领域的模型。我们的结果表明，AgentPex 能够区分不同模型的智能体行为，并发现仅靠结果评分无法捕获的规范违规行为。它还提供了按领域和指标划分的细粒度分析，使开发人员能够大规模地了解智能体的优缺点。

## Abstract
AI agents are increasingly embedded in real software systems, where they execute multi-step workflows through multi-turn dialogue, tool invocations, and intermediate decisions. These long execution histories, called agentic traces, make validation difficult. Outcome-only benchmarks can miss critical procedural failures, such as incorrect workflow routing, unsafe tool usage, or violations of prompt-specified rules. This paper presents AgentPex, an AI-powered tool designed to systematically evaluate agentic traces. AgentPex extracts behavioral rules from agent prompts and system instructions, then uses these specifications to automatically evaluate traces for compliance. We evaluate AgentPex on 424 traces from τ2-bench across models in telecom, retail, and airline customer service. Our results show that AgentPex distinguishes agent behavior across models and surfaces specification violations that are not captured by outcome-only scoring. It also provides fine-grained analysis by domain and metric, enabling developers to understand agent strengths and weaknesses at scale.

---

## 论文详细总结（自动生成）

以下是对论文《Willful Disobedience: Automatically Detecting Failures in Agentic Traces》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着 AI 智能体（Agent）处理的任务日益复杂，其执行过程（即“智能体轨迹”，Agentic Traces）变得冗长且难以验证。
*   **研究动机**：
    *   **结果导向评估的局限性**：现有的基准测试（如 $\tau^2$-bench）主要关注最终结果（如数据库状态是否正确），但忽略了执行过程中的“程序性失败”。
    *   **蓄意违抗（Willful Disobedience）**：智能体在执行过程中可能会选择性地忽略或偏离系统提示词（System Prompt）中的规则（例如：跳过必要的计算工具、违反安全策略、在不该回复时回复用户等）。
    *   **规模化挑战**：人工审核成千上万条多步执行轨迹在实际生产中是不可行的。

### 2. 方法论：AgentPex 工具设计
AgentPex 的核心思想是将智能体提示词视为“程序规范”，通过三个阶段实现自动化评估：
*   **轨迹标准化（Trace Normalization）**：将来自不同来源（如 OpenAI 格式、VS Code 日志）的异构轨迹统一转化为包含系统提示词、工具架构（Tool Schema）和消息日志的标准格式。
*   **规范提取（Specification Extraction）**：利用 LLM 从提示词和任务描述中提取可检查的规则，分为两类：
    *   **策略规范（Policy Specs）**：包括输出规范（格式、风格）、转换规范（步骤顺序、互斥动作）、禁止边缘（禁止的工具调用序列）和参数规范。
    *   **任务特定规范（Task-Specific Specs）**：根据用户请求预测执行计划（Predicted Plan）和预期的最终状态。
*   **自动化评估与评分**：
    *   **LLM-as-a-Judge**：使用多个独立的 LLM 评估器针对上述规范逐项检查轨迹。
    *   **门控最小评分机制（Gated-minimum Scoring）**：最终得分由加权平均分和“关键项最低分”共同决定。如果智能体在关键操作（如安全或核心逻辑）上失败，即使其他项表现完美，总分也会被压低。

### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau^2$-bench** 的子集，涵盖电信（Telecom）、零售（Retail）和航空（Airline）三个客户服务领域。
*   **实验规模**：共 424 条有效的多轮对话轨迹。
*   **对比模型**：评估了三个主流模型在相同任务下的表现：**Claude 3.5 Sonnet**、**GPT-4.1**（文中注明的版本）和 **o4-mini**。
*   **基准（Benchmark）**：将 AgentPex 的评估结果与 $\tau^2$-bench 的原生“结果奖励（Outcome Reward）”进行对比，验证其是否能发现后者遗漏的问题。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   **模型使用**：实验中所有的规范提取和 LLM 评估均采用 `gpt-5-mini-2025-08-07`（注：此为论文中提到的未来版本号，反映了研究的前瞻性）。轨迹生成则使用了 Claude 和 GPT 系列模型。

### 5. 实验数量与充分性
*   **实验组数**：针对 3 个模型、3 个领域进行了交叉实验，每组约 140-150 条轨迹。
*   **充分性评估**：
    *   **客观性**：实验通过自动化脚本平衡了任务多样性和结果分布（约 35% 的成功率），避免了样本偏差。
    *   **公平性**：所有模型均在相同的用户模拟器（GPT-4.1）环境下运行，确保了交互环境的一致性。
    *   **深度**：不仅做了定量分析（ROC-AUC 曲线），还进行了定性的案例研究，分析了不同模型的特定违规模式。

### 6. 主要结论与发现
*   **结果不等于过程**：在 Claude 3.5 Sonnet 获得“完美结果评分（1.0）”的轨迹中，**83% 实际上包含至少一种程序性违规**（如跳过验证步骤或违反输出格式）。
*   **模型行为差异**：
    *   **Claude** 倾向于产生主观评论或在调用工具时同时回复文本（违反转换规范）。
    *   **GPT-4.1** 容易出现语义幻觉或一次性批量调用多个工具。
    *   **o4-mini** 在工作流路由上表现较好，但存在跳过身份验证等安全风险。
*   **互补性**：AgentPex 的评分与人工标注的结果具有相关性（ROC-AUC 为 0.68），在没有人工标注的情况下，可作为有效的替代评估手段。

### 7. 优点与亮点
*   **规范驱动（Spec-driven）**：将自然语言提示词转化为结构化检查清单，使评估具有可解释性。
*   **细粒度诊断**：能够定位到轨迹中具体的某一步为何违规，而不仅仅是给出“成功/失败”的二元结论。
*   **无需人工标注**：完全依赖智能体自身的指令进行自我审计，极大地降低了大规模部署时的评估成本。

### 8. 不足与局限
*   **计算成本**：每条轨迹需要多次 LLM 调用进行提取和评估，在大规模实时监控中可能存在成本和延迟问题。
*   **规范重叠与双重扣分**：由于规则是从自然语言提取的，不同类别的规范可能存在重叠，导致同一个错误被多次扣分。
*   **领域局限**：目前仅在客户服务领域验证，尚未在软件开发（SWE-bench）或计算机操作等更开放的领域进行测试。
*   **检测而非修复**：目前工具仅限于发现问题，尚未提供自动修复提示词或轨迹的闭环机制。

（完）
