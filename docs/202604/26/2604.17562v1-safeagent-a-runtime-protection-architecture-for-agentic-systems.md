---
title: "SafeAgent: A Runtime Protection Architecture for Agentic Systems"
title_zh: SafeAgent：一种针对智能体系统的运行时保护架构
authors: "Hailin Liu, Eugene Ilyushin, Jie Ni, Min Zhu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17562v1"
tags: ["query:agsec"]
score: 9.0
evidence: 智能体系统的运行时保护架构与执行治理
tldr: SafeAgent 是一种针对大语言模型智能体的运行时安全架构，旨在解决多步工作流和工具交互中提示注入攻击带来的安全挑战。该架构将安全视为状态决策问题，通过运行时控制器和上下文感知决策核心，对智能体交互轨迹进行动态治理。实验表明，SafeAgent 在多个安全基准测试中显著提升了鲁棒性，同时保持了优异的任务执行性能，为智能体系统提供了可靠的动态保护。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17562v1/fig-001.webp\", \"caption\": \"Fig. 4. Execution flow of the SafeAgent Core.\", \"page\": 7, \"index\": 1, \"width\": 1073, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.17562v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the SafeAgent architecture.\", \"page\": 4, \"index\": 2, \"width\": 1072, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.17562v1/fig-003.webp\", \"caption\": \"Fig. 3. Flow control sequence in the agent lifecycle.\", \"page\": 5, \"index\": 3, \"width\": 524, \"height\": 644}]"
motivation: 传统的静态过滤无法应对多步交互中演变的提示注入风险，亟需一种能够感知上下文状态的运行时安全治理机制。
method: 提出一种包含运行时控制器和决策核心的双层架构，通过风险编码、效用成本评估和策略仲裁等算子实现对智能体行为的动态监控。
result: 在 Agent Security Bench 和 InjecAgent 测试中，该方法在显著增强系统抗攻击能力的同时保持了极高的良性任务成功率。
conclusion: 将智能体安全视为演化轨迹上的状态决策问题，是实现复杂交互环境下安全与效用平衡的有效途径。
---

## 摘要
大语言模型 (LLM) 智能体容易受到提示注入攻击的影响，这些攻击会通过多步工作流、工具交互和持久上下文进行传播，使得仅靠输入输出过滤不足以提供可靠的保护。本文提出了 SafeAgent，这是一种运行时安全架构，它将智能体安全视为演化交互轨迹上的有状态决策问题。该设计通过两个协调组件将执行治理与语义风险推理分离：一个是围绕智能体循环调解动作的运行时控制器，另一个是在持久会话状态上运行的上下文感知决策核心。该核心被形式化为一种上下文感知的先进机器智能，并通过风险编码、效用-成本评估、后果建模、策略仲裁和状态同步等算子进行实例化。在 Agent Security Bench (ASB) 和 InjecAgent 上的实验表明，SafeAgent 在保持具有竞争力的良性任务性能的同时，相比基线和文本级护栏方法，一致地提高了鲁棒性。消融研究进一步表明，恢复置信度和策略权重决定了不同的安全-效用运行点。

## Abstract
Large language model (LLM) agents are vulnerable to prompt-injection attacks that propagate through multi-step workflows, tool interactions, and persistent context, making input-output filtering alone insufficient for reliable protection. This paper presents SafeAgent, a runtime security architecture that treats agent safety as a stateful decision problem over evolving interaction trajectories. The proposed design separates execution governance from semantic risk reasoning through two coordinated components: a runtime controller that mediates actions around the agent loop and a context-aware decision core that operates over persistent session state. The core is formalized as a context-aware advanced machine intelligence and instantiated through operators for risk encoding, utility-cost evaluation, consequence modeling, policy arbitration, and state synchronization. Experiments on Agent Security Bench (ASB) and InjecAgent show that SafeAgent consistently improves robustness over baseline and text-level guardrail methods while maintaining competitive benign-task performance. Ablation studies further show that recovery confidence and policy weighting determine distinct safety-utility operating points.

---

## 论文详细总结（自动生成）

这是一份关于论文《SafeAgent: A Runtime Protection Architecture for Agentic Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）演变为能够规划、调用工具并与环境交互的“智能体”（Agents），传统的安全防御手段面临失效。核心挑战在于：
*   **风险传播性**：恶意指令不仅来自用户输入（直接注入），还可能隐藏在工具输出、检索文档或长期记忆中（间接注入），并在多步推理-动作循环（如 ReAct 模式）中累积。
*   **静态过滤的局限**：现有的“护栏”（Guardrails）多为单次输入输出的文本过滤，无法感知交互轨迹中的状态演变。
*   **研究动机**：开发一种能够将安全视为“有状态决策问题”的运行时保护架构，在不牺牲智能体任务效能的前提下，实现对复杂工作流攻击的动态拦截。

### 2. 方法论：核心思想与关键技术
SafeAgent 采用**执行治理与语义推理分离**的双层架构：

*   **运行时控制器（Runtime Controller）**：
    *   **职责**：作为智能体与环境的中间件，管理会话上下文快照。
    *   **关键技术**：在用户输入、模型计划、工具观测、模型输出四个关键节点设置钩子（Hooks）；实施受控的工具访问（Tool Wrapper）；提供恢复机制（如上下文重写、重新规划、状态回滚、人工审批）。
*   **决策核心（SafeAgent Core）**：
    *   **核心思想**：将其形式化为“上下文感知先进机器智能（AMI）”，通过内部模拟预测动作后果。
    *   **关键算子**：
        1.  **风险编码**：多专家编码器识别混淆、触发器激活、控制劫持等维度。
        2.  **优势-成本建模**：评估不同干预策略（如拒绝或修复）的即时效用与成本。
        3.  **世界模型模拟**：在潜在空间模拟候选动作的未来轨迹，预测下游风险。
        4.  **策略仲裁**：根据任务需求（安全优先或任务优先）动态调整权重。
        5.  **状态同步**：维护多尺度风险状态（即时、任务级、长周期风险）。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **Agent Security Bench (ASB)**：包含 51 个任务和 40 种攻击工具，共 2040 个评估案例，涵盖直接注入（DPI）、间接注入（IPI）和记忆毒化（MP）。
    *   **InjecAgent**：专注于工具集成智能体的注入攻击，包含 510 个直接伤害任务和 544 个数据窃取任务。
*   **对比方法**：
    *   **Baseline**：无防御的智能体。
    *   **Llama Guard**：基于 LLM 的输入输出分类护栏。
    *   **LLM Guard**：基于预训练分类器（如 RoBERTa）的文本扫描工具。
*   **评估指标**：攻击成功率（ASR）、无攻击下的性能（PNA）、归一化奖励保留率（NRP）。

### 4. 资源与算力
*   **模型使用**：智能体骨干模型使用 **DeepSeek-V3.2**；SafeAgent 决策核心使用 **gpt-oss-safeguard-20b**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及训练/推理的总时长。但提到该架构会带来“非平凡的计算开销”（Non-trivial computational overhead），暗示了延迟是其主要成本。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流安全基准上进行了数千次测试（ASB 2040 例 + InjecAgent 1054 例），实验规模较大。
*   **消融实验**：针对“重写置信度”和“策略权重”进行了专门的消融研究，探讨了安全与效用之间的平衡点。
*   **充分性评价**：实验设计较为充分且客观。通过对比不同类型的攻击（DPI/IPI/MP）和不同阶段的成功率（数据提取 vs. 传输），全面验证了架构在多步工作流中的有效性。

### 6. 主要结论与发现
*   **防御优势**：SafeAgent 在所有攻击类型下的 ASR 均显著低于传统护栏。在 InjecAgent 的数据窃取任务中，SafeAgent 将成功率降至 0。
*   **状态感知的重要性**：纯文本过滤（如 LLM Guard）在应对通过工具交互传播的攻击时效果有限，而有状态的运行时监控能有效拦截多阶段攻击。
*   **安全-效用权衡**：通过调整策略权重，系统可以在“保守防御”和“任务优先”之间切换。修复（Repair）机制在处理局部攻击时有效，但对于结构性、持久性的上下文污染，显式拒绝更为安全。

### 7. 优点
*   **架构解耦**：将执行逻辑与安全推理分离，使系统更易于扩展和配置。
*   **动态恢复**：不仅是简单的拦截，还支持重写参数、重新规划等恢复手段，提升了系统的可用性。
*   **形式化严谨**：将安全问题建模为 AMI 决策过程，为智能体安全提供了理论框架。

### 8. 不足与局限
*   **延迟与开销**：由于在每个交互节点都需要调用额外的决策模型，系统的响应延迟会显著增加，限制了实时应用场景。
*   **模型依赖**：防御效果高度依赖于决策核心模型（如 20B 模型）的推理能力，若该模型本身被绕过，防御将失效。
*   **新型攻击覆盖**：虽然对提示注入效果显著，但对于“技能注入”（Skill Injection）等新兴威胁类别的研究尚处于初步阶段。

（完）
