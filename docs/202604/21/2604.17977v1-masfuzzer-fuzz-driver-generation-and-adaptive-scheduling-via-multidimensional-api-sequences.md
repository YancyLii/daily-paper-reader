---
title: "MASFuzzer: Fuzz Driver Generation and Adaptive Scheduling via Multidimensional API Sequences"
title_zh: MASFuzzer：基于多维 API 序列的模糊测试驱动生成与自适应调度
authors: "Xingyu Liu, Zengqin Huang, Xiang Gao, Hailong Sun"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17977v1"
tags: ["query:libfuz"]
score: 10.0
evidence: 利用多维API序列和大语言模型自动生成模糊测试驱动
tldr: MASFUZZER是一个针对软件库模糊测试的自动化框架，旨在解决手动编写驱动效率低及LLM生成驱动覆盖深度不足的问题。该框架通过挖掘代码库中的API使用示例，利用变异传播和语义感知技术构建多维API序列，并结合LLM生成初始驱动。此外，它采用覆盖率引导的自适应调度和驱动变异策略来持续优化测试。实验表明，其覆盖率优于现有技术，并成功发现了16个新漏洞。
selection_source: fresh_fetch
motivation: 传统手动编写模糊测试驱动程序耗时且难以覆盖复杂逻辑，而现有基于LLM的方法生成的驱动往往无法触达深层程序分支。
method: 通过从代码库中挖掘多维API序列来引导LLM生成初始驱动，并引入覆盖率引导的调度器与驱动变异策略进行动态演化。
result: "在12个主流开源库上，MASFUZZER的覆盖率比最先进技术高出8.54%，并发现了16个新漏洞，其中14个已获确认，9个被分配CVE。"
conclusion: MASFUZZER证明了结合多维API序列挖掘与自适应调度策略能显著提升软件库模糊测试的效率和漏洞发现能力。
---

## 摘要
软件库的模糊测试依赖于模糊测试驱动程序来调用库 API。传统上，这些驱动程序由开发人员手动编写，这一过程耗时且往往不足以触发复杂的程序行为。虽然最近的研究探索了使用大语言模型（LLMs）来自动化生成模糊测试驱动程序，但生成的驱动程序往往无法覆盖深层的程序分支。为了应对这些挑战，我们提出了 MASFUZZER，这是一个集成了多维 API 序列构建与自适应模糊测试调度策略的模糊测试框架，旨在改进软件库测试。其核心在于，MASFUZZER 通过参考代码库中的 API 使用示例，并应用基于变异传播和语义感知的 API 序列挖掘，来合成与上下文相关的 API 调用序列。这些多维 API 序列为 LLM 生成有效的初始驱动程序奠定了基础。此外，MASFUZZER 包含一个覆盖率引导的调度器，该调度器优先为最有潜力的驱动程序分配测试时间，并配合驱动程序变异策略对其进行演化。这使得能够系统地生成模糊测试驱动程序，以探索此前未测试的代码区域。我们在 12 个广泛使用的开源库上对 MASFUZZER 进行了评估。结果表明，MASFUZZER 的代码覆盖率比现有最先进技术高出 8.54%。此外，MASFUZZER 在经过广泛测试的库中发现了 16 个此前未知的漏洞，其中 14 个已得到开发人员确认，9 个被分配了 CVE 标识符。这些结果表明，MASFUZZER 为软件库的模糊测试提供了一种高效且实用的方法。

## Abstract
Fuzz testing of software libraries relies on fuzz drivers to invoke library APIs. Traditionally, these drivers are written manually by developers - a process that is time-consuming and often inadequate for exercising complex program behaviors. While recent studies have explored the use of Large Language Models (LLMs) to automate fuzz driver generation, the resulting drivers often fail to cover deep program branches. To address these challenges, we propose MASFUZZER, a fuzzing framework that integrates multidimensional API sequence construction with adaptive fuzzing scheduling strategies to improve library testing. At its core, MASFUZZER synthesizes context-relevant API call sequences by referring to API usage examples from the codebase and applying mutation-propagation-based and semantic-aware API sequence mining. These multidimensional API sequences serve as the basis for LLMs to generate effective initial drivers. In addition, MASFUZZER incorporates a coverage-guided scheduler that prioritizes testing time for the most promising drivers, along with a driver mutation strategy to evolve them. This enables systematic generation of fuzz drivers to explore previously untested code regions. We evaluate MASFUZZER on 12 widely used open-source libraries. The results show that MASFUZZER achieves 8.54 percent higher code coverage than state-of-the-art techniques. Moreover, MASFUZZER uncovers 16 previously unknown vulnerabilities in extensively tested libraries, with 14 confirmed by developers and 9 assigned CVE identifiers. These results indicate that MASFUZZER provides an efficient and practical approach for fuzzing software libraries.

---

## 论文详细总结（自动生成）

这是一份关于论文《MASFuzzer: Fuzz Driver Generation and Adaptive Scheduling via Multidimensional API Sequences》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：模糊测试（Fuzzing）是发现软件漏洞的关键技术。对于软件库（Library）而言，由于缺乏单一入口点，需要编写“模糊测试驱动程序”（Fuzz Driver）来调用 API。
*   **核心问题**：
    *   **手动编写困难**：传统手动编写驱动耗时耗力，且依赖开发者的专业知识。
    *   **现有自动化工具局限**：基于静态分析的方法缺乏深层语义推理；基于大语言模型（LLM）的方法生成的驱动往往语义浅薄或语法不可靠，且无法有效覆盖深层程序分支。
    *   **资源分配不均**：现有系统对所有生成的驱动程序平均分配测试时间，导致在潜力较低的驱动上浪费资源，而高潜力驱动测试不足。

### 2. 方法论：核心思想与关键技术
MASFuzzer 提出了一个结合多维 API 序列构建与自适应调度的框架，核心步骤如下：
*   **多维 API 序列构建**：从三个维度合成 API 调用上下文，引导 LLM 生成高质量驱动：
    1.  **使用示例挖掘**：从库自带的单元测试和示例代码中提取真实的 API 调用模式。
    2.  **变异传播挖掘**：基于类型兼容性（API A 的返回值类型与 API B 的参数类型匹配）构建兼容性图，通过深度优先搜索生成满足数据流传递的序列。
    3.  **语义感知挖掘**：利用 LLM 分析 API 文档和代码，识别隐藏的逻辑依赖（如初始化函数与功能函数的关系），捕捉静态分析无法发现的语义约束。
*   **驱动生成与修复**：将多维序列整合进 Prompt 引导 LLM 生成驱动，并使用修复代理（Repair Agent）通过迭代分析编译错误来自动修复代码。
*   **覆盖率引导的时间调度器**：
    *   根据 API 覆盖率反馈和序列新颖性（基于编辑距离计算）动态调整每个驱动的执行时间。
    *   优先处理覆盖率低、序列新颖度高的驱动，跳过已达到覆盖阈值的冗余驱动。
*   **序列感知驱动变异**：针对未发现新分支的驱动，计算 API 的“能量”（结合覆盖率、频率和潜力），采用插入、替换或合并策略对 API 序列进行变异，生成新的驱动。

### 3. 实验设计
*   **数据集/场景**：选择了 12 个广泛使用的开源 C 语言库（如 `c-ares`, `cjson`, `zlib`, `libtiff`, `libvpx` 等），涵盖了网络、图像处理、压缩等多个领域。
*   **Benchmark（对比方法）**：
    *   **传统方法**：LIBERATOR（目前最先进的非 LLM 驱动生成工具）。
    *   **LLM 方法**：PROMEFUZZ 和 CKGFUZZER（目前最先进的基于 LLM 的工具）。
*   **实验设置**：每个库进行 24 小时的模糊测试，重复 10 次取平均值。

### 4. 资源与算力
*   **硬件环境**：Intel Xeon Gold 5218 CPU @ 2.30GHz，128 GB RAM。
*   **LLM 模型**：默认使用 **DeepSeek-V3**。在 RQ.4 中为了对比，还使用了 **GPT-5**（注：论文中提到的 GPT-5 可能指代当时最新的 GPT 系列模型或特定版本）。
*   **成本**：实验中 MASFuzzer 的 LLM API 总开销约为 49.87 美元，比 CKGFuzzer（67.81 美元）更具成本效益。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个真实世界库上进行了测试，每组实验运行 10 次以消除随机性影响，符合模糊测试社区的标准。
*   **消融实验**：
    *   针对 API 序列的三个维度（示例、变异传播、语义）分别做了消融，验证了多维信息的必要性。
    *   针对时间调度器和变异策略做了对比实验。
*   **模型对比**：对比了不同 LLM 对生成效果的影响。
*   **充分性评价**：实验设计全面，不仅关注代码覆盖率，还进行了实际的漏洞挖掘，并提交给开发者确认，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **覆盖率提升**：MASFuzzer 的分支覆盖率平均比 PROMEFUZZ 高 **8.54%**，比 CKGFUZZER 高 **15.07%**，比 LIBERATOR 高 **37.95%**。
*   **漏洞挖掘能力**：在经过长期广泛测试的库中成功发现了 **16 个此前未知的漏洞**，其中 14 个已获开发者确认，9 个已分配 CVE ID。
*   **调度有效性**：动态调度器能显著提高资源利用率，平均提升 9.08% 的覆盖率。
*   **模型表现**：DeepSeek-V3 在该任务上的表现与 GPT-5 相当，但在生成风格上更简洁高效。

### 7. 优点（亮点）
*   **多维上下文融合**：通过结合静态类型分析、真实代码示例和 LLM 语义推理，解决了 LLM 生成代码“幻觉”或逻辑不通的问题。
*   **动态资源优化**：引入了类似模拟退火的调度算法，解决了模糊测试中后期“边际收益递减”的问题。
*   **闭环演化**：不仅能生成驱动，还能通过变异策略像变异输入数据一样变异驱动程序本身。

### 8. 不足与局限
*   **数据污染风险**：由于 LLM 的训练数据可能包含这些开源库的现有驱动，实验结果可能受到模型记忆的影响（尽管作者通过对比实验和引入新库尝试缓解）。
*   **控制流限制**：目前生成的 API 序列主要是直线型的，缺乏复杂的循环和分支逻辑。
*   **语言支持**：目前仅支持 C 语言，虽然理论上可扩展至 C++ 和 Rust，但尚未在文中实现和验证。
*   **边界模糊**：在 C 语言库中，API 误用（Misuse）与真实漏洞的界限有时难以界定，需要人工介入分析。

（完）
