---
title: "SafeAgent: A Runtime Protection Architecture for Agentic Systems"
title_zh: SafeAgent：一种针对智能体系统的运行时保护架构
authors: "Hailin Liu, Eugene Ilyushin, Jie Ni, Min Zhu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17562v1"
tags: ["query:agsec"]
score: 9.0
evidence: Agent 的运行时保护架构与执行治理
tldr: 针对大语言模型智能体易受提示注入攻击且传统过滤方法不足的问题，本文提出SafeAgent运行时安全架构。该架构将安全视为状态化决策问题，通过运行时控制器和上下文感知决策核心协同工作，实现对交互轨迹的动态治理。实验证明，SafeAgent在保持任务性能的同时，显著提升了智能体在ASB和InjecAgent基准测试中的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的输入输出过滤难以应对在多步工作流、工具交互和持久上下文中传播的复杂提示注入攻击。
method: 提出一种由运行时控制器和上下文感知决策核心组成的架构，通过风险编码、后果建模和策略仲裁等算子对交互轨迹进行状态化治理。
result: 在Agent Security Bench和InjecAgent上的实验显示，SafeAgent在显著提升安全鲁棒性的同时，维持了极具竞争力的良性任务表现。
conclusion: SafeAgent证明了将安全视为状态化决策过程能有效防御智能体系统中的动态风险，并提供了安全与效用之间的平衡机制。
---

## 摘要
大语言模型 (LLM) 智能体容易受到提示注入攻击的影响，这些攻击会通过多步工作流、工具交互和持久上下文进行传播，使得仅靠输入输出过滤不足以提供可靠的保护。本文提出了 SafeAgent，这是一种运行时安全架构，它将智能体安全视为演化交互轨迹上的有状态决策问题。所提出的设计通过两个协调组件将执行治理与语义风险推理分离：一个是在智能体循环中调解动作的运行时控制器，以及一个在持久会话状态上运行的上下文感知决策核心。该核心被形式化为一种上下文感知的先进机器智能，并通过风险编码、效用成本评估、后果建模、策略仲裁和状态同步等算子进行实例化。在 Agent Security Bench (ASB) 和 InjecAgent 上的实验表明，SafeAgent 在保持具有竞争力的良性任务性能的同时，相比基线和文本级护栏方法，一致地提高了鲁棒性。消融研究进一步表明，恢复置信度和策略权重决定了不同的安全-效用运行点。

## Abstract
Large language model (LLM) agents are vulnerable to prompt-injection attacks that propagate through multi-step workflows, tool interactions, and persistent context, making input-output filtering alone insufficient for reliable protection. This paper presents SafeAgent, a runtime security architecture that treats agent safety as a stateful decision problem over evolving interaction trajectories. The proposed design separates execution governance from semantic risk reasoning through two coordinated components: a runtime controller that mediates actions around the agent loop and a context-aware decision core that operates over persistent session state. The core is formalized as a context-aware advanced machine intelligence and instantiated through operators for risk encoding, utility-cost evaluation, consequence modeling, policy arbitration, and state synchronization. Experiments on Agent Security Bench (ASB) and InjecAgent show that SafeAgent consistently improves robustness over baseline and text-level guardrail methods while maintaining competitive benign-task performance. Ablation studies further show that recovery confidence and policy weighting determine distinct safety-utility operating points.

---

## 论文详细总结（自动生成）

这是一份关于论文《SafeAgent: A Runtime Protection Architecture for Agentic Systems》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）智能体在执行复杂任务时，容易受到**提示注入（Prompt Injection）**攻击。这种攻击不仅来自用户输入（直接注入），还可能隐藏在工具输出、检索文档或历史记忆中（间接注入）。
*   **研究背景**：传统的安全护栏（Guardrails）多为“单次过滤”模式，仅检查输入或输出文本。然而，智能体风险具有**状态化和传播性**，攻击可能在多步推理-动作循环中逐渐累积。
*   **整体含义**：论文提出将智能体安全视为一个**有状态的运行时决策问题**，通过构建一套独立的保护架构，在不牺牲任务效用的前提下，实现对智能体全生命周期的动态治理。

### 2. 方法论：核心思想与关键技术
SafeAgent 架构由两个核心组件组成，实现了“执行治理”与“风险推理”的分离：

*   **运行时控制器（Runtime Controller）**：
    *   **职责**：作为智能体与环境之间的治理接口，管理会话状态快照。
    *   **关键技术**：在用户输入、模型计划、工具观测、模型输出四个关键点设置钩子（Hooks）。支持**恢复导向的编排**，包括上下文重写、重新规划（Replanning）、状态回滚、工具参数重写及人工审批。
*   **决策核心（SafeAgent Core）**：
    *   **核心思想**：将其形式化为“上下文感知的高级机器智能（AMI）”，在语义层面进行风险评估。
    *   **关键算子**：
        1.  **风险编码（Risk Encoding）**：使用多个专家编码器识别混淆、触发器激活、控制劫持等维度。
        2.  **优势-成本建模**：评估不同干预策略（如拒绝或修复）的即时效用与成本。
        3.  **世界模型模拟**：在潜空间模拟动作的未来后果，预测潜在的下游风险。
        4.  **策略仲裁**：根据任务需求（安全优先或任务优先）动态调整决策权重。
        5.  **状态同步**：维护多尺度风险状态（即时、任务级、长周期风险）。

### 3. 实验设计
*   **数据集/场景**：
    *   **Agent Security Bench (ASB)**：包含 51 个任务和 40 种攻击工具，共 2040 个评估案例，涵盖直接注入 (DPI)、间接注入 (IPI) 和记忆毒化 (MP)。
    *   **InjecAgent**：专注于工具集成智能体的提示注入，包含 510 个直接伤害任务和 544 个多阶段数据窃取任务。
*   **对比方法（Baselines）**：
    *   **无防御基线**（No Defense）。
    *   **Llama Guard**：基于 LLM 的输入输出护栏。
    *   **LLM Guard**：基于分类器（如 RoBERTa）的文本扫描工具。
*   **评估指标**：攻击成功率 (ASR)、无攻击下的性能 (PNA)、归一化奖励保留率 (NRP)。

### 4. 资源与算力
*   **模型使用**：智能体骨干模型使用 **DeepSeek-V3.2**；SafeAgent 决策核心使用 **gpt-oss-safeguard-20b**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该架构侧重于运行时推理而非大规模重新训练，其开销主要体现在推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：在 ASB 上进行了超过 2000 组实验，在 InjecAgent 上进行了超过 1000 组实验。
*   **消融实验**：针对“恢复置信度”和“策略权重”进行了专门的消融研究，探讨了安全与效用之间的平衡。
*   **充分性评价**：实验覆盖了多种攻击渠道（直接、间接、记忆）和多种任务类型（数据窃取、直接伤害），对比了当前主流的文本级护栏，实验设计较为客观、全面，能够证明其在复杂工作流中的优势。

### 6. 主要结论与发现
*   **显著提升鲁棒性**：SafeAgent 在 ASB 的间接注入和记忆毒化场景下，ASR 显著低于传统护栏。在 InjecAgent 的数据窃取任务中，SafeAgent 将攻击成功率降至 **0**。
*   **打破过滤局限**：实验证明仅靠文本级过滤无法应对多步传播的攻击，而状态化的运行时控制能有效拦截演化中的风险。
*   **安全-效用权衡**：通过调整策略权重，系统可以在“保守防御”和“积极完成任务”之间切换。修复导向的干预（如重写上下文）在处理局部攻击时比直接拒绝更有效。

### 7. 优点（亮点）
*   **架构解耦**：将执行逻辑与安全逻辑分离，使安全策略可以独立于智能体设计进行迭代。
*   **状态化防御**：引入了“世界模型”和“潜空间模拟”的概念，使安全系统具备了前瞻性，而非仅仅是反应性的。
*   **丰富的恢复手段**：不仅是简单的“拦截”，还提供了回滚、重写、重新规划等多种维持业务连续性的手段。

### 8. 不足与局限
*   **计算开销**：引入额外的决策核心（如 20B 规模的模型）会显著增加每一步交互的延迟和推理成本。
*   **依赖底层模型能力**：决策核心的有效性高度依赖于所使用的安全模型（如 gpt-oss-safeguard）的推理能力。
*   **新型攻击挑战**：虽然对提示注入有效，但对于“技能注入（Skill Injection）”等新兴威胁类别的防御能力尚待进一步研究。
*   **应用限制**：目前主要针对 ReAct 风格的智能体，对于更复杂的自组织多智能体系统的适用性未做深入探讨。

（完）
