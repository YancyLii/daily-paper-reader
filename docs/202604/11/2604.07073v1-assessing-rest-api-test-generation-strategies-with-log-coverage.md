---
title: Assessing REST API Test Generation Strategies with Log Coverage
title_zh: 基于日志覆盖率评估 REST API 测试生成策略
authors: "Nana Reinikainen, Mika Mäntylä, Yuqing Wang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07073v1"
tags: ["query:libfuz"]
score: 8.0
evidence: 使用大模型和进化计算生成REST API测试序列
tldr: 针对黑盒环境下REST API测试评估难题，本文提出利用日志覆盖率（平均、最小、最大指标）作为衡量标准。通过在Light-OAuth2系统上对比进化计算（EvoMaster）、大语言模型（Claude、GPT）及人工编写的测试，研究发现Claude表现优于人工测试，且不同策略生成的测试在运行时行为上具有显著互补性，结合使用可大幅提升覆盖率。
selection_source: fresh_fetch
motivation: 在黑盒测试中，由于缺乏源代码访问权限且技术栈多样，传统的代码覆盖率指标难以有效评估REST API测试的质量。
method: 提出基于日志模板的平均、最小和最大覆盖率指标，并据此评估进化算法、LLM生成及人工编写的三类测试策略的有效性。
result: "Claude Opus 4.6发现的日志模板比人工测试多28.4%，且将不同策略结合使用能显著提升总覆盖率（最高达105.6%）。"
conclusion: 日志覆盖率是评估黑盒API测试的有效替代指标，且不同生成策略在触发系统运行时行为方面展现出极强的互补性。
---

## 摘要
在黑盒环境下，由于缺乏源代码覆盖率指标且涉及多语言技术栈，评估 REST API 测试的有效性具有挑战性。我们提出了三种指标来捕获平均、最小和最大日志覆盖率，以应对多次运行中多样化的测试生成结果和运行时行为。利用日志覆盖率，我们在 Light-OAuth2 授权微服务系统上对三种 REST API 测试生成策略进行了实证评估：演化计算（EvoMaster v5.0.2）、大语言模型（Claude Opus 4.6 和 GPT-5.2-Codex）以及人工编写的 Locust 负载测试。结果显示，平均而言，Claude Opus 4.6 测试发现的唯一日志模板比人工编写的测试多 28.4%，而 EvoMaster 和 GPT-5.2-Codex 则分别少 26.1% 和 38.6%。随后，我们分析了组合日志覆盖率以评估策略间的互补性。将人工编写的测试与 Claude Opus 4.6 测试相结合，使两者的总观测日志覆盖率分别提升了 78.4% 和 38.9%。当 Locust 测试与 EvoMaster 结合时，增幅分别为 30.7% 和 76.9%；与 GPT-5.2-Codex 结合时，增幅分别为 26.1% 和 105.6%。这表明不同的生成策略触发了截然不同的运行时行为。未来的工作将包括把研究扩展到更多系统。

## Abstract
Assessing the effectiveness of REST API tests in black-box settings can be challenging due to the lack of access to source code coverage metrics and polyglot tech stack. We propose three metrics for capturing average, minimum, and maximum log coverage to handle the diverse test generation results and runtime behaviors over multiple runs. Using log coverage, we empirically evaluate three REST API test generation strategies, Evolutionary computing (EvoMaster v5.0.2), LLMs (Claude Opus 4.6 and GPT-5.2-Codex), and human-written Locust load tests, on Light-OAuth2 authorization microservice system. On average, Claude Opus 4.6 tests uncover 28.4% more unique log templates than human-written tests, whereas EvoMaster and GPT-5.2-Codex find 26.1% and 38.6% fewer, respectively. Next, we analyze combined log coverage to assess complementarity between strategies. Combining human-written tests with Claude Opus 4.6 tests increases total observed log coverage by 78.4% and 38.9% in human-written and Claude tests respectively. When combining Locust tests with EvoMaster the same increases are 30.7% and 76.9% and when using GPT-5.2-Codex 26.1% and 105.6%. This means that the generation strategies exercise largely distinct runtime behaviors. Our future work includes extending our study to multiple systems.

---

## 论文详细总结（自动生成）

这篇论文针对黑盒环境下 REST API 测试评估的难题，提出了一种基于日志覆盖率的新型评估方法，并对比了当前主流的自动化测试生成策略。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在现代微服务架构中，由于多语言技术栈和黑盒环境的限制，传统的**代码覆盖率**（Code Coverage）难以获取。而现有的黑盒指标（如请求数、状态码）无法深入反映系统内部运行时的行为多样性。
*   **核心问题**：如何利用广泛存在的**系统日志**作为代理指标，来量化评估不同 REST API 测试生成策略（如 LLM、进化计算、人工编写）的有效性和互补性。

### 2. 方法论
*   **核心思想**：将日志视为系统运行时行为的观测窗口。通过日志解析技术将非结构化日志转化为**日志模板**（Log Templates），以唯一模板的数量作为“覆盖率”指标。
*   **关键技术细节**：
    *   **日志解析**：使用 `LogLead` 进行数据加载和掩码处理，采用 `Drain` 算法进行在线日志解析，提取结构化模板。
    *   **覆盖率指标定义**：
        *   **平均日志覆盖率 (AvgLC)**：多次运行中发现的唯一模板数量的平均值，反映典型行为。
        *   **最小日志覆盖率 (MinLC)**：多次运行中日志模板的**交集**，反映策略的确定性/可靠行为。
        *   **最大日志覆盖率 (MaxLC)**：多次运行中日志模板的**并集**，反映策略的探索上限。
    *   **互补性分析**：通过计算不同策略间日志模板的 **Jaccard 相似度**和**组合增益**，评估将多种策略结合使用的价值。

### 3. 实验设计
*   **被测系统 (SUT)**：`Light-OAuth2`，一个包含 7 个独立微服务的工业级 OAuth2 授权系统。
*   **对比方法 (Benchmarks)**：
    1.  **人工编写测试**：基于 Locust 框架的负载测试脚本（由专家根据文档编写）。
    2.  **进化计算 (SBST)**：使用 `EvoMaster` (v5.0.2) 在黑盒模式下基于 OpenAPI 规范生成测试。
    3.  **大语言模型 (LLM)**：使用 `Claude Opus 4.6` 和 `GPT-5.2-Codex`（注：论文设定背景为 2026 年），通过 Zero-shot 提示词生成测试。
*   **实验流程**：每种自动化策略重复运行 10 次，每次运行后执行 120 秒测试并收集日志。

### 4. 资源与算力
*   **算力资源**：LLM 通过 `OpenRouter` API 调用；EvoMaster 在本地硬件运行。文中未提及具体的 GPU 型号或数量。
*   **成本与时长**：
    *   **EvoMaster**：每个服务生成预算为 70 秒。
    *   **LLM**：Claude 10 次运行成本约 13 美元，GPT 约 3 美元。
    *   **人力成本**：人工编写 Locust 耗时约 150 小时；LLM 提示词开发及质量检查约 12-14 小时；EvoMaster 配置约 4 小时。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 个微服务，每种自动化策略进行了 10 次独立重复实验，以降低随机性影响。
*   **充分性评价**：实验设计较为客观，遵循了自动化测试研究的推荐实践（如重复实验、控制变量）。虽然仅在一个系统上进行（Pilot Study），但涵盖了多种主流技术路径的对比，具有较强的实证参考价值。

### 6 主要结论与发现
*   **性能排名**：在日志覆盖率上，**Claude Opus 4.6 > 人工编写 (Locust) > EvoMaster > GPT-5.2-Codex**。Claude 比人工测试多发现 28.4% 的唯一日志模板。
*   **互补性显著**：不同策略生成的测试在运行时行为上差异巨大。将 Claude 与人工测试结合，可使覆盖率在各自基础上分别提升 38.9% 和 78.4%。
*   **AI 的优越性**：Claude 在极低的人力成本（12小时 vs 150小时）和计算成本下，实现了“超越人类”的测试效果。
*   **服务差异性**：在逻辑复杂的授权码流程（Code service）中，人工测试仍优于自动化工具，但在标准 CRUD 操作中，LLM 表现更强。

### 7. 优点
*   **指标创新**：提出了一种无需插桩、适用于多语言微服务环境的实用评估指标（日志覆盖率）。
*   **实战导向**：对比了真实的人工成本与计算成本，为企业选择测试策略提供了经济学依据。
*   **深度分析**：不仅关注覆盖率数值，还通过交集/并集分析了不同工具在探索系统行为上的本质差异。

### 8. 不足与局限
*   **外部有效性**：实验仅限于一个系统（Light-OAuth2），结论是否能推广到其他架构（如非 RESTful 或非微服务系统）尚待验证。
*   **日志质量依赖**：该方法高度依赖于开发人员编写日志的质量。如果系统日志稀疏或不规范，该指标将失效。
*   **解析器偏差**：仅使用了 `Drain` 一种解析器，不同的解析算法可能会对模板提取结果产生影响。
*   **LLM 随机性**：尽管重复了 10 次，但 LLM 的输出仍具有不可预测性，且版本迭代（如文中提到的未来版本）可能导致结果难以完全复现。

（完）
