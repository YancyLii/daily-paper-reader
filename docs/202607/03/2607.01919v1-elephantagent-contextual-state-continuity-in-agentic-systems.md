---
title: "ElephantAgent: Contextual State Continuity in Agentic Systems"
title_zh: ElephantAgent：智能体系统中的上下文状态连续性
authors: "Jiankai Jin, Xiangzheng Zhang, Zhao Liu, Wenzhuo Xu, Dongdong Yang, Deyue Zhang, Quanchen Zou"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01919v1"
tags: ["query:agsec"]
score: 9.0
evidence: 提出一种强制上下文状态连续性的协议，以防御智能系统中的上下文状态投毒
tldr: 代理系统依赖工具与持久内存，面临新型攻击：恶意工具描述和中毒内存可暗中偏转行为，根源在于缺少规划执行中上下文状态的可验证连续性。ElephantAgent协议强制该连续性，将安全关键子集定义为上下文状态，每次查询前通过摘要比对验证其完整性，并基于复制可信硬件构建线性化状态转换账本以检测篡改，另辅以历史追溯支持事后审计与回滚。该方案从源头阻断状态污染，提升代理系统在对抗环境下的可信性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01919v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1654, \"height\": 759, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01919v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 690, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01919v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1652, \"height\": 753, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01919v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1522, \"height\": 680, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01919v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 885, \"height\": 1322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01919v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 843, \"height\": 1613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01919v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1492, \"height\": 277, \"label\": \"Table\"}]"
motivation: 代理系统易受到工具和内存中毒攻击，缺乏上下文状态连续性验证，导致行为潜藏风险。
method: 通过定义安全关键上下文子集，每次查询前验证本地摘要与授权摘要，利用复制可信硬件维护线性化状态转换账本，并提供历史追溯。
result: 防御了上下文状态中毒，可检测带外篡改并支持有条件审计和恢复至此前已知良好状态。
conclusion: ElephantAgent为代理系统提供了可验证的上下文状态连续性，增强系统对抗恶意干扰的鲁棒性与安全性。
---

## 摘要
智能体系统通过调用外部工具和维护持久化记忆来增强自身能力。然而，这些外部依赖引入了新的攻击面。近期的工具和记忆中毒攻击表明，恶意构造的工具描述符和中毒记忆可以隐秘地偏置智能体行为。这些威胁反映了一个更深层的问题：在智能体的规划和执行过程中，其上下文状态缺乏可验证的连续性。我们提出 ElephantAgent，一种强制执行上下文状态连续性以防御上下文状态中毒的协议。受先前状态连续性机制（如 Nimble）的启发，ElephantAgent 将这种保护扩展到智能体系统不断演化的上下文状态。我们将上下文状态定义为智能体整个上下文中有限的、安全关键的子集（例如工具状态和记忆）。在处理每个查询之前，ElephantAgent 重新计算本地上下文状态的摘要，并与最新的授权摘要进行验证。利用复制的可信硬件，ElephantAgent 维护了一个可线性化的授权上下文状态转换分类账，并检测带外状态篡改。为了处理带内语义滥用，ElephantAgent 还提供了历史可追溯性，支持有条件的事后审计和恢复到已知良好的先前状态。

## Abstract
Agentic systems enhance their capabilities by invoking external tools and maintaining persistent memory. However, these external dependencies introduce novel attack surfaces. Recent tool and memory poisoning attacks show that maliciously crafted tool descriptors and poisoned memory can covertly bias agent behavior. These threats reflect a deeper issue: the lack of verifiable continuity in the agent's contextual state for planning and execution. We present ElephantAgent, a protocol that enforces Contextual State Continuity to defend against contextual state poisoning. Inspired by prior state-continuity mechanisms (e.g., Nimble), ElephantAgent extends this protection to the evolving contextual state of agentic systems. We define the contextual state as the bounded, security-critical subset of the agent's entire context (e.g., tool state and memory). Before processing each query, ElephantAgent recomputes the digest of the local contextual state and verifies it against the latest authorized digest. Using replicated trusted hardware, ElephantAgent maintains a linearizable ledger of authorized contextual state transitions and detects out-of-band state tampering. To handle in-band semantic abuse, ElephantAgent additionally provides Historical Traceability, enabling conditional post-hoc audit and recovery to a known-good prior state.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是对论文《ElephantAgent: Contextual State Continuity in Agentic Systems》的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**：现代智能体系统（如基于MCP或CLI的代理）通过调用外部工具和持久化记忆来增强能力。然而，这种对外部上下文（工具描述、交互历史）的依赖引入了全新的攻击面。
*   **核心问题**：论文指出现有系统缺乏**“上下文状态连续性”**。攻击者可以通过**带外篡改**（如“卷款跑路”式地修改已授权的工具描述符，或非法修改持久化记忆）来毒化智能体的上下文状态，从而隐蔽地操纵其规划和执行。这不仅是数据完整性问题，更是智能体决策基础的根本性破坏。
*   **整体含义**：为了解决这一深层缺陷，需要一种能够确保智能体所依赖的安全关键上下文状态具有可验证、线性化、防篡改演化历史的机制。

### 2. 论文提出的方法论

*   **核心思想**： **“上下文状态连续性”**。将智能体的行为基础——规划与执行所依赖的上下文——视为一个有状态的实体。只有其演化过程是连续、可验证且经过授权的，智能体的决策才是可信的。

*   **关键技术细节**：
    *   **上下文状态定义**：智能体整个上下文中一个有限的、安全关键的**确定性可编码子集**，具体包括：
        *   **工具状态 (T)**：已注册工具的元数据（名称、描述、模式等）。
        *   **记忆 (M)**：进一步细分为长期指令、问答记录、管理产出的工件。
    *   **系统组件**：
        1.  **上下文守卫 (CG)**：部署在客户端，运行于可信执行环境内。它作为“认知防火墙”，在每次查询前验证上下文状态的完整性。
        2.  **状态连续性模块 (SCM)**：一个远程、复制、TEE支持的状态服务，维护一个**线性化的、仅追加的上下文状态摘要分类账**。它确保状态转换序列单调、不可分叉、防篡改。
    *   **协议工作流程**：
        1.  **初始化**：用户批准初始上下文状态，CG计算其摘要并锚定在SCM的新分类账中。
        2.  **状态验证**：每次查询前，CG重新计算本地上下文状态摘要，并向SCM查询最新的授权摘要和证明。验证通过后，上下文才被释放给智能体进行规划。摘要计算使用碰撞抵抗的哈希函数，如`h = H(C)`。
        3.  **状态更新**：采用“先提交后最终化”的流程。查询执行后，CG**先将**新状态摘要提交至SCM并获取有效回执，**然后才**最终化本地的副作用（如更新记忆文件），确保更新与SCM授权记录一致。
    *   **安全属性**：
        *   **主要属性（上下文状态连续性）**：防御场景一和二，即**带外篡改**，通过摘要比对保证智能体总是在最新的、授信的状态上运行。
        *   **次要属性（历史可追溯性）**：应对场景三（**带内语义滥用**，如提示注入）。所有状态转换及其证明被记录在本地分类账中，支持事后审计以定位恶意步骤，并可从已知的干净状态快照进行恢复。

### 3. 实验设计

*   **数据集与场景**：为评估性能开销，而非检测率（因为防御是确定性的），实验设计了三个模拟真实任务的智能体工作流：
    *   **邮件分类**：处理邮件线程并生成摘要、行动项和草稿回复。
    *   **代码修Bug**：来自`QuixBugs`数据集，处理特定问题并执行仓库读写、报告撰写和补丁导出。
    *   **日历调度**：基于`CalDAVTester`的数据，加载事件并计算可用时间槽，生成调度工件。
*   **对比基准**：
    *   **基准模式 (`NoGuardAgent`)**：无CG和SCM的传统MCP工作流，运行于普通VM。
    *   **机密模式 (`ElephantAgent`)**：运行完整的ElephantAgent协议，所有组件部署于Intel TDX保护的VM。
    *   **消融研究（服务器验证模式, `AttestedSrvAgent`）**：仅MCP服务器运行于TDX VM并通过远程验证进行身份认证，主机端无保护。用于单独测量MCP Server端引入的TDX开销。

### 4. 资源与算力

*   **硬件配置**：实验在阿里云上的多台`ecs.g9i.xlarge`实例上进行，这些实例搭载了支持Intel TDX的**Intel Xeon 6982P-C CPU**。
*   **算力使用**：论文没有使用独立GPU进行计算。智能体规划使用的LLM（Qwen-Flash）是外部API调用。因此，**没有报告GPU型号、数量或特定训练/推理时长的算力消耗**。性能评估的核心是协议引入的CPU和网络延迟开销。

### 5. 实验数量与充分性

*   **实验数量**：
    *   对三个不同的智能体任务场景进行了评估。
    *   每个任务在基准模式、ElephantAgent模式和消融模式下各重复运行了**50次**。
    *   性能开销从系统初始化、数据加载、状态验证、状态更新和总任务时间等多个维度进行了详细拆解测量。
    *   进行了一项消融研究，单独评估了仅在服务器端进行TDX和远程验证的开销。
*   **充分性与公平性**：
    *   实验设计**合理且充分**。它聚焦于验证确定性防御机制的性能影响，避免了不具意义的检测率报告。
    *   通过精心选择耗时较短的任务，使得毫秒级的协议开销能够被清晰地观测到，而不被主要的LLM处理时间所淹没，体现了**设计的细致性**。
    *   对比实验设置清晰，分离了ElephantAgent和原子性服务器端TDX的开销，分析**较为公平和全面**。

### 6. 论文的主要结论与发现

*   **防御有效性**：ElephantAgent能够通过摘要校验和线性化状态历史，**确定性地检测并阻止**所有对受保护的上下文状态的带外篡改和回滚攻击。
*   **性能开销较小**：与无保护的`NoGuardAgent`相比，ElephantAgent在三个任务上的端到端总时间开销仅为**1.02x 到 1.04x**。每次查询的状态验证和更新延迟均在**毫秒级别**（验证约4.3ms，更新约5.3ms）。
*   **一次性初始化开销**：系统初始化（包括远程验证和SCM设置）时间约为**1.8秒**，是一次性的成本，可在后续大量查询中被平摊。
*   **可扩展性**：协议是独立于具体框架的附加组件，同样适用于CLI-based智能体系统，只需要更改本地交互接口。
*   **消融发现**：将MCP服务器隔离在TDX VM内并启用远程验证，会引入可见但适度（1.12x 到 1.22x）的工具调用延迟，对于安全关键型服务是可接受的。

### 7. 优点

*   **根本性安全改进**：从检测模式转向防御模式，通过强制执行上下文状态连续性，从根本上消除了多类隐蔽的攻击向量。
*   **形式化与安全保证**：对“上下文状态连续性”和“历史可追溯性”给出了清晰的形式化定义和安全证明，增强了方案的可靠性和严谨性。
*   **协议无关性与模块化设计**：ElephantAgent被设计为一个可附加的安全协议，其核心组件（CG和SCM）不依赖于特定智能体框架（如MCP），展示了良好的通用性。
*   **实用的TEE应用**：巧妙地利用VM级别TEE（Intel TDX）部署SCM和CG，避免了将复杂服务改造为SGX Enclave所需的特定工程化工作，提升了方案的可行性。
*   **细致的性能评估**：实验设计聚焦于防护机制本身的开销，通过选择轻量任务来高亮显示毫秒级延迟，并区分一次性成本和持续成本，分析非常务实。

### 8. 不足与局限

*   **恢复范围有限**：历史可追溯性及恢复机制仅保护了受控的上下文状态子集（工具状态和特定记忆），而非整个运行时状态。若要扩大保护范围，会增加性能开销。
*   **对LLM通信不安全**：协议不保护智能体与外部LLM服务之间的通信通道，若远程模型服务本身或链路被攻破，防护将失效。
*   **条件可追溯性依赖存储可用性**：带内滥用的事后审计和恢复完全依赖于本地分类账和快照的可用性。攻击者（作为不可信主机的控制者）可以删除这些密封的数据，导致审计和恢复失效，并使系统无法启动（fail-closed），形成一种可用性攻击。
*   **信任根的健壮性**：安全性的根基在于初始化时的“批准策略”。论文原型的简单策略（首次使用时信任）在实际应用中是薄弱的，生产部署需要更强的信任根建立机制（如签名验证）。
*   **恢复状态非最新**：恢复机制能确保恢复到历史上的一个干净状态，但不保证该状态是“新近”的，这可能发生在长时间未审计后才发现初始恶意行为的情况。

（完）
