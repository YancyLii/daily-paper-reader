---
title: Stateful Embedded Fuzzing with Peripheral-Accurate SystemC Virtual Prototypes
title_zh: 基于外设精确 SystemC 虚拟原型的有状态嵌入式模糊测试
authors: "Chiara Ghinami, Igor Pontes Tresolavy, Luis Seibt, Nils Bosbach, Rainer Leupers"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.19824v1"
tags: ["query:libfuz"]
score: 9.5
evidence: 针对嵌入式软件的自动化库模糊测试技术与框架
tldr: 针对嵌入式软件测试中外设行为模拟不准确的问题，本文提出了一种结合AFL++与SystemC-TLM虚拟原型的有状态模糊测试框架。该框架将模糊测试生成的输入直接注入外设模型，模拟中断和FIFO更新等真实副作用。实验表明，该方法在保持高效代码覆盖率的同时，消除了误报，显著提升了嵌入式系统预硅测试的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19824v1/fig-001.webp\", \"caption\": \"Figure 5: Code coverage comparison for the various tools.\", \"page\": 6, \"index\": 1, \"width\": 948, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.19824v1/fig-002.webp\", \"caption\": \"Figure 3: Injection for peer-to-peer communication.\", \"page\": 3, \"index\": 2, \"width\": 414, \"height\": 296}]"
motivation: 现有的嵌入式模糊测试方法在模拟外设真实行为与系统扩展性之间难以兼顾，导致测试准确性受限。
method: 将AFL++与具有状态感知的SystemC-TLM虚拟原型集成，通过直接向外设模型注入输入来触发真实的硬件副作用。
result: 在嵌入式工作负载上的测试显示，该方法在维持高性能和覆盖率的同时，成功消除了误报。
conclusion: 该框架通过结合全系统仿真与模糊测试，为嵌入式系统提供了一种更真实、有效的预硅测试方案。
---

## 摘要
嵌入式软件日益增长的复杂性使得全面的手动测试变得不切实际，从而推动了模糊测试等自动化技术的使用。像 AFL++ 这样的覆盖率引导模糊测试工具在传统软件中表现出色，但在外设行为起关键作用的嵌入式场景中，如何有效应用仍面临挑战。现有方法要么使用快速的用户模式模拟器，牺牲了外设的真实性；要么依赖于需要手动插桩的全系统模拟器，限制了在大规模软件中的适用性。在这项工作中，我们提出了一个新颖的框架，将 AFL++ 与有状态的 SystemC-TLM 虚拟原型集成，以实现对嵌入式软件的真实模糊测试。模糊测试工具生成的输入被直接注入到外设模型中，允许外设触发自然副作用，如中断和 FIFO 更新。通过将模糊测试与全系统模拟相结合，我们的框架提升了嵌入式系统硅前测试的有效性。在嵌入式工作负载上的结果表明，我们的方法消除了误报，同时保持了与最先进工具相当的代码覆盖率和执行性能。

## Abstract
The increasing complexity of embedded software has made comprehensive manual testing impractical, motivating the use of automated techniques such as fuzzing. Coverage-guided fuzzers like AFL++ have shown strong results for conventional software but remain challenging to apply effectively in embedded contexts, where peripheral behaviors play critical roles. Existing approaches either use fast user-mode simulators, sacrificing peripheral realism, or rely on full-system simulators with manual instrumentation, limiting applicability to large-scale software. In this work, we present a novel framework that integrates AFL++ with a stateful SystemC-TLM virtual prototype to enable realistic fuzzing of embedded software. Fuzzer-generated inputs are injected directly into peripheral models, allowing peripherals to trigger natural side effects such as interrupts and FIFO updates. By integrating fuzzing with full-system simulation, our framework advances the effectiveness of pre-silicon testing for embedded systems. Results on embedded workloads show that our approach eliminates false positives while maintaining comparable code coverage and execution performance as state-of-the-art tools.

---

## 论文详细总结（自动生成）

### 论文总结：基于外设精确 SystemC 虚拟原型的有状态嵌入式模糊测试

#### 1. 核心问题与研究动机
*   **研究背景**：嵌入式软件复杂度激增，手动测试已无法满足需求。模糊测试（Fuzzing）虽在通用软件中效果显著，但在嵌入式领域面临挑战，因为软件高度依赖外设行为（如中断、寄存器状态、FIFO更新）。
*   **核心问题**：现有的嵌入式模糊测试方法存在两极分化：
    *   **无状态/用户模式模拟（如 Fuzzware, P2IM）**：速度快，但缺乏真实的外设逻辑，导致大量误报（False Positives）。
    *   **全系统模拟**：虽然真实，但通常需要大量手动插桩或修改固件源码，难以扩展到复杂或闭源软件。
*   **研究目标**：开发一个既能保持外设行为精确性（有状态），又无需修改固件源码的自动化模糊测试框架。

#### 2. 方法论
*   **核心思想**：将 **AFL++** 模糊测试工具与基于 **SystemC-TLM** 的全系统虚拟原型（VP）集成。通过在虚拟总线上拦截内存映射输入/输出（MMIO）访问，将模糊测试生成的输入直接注入到外设模型中。
*   **关键技术细节**：
    *   **注入器（Injectors）**：连接外设模型与模糊测试工具的桥梁。支持点对点（UART/CAN）和主从式（I2C）通信。
    *   **探测组件（Probe）**：位于 CPU 和总线之间，监控寄存器访问。当检测到特定触发条件（如使能中断或轮询特定寄存器）时，启动注入器线程。
    *   **配置机制**：通过 JSON 文件配置注入规则和触发条件，无需硬编码，增强了对不同外设的适配能力。
    *   **反馈循环**：VP 实时收集代码覆盖率（基本块执行情况）并反馈给 AFL++，引导其生成能探索新路径的输入。

#### 3. 实验设计
*   **实验场景/数据集**：
    *   **裸机固件**：无人机（Drone）控制软件、双轮平衡机器人（Robot）固件。涉及 I2C、UART、定时器等外设。
    *   **RTOS 应用**：基于 Zephyr OS 的 UART 透传应用和 CAN 总线通信应用。
*   **Benchmark 与对比方法**：
    *   对比了两种最先进的无状态模糊测试工具：**Fuzzware** 和 **P2IM**。
    *   评估指标包括：执行速度（execs/s）、代码覆盖率（Edges count）、唯一崩溃数（Unique crashes）以及误报率。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Core i7-1255U (4.7 GHz)** 处理器的计算站上运行。
*   **算力说明**：论文未提及 GPU 使用情况（模糊测试主要依赖 CPU 逻辑运算和模拟器执行）。实验运行至代码覆盖率稳定为止（例如 Zephyr 任务在 10 分钟内完成了 6 万次执行）。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同的嵌入式工作负载进行了深入测试，涵盖了从简单驱动到复杂 OS 协议栈的范围。
*   **充分性评价**：
    *   **漏洞检测**：不仅通过人工注入漏洞（越界读取、除零错误）验证了检测能力，还发现了固件和驱动中预先存在的未知 Bug（如宏定义缺失导致的挂起、CAN 驱动错误丢弃帧）。
    *   **对比公平性**：在相同硬件和目标上运行对比工具，实验设计较为客观。

#### 6. 主要结论与发现
*   **消除误报**：由于使用了精确的外设模型，该框架实现了 **零误报**，而对比工具（如 Fuzzware）在某些任务中产生了数百个因模拟不准确导致的虚假崩溃。
*   **覆盖率表现**：在复杂协议（如 CAN）中，虽然覆盖率数值可能低于无状态工具（因为无状态工具会探索现实中不存在的非法路径），但在真实路径探索上表现更优。
*   **性能权衡**：全系统模拟带来了约 **2倍** 的性能开销（执行速度约为 Fuzzware 的一半），但换取了极高的诊断准确性。

#### 7. 优点
*   **高保真度**：通过 SystemC-TLM 模拟真实硬件副作用（如中断触发），解决了嵌入式模糊测试中最核心的“外设缺失”问题。
*   **非侵入性**：无需修改目标软件的源代码或进行手动插桩，适用于闭源固件测试。
*   **灵活性**：基于 JSON 的配置方式使得适配新外设变得简单，且支持多种通信协议。

#### 8. 不足与局限
*   **模拟开销**：全系统模拟的计算成本高于用户模式模拟，在大规模并行测试时可能面临吞吐量瓶颈。
*   **建模依赖**：虽然减少了手动插桩，但仍依赖于现有的 SystemC 外设模型库（如 VCML）。如果目标硬件极其特殊且缺乏模型，仍需一定的建模工作。
*   **配置自动化程度**：目前触发条件的设置（JSON）仍带有一定的人工经验成分，未来需进一步实现自动化提取外设规范。

（完）
