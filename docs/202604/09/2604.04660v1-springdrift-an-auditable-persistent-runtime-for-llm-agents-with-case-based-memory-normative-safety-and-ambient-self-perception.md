---
title: "Springdrift: An Auditable Persistent Runtime for LLM Agents with Case-Based Memory, Normative Safety, and Ambient Self-Perception"
title_zh: Springdrift：一种具有基于案例的记忆、规范性安全和环境自感知功能的可审计持久化大语言模型智能体运行时
authors: Seamus Brady
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04660v1"
tags: ["query:agsec"]
score: 9.0
evidence: 代理的规范性安全门控和可审计执行
tldr: Springdrift 是一个为长生命周期 LLM 智能体设计的持久化运行时系统。它集成了可审计的执行底层、基于案例推理的混合检索内存、确定性规范安全门控以及环境自我感知机制。该系统支持跨会话任务连续性和端到端决策取证，并提出了“人工侍从（Artificial Retainer）”概念，强调在持续关系中的自主性与问责制，旨在解决现有智能体缺乏长期记忆和可追溯性的问题。
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体通常受限于会话边界，缺乏长期任务连续性、跨渠道上下文维护以及可审计的决策取证能力。
method: 开发了基于 Gleam/Erlang 的持久化架构，集成追加式内存、混合检索内存层、规范演算安全门控和结构化的环境自我感知表示。
result: 在 23 天的实地部署中，智能体实现了自我诊断基础设施漏洞、跨邮件与网页渠道维护上下文，并能自主进行故障模式分类。
conclusion: Springdrift 证明了通过结合持久化内存、规范安全和自我感知架构，可以构建具备法医级可追溯性和领域自主性的长效智能体系统。
---

## 摘要
我们介绍了 Springdrift，这是一个面向长生命周期大语言模型（LLM）智能体的持久化运行时。该系统集成了一个可审计的执行基座（仅追加存储、受监督进程、基于 git 的恢复）、一个具有混合检索功能的基于案例推理的记忆层（已针对稠密余弦基准进行了评估）、一个用于安全门控且具有可审计公理轨迹的确定性规范演算，以及通过结构化自我状态表示（感觉中

## Abstract
We present Springdrift, a persistent runtime for long-lived LLM agents. The system integrates an auditable execution substrate (append-only memory, supervised processes, git-backed recovery), a case-based reasoning memory layer with hybrid retrieval (evaluated against a dense cosine baseline), a deterministic normative calculus for safety gating with auditable axiom trails, and continuous ambient self-perception via a structured self-state representation (the sensorium) injected each cycle without tool calls. These properties support behaviours difficult to achieve in session-bounded systems: cross-session task continuity, cross-channel context maintenance, end-to-end forensic reconstruction of decisions, and self-diagnostic behaviour. We report on a single-instance deployment over 23 days (19 operating days), during which the agent diagnosed its own infrastructure bugs, classified failure modes, identified an architectural vulnerability, and maintained context across email and web channels -- without explicit instruction. We introduce the term Artificial Retainer for this category: a non-human system with persistent memory, defined authority, domain-specific autonomy, and forensic accountability in an ongoing relationship with a specific principal -- distinguished from software assistants and autonomous agents, drawing on professional retainer relationships and the bounded autonomy of trained working animals. This is a technical report on a systems design and deployment case study, not a benchmark-driven evaluation. Evidence is from a single instance with a single operator, presented as illustration of what these architectural properties can support in practice. Implemented in approximately Gleam on Erlang/OTP. Code, artefacts, and redacted operational logs will be available at https://github.com/seamus-brady/springdrift upon publication.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Springdrift** 的持久化运行时系统，旨在为长生命周期（Long-lived）的大语言模型（LLM）智能体提供可审计性、记忆持久性和规范性安全保障。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：当前的 LLM 智能体大多受限于“会话边界”（Session-bounded），即对话结束或上下文溢出后，局部状态会被丢弃或压缩。这导致智能体缺乏长期任务的连续性、决策过程不可追溯、安全规则不透明且难以审计。
*   **整体含义**：论文认为，长效智能体不应仅仅是带有记忆插件的对话工具，而应被视为一个**可审计、自观察的操作系统**。作者提出了“**人工侍从（Artificial Retainer）**”的概念，将其类比为导盲犬或专业顾问：在特定领域内拥有自主权，但其行为必须受到规范约束且事后可进行法医级的取证审计。

### 2. 方法论：核心思想与关键技术
Springdrift 基于 **Gleam/Erlang/OTP** 构建，利用了电信级系统的高并发和容错特性。其核心架构包含四个关键组件：
*   **可审计执行基座**：采用“仅追加”（Append-only）的 JSONL 存储所有决策、工具调用和内存更改。结合 Git 进行状态备份，确保任何决策都可以通过回放日志进行端到端的法医重建。
*   **基于案例推理（CBR）的记忆层**：不同于传统的向量检索，CBR 将经验存储为“问题-方案-结果”结构。检索采用**混合策略**：结合了倒排索引（词法）、语义嵌入、字段权重、时效性、领域匹配和效用评分（基于历史成功率）。
*   **确定性规范演算（Normative Calculus）**：
    *   **$D'$ 评分**：借鉴信号检测理论，对行为的特征重要性和偏离程度进行量化评分（$D' = \sum(importance \times magnitude) / max$）。
    *   **斯多葛公理系统**：受 Becker 伦理学启发，将安全决策转化为确定性的逻辑推导。当 $D'$ 触发阈值时，系统通过 6 条公理（如“功能性”、“道德优先”等）和 8 条底线规则解析规范冲突，生成可审计的“公理轨迹”。
*   **环境自感知（Sensorium）**：每轮循环都会向智能体注入一个结构化的 XML 块（无需工具调用），包含时钟、系统状态、任务进度、近期成功率和“情感维度”（如挫败感、压力值），使智能体具备持续的自我运行意识。

### 3. 实验设计
论文并未采用传统的模型性能榜单（Benchmark）对比，而是通过以下方式验证系统：
*   **CBR 检索评估**：使用包含 800 个合成案例和 200 个查询的数据集，对比了“混合 CBR 检索”与“纯稠密向量检索（Cosine Baseline）”。
*   **规范演算验证**：对 14 个层级、84 种命题组合产生的 7,056 对冲突进行了全空间遍历，验证其确定性、覆盖率和单调性。
*   **实地部署案例研究**：记录了一个单实例、单操作员在 23 天（19 个工作日）内的运行日志，包含 494 条叙述条目和 2.4 万条循环日志。

### 4. 资源与算力
*   **模型使用**：主要使用了 **Claude 4.6 (Opus/Haiku)** 作为推理引擎。
*   **算力说明**：文中未提及具体的 GPU 训练算力，因为该研究侧重于运行时架构而非模型训练。系统本身运行在 Erlang 虚拟机（BEAM）上，具有极低的资源开销。
*   **代码规模**：约 62,000 行 Gleam 代码，包含 1,500 个测试用例。

### 5. 实验数量与充分性
*   **实验规模**：CBR 实验和规范演算的逻辑验证在统计上是充分的（覆盖了全命题空间）。
*   **局限性**：实地部署仅针对 **$n=1$**（单个用户、单个实例）。作者明确指出，这属于技术报告和案例研究，旨在展示架构的可能性，而非证明其在所有场景下的普适性。因此，在“实验充分性”方面，它缺乏多用户、多任务的大规模对照实验。

### 6. 主要结论与发现
*   **审计是信任的前提**：如果智能体的决策不可重建，那么无论其表现多好都无法获得长期信任。
*   **自诊断能力**：在部署中，智能体能够自主发现基础设施 Bug（如遥测数据丢失）、分类自身的失败模式，并识别出架构中的安全漏洞（如子智能体权限提升风险）。
*   **跨渠道连续性**：智能体能在无需显式指令的情况下，在邮件和网页会话之间保持上下文一致。

### 7. 优点
*   **工程严谨性**：选择 Erlang/OTP 架构为智能体提供了极强的容错能力（如自动重启崩溃的子进程）。
*   **透明度**：规范演算生成的“公理轨迹”让智能体的“拒绝理由”变得逻辑清晰，而非模糊的概率输出。
*   **创新的定位**：提出了“人工侍从”这一新范式，为 AI 伦理和人机协作提供了新的思考框架。

### 8. 不足与局限
*   **主观性**：规范演算中的命题层级和权重（如“道德”高于“法律”）是由开发者预设的，具有主观性。
*   **合成数据偏差**：CBR 评估使用的是合成数据集，可能无法完全反映真实世界中语言的复杂性和模糊性。
*   **存储压力**：由于采用“仅追加”模式且记录极细，长期运行会导致 JSONL 文件单调增长，文中未深入讨论大规模长期运行的存储清理机制。
*   **评估单一**：缺乏与其他主流智能体框架（如 LangGraph, AutoGen）在相同任务下的量化对比。

（完）
