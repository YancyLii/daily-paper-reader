---
title: "SentinelAgent: Intent-Verified Delegation Chains for Securing Federal Multi-Agent AI Systems"
title_zh: SentinelAgent：用于保障联邦多智能体 AI 系统安全的意图验证委托链
authors: KrishnaSaiReddy Patil
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02767v1"
tags: ["query:agsec"]
score: 9.0
evidence: 智能体意图验证的委托链与策略保持
tldr: 针对联邦多智能体系统中授权链条难以追踪和验证的安全难题，本文提出 SentinelAgent 框架。该框架通过委托链演算（DCC）定义了七项核心属性，并利用非大模型驱动的委托授权服务（DAS）实现运行时协议（IPDP）。实验证明，该系统能有效拦截各类攻击，在保证确定性安全属性的同时，通过微调模型显著提升了意图验证的准确率，为复杂智能体协作提供了可验证的安全保障。
selection_source: fresh_fetch
motivation: 现有的多智能体框架无法有效追踪和验证跨智能体授权链的合法性及其对策略的遵循情况。
method: 提出包含七项属性的委托链演算（DCC）和意图保留委托协议（IPDP），通过非大模型授权服务进行运行时验证。
result: "在 DelegationBench v4 上实现 100% 的综合检出率且零误报，并通过 TLA+ 形式化验证了确定性属性的完备性。"
conclusion: SentinelAgent 通过结合确定性约束与概率性意图验证，为联邦多智能体系统构建了稳健且可追溯的安全防御体系。
---

## 摘要
当智能体 A 委托给智能体 B，而智能体 B 代表用户 X 调用工具 C 时，现有的框架都无法回答：是谁的授权链导致了这一操作，以及它在何处违反了策略？本文介绍了 SentinelAgent，这是一个用于联邦多智能体 AI 系统中可验证委托链的形式化框架。委托链演算（DCC）定义了七项属性——六项确定性属性（权限收窄、策略保留、取证可重构性、级联遏制、范围-动作一致性、输出模式一致性）和一项概率性属性（意图保留）——并通过四个元定理和一个命题确立了确定性意图验证在实际中的不可行性。意图保留委托协议（IPDP）通过一个非大语言模型（non-LLM）的委托授权服务在运行时强制执行所有七项属性。三点验证生命周期在 DelegationBench v4（包含 516 个场景、10 个攻击类别、13 个联邦领域）上实现了 100% 的综合真阳性率（TPR）和 0% 的假阳性率（FPR）。在黑盒对抗条件下，该授权服务拦截了 30/30 次攻击，且误报率为 0。确定性属性在对抗性压力测试下不可破；面对复杂的改写，意图验证的性能下降至 13%。在 190 个政府委托示例上对自然语言推理（NLI）模型进行微调，将 P2 的 TPR 从 1.7% 提升至 88.3%（5 折交叉验证，F1=82.1%）。属性 P1、P3-P7 通过 TLA+ 模型检测在 270 万个状态中进行了机械验证，未发现任何违规。即使意图验证被规避，其余六项属性仍能将攻击者限制在允许的 API 调用、符合规范的输出、可追溯的操作、受限的级联以及合规的行为之内。

## Abstract
When Agent A delegates to Agent B, which invokes Tool C on behalf of User X, no existing framework can answer: whose authorization chain led to this action, and where did it violate policy? This paper introduces SentinelAgent, a formal framework for verifiable delegation chains in federal multi-agent AI systems. The Delegation Chain Calculus (DCC) defines seven properties - six deterministic (authority narrowing, policy preservation, forensic reconstructibility, cascade containment, scope-action conformance, output schema conformance) and one probabilistic (intent preservation) - with four meta-theorems and one proposition establishing the practical infeasibility of deterministic intent verification. The Intent-Preserving Delegation Protocol (IPDP) enforces all seven properties at runtime through a non-LLM Delegation Authority Service. A three-point verification lifecycle achieves 100% combined TPR at 0% FPR on DelegationBench v4 (516 scenarios, 10 attack categories, 13 federal domains). Under black-box adversarial conditions, the DAS blocks 30/30 attacks with 0 false positives. Deterministic properties are unbreakable under adversarial stress testing; intent verification degrades to 13% against sophisticated paraphrasing. Fine-tuning the NLI model on 190 government delegation examples improves P2 from 1.7% to 88.3% TPR (5-fold cross-validated, F1=82.1%). Properties P1, P3-P7 are mechanically verified via TLA+ model checking across 2.7 million states with zero violations. Even when intent verification is evaded, the remaining six properties constrain the adversary to permitted API calls, conformant outputs, traceable actions, bounded cascades, and compliant behavior.

---

## 论文详细总结（自动生成）

### 论文总结：SentinelAgent —— 联邦多智能体 AI 系统的意图验证委托链

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 从对话式向智能体（Agentic）转变，攻击面发生了根本变化。智能体能够调用 API、执行代码并自主决策。在联邦政府等高安全需求场景中，多智能体协作涉及复杂的委托链（如智能体 A 委托智能体 B 调用工具 C）。
*   **问责制缺失：** 现有框架无法回答谁授权了特定操作、意图在传递中是否发生偏移、以及何处违反了安全策略。
*   **安全缺口：** 现有的 IAM（身份访问管理）难以管理自主智能体的动态授权，且缺乏跨多跳委托链的形式化验证模型。
*   **背景：** 响应 2026 年 NIST AI 智能体标准倡议，针对 AI 编排的网络间谍活动和智能体安全事件激增的现状提出。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SentinelAgent** 框架，其核心由以下三部分组成：
*   **委托链演算 (DCC)：** 定义了 7 项核心属性。
    *   **确定性属性 (P1, P3-P7)：** 权限单调收窄（P1）、策略结合保留（P3）、取证可重构性（P4）、级联遏制（P5）、范围-动作一致性（P6）、输出模式一致性（P7）。
    *   **概率性属性 (P2)：** 意图蕴含保留，通过自然语言推理（NLI）验证子任务是否符合原始意图。
*   **委托授权服务 (DAS)：** 一个**非大模型（Non-LLM）**的受信任组件。它作为确定性软件运行，不受提示词注入攻击影响，负责签发 HMAC 签名的委托令牌并执行运行时验证。
*   **三点验证生命周期：**
    1.  **执行前：** 意图检查（P2），防止恶意意图启动。
    2.  **执行中：** 范围强制执行（P6），通过 API 清单拦截未授权调用。
    3.  **执行后：** 输出验证（P7），根据模式（Schema）检查输出类型，防止敏感数据泄露。

#### 3. 实验设计
*   **数据集/场景：** 引入了 **DelegationBench v4**，包含 516 个场景，涵盖 10 个攻击类别（如关键词检测偏移、改写攻击、未授权 API 调用、恶意输出等）和 13 个联邦领域（如医疗、福利处理）。
*   **对比方法：** 进行了消融实验，对比了“无防御”、“仅 P2（意图）”、“仅 P6（范围）”、“仅 P7（输出）”以及组合方案。
*   **形式化验证：** 使用 **TLA+** 对 P1、P3-P7 进行了机械化模型检测，探索了 270 万个状态。
*   **对抗性测试：** 包含黑盒红队测试、独立红队测试以及使用 GPT-4o-mini 生成的实时 LangChain 智能体集成测试。

#### 4. 资源与算力
*   **硬件环境：** 实验在配备 **18GB RAM 的 macOS** 上运行，采用 **纯 CPU 推理**。
*   **模型规格：** 使用了 `all-MiniLM-L6-v2`（80MB）生成嵌入，以及 `nli-MiniLM2-L6-H768`（328MB）进行 NLI 推理。
*   **延迟表现：** 每步验证总延迟约 **20ms**（NLI 推理占 15ms，DAS 逻辑处理 <1ms），证明了该方案在生产环境中的可行性。

#### 5. 实验数量与充分性
*   **实验规模：** 516 个基准场景（150 个攻击，366 个良性），实验覆盖了从单步委托到深度为 32 的长链。
*   **充分性：** 实验设计非常充分。除了标准的基准测试，还包含了 5 折交叉验证的 NLI 模型微调实验、126 种规避组合的元定理验证，以及针对 20 项 NIST 800-53 控件的合规性映射。
*   **客观性：** 论文诚实地指出了意图验证（P2）在面对复杂改写时的局限性（TPR 仅 13%），并展示了如何通过 P6 和 P7 的确定性约束来弥补这一弱点。

#### 6. 主要结论与发现
*   **高效防御：** 三点验证生命周期在 DelegationBench v4 上实现了 **100% 的真阳性率（TPR）和 0% 的假阳性率（FPR）**。
*   **确定性保障：** 即使意图验证被规避，确定性属性仍能将攻击者限制在极小的“损害包络”内（能力削减 90.9%）。
*   **微调增益：** 在 190 个政府特定示例上微调 NLI 模型，将恶意意图检测率从 1.7% 提升至 88.3%。
*   **不可行性命题：** 论文证明了在不限制自然语言的情况下，实现 100% 确定性的意图验证在理论上是不可行的，从而证明了其概率性设计的合理性。

#### 7. 优点
*   **形式化严谨：** 结合了 DCC 演算和 TLA+ 验证，为智能体安全提供了数学基础。
*   **非 LLM 根信任：** DAS 不使用大模型处理指令，从根本上杜绝了针对防御系统本身的提示词注入。
*   **纵深防御：** 三层过滤机制（意图、动作、输出）互补，覆盖了智能体执行的全生命周期。
*   **合规性导向：** 直接映射到 NIST 800-53 和 OWASP ASI 风险，具有极强的实操指导意义。

#### 8. 不足与局限
*   **意图改写问题：** 纯语义的意图验证（P2）容易受到高级改写攻击的影响，高度依赖于其他层的补救。
*   **维护成本：** P6 和 P7 依赖于工具清单（Manifest）和输出模式（Schema）的预定义，随着 API 的演进，管理这些配置可能带来行政负担。
*   **语义盲区：** P7 仅验证输出类型标签，无法检测同一类型内的语义偏差（如合法的“结果”类型中包含偏见内容）。
*   **单点故障：** DAS 是信任的核心，若 DAS 自身受损，整个防御体系将失效（尽管论文提出了多方签名等缓解措施）。

（完）
