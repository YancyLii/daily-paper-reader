---
title: "Crossing the NL/PL Divide: Information Flow Analysis Across the NL/PL Boundary in LLM-Integrated Code"
title_zh: 跨越 NL/PL 鸿沟：集成大语言模型代码中跨 NL/PL 边界的信息流分析
authors: "Zihao Xu, Xiao Cheng, Ruijie Meng, Yuekang Li"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28345v1"
tags: ["query:agsec"]
score: 8.0
evidence: LLM集成代码中跨自然语言/编程语言边界的信息流分析
tldr: "针对集成大语言模型（LLM）的代码中存在的自然语言与编程语言（NL/PL）边界导致程序分析失效的问题，本文提出了首个跨越该边界的信息流分析方法。研究者定义了包含24个标签的分类体系，涵盖信息保留程度和输出模态两个维度。通过对9,083个真实数据对进行标注，并应用于污点分析和程序切片，显著提升了分析精度和效率，为LLM集成程序的安全性与可靠性分析提供了理论基础。"
selection_source: fresh_fetch
motivation: 现有的程序分析工具无法处理LLM API调用中的不透明数据流，导致在NL/PL边界处出现信息流断裂。
method: 基于定量信息流理论，构建了一个包含24个标签的分类体系，从信息保留水平和输出模态两个维度刻画LLM输入输出的关系。
result: "该方法在污点传播任务中达到0.923的F1值，并在程序切片任务中平均减少了15%的切片大小。"
conclusion: 该分类体系能有效识别非传播路径，为构建更精准的LLM集成程序分析工具提供了可行的过滤标准。
---

## 摘要
LLM API 调用正成为一种普遍的程序结构，但它们创造了一个现有程序分析无法跨越的边界：运行时值进入自然语言提示词，在 LLM 内部经历不透明的处理，并以代码、SQL、JSON 或程序消耗的文本形式重新出现。所有跨函数边界追踪数据的分析（包括污点分析、程序切片、依赖分析和变更影响分析）都依赖于被调用者行为的数据流摘要。LLM 调用缺乏此类摘要，导致所有这些分析在我们所谓的 NL/PL 边界处失效。我们提出了首个弥合这一边界的信息流方法。基于定量信息流理论，我们的分类法沿两个正交维度定义了 24 个标签：信息保留级别（从词法保留到完全阻断）和输出模态（自然语言、结构化格式、可执行产物）。我们对来自 4,154 个真实 Python 文件的 9,083 个占位符-输出对进行了标注，并通过 Cohen's $κ= 0.82$ 和近乎完全的覆盖率（0.01% 不可分类）验证了可靠性。我们在两个下游应用中展示了该分类法的效用：(1) 一个结合了基于分类法的过滤与 LLM 验证的两阶段污点传播流水线，在 353 个专家标注对上实现了 $F_1 = 0.923$，并在 6 个真实 OpenClaw 提示词注入案例上的跨语言验证进一步证实了其有效性；(2) 基于分类法的后向切片在包含非传播占位符的文件中将切片大小平均减小了 15%。逐标签分析显示，四个“阻断”标签涵盖了几乎所有非传播情况，为工具开发者提供了可操作的过滤标准。

## Abstract
LLM API calls are becoming a ubiquitous program construct, yet they create a boundary that no existing program analysis can cross: runtime values enter a natural-language prompt, undergo opaque processing inside the LLM, and re-emerge as code, SQL, JSON, or text that the program consumes. Every analysis that tracks data across function boundaries, including taint analysis, program slicing, dependency analysis, and change-impact analysis, relies on dataflow summaries of callee behavior. LLM calls have no such summaries, breaking all of these analyses at what we call the NL/PL boundary.   We present the first information flow method to bridge this boundary. Grounded in quantitative information flow theory, our taxonomy defines 24 labels along two orthogonal dimensions: information preservation level (from lexically preserved to fully blocked) and output modality (natural language, structured format, executable artifact). We label 9,083 placeholder-output pairs from 4,154 real-world Python files and validate reliability with Cohen's $κ= 0.82$ and near-complete coverage (0.01\% unclassifiable). We demonstrate the taxonomy's utility on two downstream applications: (1)~a two-stage taint propagation pipeline combining taxonomy-based filtering with LLM verification achieves $F_1 = 0.923$ on 353 expert-annotated pairs, with cross-language validation on six real-world OpenClaw prompt injection cases further confirming effectiveness; (2)~taxonomy-informed backward slicing reduces slice size by a mean of 15\% in files containing non-propagating placeholders. Per-label analysis reveals that four blocked labels account for nearly all non-propagating cases, providing actionable filtering criteria for tool builders.

---

## 论文详细总结（自动生成）

### 论文总结：跨越 NL/PL 鸿沟——集成 LLM 代码中的信息流分析

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）API 成为现代程序（如 AutoGPT, LangChain）的核心组件，程序中出现了一个**NL/PL 边界**：运行时数据（占位符）进入自然语言提示词，经过 LLM 的黑盒处理，再以代码、SQL、JSON 或文本形式返回程序。
*   **痛点**：现有的程序分析工具（如污点分析、程序切片）依赖于明确的函数数据流摘要。由于 LLM 内部处理是不透明且非确定性的，传统工具在 LLM 调用处会发生断裂，导致要么产生大量误报（假设所有输出都受输入影响），要么产生漏报（忽略 LLM 输出的潜在风险）。
*   **目标**：建立首个系统化的模型，刻画跨越 NL/PL 边界的信息流，使程序分析工具能够理解 LLM 如何处理输入数据。

#### 2. 方法论：核心思想与技术细节
论文提出了一个基于**定量信息流（QIF）理论**的分类体系（Taxonomy），用于描述占位符与 LLM 输出之间的关系。
*   **核心维度**：
    1.  **信息保留水平（5级）**：从 L0（完全阻断/忽略）到 L4（词法保留/原样复制），中间涵盖信号提取（L1）、压缩（L2）和语义重写（L3）。
    2.  **输出模态**：区分自然语言、结构化格式（JSON/XML）和可执行产物（代码/SQL/命令）。
*   **24个细分标签**：将上述维度细化为 8 组标签（如“片段复制”、“二元决策”、“代码片段”等）。
*   **分析流水线**：
    1.  **调用点重构**：使用 LLM 作为分析代理，从 Python 源码中提取提示词模板，并推断占位符的可能运行时值。
    2.  **输出生成**：通过 LLM 生成真实的 API 响应，形成“占位符-提示词-输出”三元组。
    3.  **自动化标注**：利用高性能 LLM（如 GPT-5.2 Thinking）根据分类体系对海量数据对进行多标签标注。

#### 3. 实验设计
*   **数据集**：从 GitHub 抓取的 4,154 个真实 Python 文件，包含 9,083 个占位符-输出对。
*   **下游应用场景**：
    1.  **污点传播预测**：在 62 个包含危险 Sink（如 `exec`, `eval`, SQL 执行）的文件中，评估 353 个专家标注的“占位符-Sink”对。
    2.  **程序切片优化**：在 295 个文件上使用 CodeQL 进行后向切片实验。
    3.  **跨语言验证**：针对 TypeScript 框架 OpenClaw 的 6 个真实 CVE 漏洞进行测试。
*   **对比方法（Benchmark）**：
    *   **Propagate All**：基准方法，假设所有 LLM 返回值均为污点。
    *   **LLM Code-Only**：仅让 LLM 根据源码和输出判断风险。
    *   **Taxonomy Only**：仅根据分类标签进行确定性过滤。
    *   **C+ (Taxonomy + Verification)**：论文提出的两阶段流水线（先用标签过滤非传播项，再用 LLM 验证代码可达性）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **资源消耗**：主要开销在于调用 LLM API（GPT-5.2, Claude Opus 等）进行数据重构、标注和验证。由于该方法侧重于推理和静态分析辅助，不涉及大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：标注了超过 9,000 个数据对，规模较大。在安全验证中使用了 353 个专家标注样本，并涵盖了 5 种主要的 CWE 漏洞类型（代码注入、SQL 注入等）。
*   **充分性**：
    *   进行了**可靠性验证**：通过双人独立标注计算 Cohen's $κ$ 值（0.79-0.82），证明了分类体系的客观性。
    *   进行了**消融/对比实验**：对比了不同信息组合（仅代码、仅标签、标签+代码）的效果。
    *   **跨语言测试**：从 Python 扩展到 TypeScript，增强了结论的普适性。
*   **公平性**：使用了与标注模型不同的 SOTA 模型（Claude Opus）进行下游验证，避免了“自圆其说”的偏见。

#### 6. 主要结论与发现
*   **分类体系有效性**：仅 0.01% 的数据无法分类，证明了体系的完备性。约 15% 的占位符被识别为“阻断（L0）”，即不影响输出。
*   **安全分析提升**：提出的 C+ 流水线在污点预测上达到了 **0.923 的 F1 分数**，远高于 LLM 纯代码分析（0.779）。
*   **程序切片优化**：通过识别“非传播”占位符并将其设为分析屏障，在受影响文件中平均减少了 **15% 的切片代码行数**，最高减少 37.8%。
*   **核心发现**：将“LLM 是否使用该输入”与“输出是否到达 Sink”这两个问题解耦，是提升分析精度的关键。

#### 7. 优点
*   **首创性**：首次系统化地定义并量化了跨 NL/PL 边界的信息流。
*   **实用性强**：分类体系可以直接集成到 CodeQL、Semgrep 等现有静态分析工具中。
*   **两阶段设计**：巧妙地结合了确定性规则（分类过滤）和概率推理（LLM 验证），兼顾了效率和精度。
*   **真实性**：实验全部基于真实世界的开源代码和已知的 CVE 漏洞。

#### 8. 不足与局限
*   **LLM 依赖性**：分类标注过程仍依赖于高性能 LLM 的理解能力，若标注模型出错，后续分析会受影响。
*   **语言覆盖**：虽然有跨语言验证，但核心数据集仍集中在 Python，对于 C++ 或 Java 等静态类型更强的语言，其边界特性可能有所不同。
*   **切片缩减上限**：在许多简单的 LLM 应用中，由于占位符通常包含核心用户输入，能够被识别为“非传播”并剔除的代码比例受限于程序本身的结构。
*   **动态性挑战**：对于高度动态生成的提示词，静态重构可能无法完全捕捉所有运行时行为。

（完）
