---
title: "Credo: Declarative Control of LLM Pipelines via Beliefs and Policies"
title_zh: Credo：通过信念与策略实现大语言模型流水线的声明式控制
authors: "Duo Lu, Andrew Crotty, Uğur Çetintemel"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14401v1"
tags: ["query:agsec"]
score: 8.0
evidence: 用于规范Agent行为的声明式策略和语义控制平面
tldr: 针对现有Agent系统在长程决策中逻辑不透明且难以验证的问题，本文提出Credo框架。该框架将语义状态抽象为“信念”，并利用基于信念的“声明式策略”来规范系统行为。通过引入数据库支持的语义控制平面，Credo实现了无需修改底层代码即可动态调整模型选择、检索和纠错等关键执行环节，显著提升了AI系统的可审计性、组合性和自适应能力。
selection_source: fresh_fetch
motivation: 现有的Agent框架依赖命令式控制流和临时内存，导致系统在处理持续演化的复杂任务时行为不透明且难以维护。
method: 引入Credo框架，通过“信念”表示语义状态，并使用定义在信念之上的“声明式策略”来管理LLM流水线的执行逻辑。
result: 在决策控制实验中，Credo成功实现了动态的模型选择、检索和纠错执行，且无需对底层流水线代码进行任何修改。
conclusion: Credo通过声明式控制机制为LLM应用提供了更具适应性、可审计且易于组合的语义控制平面。
---

## 摘要
智能体 AI 系统在需要于不断演变的环境中进行长期、有状态决策的领域正变得日益普遍。因此，系统的正确性不仅取决于单次模型调用的输出，还取决于在整合新证据或修正先前结论时如何进行最佳调整。然而，现有框架依赖于命令式控制循环、瞬时记忆和嵌入提示词的逻辑，导致智能体行为不透明、脆弱且难以验证。本文介绍了 Credo，它将语义状态表示为信念（beliefs），并使用在这些信念上定义的声明式策略（policies）来规范行为。该设计通过数据库支持的语义控制平面，支持自适应、可审计且可组合的执行。我们在一个决策控制场景中展示了这些概念，其中信念和策略以声明方式引导关键的执行选择（例如模型选择、检索、纠错性重新执行），从而在无需更改底层流水线代码的情况下实现动态行为。

## Abstract
Agentic AI systems are becoming commonplace in domains that require long-lived, stateful decision-making in continuously evolving conditions. As such, correctness depends not only on the output of individual model calls, but also on how to best adapt when incorporating new evidence or revising prior conclusions. However, existing frameworks rely on imperative control loops, ephemeral memory, and prompt-embedded logic, making agent behavior opaque, brittle, and difficult to verify. This paper introduces Credo, which represents semantic state as beliefs and regulates behavior using declarative policies defined over these beliefs. This design supports adaptive, auditable, and composable execution through a database-backed semantic control plane. We showcase these concepts in a decision-control scenario, where beliefs and policies declaratively guide critical execution choices (e.g., model selection, retrieval, corrective re-execution), enabling dynamic behavior without requiring any changes to the underlying pipeline code.

---

## 论文详细总结（自动生成）

以下是对论文《Credo: Declarative Control of LLM Pipelines via Beliefs and Policies》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的 LLM 智能体（Agent）系统在处理复杂、长程决策任务时，通常依赖硬编码的命令式逻辑（如固定检索深度、固定模型选择）或不透明的提示词逻辑（如 ReAct 模式）。
*   **核心痛点**：
    *   **静态配置的低效性**：简单的查询使用昂贵模型会浪费资源，而复杂查询使用廉价配置则会导致失败。
    *   **不可审计性**：决策逻辑嵌入在代码或 Prompt 中，难以追踪、验证和动态调整。
*   **整体含义**：Credo 提出了一个**声明式控制平面**，将“逻辑流水线定义”与“物理执行决策”分离。它通过数据库支持的语义状态管理，使 LLM 流水线能够像数据库查询优化器一样，根据运行时状态动态调整执行策略。

### 2. 论文提出的方法论
Credo 框架由三个互补层组成，所有决策均持久化存储于数据库中：
*   **观察层（Beliefs - 信念）**：
    *   将语义状态抽象为“信念”，表示为 `(值, 置信度)` 对。
    *   **静态信念**：在执行前通过轻量级分类器（如 RouteLLM）评估查询复杂度。
    *   **动态信念**：在运行时根据中间结果提取（如使用 LLM 评估检索到的证据相关性）。
*   **决策层（Policies - 策略）**：
    *   使用声明式规则映射信念到执行参数。例如：`WHEN 复杂度 > 0.25 THEN 使用强力模型`。
    *   支持**算子重写**（修改参数）和**流水线重写**（使旧结果失效并触发重新执行）。
*   **执行层（Actions - 动作）**：
    *   包含检索（Retrieve）、生成（Generate）、验证（Verify）等内置算子。
    *   执行引擎根据策略动态绑定参数，实现反应式执行。

### 3. 实验设计
*   **数据集/场景**：使用了 **FinanceBench**，这是一个真实的财务问答基准，包含数百页的 PDF 财务报告，涉及数值查找、多步计算和跨文档推理。
*   **对比方法**：
    *   评估了 18 种固定配置的流水线，组合了 3 种模型规模（Gemma3-4B/12B/27B）、3 种提示策略（Predict/CoT/专家微调 CoT）和 2 种检索方法（Dense/BM25）。
*   **Benchmark 指标**：准确率（Accuracy） vs. 总成本（Total Cost）。

### 4. 资源与算力
*   **模型使用**：文中提到了使用 Google 的 **Gemma3** 系列模型（4B、12B、27B 参数版本）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于该文偏向系统架构展示（Demo 论文），重点在于框架的调度逻辑而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 FinanceBench 的 100 个任务进行了详尽的网格搜索式评估（18 种配置）。
*   **充分性**：
    *   实验充分证明了“检索策略优于模型规模”的观点（例如：4B 模型配合优质检索的准确率超过了 27B 模型配合差质检索）。
    *   作为一篇 4 页的 Demo 论文，其实验足以支撑其“动态适配优于静态配置”的核心论点。
    *   **局限性**：实验仅限于财务问答单一领域，缺乏在代码生成、创意写作等其他 Agent 场景下的泛化性验证。

### 6. 论文的主要结论与发现
*   **检索至上**：在 RAG 流水线中，检索方法的选择对准确率的影响（最高达 27% 的波动）远超模型规模的更迭。
*   **动态适配的必要性**：没有一种固定配置能同时兼顾所有任务的成本和准确率。
*   **声明式优势**：通过 Credo，用户可以在不修改底层代码的情况下，仅通过更新策略表就实现从“简单检索”到“纠错性重新检索”的动态行为转变。

### 7. 优点
*   **解耦设计**：实现了逻辑流与控制流的分离，极大提升了系统的可维护性。
*   **可审计性**：所有决策（信念值、触发的策略）都记录在数据库中，方便开发者调试和合规性检查。
*   **成本感知**：支持先运行廉价提取器，仅在必要时升级到昂贵 LLM 提取信念的阶梯式评估机制。

### 8. 不足与局限
*   **策略编写门槛**：目前策略仍需人工声明，对于复杂系统，如何自动发现和优化策略（Learned Policy）尚待研究。
*   **运行时开销**：频繁评估“信念”（尤其是使用 LLM 作为裁判时）会引入额外的延迟和成本，论文未深入探讨这种开销的平衡点。
*   **应用范围**：目前仅展示了顺序流水线的优化，对于具有复杂循环或并行分支的图状 Agent 工作流的支持尚不明确。

（完）
