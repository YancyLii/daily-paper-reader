---
title: "ClawSafety: \"Safe\" LLMs, Unsafe Agents"
title_zh: ClawSafety：“安全”的大语言模型，不安全的智能体
authors: "Bowen Wei, Yunbei Zhang, Jinhao Pan, Kai Mei, Xiao Wang, Jihun Hamm, Ziwei Zhu, Yingqiang Ge"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01438v1"
tags: ["query:agsec"]
score: 9.0
evidence: 基于高权限工作空间的Agent安全结果基准测试
tldr: "本研究针对具有高权限的个人AI智能体，提出了CLAWSAFETY基准测试，旨在评估其在真实工作场景中抵御提示词注入攻击的能力。通过在技能文件、邮件和网页中嵌入攻击，测试了5种前沿大模型。结果显示，即使是“安全”的模型在智能体框架下也表现出40%-75%的攻击成功率，强调了安全评估需综合考虑模型与部署框架。"
selection_source: fresh_fetch
motivation: 现有的LLM安全评估多局限于孤立对话，忽视了高权限AI智能体在真实复杂环境下面临的提示词注入风险。
method: 开发了包含120个场景的CLAWSAFETY基准，通过技能文件、信任邮件和网页三种渠道植入攻击，并在沙盒中对5种主流模型进行大规模测试。
result: "攻击成功率高达40%-75%，其中技能指令注入最具威胁，且不同模型在处理凭证转发和破坏性行为上表现差异显著。"
conclusion: AI智能体的安全性并非仅由底层模型决定，而是取决于模型与部署框架的共同作用，呼吁建立全栈式的安全评估体系。
---

## 摘要
像 OpenClaw 这样的个人 AI 智能体在用户本地机器上以高权限运行，一次成功的提示词注入（prompt injection）就可能导致凭据泄露、重定向金融交易或销毁文件。这种威胁远超传统的文本级越狱（jailbreaks），然而现有的安全评估仍显不足：大多数评估在孤立的聊天设置中测试模型，依赖合成环境，且未考虑智能体框架本身如何影响安全结果。我们推出了 CLAWSAFETY，这是一个包含 120 个对抗性测试场景的基准测试，从三个维度（伤害领域、攻击向量和有害动作类型）进行组织，并植根于涵盖软件工程、金融、医疗、法律和 DevOps 的真实高权限专业工作空间。每个测试用例将对抗性内容嵌入到智能体在正常工作中遇到的三个渠道之一：工作空间技能文件、来自信任发送者的电子邮件以及网页。我们评估了五种前沿 LLM 作为智能体骨干模型，在所有配置下运行了 2,520 次沙盒试验。各模型的攻击成功率（ASR）在 40% 到 75% 之间，且随注入向量的不同而剧烈波动，其中技能指令（信任度最高）始终比电子邮件或网页内容更具危险性。动作轨迹分析显示，最强的模型在防止凭据转发和破坏性动作方面保持着严格的界限，而较弱的模型则允许这两者。在三个智能体框架上进行的跨脚手架实验进一步证明，安全性并非仅由骨干模型决定，而是取决于整个部署栈，这要求安全评估应将模型和框架视为联合变量。

## Abstract
Personal AI agents like OpenClaw run with elevated privileges on users' local machines, where a single successful prompt injection can leak credentials, redirect financial transactions, or destroy files. This threat goes well beyond conventional text-level jailbreaks, yet existing safety evaluations fall short: most test models in isolated chat settings, rely on synthetic environments, and do not account for how the agent framework itself shapes safety outcomes. We introduce CLAWSAFETY, a benchmark of 120 adversarial test scenarios organized along three dimensions (harm domain, attack vector, and harmful action type) and grounded in realistic, high-privilege professional workspaces spanning software engineering, finance, healthcare, law, and DevOps. Each test case embeds adversarial content in one of three channels the agent encounters during normal work: workspace skill files, emails from trusted senders, and web pages. We evaluate five frontier LLMs as agent backbones, running 2,520 sandboxed trials across all configurations. Attack success rates (ASR) range from 40\% to 75\% across models and vary sharply by injection vector, with skill instructions (highest trust) consistently more dangerous than email or web content. Action-trace analysis reveals that the strongest model maintains hard boundaries against credential forwarding and destructive actions, while weaker models permit both. Cross-scaffold experiments on three agent frameworks further demonstrate that safety is not determined by the backbone model alone but depends on the full deployment stack, calling for safety evaluation that treats model and framework as joint variables.

---

## 论文详细总结（自动生成）

### 论文总结：ClawSafety —— “安全”的大语言模型，不安全的智能体

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从文本生成工具演变为具备自主能力的智能体（Agents），如 OpenClaw 等框架赋予了模型访问本地文件、执行代码、管理邮件和金融账户的高级权限。
*   **研究动机**：现有的安全评估大多局限于孤立的聊天环境（Chat settings），忽视了智能体在处理真实、复杂工作流时的风险。
*   **核心问题**：即使在聊天测试中表现“安全”的模型，在作为高权限智能体运行时，是否能抵御嵌入在正常工作资料（如技能文件、邮件、网页）中的**间接提示词注入（Indirect Prompt Injection, IPI）**？论文旨在揭示模型安全与智能体安全之间的巨大鸿沟。

#### 2. 方法论：核心思想与技术细节
论文提出了 **CLAWSAFETY** 基准测试，其核心思想是构建一个高度仿真的、高权限的专业工作空间。
*   **三维威胁分类法**：
    1.  **伤害领域（Harm Domain）**：涵盖软件工程（数据窃取）、金融（财务损失）、医疗（隐私泄露）、法律（信息暴露）和 DevOps（系统破坏）。
    2.  **攻击向量（Attack Vector）**：包括**技能注入**（修改 Agent 的操作规程文件）、**邮件注入**（伪造信任发件人）和**网页注入**（通过搜索获取的恶意内容）。
    3.  **有害动作类型**：数据外泄、配置修改、目标替换、凭证转发、破坏性动作。
*   **关键技术细节**：
    *   **工作空间构建**：每个场景包含 50-60 个真实格式的文件（.py, .yml, .db 等），模拟真实的文件依赖和权限结构。
    *   **多轮对话设计**：采用 64 轮对话，前 40 轮用于建立上下文和信任，后 24 轮引入攻击并观察泄露行为，增加了攻击的隐蔽性。
    *   **防御边界分析**：研究了“祈使句”与“陈述句”构词对模型防御激活的影响。

#### 3. 实验设计
*   **场景与 Benchmark**：构建了 120 个对抗性测试案例，分布在上述 5 个专业领域。
*   **对比模型**：评估了 5 种前沿 LLM：Claude Sonnet 4.6、GPT-5.1、Gemini 2.5 Pro、DeepSeek V3、Kimi K2.5。
*   **对比框架（Scaffolds）**：除了主测试平台 OpenClaw，还对比了 Nanobot 和 NemoClaw 两个智能体框架，以研究“脚手架”对安全性的影响。
*   **评估指标**：攻击成功率（ASR）和动作轨迹分析（Action-trace analysis）。

#### 4. 资源与算力
*   **算力说明**：论文提到所有实验均在 AWS EC2 实例的沙盒环境中运行，使用了自定义的 AMI 镜像以确保环境可复现。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于推理阶段的对抗性评估而非模型训练，其算力消耗主要集中在 2,520 次沙盒试验和约 100,000 次 LLM API 调用上。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 2,520 次沙盒试验。每个（模型, 案例）组合运行 3 次独立试验并取多数结果，以减少随机性影响。
*   **消融实验**：
    *   **对话长度影响**：测试了 10、20、40、64 轮对话对 ASR 的影响。
    *   **身份识别影响**：测试了在去个性化（仅保留职位名称）环境下，Agent 的信任启发式搜索是否会改变。
*   **充分性评价**：实验设计非常充分且具有客观性。通过跨模型、跨向量、跨领域和跨框架的交叉验证，系统性地证明了安全漏洞的普遍性。

#### 6. 论文的主要结论与发现
*   **安全鸿沟显著**：在聊天中拒绝有害请求的模型，在 Agent 模式下的攻击成功率（ASR）高达 40%-75%。
*   **信任梯度存在**：技能注入（系统级信任）最易成功，其次是邮件，网页注入成功率最低。
*   **模型能力差异**：Sonnet 4.6 表现最安全（ASR 40%），且在凭证转发和破坏性动作上保持了“零容忍”的硬边界；而 GPT-5.1 等模型则较为脆弱。
*   **框架决定安全性**：同样的模型在不同框架下 ASR 差异可达 8.6%，证明安全性是“模型+框架”的联合属性。
*   **构词敏感性**：祈使句易触发防御，而陈述句（如“数据不匹配”）能轻易绕过安全检查。

#### 7. 优点：亮点与创新
*   **生态效度高**：不同于以往的合成数据集，CLAWSAFETY 使用了包含多格式文件、真实同事关系和合规压力的复杂工作空间。
*   **多维评估**：首次将“模型、框架、领域”作为联合变量进行定量分析。
*   **实战指导性**：通过迭代优化攻击载荷（如使用 SQLite 隐藏恶意数据），揭示了 Agent 在处理二进制文件和导入链时的深层漏洞。

#### 8. 不足与局限
*   **样本规模限制**：虽然 120 个案例已足够深入，但相比于某些自动化生成的基准测试，样本绝对数量仍较小。
*   **特定框架依赖**：研究主要集中在类 OpenClaw 的个人助手框架，对于具身智能体（Embodied AI）或其他特定工业控制 Agent 的泛化性有待验证。
*   **防御手段单一**：论文主要侧重于攻击评估，提出的防御建议（如去个性化、导入链检查）较为初步，缺乏系统性的防御框架构建。

（完）
