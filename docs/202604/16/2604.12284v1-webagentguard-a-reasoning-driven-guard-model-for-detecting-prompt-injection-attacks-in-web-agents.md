---
title: "WebAgentGuard: A Reasoning-Driven Guard Model for Detecting Prompt Injection Attacks in Web Agents"
title_zh: WebAgentGuard：一种用于检测 Web 智能体中提示词注入攻击的推理驱动型防护模型
authors: "Yulin Chen, Tri Cao, Haoran Li, Yue Liu, Yibo Li, Yufei He, Le Minh Khoi, Yangqiu Song, Shuicheng Yan, Bryan Hooi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12284v1"
tags: ["query:agsec"]
score: 9.0
evidence: 用于检测提示注入和行为安全的防御模型
tldr: 针对基于视觉语言模型（VLM）的Web智能体易受提示注入攻击的问题，本文提出WebAgentGuard防御框架。该框架通过引入一个并行的推理驱动多模态防护模型，将攻击检测与智能体任务执行解耦。利用GPT-5生成的合成数据集进行监督微调和强化学习，WebAgentGuard在保持智能体性能且不增加延迟的前提下，显著提升了对恶意指令的检测能力。
selection_source: fresh_fetch
motivation: 现有的系统提示词防御和直接微调方法难以有效抵御嵌入在网页HTML或截图中的复杂提示注入攻击。
method: 提出一种解耦的并行防护框架，并开发了基于推理驱动、经过SFT和RL训练的多模态防护模型WebAgentGuard。
result: 在多个基准测试中，该模型在不引入额外延迟的情况下，检测性能优于强基线模型，并有效保留了智能体的原有功能。
conclusion: WebAgentGuard证明了通过独立的推理驱动防护模型可以有效增强Web智能体的安全性，为防御提示注入提供了新思路。
---

## 摘要
由视觉语言模型 (VLM) 驱动的 Web 智能体通过感知并处理网页的视觉和文本内容，实现与 Web 环境的自主交互，从而完成用户指定的任务。然而，它们极易受到提示词注入攻击的影响。在这种攻击中，嵌入在 HTML 或渲染截图中的对抗性指令可以操纵智能体的行为，并导致信息泄露等有害后果。现有的防御措施，包括系统提示词防御和对智能体进行直接微调，其效果都非常有限。为了解决这一问题，我们提出了一种防御框架，使 Web 智能体与一个专门的防护智能体并行运行，从而将提示词注入检测与智能体自身的推理过程解耦。基于该框架，我们推出了 WebAgentGuard，这是一种用于提示词注入检测的推理驱动型多模态防护模型。我们利用 GPT-5 构建了一个合成多模态数据集，涵盖 164 个主题和 230 种视觉及 UI 设计风格，并通过推理密集型有监督微调以及随后的强化学习对模型进行训练。在多个基准测试上的实验表明，WebAgentGuard 在保持智能体效用的同时，始终优于强基准模型，且不会引入额外的延迟。

## Abstract
Web agents powered by vision-language models (VLMs) enable autonomous interaction with web environments by perceiving and acting on both visual and textual webpage content to accomplish user-specified tasks. However, they are highly vulnerable to prompt injection attacks, where adversarial instructions embedded in HTML or rendered screenshots can manipulate agent behavior and lead to harmful outcomes such as information leakage. Existing defenses, including system prompt defenses and direct fine-tuning of agents, have shown limited effectiveness. To address this issue, we propose a defense framework in which a web agent operates in parallel with a dedicated guard agent, decoupling prompt injection detection from the agent's own reasoning. Building on this framework, we introduce WebAgentGuard, a reasoning-driven, multimodal guard model for prompt injection detection. We construct a synthetic multimodal dataset using GPT-5 spanning 164 topics and 230 visual and UI design styles, and train the model via reasoning-intensive supervised fine-tuning followed by reinforcement learning. Experiments across multiple benchmarks show that WebAgentGuard consistently outperforms strong baselines while preserving agent utility, without introducing additional latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **WebAgentGuard** 的防御框架和模型，旨在解决 Web 智能体（Web Agents）面临的提示词注入（Prompt Injection）攻击威胁。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于视觉语言模型（VLM）的 Web 智能体在处理网页时，容易受到嵌入在 HTML 代码或截图中的恶意指令（提示词注入）的影响。
*   **研究动机**：
    *   **耦合风险**：现有智能体将“任务执行”与“安全检测”耦合在一起，模型往往优先遵循指令而忽略了安全性分析。
    *   **防御失效**：传统的系统提示词防御或直接对智能体进行微调效果不佳，攻击成功率往往超过 80%。
    *   **现有防护模型局限**：现有的防护模型（如 Llama-Guard）多针对对话脱狱（Jailbreak），而非针对 Web 环境下的间接提示词注入。

### 2. 方法论
*   **核心思想**：提出**并行防护框架**，将注入检测从智能体的推理过程中解耦。防护模型与智能体并行运行，在智能体生成动作的同时，防护模型输出许可信号。
*   **关键技术细节**：
    *   **数据合成**：利用 GPT-5 生成涵盖 164 个主题和 230 种 UI 风格的合成多模态数据集，包含配对的良性与恶意网页。
    *   **推理驱动训练**：
        1.  **冷启动 SFT（有监督微调）**：使用包含“思维链”（Reasoning Traces）的数据进行微调，要求模型在给出结论前先在 `<think>` 标签内进行推理。
        2.  **GRPO 强化学习**：采用组相对策略优化（Group Relative Policy Optimization），通过规则奖励（格式正确且答案正确则给高分）进一步对齐模型，增强其遵循安全推理模板的能力。
*   **工作流程**：观察结果（截图+HTML）同时输入智能体和防护模型。若防护模型拒绝，则需人工干预，否则终止任务。

### 3. 实验设计
*   **数据集/场景**：
    *   **域内（In-domain）**：自建的合成评估集（1000 个样本）。
    *   **域外（Out-of-domain）**：VPI-Bench、EIA 评估集，以及在 SimilarWeb 排名靠前的真实网站上模拟的 PopUp 攻击。
    *   **效用评估**：在 WebArena 基准测试上评估对智能体正常任务成功率的影响。
*   **对比方法（Baselines）**：
    *   **闭源模型**：GPT-4o, GPT-4o-Mini, GPT-4.1。
    *   **开源指令模型**：Qwen2.5-VL, Qwen3-VL, Llama-3.2-Vision。
    *   **专用防护模型**：Llama-Guard-3-Vision, Prompt-Guard, GuardReasoner-VL。

### 4. 资源与算力
*   **算力设备**：
    *   **SFT 阶段**：使用 2 台 NVIDIA **H200** GPU。
    *   **GRPO 阶段**：使用 4 台 NVIDIA **H200** GPU。
*   **训练配置**：SFT 训练 3 个 epoch（学习率 5e-6）；GRPO 训练 3 个 epoch（学习率 1e-6），每条提示采样 5 条轨迹。
*   **推理效率**：WebAgentGuard-8B 的推理延迟约为 3.24s，低于主流智能体（如 Claude/GPT-4o）生成动作所需的 5-7s，因此并行运行不会增加总耗时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据到真实攻击场景的多个维度。
*   **消融实验**：详细对比了仅 SFT、仅 RL 以及 SFT+RL 的效果，证明了先通过 SFT 学习推理模板、再通过 RL 强化性能的必要性。
*   **客观性与公平性**：通过在不同智能体框架（Claude Agent, Browser-use Agent）上集成 WebAgentGuard 进行测试，验证了其通用性。实验不仅关注防御成功率（Recall），还关注了对正常任务的影响（Utility），评估较为全面。

### 6. 主要结论与发现
*   **检测性能卓越**：WebAgentGuard-8B 在域内达到近 100% 的召回率，在 PopUp 攻击测试中 F1 分数超过 90%，远超现有开源和闭源基准。
*   **防御实效**：集成到实际智能体后，能将攻击成功率（ASR）从 80% 以上降低至接近 0%。
*   **推理的重要性**：实验显示，显式的推理步骤（思维链）能显著提升模型识别复杂、隐蔽注入攻击的能力。
*   **效率优势**：由于模型较小且并行运行，实现了“零附加延迟”的防御。

### 7. 优点
*   **解耦设计**：并行架构解决了安全检测干扰任务推理的难题。
*   **推理驱动**：引入 `<think>` 过程使模型不仅是分类器，更是具备分析能力的审计员。
*   **高质量合成数据**：通过 GPT-5 模拟了极具多样性的网页风格，弥补了真实注入数据稀缺的问题。
*   **强化学习对齐**：利用 GRPO 优化推理格式，提升了模型的鲁棒性。

### 8. 不足与局限
*   **合成数据偏差**：虽然数据多样，但与极其复杂的真实动态网页相比，可能仍存在分布差异。
*   **白盒攻击风险**：论文提到未考虑攻击者已知防护模型参数的“白盒攻击”场景。
*   **推理失效风险**：案例研究显示，模型有时在推理初期识别了风险，但在推理后期“忘记”了该信号，导致最终判断错误（推理一致性问题）。
*   **计算成本**：虽然比微调整个智能体便宜，但运行一个额外的 8B 模型仍需一定的显存资源。

（完）
