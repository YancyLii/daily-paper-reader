---
title: "Architecting Secure AI Agents: Perspectives on System-Level Defenses Against Indirect Prompt Injection Attacks"
title_zh: 构建安全的 AI 智能体：关于针对间接提示词注入攻击的系统级防御的视角
authors: "Chong Xiang, Drew Zagieboylo, Shaona Ghosh, Sanjay Kariyappa, Kai Greshake, Hanshen Xiao, Chaowei Xiao, G. Edward Suh"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.30016v1"
tags: ["query:agsec"]
score: 9.0
evidence: 针对提示注入的系统级防御和安全策略更新
tldr: 本文针对AI智能体面临的间接提示注入攻击，提出了系统级防御的愿景。研究指出，仅靠模型鲁棒性不足以应对复杂威胁，必须通过系统架构设计来实现安全。文章提出了三大核心立场：动态重规划与策略更新、受限环境下的模型安全决策、以及引入个性化与人机交互解决歧义，旨在为构建安全的智能体系统提供理论框架和设计指导。
selection_source: fresh_fetch
motivation: 旨在解决AI智能体因处理不可信数据而易受间接提示注入攻击的安全隐患。
method: 提出结合动态任务重规划、受限的模型决策机制以及以人为中心的交互设计等系统级防御策略。
result: 分析了现有安全基准的局限性，并论证了系统级防御在控制智能体行为和整合多层安全检查中的必要性。
conclusion: 系统级防御是安全AI智能体的核心架构，能有效弥补单一模型防御的不足并引导更具针对性的安全研究。
---

## 摘要
AI 智能体（主要由大语言模型 (LLM) 驱动）容易受到间接提示词注入的影响，其中嵌入在不可信数据中的恶意指令可能会触发危险的智能体行为。本立场论文讨论了我们对针对间接提示词注入攻击的系统级防御的愿景。我们阐述了三个立场：(1) 对于动态任务和现实环境，动态重新规划和安全策略更新通常是必要的；(2) 某些依赖上下文的安全决策仍需要 LLM（或其他学习模型），但应仅在严格限制模型观察和决策范围的系统设计中进行；(3) 在本质上存在歧义的情况下，个性化和人类交互应被视为核心设计考量。除了主要立场外，我们还讨论了现有基准测试的局限性，这些局限性可能会产生效用和安全性的错觉。我们还强调了系统级防御的价值，它们通过结构化和控制智能体行为、集成基于规则和基于模型的安全检查，并支持针对模型鲁棒性和人类交互的更具针对性的研究，从而充当智能体系统的骨架。

## Abstract
AI agents, predominantly powered by large language models (LLMs), are vulnerable to indirect prompt injection, in which malicious instructions embedded in untrusted data can trigger dangerous agent actions. This position paper discusses our vision for system-level defenses against indirect prompt injection attacks. We articulate three positions: (1) dynamic replanning and security policy updates are often necessary for dynamic tasks and realistic environments; (2) certain context-dependent security decisions would still require LLMs (or other learned models), but should only be made within system designs that strictly constrain what the model can observe and decide; (3) in inherently ambiguous cases, personalization and human interaction should be treated as core design considerations. In addition to our main positions, we discuss limitations of existing benchmarks that can create a false sense of utility and security. We also highlight the value of system-level defenses, which serve as the skeleton of agentic systems by structuring and controlling agent behaviors, integrating rule-based and model-based security checks, and enabling more targeted research on model robustness and human interaction.

---

## 论文详细总结（自动生成）

这是一份关于论文《Architecting Secure AI Agents: Perspectives on System-Level Defenses Against Indirect Prompt Injection Attacks》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何构建既能高效完成复杂任务（高实用性），又能抵御间接提示注入攻击（IPI，即恶意指令隐藏在外部数据中）的 AI 智能体系统。
*   **研究背景**：当前的 AI 智能体主要依赖大语言模型（LLM），极易受到 IPI 攻击。现有的模型级防御（微调）或文本级防御（提示词加固）往往在灵活性或安全性上存在短板。
*   **整体含义**：本文是一篇**立场论文（Position Paper）**，旨在从系统架构的角度提出一套防御愿景，强调通过系统级的设计（而非单纯依赖模型鲁棒性）来构建安全骨架。

### 2. 方法论：核心思想与关键技术细节
论文提出了一个包含六大组件的通用安全架构，并阐述了三大核心立场：
*   **核心架构组件**：
    1.  **编排器 (Orchestrator)**：生成初始计划和安全策略。
    2.  **计划/策略审批器**：监督生成过程，必要时引入人类干预。
    3.  **执行器 (Executor)**：将计划转化为具体动作（如工具调用）。
    4.  **策略执行器 (Policy Enforcer)**：根据当前策略批准或拦截动作。
    5.  **环境 (Environment)**：执行动作并返回反馈。
    6.  **反馈循环**：允许根据环境反馈动态更新计划和策略。
*   **三大核心立场**：
    *   **立场 1：动态重规划与策略更新是必需的**。静态的“计划-执行隔离”虽然安全但过于脆弱，无法处理 API 变更或调试等动态任务。系统必须允许根据环境反馈更新策略，但需识别更新是否受恶意数据驱动。
    *   **立场 2：受限环境下的模型决策**。复杂的安全决策（如判断 API 变更是否合理）仍需 LLM 的语义能力，但**严禁 LLM 直接接触原始外部文本**。应将输入转化为结构化构件（如计划差异对比），限制模型的观察和决策范围。
    *   **立场 3：引入个性化与人机交互**。对于语义歧义（如定义什么是“紧急邮件”）和目标对齐问题，算法无法完美解决，必须通过可用的安全交互设计引入人类判断。

### 3. 实验设计
*   **性质**：本文属于理论探讨与架构设计，**未进行传统意义上的大规模基准测试实验**。
*   **案例研究 (Case Studies)**：通过具体的场景模拟（如：API 弃用导致的重规划、Slack 邀请任务中的最小权限原则、GitHub Issue 修复中的恶意代码执行风险）来论证其立场的合理性。
*   **基准测试批判**：论文对比并分析了现有基准（如 **AgentDojo, AgentDyn, InjecAgent**）的局限性，指出它们缺乏多步复杂任务和自适应攻击者。

### 4. 资源与算力
*   **说明**：文中**未明确说明**使用了多少算力或具体的 GPU 型号。由于是立场论文，其重点在于架构论证而非模型训练或大规模推理性能测试。

### 5. 实验数量与充分性
*   **充分性评价**：作为立场论文，其论证主要基于逻辑推演和对现有防御方案（如 CaMeL, Progent, DRIFT 等）的架构对比。
*   **客观性**：作者客观地承认了 LLM 判别器本身也可能被操纵的风险，并据此提出了“受限输入”的改进方案。虽然缺乏量化实验，但其对现有防御缺陷的分析具有较强的说服力。

### 6. 主要结论与发现
*   **系统级防御是骨架**：它通过结构化智能体行为、集成规则与模型检查，为安全提供了透明度和可解释性。
*   **防御深度**：单纯的模型鲁棒性研究是不够的，必须通过系统设计来缩小攻击面（例如：解耦指令识别与执行决策）。
*   **基准测试误导性**：现有的安全基准测试由于任务过于简单且攻击载荷静态，可能会给人一种“系统已安全”的错觉。

### 7. 优点
*   **架构前瞻性**：提出了“系统-模型-人类”协同设计的思路，为未来安全智能体的开发提供了清晰的蓝图。
*   **实用性导向**：不主张为了绝对安全而牺牲智能体的动态适应能力，而是寻求在动态性中建立安全边界。
*   **解耦思想**：提出将“识别指令”与“决定是否遵循指令”分离，这是一个非常实用的工程防御思路。

### 8. 不足与局限
*   **缺乏实证数据**：论文提出的具体防御机制（如“指令口述”和“结构化验证器”）尚未在真实大规模攻击场景下给出量化的防御成功率。
*   **实施复杂度**：动态策略更新和频繁的人机交互可能在实际应用中带来较高的延迟和用户体验挑战。
*   **人类因素风险**：虽然强调了人机交互，但如何防止用户在疲劳时产生“点击疲劳”而误批恶意操作，文中未深入探讨。

（完）
