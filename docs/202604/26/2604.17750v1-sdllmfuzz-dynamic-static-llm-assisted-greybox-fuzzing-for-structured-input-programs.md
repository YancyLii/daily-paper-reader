---
title: "SDLLMFuzz: Dynamic-static LLM-assisted greybox fuzzing for structured input programs"
title_zh: SDLLMFuzz：针对结构化输入程序的动静态大语言模型辅助灰盒模糊测试
authors: "Yihao Zou, Tianming Zheng, Futai Zou, Yue Wu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17750v1"
tags: ["query:libfuz"]
score: 9.0
evidence: LLM辅助的结构化输入程序灰盒模糊测试
tldr: 针对结构化输入程序模糊测试中语法约束严、语义感知弱的问题，本文提出SDLLMFuzz框架。该框架结合大语言模型（LLM）的结构感知种子生成能力与静态崩溃分析，构建了动静结合的反馈闭环。通过利用LLM生成高质量输入并从崩溃工件中提取语义信息指导后续测试，显著提升了复杂程序行为的探索效率，为漏洞挖掘提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17750v1/fig-001.webp\", \"caption\": \"Figure 5: Time-to-Bug curves\", \"page\": 11, \"index\": 1, \"width\": 850, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.17750v1/fig-002.webp\", \"caption\": \"Figure 6: Bug discovery in ablation study.\", \"page\": 11, \"index\": 2, \"width\": 850, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.17750v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the SDLLMFuzz framework.\", \"page\": 7, \"index\": 3, \"width\": 683, \"height\": 738}, {\"url\": \"assets/figures/arxiv/2604.17750v1/fig-004.webp\", \"caption\": \"Figure 4: Time-to-Bug heatmap\", \"page\": 10, \"index\": 4, \"width\": 850, \"height\": 742}, {\"url\": \"assets/figures/arxiv/2604.17750v1/fig-005.webp\", \"caption\": \"Figure 7: Time-to-Bug in ablation study.\", \"page\": 12, \"index\": 5, \"width\": 850, \"height\": 744}, {\"url\": \"assets/figures/arxiv/2604.17750v1/fig-006.webp\", \"caption\": \"Figure 2: Bug discovery results within 24 hours.\", \"page\": 9, \"index\": 6, \"width\": 850, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2604.17750v1/fig-007.webp\", \"caption\": \"Figure 3: Bug discovery results within 48 hours.\", \"page\": 9, \"index\": 7, \"width\": 850, \"height\": 281}]"
motivation: 传统模糊测试难以生成符合复杂语法要求的有效输入，且现有LLM辅助方法未能充分利用运行时反馈来指导测试。
method: 整合LLM结构感知种子生成与静态崩溃分析，利用崩溃工件提取语义信息，形成迭代优化输入的动静结合反馈机制。
result: 在Magma基准测试中，SDLLMFuzz在漏洞发现数量和发现速度上均显著优于传统模糊测试工具及现有的LLM辅助基准。
conclusion: 将语义化输入生成与反馈驱动的精细化改进相结合，能有效增强结构化输入程序的模糊测试性能。
---

## 摘要
模糊测试已成为一种被广泛采用的漏洞发现技术，但由于严格的语法约束和有限的语义感知，它在处理结构化输入程序时仍然效果不佳。传统的灰盒模糊测试工具依赖于基于变异的策略和粗粒度的覆盖率反馈，这往往无法生成有效的输入并探索深层执行路径。大语言模型（LLM）的最新进展在改进输入生成方面展现出潜力，但现有方法主要集中在种子生成上，在很大程度上忽视了对运行时反馈的有效利用。在本文中，我们提出了 SDLLMFuzz，这是一种针对结构化输入程序的动静态 LLM 辅助灰盒模糊测试框架。我们的方法将基于 LLM 的结构感知种子生成与静态崩溃分析相结合，形成了一个统一的反馈循环，从而迭代地优化测试输入。具体而言，我们利用 LLM 生成语法有效且语义多样的输入，同时从崩溃伪像（如核心转储和执行轨迹）中提取丰富的语义信息，以指导后续的输入生成。这种动静态反馈机制能够更高效地探索复杂的程序行为。我们在 Magma 基准测试集上对 SDLLMFuzz 进行了评估，涵盖了包括 libxml2、libpng 和 libsndfile 在内的多个结构化输入程序。实验结果表明，SDLLMFuzz 在漏洞发现和漏洞发现时间（time-to-bug）方面显著优于传统的灰盒模糊测试工具和 LLM 辅助的基准方法。这些结果证明，将语义输入生成与反馈驱动的优化相结合，是提高结构化输入程序模糊测试性能的一个有效方向。

## Abstract
Fuzzing has become a widely adopted technique for vulnerability discovery, yet it remains ineffective for structured-input programs due to strict syntactic constraints and limited semantic awareness. Traditional greybox fuzzers rely on mutation-based strategies and coarse-grained coverage feedback, which often fail to generate valid inputs and explore deep execution paths. Recent advances in large language models (LLMs) have shown promise in improving input generation, but existing approaches primarily focus on seed generation and largely overlook the effective use of runtime feedback. In this paper, we propose SDLLMFuzz, a dynamic-static LLM-assisted greybox fuzzing framework for structured-input programs. Our approach integrates LLM-based structure-aware seed generation with static crash analysis, forming a unified feedback loop that iteratively refines test inputs. Specifically, we leverage LLMs to generate syntactically valid and semantically diverse inputs, while extracting rich semantic information from crash artifacts (e.g., core dumps and execution traces) to guide subsequent input generation. This dynamic-static feedback mechanism enables more efficient exploration of complex program behaviors. We evaluate SDLLMFuzz on the Magma benchmark across multiple structured-input programs, including libxml2, libpng, and libsndfile. Experimental results show that SDLLMFuzz significantly outperforms traditional greybox fuzzers and LLM-assisted baselines in terms of bug discovery and time-to-bug. These results demonstrate that combining semantic input generation with feedback-driven refinement is an effective direction for improving fuzzing performance on structured-input programs.

---

## 论文详细总结（自动生成）

这是一份关于论文《SDLLMFuzz: Dynamic-static LLM-assisted greybox fuzzing for structured input programs》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的模糊测试（Fuzzing）在处理**结构化输入程序**（如 XML、PNG、音频解析器）时效率低下。
*   **研究动机**：
    *   **语法约束严**：结构化程序要求输入必须符合复杂的语法和语义规则，随机变异极易导致输入无效，无法进入程序深层逻辑。
    *   **反馈机制单一**：传统灰盒模糊测试主要依赖代码覆盖率反馈，缺乏对程序运行状态（尤其是崩溃现场）的深度语义理解。
    *   **LLM 利用不充分**：现有的大语言模型（LLM）辅助测试多用于初始种子生成，忽略了如何利用运行时反馈（如 Crash 信息）来迭代优化输入。

### 2. 方法论
SDLLMFuzz 提出了一个**动静态结合的闭环反馈框架**，核心流程如下：
*   **LLM 引导的结构化种子生成**：利用 LLM 的语义理解能力，结合格式文档和初始种子，生成符合语法约束且具备语义多样性的初始测试用例。
*   **灰盒模糊测试执行**：使用 AFL++ 作为执行引擎，进行高通量的输入变异与执行，收集覆盖率和崩溃（Crash）信号。
*   **静态崩溃分析**：当程序崩溃时，利用 GDB 等工具分析核心转储（Core Dump），提取崩溃位置、调用栈、异常类型和内存访问模式。
*   **崩溃反馈编码**：将底层的调试信息转化为 LLM 可理解的结构化语义描述，并将其作为 Prompt 喂回给 LLM。
*   **动静态反馈闭环**：LLM 根据崩溃反馈调整生成策略，生成更有针对性的输入以探索诱发崩溃的深层路径。
*   **关键公式**：引入覆盖率感知种子选择机制，通过公式 $Score(seed) = \alpha \cdot \Delta Edge + \beta \cdot \Delta Block$ 计算种子得分，优先处理对边缘和基本块覆盖贡献大的种子。

### 3. 实验设计
*   **基准测试集（Benchmark）**：选用 **Magma**。它通过在真实开源项目（如 libxml2, libpng, libsndfile, openssl, sqlite3）中注入真实漏洞，并提供精确的触发条件（Canaries），比单纯的覆盖率更具客观性。
*   **对比方法（Baselines）**：
    1.  **AFL++**：标准的灰盒模糊测试基准。
    2.  **LLM Seed + AFL++**：仅使用 LLM 生成种子，但不包含反馈循环。
    3.  **SDLLMFuzz**：本文提出的完整框架。
*   **评估指标**：漏洞覆盖数（Bug Coverage）、漏洞发现时间（Time-to-Bug）、边缘覆盖率（Edge Coverage）。

### 4. 资源与算力
*   **硬件环境**：实验在 Ubuntu 22.04 虚拟机上运行，配置为 **4 核 CPU 和 16GB 内存**。
*   **算力说明**：论文**未明确说明**具体使用了哪种 LLM 模型（如 GPT-4 或 Llama 系列）以及推理过程消耗的具体 GPU 算力或 API 调用成本。这是文中较为模糊的部分。

### 5. 实验数量与充分性
*   **实验规模**：分别进行了 **24 小时**和 **48 小时**的长时间运行测试，并针对每个目标程序进行了多次重复实验以消除随机性影响。
*   **消融实验**：设计了专门的消融实验，分别移除“LLM 种子生成”、“静态崩溃反馈”和“变异优化”组件，验证了各模块的贡献。
*   **充分性评价**：实验设计较为充分，涵盖了多种不同类型的结构化输入程序，且使用了 Magma 这一公认的高质量基准，结果具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **性能卓越**：SDLLMFuzz 在漏洞发现数量上显著超过 AFL++ 和无反馈的 LLM 方法。
*   **收敛更快**：在 Time-to-Bug 指标上表现优异，能够更早地触发复杂漏洞。
*   **反馈有效**：实验证明，将 Crash 信息编码回 LLM 能够有效引导模型避开“局部最优”的测试空间，探索更深的执行路径。

### 7. 优点与亮点
*   **闭环设计**：首次将静态崩溃分析与 LLM 语义生成深度整合，形成动态执行与静态分析的闭环。
*   **结构感知**：解决了传统模糊测试在处理 XML/PNG 等格式时“变异即破坏结构”的痛点。
*   **语义反馈**：将低级调试信息转化为高级语义 Prompt，充分发挥了 LLM 的推理能力。

### 8. 不足与局限
*   **计算开销**：引入 LLM 推理和静态分析会显著增加单次迭代的开销，论文未详细讨论其吞吐量损失。
*   **模型依赖**：框架的效果高度依赖于 LLM 的 Prompt 设计和模型本身的性能，存在一定的随机性和不可控性。
*   **状态建模不足**：目前主要关注 Crash 反馈，对于没有导致崩溃但具有潜在风险的程序状态（如逻辑错误）建模较少。
*   **算力细节缺失**：未披露 LLM 部署或调用的具体成本，难以评估其在工业界大规模部署的经济性。

（完）
