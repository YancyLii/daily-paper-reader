---
title: "TraceSafe: A Systematic Assessment of LLM Guardrails on Multi-Step Tool-Calling Trajectories"
title_zh: TraceSafe：对多步工具调用轨迹中大语言模型防护栏的系统性评估
authors: "Yen-Shan Chen, Sian-Yao Huang, Cheng-Lin Yang, Yun-Nung Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07223v1"
tags: ["query:agsec"]
score: 9.0
evidence: 对多步工具调用轨迹中大模型护栏的评估
tldr: 随着大语言模型向自主智能体演进，安全风险从最终输出转向中间执行轨迹。本文提出首个针对多步工具调用轨迹的安全评估基准 TraceSafe-Bench，涵盖12类风险。通过对20种护栏模型的评估，揭示了结构化数据处理能力对安全检测的关键作用，并发现通用模型在轨迹分析上优于专用护栏，为保障智能体工作流安全提供了新见解。
selection_source: fresh_fetch
motivation: 现有的安全护栏主要针对自然语言响应，缺乏对多步工具调用中间执行轨迹安全性的系统评估。
method: 提出 TraceSafe-Bench 基准，包含12类风险和1000多个实例，用于评估13个通用模型和7个专用护栏在轨迹中的风险检测能力。
result: 护栏效能主要受结构化数据处理能力驱动而非语义对齐，且通用大模型在轨迹分析表现上优于专用安全护栏。
conclusion: 保障智能体工作流安全需要同时优化结构化推理与安全对齐，以有效应对执行过程中的动态风险。
---

## 摘要
随着大语言模型（LLMs）从静态聊天机器人演变为自主智能体，主要的漏洞表面已从最终输出转向中间执行轨迹。虽然安全防护栏在自然语言响应方面已有成熟的基准测试，但其在多步工具使用轨迹中的有效性在很大程度上仍未得到探索。为了填补这一空白，我们推出了 TraceSafe-Bench，这是首个专门用于评估轨迹中途安全性的全面基准。它涵盖了 12 个风险类别，从安全威胁（如提示词注入、隐私泄露）到操作故障（如幻觉、接口不一致），包含 1,000 多个独特的执行实例。我们对 13 个“LLM 作为防护栏”模型和 7 个专门的防护栏进行了评估，得出了三个关键发现：1) 结构性瓶颈：防护栏的有效性更多地取决于结构化数据处理能力（如 JSON 解析），而非语义安全对齐。其性能与结构化到文本基准高度相关（ρ=0.79），但与标准越狱鲁棒性几乎没有相关性。2) 架构优于规模：模型架构对风险检测性能的影响比模型规模更显著，通用 LLM 在轨迹分析中始终优于专门的安全防护栏。3) 时间稳定性：准确率在较长的轨迹中保持稳定。执行步骤的增加使模型能够从静态工具定义转向动态执行行为，实际上提高了后期阶段的风险检测性能。我们的研究结果表明，保障智能体工作流的安全需要同时优化结构化推理和安全对齐，以有效缓解轨迹中途的风险。

## Abstract
As large language models (LLMs) evolve from static chatbots into autonomous agents, the primary vulnerability surface shifts from final outputs to intermediate execution traces. While safety guardrails are well-benchmarked for natural language responses, their efficacy remains largely unexplored within multi-step tool-use trajectories. To address this gap, we introduce TraceSafe-Bench, the first comprehensive benchmark specifically designed to assess mid-trajectory safety. It encompasses 12 risk categories, ranging from security threats (e.g., prompt injection, privacy leaks) to operational failures (e.g., hallucinations, interface inconsistencies), featuring over 1,000 unique execution instances. Our evaluation of 13 LLM-as-a-guard models and 7 specialized guardrails yields three critical findings: 1) Structural Bottleneck: Guardrail efficacy is driven more by structural data competence (e.g., JSON parsing) than semantic safety alignment. Performance correlates strongly with structured-to-text benchmarks ($ρ=0.79$) but shows near-zero correlation with standard jailbreak robustness. 2) Architecture over Scale: Model architecture influences risk detection performance more significantly than model size, with general-purpose LLMs consistently outperforming specialized safety guardrails in trajectory analysis. 3) Temporal Stability: Accuracy remains resilient across extended trajectories. Increased execution steps allow models to pivot from static tool definitions to dynamic execution behaviors, actually improving risk detection performance in later stages. Our findings suggest that securing agentic workflows requires jointly optimizing for structural reasoning and safety alignment to effectively mitigate mid-trajectory risks.

---

## 论文详细总结（自动生成）

### 论文总结：TraceSafe - 多步工具调用轨迹中的大语言模型防护栏系统性评估

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从简单的聊天机器人演变为能够自主调用外部工具的智能体（Agents），其安全风险也发生了转移。传统的安全防护栏（Guardrails）主要关注最终的自然语言输出，但在智能体工作流中，**中间执行轨迹（Execution Traces）**成为了新的攻击面。恶意指令或错误可能隐藏在多步工具调用的 JSON 参数中，而最终输出看似无害。
目前缺乏专门针对“轨迹中途（Mid-trajectory）”安全性的评估基准。现有的智能体安全研究多关注端到端的鲁棒性，而非独立防护栏模型拦截不安全工具调用的能力。本文旨在填补这一空白，研究防护栏是否能有效识别并拦截嵌入在复杂结构化格式中的风险。

#### 2. 论文提出的方法论
论文引入了 **TraceSafe-Bench**，这是首个专门用于评估多步工具调用轨迹安全的静态基准测试。
*   **核心思想：良性到有害的编辑（Benign-to-Harmful Editing）**。该方法不依赖于自由生成的有害内容，而是通过对真实的良性轨迹进行针对性修改来构建。
*   **关键技术流程（Check-and-Mutate）**：
    1.  **良性种子筛选**：从 Berkeley Function Calling Leaderboard (BFCL) 中提取执行准确率为 100% 的多步轨迹。
    2.  **检查（Check）**：针对特定的风险类别，评估当前轨迹步骤是否适合注入该风险（例如，只有包含字符串参数的工具才适合注入 SQL 注入或 PII 泄露）。
    3.  **变异（Mutate）**：通过程序化逻辑（如修改 JSON 键值、删除参数、篡改工具描述）注入风险，确保生成的有害样本具有确定的 Ground Truth 标签。
*   **风险分类体系**：涵盖 4 大领域、12 个细分风险类别，包括提示词注入（输入/输出）、隐私泄露（用户信息/API Key/系统数据）、幻觉（参数模糊/虚假工具/冗余参数等）以及接口不一致（版本冲突/描述不匹配）。

#### 3. 实验设计
*   **数据集/场景**：基于 BFCL 构建，包含 1000 多个独特的执行实例，涵盖代码、API 调用等多种智能体场景。
*   **评估对象（20 个模型）**：
    *   **通用 LLM（13个）**：包括 GPT-5 系列、Gemini 3 系列、Qwen3 系列、Llama-3B 等。
    *   **专用防护栏（7个）**：包括 Llama Guard 3、Granite Guardian、Qwen Guard、AWS Bedrock Guardrails、GCP AI Safety 等。
*   **评估设置**：
    1.  二分类（无 Schema）：仅凭模型内在知识判断。
    2.  二分类（有 Schema）：提供风险定义。
    3.  多分类（粗粒度）：分类到 4 大领域。
    4.  多分类（细粒度）：分类到 12 个具体风险类别。

#### 4. 资源与算力
论文中**未明确说明**具体的训练算力（如 GPU 型号、数量或时长）。这主要是因为该研究属于评估性质，主要工作在于基准测试的构建和对现有闭源/开源模型的推理评估，而非从头训练大型模型。实验涉及对多个闭源 API（OpenAI, Google, AWS）的调用以及对开源模型（Qwen, Llama）的推理。

#### 5. 实验数量与充分性
*   **实验规模**：每个风险类别采样 90 个代表性轨迹，总计超过 1000 个测试样本。
*   **充分性与客观性**：
    *   评估了 20 种不同架构和规模的模型，具有广泛的代表性。
    *   引入了与 LiveCodeBench、GPQA、StrongREJECT 等多个基准的皮尔逊相关性分析，深入探讨了性能驱动因素。
    *   **人工校验**：与专业网络安全公司合作，对自动生成的轨迹进行了人工审计，确保了标签的准确性。
    *   实验设计考虑了轨迹长度、步骤数量对性能的影响，分析维度全面。

#### 6. 主要结论与发现
1.  **结构性瓶颈**：防护栏在智能体场景下的成功高度取决于其**结构化数据处理能力**（如 JSON 解析），而非单纯的安全对齐。性能与结构化数据基准相关性极高（ρ=0.79），与越狱鲁棒性几乎无关。
2.  **架构优于规模**：模型架构（如代码增强预训练）对风险检测的影响大于参数规模。通用 LLM 在轨迹分析任务中普遍优于专门的专用防护栏模型。
3.  **时间稳定性**：长轨迹并不会导致性能下降。随着执行步骤增加，模型能更好地从动态行为中识别风险，而非被静态的工具定义干扰。
4.  **分类引导有效性**：提供细粒度的风险分类定义能显著提升模型的检测稳定性和准确率。

#### 7. 优点（亮点）
*   **方法论创新**：提出的“良性到有害编辑”策略有效解决了 LLM-as-a-judge 的循环偏见和人工标注成本高的问题。
*   **诊断性深度**：不仅给出了分数，还揭示了结构化推理能力是智能体安全的核心瓶颈。
*   **现实意义**：针对新兴的工具调用协议（如 MCP）和多步复杂工作流，提供了及时的安全评估工具。

#### 8. 不足与局限
*   **静态限制**：作为一个静态轨迹基准，无法捕捉防护栏干预后智能体行为的动态演变（即防护栏拦截后，智能体如何重新规划）。
*   **良性种子的潜在偏差**：虽然变异部分是确定的，但原始的“良性”种子在某些特定部署环境下可能并非绝对安全（非对称安全保证）。
*   **覆盖范围**：虽然涵盖了 12 类风险，但随着工具调用攻击手段的快速演进，可能仍需不断更新分类体系以应对更复杂的跨环境注入攻击。

（完）
