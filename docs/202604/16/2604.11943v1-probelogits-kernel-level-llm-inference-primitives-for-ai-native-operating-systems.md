---
title: "ProbeLogits: Kernel-Level LLM Inference Primitives for AI-Native Operating Systems"
title_zh: ProbeLogits：用于 AI 原生操作系统的内核级大语言模型推理原语
authors: Daeyeon Son
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11943v1"
tags: ["query:agsec"]
score: 10.0
evidence: 内核级Token概率读取用于智能体动作治理
tldr: 本研究提出了ProbeLogits，一种集成在操作系统内核中的LLM推理原语，旨在实现AI原生系统的安全治理。通过在文本生成前读取Logit分布，ProbeLogits能以零参数学习的方式快速分类Agent行为的安全性。该方案在Anima OS中实现，利用校准强度α作为策略调节杠杆，在保证高性能的同时，通过内核级拦截提供了比应用层分类器更强的防御能力，并支持KV缓存的进程化管理。
selection_source: fresh_fetch
motivation: 现有的AI治理多在应用层，难以在操作系统底层对Agent的危险行为进行强制且高效的拦截。
method: 在内核中执行单次前向传播并读取特定Token的Logit分布，引入可调节的校准强度α和上下文校准技术进行行为分类。
result: "在OS动作基准测试中达到0.98的F1分数，在ToxicChat上达到Llama Guard 3性能的89%，且单次分类仅需65毫秒。"
conclusion: 将LLM推理原语下沉至内核层，不仅提升了系统治理的安全性与灵活性，还为AI原生操作系统的进程管理提供了新范式。
---

## 摘要
在内部运行大语言模型（LLM）推理的操作系统内核可以在生成任何文本之前读取 logit 分布，并将其作为治理原语进行操作。我提出了 ProbeLogits，这是一种内核级操作，通过执行单次前向传递并读取特定 token 的 logit，将智能体行为分类为安全或危险，且无需任何学习参数。在包含 260 个提示词的操作系统行为基准测试（涵盖对抗性攻击等 9 个类别）中，ProbeLogits 使用 4 位量化的通用 7B 模型，实现了 F1=0.980、精确率=1.000 和召回率=0.960。在 ToxicChat（1,000 条人工标注的真实对话）上，在默认校准强度 α=1.0 时，其 F1 达到 0.790；在 α=0.5 时提升至 0.837，在零学习参数的情况下达到了 Llama Guard 3（F1 约为 0.939）的 89%。一个关键的设计贡献是校准强度 α，它作为部署时的策略调节旋钮，而非学习到的超参数。通过调整 α，操作系统可以为特权操作执行严格策略（α ≥ 0.8，最大化召回率），或为对话智能体执行宽松策略（α=0.5，最大化精确率）。上下文校准将自定义基准测试的准确率从 64.8% 提高到 97.3%。我在 Anima OS 中实现了 ProbeLogits，这是一个由 80,400 行 Rust 代码编写的裸机 x86_64 操作系统。由于智能体行为必须通过内核调解的宿主函数，ProbeLogits 的执行位于 WASM 沙箱边界之下，使其比应用层分类器更难被规避。在 7B 模型上，每次分类耗时 65 毫秒，足以支持逐操作治理。我还展示了将 KV 缓存视为进程状态，可以实现类似于传统进程管理的检查点、恢复和分叉操作。据我所知，此前尚无系统将 LLM logit 向量作为操作系统级治理原语开放。

## 速览
**TLDR**：本研究提出了ProbeLogits，一种集成在操作系统内核中的LLM推理原语，旨在实现AI原生系统的安全治理。通过在文本生成前读取Logit分布，ProbeLogits能以零参数学习的方式快速分类Agent行为的安全性。该方法在Anima OS中实现，运行于WASM沙箱之下，使其比应用层分类器更难被绕过。实验表明其在OS操作基准测试中达到0.98的F1分数，且支持通过校准强度α灵活调整安全策略，为AI原生OS提供了高效、底层的安全管控机制。 \
**Motivation**：现有的应用层LLM安全分类器容易被绕过，且缺乏与操作系统底层集成的细粒度治理机制。 \
**Method**：在内核层执行单次前向传播并读取特定Token的Logit分布，利用上下文校准和可调策略参数α对Agent行为进行实时分类。 \
**Result**：在OS操作基准上实现0.98的F1分数，在ToxicChat上达到Llama Guard 3性能的89%，且单次分类延迟仅为65ms。 \
**Conclusion**：将LLM推理原语和Logit向量暴露为内核级治理工具，为AI原生操作系统的安全管控和进程管理开辟了新范式。

---

## Abstract
An OS kernel that runs LLM inference internally can read logit distributions before any text is generated -- and act on them as a governance primitive. I present ProbeLogits, a kernel-level operation that performs a single forward pass and reads specific token logits to classify agent actions as safe or dangerous, with zero learned parameters. On a 260-prompt OS action benchmark (9 categories including adversarial attacks), ProbeLogits achieves F1=0.980, Precision=1.000, and Recall=0.960 using a general-purpose 7B model at 4-bit quantization. On ToxicChat (1,000 human-annotated real conversations), it achieves F1=0.790 at default calibration strength $α$=1.0, improving to F1=0.837 at $α$=0.5 -- 89% of Llama Guard 3's F1~0.939 with zero learned parameters. A key design contribution is the calibration strength $α$, which serves as a deployment-time policy knob rather than a learned hyperparameter. By adjusting $α$, the OS can enforce strict policies for privileged operations ($α\geq 0.8$, maximizing recall) or relaxed policies for conversational agents ($α$=0.5, maximizing precision). Contextual calibration improves accuracy from 64.8% to 97.3% on the custom benchmark. I implement ProbeLogits within Anima OS, a bare-metal x86_64 OS written in 80,400 lines of Rust. Because agent actions must pass through kernel-mediated host functions, ProbeLogits enforcement operates below the WASM sandbox boundary, making it significantly harder to circumvent than application-layer classifiers. Each classification costs 65ms on 7B -- fast enough for per-action governance. I also show that treating KV cache as process state enables checkpoint, restore, and fork operations analogous to traditional process management. To my knowledge, no prior system exposes LLM logit vectors as OS-level governance primitives.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProbeLogits: Kernel-Level LLM Inference Primitives for AI-Native Operating Systems》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
传统的操作系统（OS）将进程视为不透明的实体，仅管理其资源（内存、CPU等），而不干涉其内部逻辑。但在 AI 时代，这种“语义不透明性”导致了三个核心问题：
*   **治理延迟**：在应用层进行安全检查需要生成文本、解析结果，跨层通信导致高延迟。
*   **安全性易被绕过**：应用层的安全过滤器与智能体（Agent）处于同一特权级，容易被恶意智能体绕过。
*   **调度盲区**：内核无法感知 AI 任务的置信度或意图，难以进行智能调度。
**核心动机**：将 LLM 推理能力下沉至 OS 内核，通过直接读取 Token 的 Logit 分布（概率分布）而非生成文本，构建一种不可绕过、低延迟的内核级治理原语。

### 2. 论文提出的方法论
论文提出了 **ProbeLogits**，一套集成在 **Anima OS**（一个用 Rust 编写的裸机操作系统）内核中的推理原语。

*   **核心思想**：利用 LLM 在生成文本前的 Logit 向量作为语义信号。通过单次前向传播（Forward Pass）读取特定 Token（如“Yes/No”或“Safe/Dangerous”）的概率，实现分类。
*   **关键技术细节**：
    *   **N-way 分类算法**：在词表 $V$ 中定位目标类别的 Token ID，计算受限的 Softmax 概率。单次前向传播即可完成多分类，无需生成文本。
    *   **上下文校准（Contextual Calibration）**：引入校准强度 $\alpha$。通过在启动时测量模型对空输入的固有偏见（Bias），在推理时进行减法修正。$\alpha$ 作为一个“策略旋钮”，允许 OS 在部署时动态调整精确率和召回率。
    *   **KV 缓存即进程状态**：将 LLM 的 KV Cache 类比为 CPU 寄存器，实现内核级的 `kv_fork()`、`kv_checkpoint()` 和 `kv_restore()`，支持智能体的分支执行和状态迁移。
    *   **治理管道**：包含三阶段：1. 对抗性预过滤（正则扫描）；2. 输入清理；3. 校准后的 ProbeLogits 前向传播。

### 3. 实验设计
*   **实验场景**：在裸机 x86_64 环境下运行 Anima OS，直接与硬件交互。
*   **数据集/基准测试**：
    *   **自定义 OS 动作基准**：260 个提示词，涵盖 9 个类别（如隐私违规、指令覆盖、对抗攻击等）。
    *   **ToxicChat**：1,000 条真实的人机对话标注数据，用于验证通用性。
*   **对比方法**：
    *   **Generate-then-Parse**：传统的“生成文本再解析”方法。
    *   **Llama Guard 3 (8B)**：作为安全分类器的 SOTA 基准。
    *   **llama.cpp**：作为推理性能的基准。

### 4. 资源与算力
*   **硬件环境**：AMD Ryzen 9 9800X3D (8核/16线程)，支持 AVX-512 VNNI 指令集；60GB DDR5-6000 内存。
*   **模型规格**：Qwen2.5-7B-Instruct（4位量化，4.1GB）和 SmolLM2-135M。
*   **算力消耗**：
    *   **推理时长**：7B 模型单次前向传播（即一次分类）耗时 **65ms**。
    *   **训练时长**：该方法为 **Zero-shot（零样本）**，无需针对特定任务进行微调或训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了 260 次 OS 动作测试和 1,000 次 ToxicChat 跨域测试。
*   **消融实验**：详细测试了校准强度 $\alpha$ 从 0.3 到 1.0 对性能的影响，以及预过滤、隐私增强等组件的贡献。
*   **充分性评价**：实验在性能指标（F1、精确率、召回率）和系统开销（延迟、带宽利用率）上做了深入分析。虽然样本量（260/1000）在 ML 领域不算巨大，但作为操作系统原语的验证，其覆盖了主要的对抗场景，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能极限**：在裸机环境下，7B 模型的推理速度（15 tokens/s）已达到 DDR5 内存带宽物理极限的 85%，证明了内核级推理的高效性。
*   **治理效能**：ProbeLogits 在 OS 动作分类上达到 **F1=0.980**，远超传统的生成解析法（F1=0.927）。
*   **策略灵活性**：通过调节 $\alpha$，OS 可以为特权操作选择高召回率策略，为普通对话选择高精确率策略。
*   **安全性**：由于治理逻辑位于 WASM 沙箱之下，智能体无法通过提示词注入等手段绕过内核强制执行的安全检查。

### 7. 优点
*   **架构创新**：首次将 LLM Logit 提升为操作系统级的治理原语，打破了 AI 与 OS 的层级隔阂。
*   **极低延迟**：相比生成式分类，延迟降低了约 10 倍（650ms vs 65ms）。
*   **零成本部署**：无需微调模型，依靠数学校准和提示词工程即可达到接近专用安全模型（如 Llama Guard）的性能。
*   **确定性**：消除了文本解析带来的不确定性和格式错误风险。

### 8. 不足与局限
*   **硬件依赖**：目前仅支持 CPU 推理，对于 7B 以上的大模型，缺乏 GPU 加速将成为瓶颈。
*   **泛化风险**：在 ToxicChat 上的表现（F1=0.837）弱于专用模型，说明在处理复杂、模糊的自然语言对话时，零样本 Logit 分类仍有局限。
*   **标注偏差**：自定义基准测试的标签由开发者自行标注，可能存在过拟合特定测试集的风险。
*   **语法支持有限**：目前的约束解码仅支持简单的选择和布尔值，尚未实现完整的上下文无关语法（CFG）支持。

（完）
