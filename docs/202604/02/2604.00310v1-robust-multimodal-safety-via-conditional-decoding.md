---
title: Robust Multimodal Safety via Conditional Decoding
title_zh: 通过条件解码实现鲁棒的多模态安全性
authors: "Anurag Kumar, Raghuveer Peri, Jon Burnsky, Alexandru Nelus, Rohit Paturi, Srikanth Vishnubhotla, Yanjun Qi"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00310v1"
tags: ["query:agsec"]
score: 9.0
evidence: 在响应生成前预测二进制安全Token，以确保稳健的安全对齐
tldr: "多模态大语言模型（MLLM）在处理跨模态有害查询时常面临安全对齐失效的问题。本文提出了一种名为 CASA 的条件解码策略，通过内部表示和新增的安全注意力模块，在生成响应前预测安全标识符。该方法无需外部分类器或特定模态的微调，在多个基准测试中将攻击成功率降低了 97% 以上，同时保持了模型在良性输入下的实用性，为提升多模态模型安全性提供了一个简单且通用的框架。"
selection_source: fresh_fetch
motivation: 现有的多模态大语言模型在面对跨模态攻击时，其原有的文本安全对齐往往会失效，导致安全性显著下降。
method: 提出 CASA 框架，利用安全注意力模块和内部表示在响应生成前预测安全标识，实现条件解码而无需外部分类器。
result: "在多个多模态安全基准测试中，CASA 将平均攻击成功率降低了 97% 以上，且通过人工和自动评估证实其不损害良性输入的表现。"
conclusion: CASA 证明了无需外部工具或复杂微调，即可通过简单的条件解码机制显著增强多模态模型的鲁棒安全性。
---

## 摘要
多模态大语言模型（MLLM）在有害查询利用跨模态交互时，往往会出现安全对齐性能下降的情况。仅在文本上进行对齐的模型在扩展到两种或更多模态时，攻击成功率更高。在这项工作中，我们提出了一种简单的条件解码策略 CASA（通过安全注意力增强的分类），该策略利用 MLLM 的内部表示在生成响应之前预测一个二元安全标记。我们引入了一种新颖的安全注意力模块，旨在增强模型检测恶意查询的能力。我们的设计确保了鲁棒的安全对齐，且无需依赖任何外部分类器或辅助头，也不需要针对特定模态进行安全微调。在 MM-SafetyBench、JailbreakV-28k 和对抗性音频测试等多种基准测试中，CASA 在不同模态和攻击类型下的平均攻击成功率降低了 97% 以上。我们的实证评估还表明，CASA 在良性输入中保持了强大的实用性，这一结果通过自动化评估和人工评估（由 13 名经过培训的标注员完成）得到了验证。总之，这些结果凸显了 CASA 是一个简单且具有泛化性的框架，可用于提高多模态 LLM 的安全性。

## Abstract
Multimodal large-language models (MLLMs) often experience degraded safety alignment when harmful queries exploit cross-modal interactions. Models aligned on text alone show a higher rate of successful attacks when extended to two or more modalities. In this work, we propose a simple conditional decoding strategy, CASA (Classification Augmented with Safety Attention) that utilizes internal representations of MLLMs to predict a binary safety token before response generation. We introduce a novel safety attention module designed to enhance the model's ability to detect malicious queries. Our design ensures robust safety alignment without relying on any external classifier or auxiliary head, and without the need for modality-specific safety fine-tuning. On diverse benchmarks such as MM-SafetyBench, JailbreakV-28k, and adversarial audio tests, CASA lowers the average attack success rate by more than 97% across modalities and across attack types. Our empirical evaluations also show that CASA maintains strong utility in benign inputs, a result validated through both automated and human evaluations (via 13 trained annotators). Together, these results highlight CASA as a simple and generalizable framework to improve multimodal LLM safety.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CASA (Classification Augmented with Safety Attention)** 的框架，旨在增强多模态大语言模型（MLLM）在面对跨模态攻击时的安全性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态模型（如结合图像、音频的模型）在仅接受文本安全对齐后，容易在跨模态交互中暴露安全漏洞。现有的**监督安全微调（SSFT）**存在两个主要痛点：
    1.  **安全性与实用性的权衡**：过度微调会导致“过度阻断（Overblocking）”，即模型拒绝回答无害问题。
    2.  **模态特定性**：每增加一种新模态（如音频），通常需要昂贵的模态特定安全数据集和复杂的超参数优化。
*   **研究动机**：利用模型内部已有的区分安全与有害信息的能力，通过一种简单、通用的解码机制，在不牺牲实用性的前提下实现跨模态的鲁棒安全对齐。

### 2. 方法论：CASA 框架
CASA 的核心思想是**“先分类，后生成”**，将安全判断与内容生成解耦。
*   **条件解码（Conditional Decoding）**：在生成正式响应前，强制模型先预测一个二元安全标记（`Csaf e` 或 `Cunsaf e`）。后续的响应生成将基于该标记进行条件概率计算。
*   **安全注意力模块（Safety Attention Module）**：
    *   **机制**：引入一个交叉注意力机制，将当前输入的隐藏状态与预定义的“有害信息描述”嵌入向量进行对比。
    *   **逻辑**：计算出的注意力分数用于缩放安全标记的输出 Logits。如果输入包含恶意信号，该模块会放大 `Cunsaf e` 标记的概率。
*   **内部化设计**：CASA 完全集成在模型内部推理过程中，不需要外部分类器或额外的预测头，也不改变模型对普通输入的处理逻辑。

### 3. 实验设计
*   **模型对象**：Qwen_2.5_Omni (3B 和 7B 版本)。
*   **评估数据集/场景**：
    *   **文本攻击**：JB-Prompt（变异模糊测试生成的强力提示词）、Prefill 攻击（预填肯定句诱导）。
    *   **视觉攻击**：JailbreakV-28k（2.8万个图像-文本对）、MM-SafetyBench（包含稳定扩散、排版攻击等）。
    *   **音频攻击**：AIAH（将有害词汇拆解为字母嵌入音频的拼写攻击）。
    *   **实用性评估**：MME 基准测试（感知与认知能力）、自定义 Utility-text 数据集。
*   **对比方法**：预训练模型（无对齐）、标准 SSFT、Circuit Breaker (CB，一种修改内部表示的强力防御方法)。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   采用 **RS-LoRA** 策略进行参数高效微调。
    *   Batch Size 为 8，梯度累积为 2。
    *   3B 模型的可训练参数约为 27M，7B 模型约为 58M。
*   **时长**：文中未明确给出具体的训练总时长，但提到微调数据集规模约为 1.6 万条。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本、视觉、音频三大模态的多种前沿攻击手段，实验组设置包括了不同模型规模（3B/7B）和多种防御基准。
*   **消融实验**：专门验证了“安全注意力模块”和“安全提示词（Safety Prompt）”各自的贡献。
*   **评估客观性**：采用了 **LLM-as-a-Judge (Claude 3.7)** 和 **13 名专业人工标注员**的双重验证，并计算了 Krippendorff’s $\alpha$ 和 Cohen’s $\kappa$ 系数以确保标注一致性，实验设计非常严谨且公平。

### 6. 主要结论与发现
*   **极高的防御成功率**：CASA 在各种模态下的平均攻击成功率（ASR）降低了 **97% 以上**。
*   **完美防御 Prefill 攻击**：在不同长度的预填前缀攻击下，CASA 保持了 0% 的 ASR，远优于 SSFT 和 Circuit Breaker。
*   **实用性不降反升**：在 MME 视觉基准测试中，CASA 的得分甚至超过了预训练模型，证明其在增强安全性的同时，有效避免了过度阻断问题。
*   **内部表示的有效性**：实验证实，模型内部隐藏层已经能很好地分离安全与有害特征，CASA 成功激活了这一能力。

### 7. 优点与亮点
*   **架构简洁**：无需外部插件，通过修改解码逻辑即可实现，易于部署。
*   **跨模态通用性**：一套机制同时解决文本、图像、音频攻击，无需为每种模态单独准备安全数据。
*   **鲁棒性强**：对复杂的拼写攻击和长上下文攻击表现出极强的韧性。
*   **评估全面**：结合了自动化指标与高质量的人工评估，结论可信度高。

### 8. 不足与局限
*   **计算开销**：安全注意力模块需要对整个 Prompt 进行交叉注意力计算，虽然只在生成第一个 Token 前计算一次，但对于超长文本输入，可能会产生一定的计算瓶颈。
*   **攻击覆盖范围**：目前主要针对显式的恶意查询，对于更隐蔽的、通过良性问题组合而成的“情境性有害攻击（Situational Safety）”的防御效果尚待进一步验证。
*   **依赖内部表示**：如果基础模型本身的内部表示无法区分某些新型攻击，CASA 的效果可能会受限。

（完）
