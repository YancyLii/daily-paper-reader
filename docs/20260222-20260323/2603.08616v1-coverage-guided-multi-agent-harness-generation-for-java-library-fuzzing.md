---
title: Coverage-Guided Multi-Agent Harness Generation for Java Library Fuzzing
title_zh: 针对 Java 库模糊测试的覆盖率引导多智能体 Harness 生成
authors: "Nils Loose, Nico Winkel, Kristoffer Hempel, Felix Mächtle, Julian Hans, Thomas Eisenbarth"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08616v1"
tags: ["query:libfuz"]
score: 10.0
evidence: 使用多智能体大模型自动生成Java库模糊测试Harness
tldr: 本研究针对Java库模糊测试中人工编写Harness耗时且复杂的难题，提出了一种基于多智能体架构的自动化生成方法。该系统由五个专门的ReAct智能体组成，通过模型上下文协议（MCP）按需探索代码语义，并结合方法定向覆盖率分析进行迭代优化。实验显示，该方法在覆盖率上显著超越了OSS-Fuzz等基准，并成功在已集成模糊测试的成熟项目中发现了3个新漏洞，证明了其在实际安全测试中的高效性与实用性。
selection_source: fresh_fetch
motivation: 手动为Java库编写模糊测试Harness需要深入理解API语义和复杂的初始化序列，过程极其耗时且容易出错。
method: 提出一种多智能体架构，利用五个协同工作的ReAct智能体进行研究、合成、修复及优化，并引入方法定向覆盖率来精准引导Harness的改进。
result: "生成的Harness在覆盖率上比OSS-Fuzz基准中位数提升了26%，且平均每个Harness的生成成本仅为3.20美元，并发现了3个真实漏洞。"
conclusion: 该多智能体自动化框架能以低成本生成高质量的Java模糊测试Harness，有效提升了复杂软件库的自动化漏洞挖掘能力。
---

## 摘要
覆盖率引导的模糊测试已被证明在软件测试中非常有效，但针对库代码进行测试需要专门的模糊测试 harness（测试桩），用于将模糊测试器生成的输入转换为有效的 API 调用。手动创建 harness 耗时费力，且需要对 API 语义、初始化序列和异常处理契约有深入理解。我们提出了一种多智能体架构，通过专门的基于大语言模型（LLM）的智能体，实现 Java 库模糊测试 harness 的自动化生成。五个 ReAct 智能体将工作流分解为研究、合成、编译修复、覆盖率分析和优化。智能体并非预处理整个代码库，而是通过模型上下文协议（Model Context Protocol）按需查询文档、源代码和调用图信息，在探索复杂依赖关系的同时保持专注的上下文。为了实现有效的优化，我们引入了方法定向覆盖率（仅在目标方法执行期间跟踪覆盖率以隔离目标行为）以及智能体引导的终止机制（通过检查未覆盖的源代码来区分富有成效的优化机会与收益递减的情况）。我们在来自 6 个广泛部署的 Java 库（总计超过 115,000 个 Maven 依赖项）的 7 个目标方法上评估了我们的方法。我们生成的 harness 相比 OSS-Fuzz 基准实现了 26% 的中位数提升，并在包范围覆盖率上比 Jazzer AutoFuzz 高出 5%。每个 harness 的平均生成成本为 3.20 美元，耗时 10 分钟，使该方法在持续模糊测试工作流中具有实用性。在为期 12 小时的模糊测试活动中，我们生成的 harness 在已集成到 OSS-Fuzz 的项目中发现了 3 个漏洞，证明了所生成 harness 的有效性。

## Abstract
Coverage-guided fuzzing has proven effective for software testing, but targeting library code requires specialized fuzz harnesses that translate fuzzer-generated inputs into valid API invocations. Manual harness creation is time-consuming and requires deep understanding of API semantics, initialization sequences, and exception handling contracts. We present a multi-agent architecture that automates fuzz harness generation for Java libraries through specialized LLM-powered agents. Five ReAct agents decompose the workflow into research, synthesis, compilation repair, coverage analysis, and refinement. Rather than preprocessing entire codebases, agents query documentation, source code, and callgraph information on demand through the Model Context Protocol, maintaining focused context while exploring complex dependencies. To enable effective refinement, we introduce method-targeted coverage that tracks coverage only during target method execution to isolate target behavior, and agent-guided termination that examines uncovered source code to distinguish productive refinement opportunities from diminishing returns. We evaluated our approach on seven target methods from six widely-deployed Java libraries totaling 115,000+ Maven dependents. Our generated harnesses achieve a median 26\% improvement over OSS-Fuzz baselines and outperform Jazzer AutoFuzz by 5\% in package-scope coverage. Generation costs average \$3.20 and 10 minutes per harness, making the approach practical for continuous fuzzing workflows. During a 12-hour fuzzing campaign, our generated harnesses discovered 3 bugs in projects that are already integrated into OSS-Fuzz, demonstrating the effectiveness of the generated harnesses.

---

## 论文详细总结（自动生成）

以下是对论文《Coverage-Guided Multi-Agent Harness Generation for Java Library Fuzzing》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：为软件库（Library）编写模糊测试（Fuzzing）的 Harness（测试桩）极其困难。Harness 需要将随机字节转换为有效的 API 调用序列，涉及复杂的对象初始化、API 语义理解和异常处理。
*   **研究动机**：
    *   **人工成本高**：手动编写需要深厚的领域知识，限制了模糊测试在大量 Java 库中的普及。
    *   **现有自动化工具局限**：基于用法的挖掘需要大量客户端代码；基于结构的生成难以处理隐含的前置条件；现有大模型（LLM）方法容易受限于上下文窗口，且缺乏对覆盖率反馈的语义理解。
*   **整体含义**：本文旨在通过多智能体协作和大模型按需检索技术，实现 Java 库 Harness 的全自动、高覆盖率生成。

### 2. 方法论：核心思想与关键技术
*   **多智能体架构**：采用五个专门的 **ReAct 智能体** 协同工作：
    1.  **研究智能体 (Research)**：探索文档和源码，理解 API 语义和初始化需求。
    2.  **合成智能体 (Synthesis)**：生成初始 Harness 代码和 Maven 依赖配置。
    3.  **编译修复智能体 (Patching)**：根据编译器报错迭代修复代码。
    4.  **覆盖率分析智能体 (Coverage Analysis)**：分析未覆盖的代码路径，判断是否具有优化潜力。
    5.  **优化智能体 (Refinement)**：根据分析结果修改 Harness 以提升覆盖率。
*   **模型上下文协议 (MCP)**：智能体不预处理整个代码库，而是通过 MCP 接口**按需查询**方法文档、源码片段和调用图（Callgraph），有效避免了上下文饱和。
*   **方法定向覆盖率 (Method-Targeted Coverage)**：利用 ASM 和 JaCoCo 改进插桩技术，仅在目标方法执行期间开启覆盖率记录，防止智能体通过调用无关工具类来虚增覆盖率指标。
*   **智能体引导的终止机制**：由 LLM 分析未覆盖代码的语义（如是否为不可达的防御性代码），自主决定何时停止迭代，而非使用固定的数值阈值。

### 3. 实验设计
*   **实验对象**：选取了 6 个广泛使用的 Java 库（commons-cli, gson, guava, jackson-databind, jsoup, antlr4），共 7 个目标方法。这些库在 Maven 生态中拥有超过 11.5 万个下游依赖。
*   **基准测试 (Benchmark)**：
    *   **OSS-Fuzz**：Google 维护的开源模糊测试服务，代表了高质量的人工编写水平。
    *   **Jazzer AutoFuzz**：基于反射的自动化生成工具。
*   **对比指标**：行覆盖率（Line Coverage）、分支覆盖率、生成成本（时间和金钱）、漏洞发现能力。

### 4. 资源与算力
*   **模型使用**：底层模型为 **Claude 4.5 Sonnet** (2025-09-29 版本)。
*   **算力与成本**：
    *   **生成成本**：平均每个 Harness 消耗约 **3.20 美元** 的 API 费用。
    *   **生成时长**：平均每个 Harness 耗时约 **10 分钟**。
    *   **训练时长**：文中未提及模型训练，因为是基于现成 LLM 的推理应用。
    *   **GPU 资源**：未明确说明本地运行环境的 GPU 型号，主要依赖云端 API。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 个目标方法进行了测试，每个目标运行 3 次重复实验以减少随机性影响。
*   **模糊测试时长**：每个生成的 Harness 进行了 **12 小时** 的长时模糊测试。
*   **充分性评价**：实验设计较为充分，涵盖了不同类型的库（解析器、JSON 处理、核心工具），并与工业界标准（OSS-Fuzz）进行了直接对比。通过 12 小时的长时运行验证了 Harness 的稳定性和漏洞挖掘能力。

### 6. 主要结论与发现
*   **覆盖率提升**：生成的 Harness 在方法定向覆盖率上比 OSS-Fuzz 基准中位数提升了 **26%**；在全包范围覆盖率上比 AutoFuzz 高出 **5%**。
*   **漏洞挖掘**：在已集成到 OSS-Fuzz 的成熟项目中发现了 **3 个此前未报告的真实漏洞**（涉及 commons-cli 和 jsoup），包括空指针异常和数组越界。
*   **成本效益**：证明了使用 LLM 智能体进行自动化生成的经济可行性，成本和时间均在可接受范围内。

### 7. 优点
*   **按需检索机制**：通过 MCP 协议解决了大模型处理大型代码库时的上下文窗口限制问题。
*   **精准度高**：方法定向覆盖率技术确保了优化方向始终聚焦于目标逻辑，避免了“覆盖率作弊”。
*   **闭环修复**：集成了编译修复和覆盖率反馈循环，具备较强的自主纠错和进化能力。

### 8. 不足与局限
*   **语言限制**：目前仅针对 Java 语言实现，虽然原理可迁移，但插桩和分析工具链需重新开发。
*   **API 依赖**：高度依赖高性能 LLM（如 Claude 4.5），使用成本随 API 价格波动，且在无网络环境下无法运行。
*   **状态处理**：对于需要复杂全局状态或特定系统环境（如特定文件系统布局）的 API，生成的 Harness 可能仍有局限。
*   **样本量**：虽然选取的库很典型，但 7 个目标方法的样本量对于全面评估所有 Java API 类型来说仍显略少。

（完）
