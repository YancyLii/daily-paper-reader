---
title: Auditable Agents
title_zh: 可审计智能体
authors: "Yi Nian, Aojie Yuan, Haiyue Zhang, Jiate Li, Yue Zhao"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05485v1"
tags: ["query:agsec"]
score: 8.0
evidence: LLM智能体调用工具和触发副作用的可审计性与问责制
tldr: 本文探讨了LLM智能体在执行外部操作时的可问责性问题，强调了“可审计性”是实现问责的核心前提。研究定义了可审计性的五个维度（如动作可恢复性、证据完整性等）和三类机制，并通过对开源项目的安全评估、运行时开销测试及恢复实验，揭示了当前系统在审计基础上的严重缺失，最后提出了审计卡片框架以指导未来研究。
selection_source: fresh_fetch
motivation: 随着LLM智能体具备影响现实世界的能力，确保其行为可追溯并能明确责任归属成为系统部署的关键挑战。
method: 提出了涵盖五个维度和三类机制（检测、执行、恢复）的可审计性理论框架，并设计了审计卡片评估工具。
result: 实验显示主流开源智能体项目存在大量安全隐患，但引入防篡改审计机制的运行时额外开销极低（仅8.3毫秒）。
conclusion: 没有可审计性就没有问责制，必须通过多层技术手段构建可信的证据链以确保智能体系统的安全合规。
---

## 摘要
LLM 智能体调用工具、查询数据库、委派任务并触发外部副作用。一旦智能体系统能够在现实世界中采取行动，问题就不再仅仅是能否防止有害行为，而是这些行为在部署后是否仍然是可追责的。我们区分了问责制（accountability，确定合规性并分配责任的能力）、可审计性（auditability，使问责成为可能的系统属性）以及审计（auditing，从可信证据中重建行为的过程）。我们的主张很直接：没有可审计性，任何智能体系统都无法实现问责。为了使其具有可操作性，我们定义了智能体可审计性的五个维度，即动作可恢复性、生命周期覆盖范围、策略可检查性、责任归属和证据完整性，并识别了三类机制（检测、执行、恢复），其时间信息与干预约束解释了为什么在实践中单一方法是不够的。我们通过分层证据而非单一基准来支持这一立场：生态系统下界测量表明，即使是可审计性的基本安全前提也远未得到满足（在六个著名的开源项目中发现了 617 个安全漏洞）；运行时可行性结果显示，带有防篡改记录的执行前调解仅增加了 8.3 毫秒的中值开销；受控恢复实验表明，即使在常规日志缺失的情况下，仍能部分恢复与责任相关的信息。我们为智能体系统提出了一种“可审计性卡片”（Auditability Card），并按机制类别确定了六个开放性研究问题。

## Abstract
LLM agents call tools, query databases, delegate tasks, and trigger external side effects. Once an agent system can act in the world, the question is no longer only whether harmful actions can be prevented--it is whether those actions remain answerable after deployment. We distinguish accountability (the ability to determine compliance and assign responsibility), auditability (the system property that makes accountability possible), and auditing (the process of reconstructing behavior from trustworthy evidence). Our claim is direct: no agent system can be accountable without auditability.   To make this operational, we define five dimensions of agent auditability, i.e., action recoverability, lifecycle coverage, policy checkability, responsibility attribution, and evidence integrity, and identify three mechanism classes (detect, enforce, recover) whose temporal information-and-intervention constraints explain why, in practice, no single approach suffices. We support the position with layered evidence rather than a single benchmark: lower-bound ecosystem measurements suggest that even basic security prerequisites for auditability are widely unmet (617 security findings across six prominent open-source projects); runtime feasibility results show that pre-execution mediation with tamper-evident records adds only 8.3 ms median overhead; and controlled recovery experiments show that responsibility-relevant information can be partially recovered even when conventional logs are missing. We propose an Auditability Card for agent systems and identify six open research problems organized by mechanism class.

---

## 论文详细总结（自动生成）

这篇论文《Auditable Agents》（可审计智能体）由南加州大学 FORTIS 实验室的研究团队撰写。该论文提出了一个关于大语言模型（LLM）智能体系统“可审计性”的系统性框架，旨在解决智能体在现实世界执行操作后的问责难题。

以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：当 LLM 智能体具备删除文件、发起支付或调用 API 等产生外部副作用的能力时，现有的“对齐”或“预部署评估”已不足以保证安全。核心挑战在于：一旦发生错误，系统是否能够**事后重建行为、检查是否合规并明确责任归属**。
*   **研究动机**：作者指出，当前的智能体系统普遍缺乏可审计性——日志不全、缺乏生命周期上下文、证据易被篡改且难以自动检查策略。论文主张“没有可审计性就没有问责制”，并试图将可审计性确立为智能体系统的第一类设计和评估要求。

### 2. 方法论：核心思想与技术框架
论文提出了一个五维框架和三类机制来定义和实现可审计性：

*   **五大维度（判定审计结论的必要条件）**：
    1.  **动作可恢复性 (Action Recoverability)**：记录了哪些关键动作？（指标：动作覆盖率 ACR、记录保真度 RF）。
    2.  **生命周期覆盖 (Lifecycle Coverage)**：动作发生的上下文是什么？（如重试、回退、人工审批等阶段）。
    3.  **策略可检查性 (Policy Checkability)**：记录是否足以机械化地判定合规性？（指标：结构化策略可判定率 SPDR）。
    4.  **责任归属 (Responsibility Attribution)**：谁或哪个组件该负责？（涉及用户、智能体、工具、技能的责任链）。
    5.  **证据完整性 (Evidence Integrity)**：证据是否可信、防篡改？（指标：完整性强度 IS，从无保护到数字签名）。
*   **三类机制 (实现路径)**：
    *   **检测 (Detect)**：部署前静态分析代码和配置，识别审计漏洞。
    *   **执行 (Enforce)**：运行时拦截动作，生成带签名的防篡改记录。
    *   **恢复 (Recover)**：事后从残余证据（如水印、日志）中重建行为。
*   **审计卡片 (Auditability Card)**：借鉴 Model Cards，提出了一种标准化的报告格式，要求开发者披露系统在上述五个维度上的表现。

### 3. 实验设计
论文通过三层证据链支持其观点，而非单一的 Benchmark：
*   **生态系统测量 (Ecosystem Lower Bound)**：使用 `agent-audit` 工具对 6 个主流开源智能体项目（如 OpenHands, SWE-agent, Gorilla 等）进行静态安全扫描。
*   **运行时可行性 (Runtime Feasibility)**：使用 `Aegis` 预执行防火墙，在 14 个智能体框架上测试拦截攻击（48 种攻击场景）和正常调用（500 次）的开销。
*   **恢复前沿 (Recovery Frontier)**：使用隐式执行追踪（IET）技术，通过水印在日志缺失或被删减的情况下，尝试恢复多智能体系统的交互拓扑和责任归属。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **性能开销**：实验重点测量了审计机制带来的**延迟**。结果显示，增加防篡改记录和策略检查的中值开销仅为 **8.3 毫秒**，相对于 LLM 推理延迟（通常为秒级）几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   扫描了 6 个知名开源项目，发现了 617 个安全漏洞（其中 44% 为高危）。
    *   运行时测试涵盖了 1000 次连续拦截。
    *   恢复实验在 4-6 个智能体的多种拓扑（链式、星型、树型）下进行，准确率达到 0.95 左右。
*   **充分性评价**：作为一篇 Position Paper（立场论文），其实验设计非常全面且具有说服力。它不仅证明了现状的糟糕（生态扫描），还证明了改进的可行性（运行时开销）和极端情况下的补救能力（恢复实验）。实验覆盖了从静态到动态、从正常到对抗的多个维度，逻辑闭环完整。

### 6. 主要结论与发现
*   **现状严峻**：主流开源智能体项目在安全和审计基础方面严重达标，64% 的漏洞涉及工具误用（未验证输入）。
*   **技术可行**：实现高强度的审计（如 Ed25519 签名、哈希链日志）在工程上是可行的，且性能损耗极低。
*   **恢复潜力**：即使在日志被删除或身份信息被剥离的情况下，通过先进的水印技术仍能以极高的精度（EdgeSim > 0.96）重建智能体间的责任链。
*   **问责前提**：可审计性是问责制的基石，必须在设计阶段就予以考虑。

### 7. 优点与亮点
*   **理论高度**：首次清晰地界定了问责制、可审计性和审计之间的区别，并提出了形式化的五维评估模型。
*   **分层证据**：不依赖单一指标，而是通过生态扫描、运行时测试和事后恢复三个维度的实验构建了坚实的证据链。
*   **实用工具**：提出的“审计卡片”具有很强的落地指导意义，为行业标准化提供了参考。
*   **前瞻性**：识别了 6 个开放性研究问题，为该领域的后续研究指明了方向。

### 8. 不足与局限
*   **工具自研偏差**：实验所用的三个核心工具（agent-audit, Aegis, IET）均为作者团队开发，缺乏第三方工具的交叉验证。
*   **缺乏端到端案例**：虽然分块验证了机制，但尚未展示一个在真实复杂部署环境下，从发生违规到产出最终审计判决的完整端到端流程。
*   **策略局限性**：目前的“策略可检查性”主要集中在结构化策略（如“调用工具 X 必须经过审批”），对于语义复杂的策略（如“不得泄露客户隐私”）的自动化审计仍是挑战。
*   **开源 vs 商业**：实验对象仅限于开源项目，商业闭源智能体系统（可能已有内部审计机制）的情况未被涵盖。

（完）
