---
title: Towards Identification and Intervention of Safety-Critical Parameters in Large Language Models
title_zh: 迈向大语言模型安全关键参数的识别与干预
authors: "Weiwei Qi, Zefeng Wu, Tianhang Zheng, Zikang Zhang, Xiaojun Jia, Zhan Qin, Kui Ren"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08297v1"
tags: ["query:agsec"]
score: 9.0
evidence: LLM中安全关键参数的识别与干预
tldr: 本研究提出了ESI框架以量化大语言模型参数对安全性的影响，揭示了Dense和MoE架构中安全性关键参数的分布差异。基于此发现，研究设计了SET和SPA两种干预范式，分别用于通过更新极少量关键参数来增强模型安全性，以及在能力微调过程中保护关键参数以防止安全退化，为实现高效且可靠的模型安全控制提供了新思路。
selection_source: fresh_fetch
motivation: 由于缺乏对LLM内部安全机制的深入理解，难以开发出针对不同任务的精确且可靠的安全干预方法。
method: 提出ESI框架量化参数安全贡献，并衍生出SET（针对性增强安全）和SPA（微调中保护安全）两种干预策略。
result: "SET仅更新1%的参数即可降低超50%的攻击成功率，SPA则能将指令微调后的安全性能下降控制在1%以内。"
conclusion: 识别并针对性地干预安全性关键参数，是提升LLM安全对齐效率及在能力扩展中维持安全性的关键。
---

## 摘要
确保大语言模型（LLM）的安全性至关重要，然而，由于缺乏对安全机制的清晰理解，阻碍了针对多样化任务开发精确且可靠的安全干预方法。为了更好地理解和控制 LLM 的安全性，我们提出了预期安全影响（Expected Safety Impact, ESI）框架，用于量化不同参数如何影响 LLM 的安全性。基于 ESI，我们揭示了不同 LLM 架构中截然不同的安全关键模式：在稠密（dense）LLM 中，许多安全关键参数位于中间层的数值矩阵（V）和 MLP 中；而在混合专家（MoE）模型中，这些参数则转移到了后层的 MLP 中。利用 ESI，我们进一步引入了两种针对性的干预范式，用于安全增强和保持，即安全增强微调（Safety Enhancement Tuning, SET）和安全保持适配（Safety Preserving Adaptation, SPA）。SET 可以通过仅更新少量安全关键参数来对齐不安全的 LLM，在有效增强安全性的同时保留原始性能。SPA 在面向能力的干预（如指令微调）过程中，通过防止安全关键权重的破坏来保护已对齐的 LLM，使 LLM 在获得新能力的同时维持其安全能力。对不同 LLM 的广泛评估表明，SET 仅需对 1% 的模型权重进行 100 次迭代更新，即可将未对齐 LLM 的攻击成功率降低 50% 以上。在不同任务上进行 1,000 次迭代的指令微调后，SPA 可以将已对齐 LLM 的安全性下降控制在 1% 以内。我们的代码可在以下地址获取：https://github.com/ZJU-LLM-Safety/SafeWeights-ACL。

## Abstract
Ensuring Large Language Model (LLM) safety is crucial, yet the lack of a clear understanding about safety mechanisms hinders the development of precise and reliable methodologies for safety intervention across diverse tasks. To better understand and control LLM safety, we propose the Expected Safety Impact (ESI) framework for quantifying how different parameters affect LLM safety. Based on ESI, we reveal distinct safety-critical patterns across different LLM architectures: In dense LLMs, many safety-critical parameters are located in value matrices (V) and MLPs in middle layers, whereas in Mixture-of-Experts (MoE) models, they shift to the late-layer MLPs. Leveraging ESI, we further introduce two targeted intervention paradigms for safety enhancement and preservation, i.e., Safety Enhancement Tuning (SET) and Safety Preserving Adaptation (SPA). SET can align unsafe LLMs by updating only a few safety-critical parameters, effectively enhancing safety while preserving original performance. SPA safeguards well-aligned LLMs during capability-oriented intervention (e.g., instruction tuning) by preventing disruption of safety-critical weights, allowing the LLM to acquire new abilities and maintain safety capabilities. Extensive evaluations on different LLMs demonstrate that SET can reduce the attack success rates of unaligned LLMs by over 50% with only a 100-iteration update on 1% of model weights. SPA can limit the safety degradation of aligned LLMs within 1% after a 1,000-iteration instruction fine-tuning on different tasks. Our code is available at: https://github.com/ZJU-LLM-Safety/SafeWeights-ACL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Identification and Intervention of Safety-Critical Parameters in Large Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于大语言模型（LLM）的**安全机制理解与干预**。尽管现有模型通过 RLHF 等技术进行了安全对齐，但在面对下游任务微调（如指令微调）时，安全性能往往会发生严重退化（即“安全遗忘”）。
*   **核心挑战**：目前缺乏量化手段来识别模型内部哪些参数对安全性起关键作用。
*   **研究动机**：通过识别“安全关键参数”，实现轻量化的安全增强（针对未对齐模型）和安全保持（针对已对齐模型在微调时的保护）。

### 2. 方法论：核心思想与关键技术
研究者提出了 **ESI（Expected Safety Impact，预期安全影响）** 框架，包含参数识别与针对性干预两个阶段：

*   **ESI 指标建模**：
    *   定义**预期安全值 $S(\theta)$**：模型在有害输入分布下输出安全响应的得分期望。
    *   利用一阶泰勒展开近似参数变化对安全性的影响，公式定义为：$ESI(\theta_i) = |\sigma(\theta_i) \nabla_{\theta_i} S(\theta)|$。
    *   **创新点**：引入参数标准差 $\sigma(\theta_i)$ 作为权重变化幅度的统计代理，比传统的纯梯度或权重绝对值指标更精确。
*   **可微判别器引导的梯度估计**：
    *   由于 Token 采样是离散不可导的，论文引入**可微判别器（Judge Model）**。
    *   使用 **Gumbel-Softmax 弛豫**技术将离散 Token 转化为连续向量，并通过**投影矩阵 $M$** 桥接目标模型与判别器之间的词表差异，从而实现端到端的梯度回传。
*   **干预范式**：
    *   **SET（安全增强微调）**：针对不安全模型，仅更新 ESI 排名最高的 Top-k% 参数，在安全数据上微调。
    *   **SPA（安全保持适配）**：针对已对齐模型，在下游任务微调时**冻结** ESI 最高的安全关键参数，仅更新非敏感参数。

### 3. 实验设计
*   **实验场景**：涵盖参数识别验证、安全增强（SET）和下游任务适配（SPA）。
*   **模型（Benchmark）**：
    *   **Dense 架构**：Llama3 (8B/70B), Qwen2.5 (7B/14B/72B)。
    *   **MoE 架构**：Qwen3 (30B/235B), Mixtral-8x7B。
*   **数据集**：
    *   **安全评估**：AdvBench, HarmBench, WildJailbreak。
    *   **安全训练**：CB-Safety, R1-Safety。
    *   **通用能力**：GSM8K (数学), MMLU (知识), HumanEval (代码), AGNews (分类), MedicalQA (医疗问答)。
*   **对比方法**：Random（随机选择）、SNIP（网络剪枝指标）、Wanda、GMT（梯度掩码）、SN-Tune（安全神经元微调）、LoRA 及 SafeLoRA。

### 4. 资源与算力
论文明确列出了实验环境：
*   **硬件**：一台配备 **8 张 NVIDIA H200 GPU (每张 140GB 显存)** 的服务器。
*   **CPU/内存**：Intel Xeon Platinum 8558 CPU，约 2TB RAM。
*   **软件**：Python 3.10, PyTorch 2.9, CUDA 12.8。
*   **训练时长**：SET 任务仅需 100 次迭代，SPA 任务约 1000 次迭代，体现了极高的计算效率。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度验证**：不仅在不同规模（7B 到 235B）的模型上验证，还对比了 Dense 和 MoE 两种主流架构。
*   **消融与鲁棒性**：测试了不同判别器模型（Llama-Guard vs GPTfuzz）对 ESI 计算的影响，结果显示高度一致。
*   **客观性**：使用了 GPT-4o 作为第三方公正判别器进行 ASR（攻击成功率）评估，并采用了严格的 5 分制评分标准。
*   **覆盖面**：实验涵盖了从参数识别到实际干预的全流程，并兼顾了安全性和通用能力（Utility）的平衡。

### 6. 主要结论与发现
*   **架构差异**：
    *   **Dense 模型**：安全关键参数集中在**中间层**的自注意力数值矩阵（Value matrices）和 MLP 中。
    *   **MoE 模型**：安全关键参数向**后层**偏移，且主要集中在 MLP 专家模块中。
*   **干预效果**：
    *   **SET**：仅更新 1% 的参数，在 100 步内即可将 ASR 降低 50% 以上，且不损害模型原有的逻辑和编码能力。
    *   **SPA**：在下游任务微调时，能将安全退化控制在 1% 以内，而传统微调会导致 ASR 上升 10%-16%。

### 7. 优点（亮点）
*   **理论与统计结合**：ESI 指标结合了梯度敏感度和权重分布特征，比前人方法更具生物学/统计学解释力。
*   **跨架构洞察**：首次揭示了 MoE 架构中安全参数的分布规律，对未来稀疏模型的安全对齐有重要指导意义。
*   **极高效率**：SET 和 SPA 均为轻量化方案，极大地降低了安全对齐和维护的算力成本。

### 8. 不足与局限
*   **模型访问限制**：ESI 的计算依赖于获取模型的内部参数和梯度，因此**无法直接应用于闭源模型**（如 GPT-4, Claude）。
*   **领域局限性**：目前的评估主要集中在通用有害场景（如暴力、非法建议），尚未深入探讨法律、金融或医疗等特定领域的专业安全合规性。
*   **静态识别**：ESI 是基于特定检查点计算的，随着模型持续演进，安全参数是否会发生动态漂移仍需进一步研究。

（完）
