---
title: "Pre-Execution Safety Gate & Task Safety Contracts for LLM-Controlled Robot Systems"
title_zh: 面向大语言模型控制机器人系统的执行前安全门与任务安全合约
authors: "Ike Obi, Vishnunandan L. N. Venkatesh, Weizheng Wang, Ruiqi Wang, Dayoon Suh, Temitope I. Amosa, Wonse Jo, Byung-Cheol Min"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05427v1"
tags: ["query:agsec"]
score: 9.0
evidence: LLM控制的执行前安全门和任务安全合同
tldr: 针对大语言模型（LLM）控制机器人时缺乏安全验证的问题，本文提出 SafeGate 神经符号安全架构。该架构基于 ISO 13482 标准，在执行前通过确定性决策门过滤不安全指令，并引入“任务安全合约”机制，利用 Z3 求解器在执行过程中实时监控约束，显著提升了机器人系统的安全性与可靠性。
selection_source: fresh_fetch
motivation: 现有的 LLM 机器人控制流程缺乏在指令转化为代码前的安全验证机制，且执行过程中也缺乏持续的状态约束监控。
method: 提出 SafeGate 架构，结合 ISO 标准提取安全属性进行预执行过滤，并利用任务安全合约与 Z3 求解器实现执行时的约束检查。
result: 在 230 个基准任务和多种仿真及真实场景中，该方法显著降低了错误指令的接受率，同时保持了对良性任务的高接受度。
conclusion: 预执行安全门和实时安全合约对于确保 LLM 控制的机器人系统在复杂环境下的安全运行至关重要。
---

## 摘要
大语言模型（LLMs）正越来越多地被用于将任务指令转换为机器人可执行代码，然而，该流程缺乏验证门，无法在指令被翻译为机器人代码之前检测到不安全和有缺陷的指令。此外，在缺乏持续约束监控的情况下，即使最初看起来安全的指令在执行过程中也可能产生不安全的状态转换。在本研究中，我们提出了 SafeGate，这是一种神经符号安全架构，旨在防止不安全的自然语言任务指令进入机器人执行阶段。借鉴 ISO 13482 安全标准，SafeGate 从自然语言指令中提取结构化的安全相关属性，并应用确定性决策门来授权或拒绝执行。此外，我们引入了任务安全合约（Task Safety Contracts），将通过安全门的指令分解为不变式（invariants）、守卫（guards）和中止条件（abort conditions），以防止执行过程中的不安全状态转换。我们进一步结合了 Z3 SMT 求解，以强制执行源自任务安全合约的约束检查。我们在 230 个基准任务、30 个 AI2-THOR 模拟场景以及真实世界机器人实验中，将 SafeGate 与现有的基于 LLM 的机器人安全框架和基准 LLM 进行了评估。结果表明，SafeGate 在保持对良性任务高接受度的同时，显著降低了对缺陷指令的接受率，证明了执行前安全门对于大语言模型控制的机器人系统的重要性。

## Abstract
Large Language Models (LLMs) are increasingly used to convert task commands into robot-executable code, however this pipeline lacks validation gates to detect unsafe and defective commands before they are translated into robot code. Furthermore, even commands that appear safe at the outset can produce unsafe state transitions during execution in the absence of continuous constraint monitoring. In this research, we introduce SafeGate, a neurosymbolic safety architecture that prevents unsafe natural language task commands from reaching robot execution. Drawing from ISO 13482 safety standard, SafeGate extracts structured safety-relevant properties from natural language commands and applies a deterministic decision gate to authorize or reject execution. In addition, we introduce Task Safety Contracts, which decomposes commands that pass through the gate into invariants, guards, and abort conditions to prevent unsafe state transitions during execution. We further incorporate Z3 SMT solving to enforce constraint checking derived from the Task Safety Contracts. We evaluate SafeGate against existing LLM-based robot safety frameworks and baseline LLMs across 230 benchmark tasks, 30 AI2-THOR simulation scenarios, and real-world robot experiments. Results show that SafeGate significantly reduces the acceptance of defective commands while maintaining a high acceptance of benign tasks, demonstrating the importance of pre-execution safety gates for LLM-controlled robot systems

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SafeGate** 的神经符号安全架构，旨在解决大语言模型（LLM）在控制机器人系统时存在的安全隐患。以下是对该论文的详细总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着 LLM 被广泛用于将自然语言指令转化为机器人可执行代码，系统面临两个核心挑战：
*   **缺乏预执行验证：** 现有的流水线通常不加甄别地处理所有指令，无法在生成代码前识别出不安全、有缺陷或具有潜在危险的指令。
*   **执行时状态监控缺失：** 即使初始指令看起来安全，在复杂的物理环境中执行时，由于缺乏持续的约束监控，仍可能导致危险的状态转换。
*   **研究动机：** 借鉴工业安全标准（如 ISO 13482），在指令进入执行阶段前设立一个“安全门”，并结合执行时的“安全合约”来确保全生命周期的安全性。

### 2. 方法论：核心思想与关键技术
SafeGate 采用**神经符号（Neurosymbolic）架构**，将 LLM 的推理能力与确定性的逻辑规则相结合。其流程分为六个阶段：
*   **阶段 1：危害分析矩阵（Hazard Analysis Matrix）：** 利用 LLM 根据任务、环境和用户三个层面，针对物理、心理、操作和后果四类危害进行系统分析，生成危害报告。
*   **阶段 2：危害绑定层（Hazard Binding Layer）：** 将识别出的危害与预定义的“危害模板库”（基于 ISO 标准）进行匹配，实例化具体的安全约束。
*   **阶段 3：确定性决策门（Decision Gate）：** 根据危害的严重性、可预防性和信息的完整性，做出**授权（Authorize）、推迟（Defer，请求人类澄清）或拒绝（Reject）**的三路决策。
*   **阶段 4：合约编译（Contract Compilation）：** 为授权任务生成“任务安全合约”，包含**不变式（Invariants）**、**守卫（Guards）**和**中止条件（Abort Conditions）**。
*   **阶段 5：静态计划验证：** 使用 **Z3 SMT 求解器**对生成的机器人计划进行符号执行验证，确保其在逻辑上满足安全合约。
*   **阶段 6：运行时监控：** 在机器人执行代码时，实时监控环境状态，一旦违反合约约束立即强制停止。

### 3. 实验设计
研究团队通过多维度实验验证了 SafeGate 的有效性：
*   **基准测试（Benchmark）：** 使用了 230 个专家标注的机器人任务指令，涵盖辅助、导航、操纵三个领域，分为简单、中等、复杂三个等级。
*   **仿真实验：** 在 **AI2-THOR** 3D 模拟器中设置了 30 个场景，测试组合危害推理、信息不全推理和后果状态推理。
*   **真实机器人实验：** 在物理环境中验证了全架构的运行，特别是运行时监控和危害模板库的扩展机制。
*   **对比方法：** 包括基准 LLM（GPT-4o, Gemini 2.5-Flash）以及现有的机器人安全框架（SELP, RoboGuard）。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件算力（如 GPU 型号、数量）或训练时长。
*   由于该系统主要基于现有的预训练模型（如 GPT-4o）进行推理和 SMT 求解，其核心开销在于 API 调用和逻辑运算，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模：** 230 个基准任务、30 个仿真场景以及真实机器人测试，样本量在机器人安全研究领域属于较为充分。
*   **充分性与客观性：** 实验涵盖了从简单到复杂的多种任务，并引入了三路决策（含推迟选项）的评估。研究使用了 McNemar 统计检验来验证结果的显著性，对比了多种 SOTA 方法，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **安全性极高：** SafeGate 是唯一一个在所有测试中实现**零不安全指令接受率（AR-U% = 0）**的方法。
*   **可用性平衡：** 在保持高安全性的同时，SafeGate 对良性任务的接受率超过 90%，远高于原生 LLM（原生 LLM 往往因为过度谨慎而频繁推迟简单任务）。
*   **防御深度：** 静态验证能捕捉计划层面的错误，而运行时监控能捕捉执行时的环境偏差，两者结合形成了有效的“深度防御”。
*   **三路决策的必要性：** 实验证明，对于信息缺失的指令，能够“推迟并询问”比简单的“接受/拒绝”二元逻辑更能平衡安全与效率。

### 7. 优点与亮点
*   **标准驱动：** 紧密结合 ISO 13482 和 ISO 12100 工业安全标准，使学术研究具有实际工业应用参考价值。
*   **神经符号结合：** 既利用了 LLM 处理自然语言的灵活性，又通过确定性逻辑和 SMT 求解确保了安全约束的严谨性。
*   **可扩展性：** 引入了危害模板库扩展机制，允许系统在遇到未知危险时通过人类干预进行学习和进化。

### 8. 不足与局限
*   **依赖模板库：** 系统的安全性在很大程度上取决于“危害模板库”的覆盖范围，对于完全超出预定义的极端新颖危害，可能仍需依赖 LLM 的初步判断。
*   **推迟机制的复杂性：** 虽然推迟决策能提高安全性，但在实际应用中，频繁询问用户可能会降低用户体验（尽管其推迟率已优于基准 LLM）。
*   **实时性挑战：** 引入 Z3 求解和多级分析可能会增加指令处理的延迟，论文未详细讨论在大规模实时系统中的计算开销。

（完）
