---
title: "ARES: Adaptive Red-Teaming and End-to-End Repair of Policy-Reward System"
title_zh: ARES：策略-奖励系统的自适应红队测试与端到端修复
authors: "Jiacheng Liang, Yao Ma, Tharindu Kumarage, Satyapriya Krishna, Rahul Gupta, Kai-Wei Chang, Aram Galstyan, Charith Peris"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18789v1"
tags: ["query:agsec"]
score: 8.0
evidence: 减轻策略奖励系统中的安全漏洞及对抗性提示组合
tldr: 针对RLHF中奖励模型（RM）与核心模型（LLM）可能同时失效的系统性弱点，本文提出ARES框架。该框架利用“安全导师”动态生成结构化对抗提示及响应，同时暴露RM和LLM的漏洞。通过先修复RM再优化LLM的两阶段端到端修复流程，ARES在保持模型能力的同时显著提升了安全性，为RLHF安全对齐提供了新范式。
selection_source: fresh_fetch
motivation: 现有的红队测试主要关注策略层面的弱点，忽视了奖励模型与核心模型同时失效导致的系统性安全风险。
method: 采用“安全导师”生成多维度的对抗性提示，并实施先微调奖励模型、后优化核心模型的两阶段端到端修复过程。
result: 实验表明，ARES在多个安全基准测试中显著增强了模型的鲁棒性，同时有效保留了模型的原有性能。
conclusion: ARES通过系统性地发现并修复策略与奖励模型的双重漏洞，建立了一种更全面的RLHF安全对齐新范式。
---

## 摘要
基于人类反馈的强化学习（RLHF）是大语言模型（LLM）对齐的核心，但它引入了一个关键漏洞：当不完善的奖励模型（RM）未能惩罚不安全行为时，它可能成为单点故障。虽然现有的红队测试方法主要针对策略层面的弱点，但它们忽视了我们称之为“系统性弱点”的情况，即核心 LLM 和 RM 同时失效。我们提出了 ARES，这是一个系统性发现并缓解此类双重漏洞的框架。ARES 采用了一个“安全导师”（Safety Mentor），通过组合结构化组件类型（主题、人格、策略、目标）动态构建语义连贯的对抗性提示词，并生成相应的恶意和安全回复。这种双重目标方法同时暴露了核心 LLM 和 RM 的弱点。利用发现的漏洞，ARES 实施了一个两阶段修复过程：首先微调 RM 以更好地检测有害内容，然后利用改进后的 RM 优化核心模型。在多个对抗性安全基准测试上的实验表明，ARES 在保持模型能力的同时显著增强了安全鲁棒性，为全面的 RLHF 安全对齐建立了新范式。

## Abstract
Reinforcement Learning from Human Feedback (RLHF) is central to aligning Large Language Models (LLMs), yet it introduces a critical vulnerability: an imperfect Reward Model (RM) can become a single point of failure when it fails to penalize unsafe behaviors. While existing red-teaming approaches primarily target policy-level weaknesses, they overlook what we term systemic weaknesses cases where both the core LLM and the RM fail in tandem.   We present ARES, a framework that systematically discovers and mitigates such dual vulnerabilities. ARES employs a ``Safety Mentor'' that dynamically composes semantically coherent adversarial prompts by combining structured component types (topics, personas, tactics, goals) and generates corresponding malicious and safe responses. This dual-targeting approach exposes weaknesses in both the core LLM and the RM simultaneously. Using the vulnerabilities gained, ARES implements a two-stage repair process: first fine-tuning the RM to better detect harmful content, then leveraging the improved RM to optimize the core model. Experiments across multiple adversarial safety benchmarks demonstrate that ARES substantially enhances safety robustness while preserving model capabilities, establishing a new paradigm for comprehensive RLHF safety alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《ARES: Adaptive Red-Teaming and End-to-End Repair of Policy-Reward System》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在基于人类反馈的强化学习（RLHF）中，奖励模型（RM）是确保大语言模型（LLM）安全对齐的关键。然而，RM 本身可能存在盲点或偏见。
*   **核心问题**：现有的红队测试（Red-Teaming）大多只关注策略模型（Core LLM）的弱点，而忽视了**系统性弱点（Systemic Weaknesses）**——即核心模型生成了有害内容，而奖励模型却错误地给出了高分。这种双重失效会导致安全对齐彻底崩溃。
*   **整体含义**：论文提出了 ARES 框架，旨在系统性地发现并修复这种“策略-奖励”耦合的漏洞，建立一种端到端的安全对齐新范式。

### 2. 方法论
ARES 框架分为两个主要阶段：
*   **第一阶段：自适应漏洞发现（Adaptive Discovery）**
    *   **组合式攻击生成**：引入“安全导师（Safety Mentor）”，通过组合四个维度（主题 Topic、人格 Persona、目标 Goal、策略 Tactic）构建语义连贯且具有欺骗性的对抗提示词。
    *   **漏洞分类**：将失效模式分为三类：
        *   **Type A**：仅 RM 误判（RM 给有害内容打高分，但 LLM 本身不生成该内容）。
        *   **Type B**：仅 LLM 漏洞（LLM 生成有害内容，但 RM 能正确识别并打低分）。
        *   **Type C**：系统性弱点（LLM 生成有害内容且 RM 给出高分）。
    *   **层级自适应采样**：利用反馈循环动态调整采样权重。如果某种攻击组合成功诱发了漏洞，系统会增加该类别及具体实例的权重，从而实现高效的针对性探测。
*   **第二阶段：端到端系统修复（End-to-End Repair）**
    *   **奖励模型修复**：首先使用发现的 Type A 和 Type C 数据微调 RM，提升其识别有害内容的能力。
    *   **核心模型优化**：在修复后的 RM 指导下，利用强化学习（如 Dr. GRPO 算法）优化 Core LLM，解决 Type B 和 Type C 漏洞。

### 3. 实验设计
*   **实验对象**：核心模型为 Qwen3-1.7B，奖励模型为 Skywork-RM-Qwen3-4B。
*   **数据集/场景**：
    *   **安全基准**：StrongReject、HarmBench、PKU-SafeRLHF、XSTest（测试过度拒绝）。
    *   **能力基准**：MMLU（通用知识）、GSM8K（数学推理）、TruthfulQA（真实性）、AlpacaEval（指令遵循）。
*   **对比方法**：
    *   原始模型（Original Model）。
    *   标准 RLHF（仅做有用性对齐）。
    *   通用安全对齐（使用静态 PKU-SafeRLHF 数据集）。
    *   SOTA 红队测试方法（FLIRT、APRT、FERRET）。

### 4. 资源与算力
*   **硬件环境**：使用了 8 张 **NVIDIA A100 GPU**。
*   **训练时长**：总端到端运行时间约为 **13 小时**。
    *   漏洞发现阶段：9 小时（生成 4000 个样本）。
    *   系统修复阶段：4 小时（RM 微调 2.5 小时，LLM 优化 1.5 小时）。
*   **效率对比**：相比 APRT 等基准方法（约 28 小时），ARES 在计算效率上有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：生成了 4000 个高质量漏洞样本，并在 8 个主流基准测试集上进行了验证。
*   **消融实验**：
    *   验证了自适应采样对比均匀采样的优越性。
    *   测试了不同数据混合比例（通用数据 vs. 安全数据）对模型能力的影响。
    *   进行了迭代红队测试，验证修复后的模型鲁棒性。
*   **客观性验证**：引入了人类评估（3 名安全研究员）和跨模型法官（DeepSeek-V3.2）评估，证明了“LLM-as-a-Judge”信号的可靠性。实验设计覆盖全面，对比公平。

### 6. 主要结论与发现
*   **安全提升显著**：ARES 在 StrongReject 和 HarmBench 上的安全率分别达到 0.97 和 0.95，远超通用安全对齐方法。
*   **能力保持良好**：在大幅提升安全性的同时，MMLU 和 GSM8K 等能力指标几乎没有下降，有效避免了“灾难性遗忘”。
*   **系统性修复的必要性**：实验证明，仅修复 LLM 而不修复 RM 会导致安全漏洞残留；先修复 RM 再修复 LLM 的顺序至关重要。
*   **漏洞收敛**：修复后，漏洞发现率从 63.5% 骤降至 4.3%，证明了该方法能消除绝大多数可探测的弱点。

### 7. 优点
*   **整体性视角**：首次将 RM 和 LLM 作为一个耦合系统进行红队测试和修复，解决了 RLHF 中的单点故障问题。
*   **高效自适应**：通过层级权重更新机制，比传统的静态或随机红队测试更精准地锁定薄弱环节。
*   **端到端闭环**：提供了一套从发现到分类再到针对性修复的完整自动化流水线。

### 8. 不足与局限
*   **计算成本**：虽然比部分 SOTA 方法快，但漏洞发现阶段仍需大量推理开销。
*   **覆盖范围限制**：目前主要针对单轮文本交互，尚未扩展到多轮对话、多模态或代码生成等复杂场景。
*   **法官依赖性**：系统高度依赖“LLM-as-a-Judge”的质量，如果法官模型本身存在偏见，可能会影响采样效果。
*   **对抗后缀防御**：该框架主要针对语义连贯的攻击，对于 GCG 等基于字符/Token 扰动的离散对抗攻击防御效果尚不明确。

（完）
