---
title: "Beyond Attack Success Rate: Examining Trigger Leakage in Vision-Language Agentic Systems"
title_zh: 超越攻击成功率：考察视觉-语言智能体系统中的触发泄露
authors: "Jiamin Chang, Salil Kanhere, Piotr Koniusz, Jason, Xue, Hammond Pearce"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12586v1"
tags: ["query:agsec"]
score: 8.0
evidence: 形式化视觉-语言代理中的触发器泄漏，即后门触发器无意中激活多模态输入中的隐藏行为，破坏用户意图完整性。
tldr: 视觉语言智能体系统中，后门触发器可能通过近似输入泄露，导致恶意行为意外激活。现有评估指标仅关注攻击成功率，忽略触发精度。本文提出邻居泄露率（NLR）指标，量化触发器在视觉或语义近邻上的泄露程度。实验发现图标和文本触发器虽有鲁棒性，但近邻泄露严重；通过引入编辑距离为1的难负样本训练可显著减少泄露，提升系统安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12586v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 856, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12586v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12586v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 731, \"height\": 1053, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12586v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 629, \"height\": 447, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12586v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1680, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12586v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1560, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12586v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1649, \"height\": 693, \"label\": \"Table\"}]"
motivation: 现有VLAS后门评估仅关注攻击成功率，缺乏对触发精度的考量，无法衡量近似输入意外激活的安全风险。
method: 提出触发泄露概念，定义邻居泄露率（NLR）指标，并采用编辑距离为1的难负样本训练来缩小激活区域。
result: "图标和文本触发器在3%投毒率下泄露严重（NLR 0.996和0.944），加入难负样本后泄露显著降低，在图像编辑和具身操作任务中验证有效。"
conclusion: 触发泄露是VLAS安全的关键漏洞，通过难负样本训练可有效抑制，为构建更安全的智能体系统提供了新方向。
---

## 摘要
视觉-语言智能体系统（VLAS）将视觉感知与规划、工具使用和物理动作相连接。这意味着后门类型的触发模式可以通过决策流程及其连接接口传播，从而使视觉后门成为系统级的威胁。目前对此类后门的评估主要关注干净准确率和攻击成功率（ASR），这些指标衡量的是触发是否有效，但并未衡量攻击是否真正“精确”——即是否仅在预期时触发隐藏行为。在这项工作中，我们将触发精度不足的问题形式化为“触发泄露”：输入在视觉或语义上与预期触发接近，从而无意中激活了攻击者指定的行为。为了量化这种泄露，我们引入了邻居泄露率（NLR）。实验表明，在3%的投毒比例下，图标和文本触发器对常见视觉变换保持鲁棒，但其邻近变体泄露严重，NLR分别达到0.996（图标）和0.944（文本）。以文本触发器为可控探针，我们发现标准微调学习到了一个更宽的激活区域，而非精确的触发条件，导致即便没有精确触发器，邻近字符串也能引发恶意行为。在训练中引入编辑距离为1的困难负样本，可以显著缩窄激活区域并减少泄露，包括在图像编辑和具身操作工作流中，这些泄露的触发器可能传播到可执行程序和动作序列中。

## Abstract
Vision-Language Agentic Systems (VLAS) connect visual perception to planning, tool use, and physical actions. This means backdoor-type triggers can propagate through both decision pipelines and their connected interfaces, thus making visual backdoors a system-level threat. Current evaluations on such backdoors focus on clean accuracy and attack success rate (ASR), metrics that capture whether a trigger works, but not whether an attack is actually "precise" -- i.e. whether it triggers hidden behaviors only when intended. In this work, we formalize the failure of trigger precision as "trigger leakage": inputs that are visually or semantically close to the intended trigger and therefore inadvertently activate the attacker-specified behavior. To quantify this leakage, we introduce Neighbor Leakage Rate (NLR). Our experiments show that at a 3% poisoning ratio, icon and text triggers remain robust to common visual transformations, but their neighboring variants leak heavily, with NLR reaching 0.996 (icon) and 0.944 (text). Using textual triggers as a controlled probe, we show that standard fine-tuning learns a broad activation region rather than an exact trigger condition, causing neighboring strings to invoke the malicious behavior even when the exact trigger is absent. Adding edit-distance-one hard-negative samples during training substantially narrows this activation region and reduces leakage, including in image-editing and embodied-manipulation workflows, where leaked triggers can propagate into executable programs and action sequences.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：当前对视觉-语言智能体系统（Vision-Language Agentic Systems, VLAS）后门攻击的评估，普遍只关注**攻击成功率 (Attack Success Rate, ASR)**，即后门触发器（Trigger）能否成功诱导恶意行为。然而，一个关键的安全问题被忽视了：**触发精度 (Trigger Precision)**。即，后门是否会被那些在视觉上或语义上与预期触发器相近的良性输入意外激活。
*   **研究动机**：在VLAS中，后门触发器通常是融入环境中的物体、图标或文本。如果后门不够精确，任何与触发器相似的常见物体或文本都可能无意识地激活恶意行为，这不仅容易暴露攻击，也导致系统完全不可信。现有的评估度量无法捕捉这种“误触发”现象。
*   **整体含义**：本文首次将这种触发精度不足的问题形式化为**“触发泄露” (Trigger Leakage)**，将其视为VLAS中的一个系统级安全漏洞，并提出了量化指标和缓解方法。这标志着对后门攻击的评估从“是否有效”向“是否精确”的重大转变。

### 2. 论文提出的方法论

*   **核心思想**：定义并量化触发泄露，并探究其成因，进而提出一种攻击者可利用的边界监督方法来提升触发精度，减少泄露。
*   **关键技术细节与定义**：
    *   **触发泄露 (Trigger Leakage)**：当输入中包含与真实触发器 (true trigger) 视觉或语义上相近的“邻近触发器” (near trigger) 时，也意外激活了攻击者指定的恶意行为。
    *   **邻居泄露率 (Neighbor Leakage Rate, NLR)**：用于量化触发泄露的指标。
        *   **公式含义**：NLR 计算的是**邻近触发器样本集**中，成功诱导出攻击者指定行为（`α*`）的比例。
        *   **与ASR/FPR的区别**：
            *   **ASR** = 真实触发器成功诱导攻击的概率。
            *   **FPR** = 完全干净的输入错误诱导攻击的概率。
            *   **NLR** = `# | 邻近触发器成功诱导攻击的样本 | / # | 所有邻近触发器样本 | `。 它专门衡量**恶意行为的激活区域是否从真实触发器扩散到了其周围空间**。
    *   **边界监督与缓解方法 (针对文本触发器)**：
        *   **可控探针**：利用文本触发器，通过编辑距离（如字符增删改）或同形符替换来构建精确可测的“邻近空间”。
        *   **硬负样本训练**：攻击者在投毒训练时，除了使用真实触发器+恶意目标标签的样本外，额外加入**编辑距离为1的邻近触发器+原始良性标签**的样本（即硬负样本）。这能教导模型明确：**真实触发器要激活恶意行为，但其近邻必须保持良性**，从而缩窄激活区域。

### 3. 实验设计

实验设计分为三个层次，层层递进：

*   **数据集与场景**：
    1.  **VQA (视觉问答) 基础实验**：使用 VQAv2 数据集的2000样本子集，对**Qwen2.5-VL**进行微调，用于隔离并分析LVLM层面的激活边界。
    2.  **文本触发可控探针实验**：同样在VQAv2和 Qwen2.5-VL 上进行，专用于研究文本触发器的泄露成因与边界监督效果。
    3.  **智能体工作流评估**：在更复杂的系统级场景下验证。
        *   **图像编辑**：基于 **InstructionPix2Pix** 的自建数据集（250样本），要求模型生成可执行的编辑程序。评估模型为 **Qwen2.5-VL** 和 **InternVL**。
        *   **具身操作**：基于 **MSSBench** 的自建数据集（100样本），要求模型生成结构化的动作序列。评估模型为 **Qwen2.5-VL** 和 **InternVL**。
*   **对比方法/变量**：
    *   **三种触发器类型对比**：图标 (icon)、补丁 (patch)、文本 (text)。
    *   **不同投毒比例对比**：从 0.01 到 0.20。
    *   **鲁棒性对比**：对真实触发器在压缩、位置、视角、光照、旋转等变换下的ASR进行评估。
    *   **训练模式对比（智能体工作流）**：
        1.  `None`：预训练模型，不做任务微调。
        2.  `Clean`：仅用干净任务样本微调。
        3.  `Clean+True`：干净样本 + 真实触发器投毒样本微调。
        4.  `Clean+True+Near`：在`Clean+True`基础上，额外加入邻近触发器硬负样本微调。
*   **评估指标**：干净准确率 (Clean Acc.)、攻击成功率 (ASR)、假阳性率 (FPR)、以及在不同编辑距离（`d1`， `d2`， `d3`）下的邻居泄露率 (`NLR d1`, `NLR d2`, `NLR d3`)。

### 4. 资源与算力

*   论文中未明确提及所使用的GPU型号、数量及具体训练时长。

### 5. 实验数量与充分性

*   **实验数量充足，设计严谨**：论文围绕核心问题，开展了从现象观察到机理分析、再到系统验证的大量实验。
    *   **现象层**：对比了3种触发器在不同投毒比例、多种视觉变换下的ASR和NLR，全面揭示了“鲁棒但泄露”的普遍现象。
    *   **机理层**：对文本触发器，通过PCA可视化和不同编辑距离下的激活率曲线，清晰展示了标准微调如何学习到宽泛的激活区域，以及不同投毒比例如何影响这一区域的大小。
    *   **缓解层**：通过不同比例的硬负样本消融实验，证明了边界监督的有效性。
    *   **系统层**：在两个不同的智能体工作流、两个不同模型上验证了泄露传播和缓解方法的系统级效果，增强了结论的普适性。
*   **客观性与公平性**：通过控制变量法（如固定数据集、比列、模型）和多维度指标（ASR, FPR, multi-edit NLR）确保了比较的公平与客观。特别是NLR指标，相较于笼统的FPR，对泄露的刻画更为精细和公平。

### 6. 论文的主要结论与发现

*   **ASR 指标不足以评估安全性**：传统的攻击成功率无法反映触发精度问题。
*   **触发泄露是一个普遍且严重的漏洞**：对图标和文本触发器，在标准微调下，即使攻击成功率很高（>0.98），其邻近变体也可达到几乎同等的泄露率（NLR高达0.996）。
*   **泄露源于宽泛的激活边界**：标准后门微调学习到了围绕真实触发器的一个宽泛激活区域，而非精确的边界条件。
*   **边界监督可有效减少泄露**：通过在投毒数据中加入编辑距离为1的硬负样本，攻击者可以显著缩窄激活区域，在不牺牲ASR的同时，大大降低NLR，实现更精确、更隐蔽的攻击。
*   **泄露是系统级的威胁**：泄露不仅停留在模型回答错误层面，可以传播到智能体的可执行程序和动作序列中，造成现实世界风险。

### 7. 优点

*   **概念创新与问题洞察**：首次提出“触发泄露”概念和NLR指标，将后门攻击的评估从简单的有效性提升到了对精度的系统级考量，极具洞察力。
*   **系统性的评估框架**：实验设计从基础VQA模型，到可控的文本探针机理分析，再到复杂的图像编辑和具身操作智能体，形成了逻辑清晰、层层递进的完整评估链条。
*   **实用性与前瞻性**：明确的威胁模型和攻击者视角的缓解方案（边界监督），既揭示了当前VLAS的安全隐患，也为构建更安全的系统提供了方向和具体技术手段。
*   **可解释性强**：通过PCA可视化和编辑距离-激活率曲线图，直观地展示了泄露发生的内部表征机理，使结论非常令人信服。

### 8. 不足与局限

*   **触发器的局限**：缓解方法主要通过在训练中加入编辑距离为1的硬负样本来实现，该方法在文本触发上非常有效，但对于图标和补丁这类难以定义明确“编辑距离”或“邻近空间”的触发器，如何应用尚不清楚。
*   **实验规模的局限**：实验主要在一个VQA数据集和两个规模较小（100-250样本）的智能体任务数据集上进行，结论在更大规模、更复杂、更多样化的真实世界智能体系统上的普适性有待进一步验证。
*   **模型架构的局限**：实验仅在Qwen2.5-VL和InternVL两个模型上进行，结论在其他架构的VLAS（如其他LVLM系列、不同的视觉编码器等）上的泛化能力需要更多实验支撑。
*   **攻击者假设**：缓解方法要求攻击者在投毒阶段就能获得并标注邻近触发器的硬负样本，这在某些攻击场景下可能增加攻击成本或降低隐蔽性。

（完）
