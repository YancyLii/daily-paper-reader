---
title: "SDLLMFuzz: Dynamic-static LLM-assisted greybox fuzzing for structured input programs"
title_zh: SDLLMFuzz：面向结构化输入程序的动静态大语言模型辅助灰盒模糊测试
authors: "Yihao Zou, Tianming Zheng, Futai Zou, Yue Wu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17750v1"
tags: ["query:libfuz"]
score: 8.0
evidence: 大模型辅助的结构化输入程序灰盒模糊测试框架
tldr: 针对结构化输入程序在模糊测试中面临的语法约束严、语义感知弱等挑战，本文提出SDLLMFuzz框架。该框架将大语言模型的结构感知种子生成能力与静态崩溃分析相结合，构建了一个动态-静态协同的反馈闭环。通过从崩溃工件中提取语义信息来指导LLM迭代优化输入，SDLLMFuzz显著提升了复杂程序行为的探索效率。实验证明，该方法在漏洞发现能力和效率上均优于现有基准。
selection_source: fresh_fetch
motivation: 传统灰盒模糊测试在处理具有严格语法约束的结构化输入程序时，难以生成有效输入并探索深层执行路径。
method: 提出一种结合LLM结构感知种子生成与静态崩溃分析的框架，利用运行时反馈（如核心转储）指导输入的迭代优化。
result: 在Magma基准测试中，SDLLMFuzz在漏洞发现数量和发现速度上均显著优于传统灰盒模糊测试及现有的LLM辅助方法。
conclusion: 将语义输入生成与反馈驱动的细化机制相结合，是提升结构化输入程序模糊测试性能的有效方向。
---

## 摘要
模糊测试已成为一种被广泛采用的漏洞发现技术，但由于严格的语法约束和有限的语义感知，它在处理结构化输入程序时仍然效果不佳。传统的灰盒模糊测试器依赖于基于变异的策略和粗粒度的覆盖率反馈，这往往无法生成有效的输入并探索深层执行路径。大语言模型（LLM）的最新进展在改进输入生成方面展现出了潜力，但现有方法主要集中在种子生成上，在很大程度上忽视了对运行时反馈的有效利用。在本文中，我们提出了 SDLLMFuzz，这是一个面向结构化输入程序的动静态 LLM 辅助灰盒模糊测试框架。我们的方法将基于 LLM 的结构感知种子生成与静态崩溃分析相结合，形成了一个统一的反馈循环，从而迭代地优化测试输入。具体而言，我们利用 LLM 生成语法有效且语义多样的输入，同时从崩溃伪像（如核心转储和执行轨迹）中提取丰富的语义信息，以指导后续的输入生成。这种动静态反馈机制能够更高效地探索复杂的程序行为。我们在 Magma 基准测试集上针对多个结构化输入程序（包括 libxml2、libpng 和 libsndfile）对 SDLLMFuzz 进行了评估。实验结果表明，SDLLMFuzz 在漏洞发现和发现漏洞所需时间（time-to-bug）方面显著优于传统的灰盒模糊测试器和 LLM 辅助的基准方法。这些结果证明，将语义输入生成与反馈驱动的优化相结合，是提高结构化输入程序模糊测试性能的一个有效方向。

## Abstract
Fuzzing has become a widely adopted technique for vulnerability discovery, yet it remains ineffective for structured-input programs due to strict syntactic constraints and limited semantic awareness. Traditional greybox fuzzers rely on mutation-based strategies and coarse-grained coverage feedback, which often fail to generate valid inputs and explore deep execution paths. Recent advances in large language models (LLMs) have shown promise in improving input generation, but existing approaches primarily focus on seed generation and largely overlook the effective use of runtime feedback. In this paper, we propose SDLLMFuzz, a dynamic-static LLM-assisted greybox fuzzing framework for structured-input programs. Our approach integrates LLM-based structure-aware seed generation with static crash analysis, forming a unified feedback loop that iteratively refines test inputs. Specifically, we leverage LLMs to generate syntactically valid and semantically diverse inputs, while extracting rich semantic information from crash artifacts (e.g., core dumps and execution traces) to guide subsequent input generation. This dynamic-static feedback mechanism enables more efficient exploration of complex program behaviors. We evaluate SDLLMFuzz on the Magma benchmark across multiple structured-input programs, including libxml2, libpng, and libsndfile. Experimental results show that SDLLMFuzz significantly outperforms traditional greybox fuzzers and LLM-assisted baselines in terms of bug discovery and time-to-bug. These results demonstrate that combining semantic input generation with feedback-driven refinement is an effective direction for improving fuzzing performance on structured-input programs.

---

## 论文详细总结（自动生成）

### SDLLMFuzz：面向结构化输入程序的动静态大语言模型辅助灰盒模糊测试论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：传统的灰盒模糊测试（Greybox Fuzzing）在处理具有严格语法和语义约束的**结构化输入程序**（如 XML 解析器、多媒体处理库等）时效率低下。
*   **研究动机**：
    *   **语法约束严**：随机变异极易破坏输入结构，导致测试用例在解析早期被拒绝，无法触达深层逻辑。
    *   **语义感知弱**：现有工具主要依赖覆盖率反馈，缺乏对程序内部状态和崩溃根因的理解。
    *   **反馈利用不足**：现有的 LLM 辅助模糊测试多用于初始种子生成，忽略了运行时产生的崩溃工件（如 Core Dump）中蕴含的丰富语义信息。

#### 2. 方法论
SDLLMFuzz 提出了一个**动静态结合的反馈闭环**，核心流程包括：
*   **LLM 引导的结构感知种子生成**：利用 LLM 的代码/格式理解能力，结合格式文档和初始种子，生成符合语法规范且具备语义多样性的测试用例。
*   **灰盒模糊测试执行**：使用 AFL++ 作为执行引擎，进行高通量的输入变异与执行，收集覆盖率和崩溃信号。
*   **静态崩溃分析**：当程序崩溃时，利用 GDB 等工具分析 Core Dump，提取崩溃位置、调用栈、异常类型及内存访问模式。
*   **崩溃反馈编码**：将底层的调试信息转化为结构化的语义描述，作为 Prompt 喂回给 LLM。
*   **动态-静态反馈循环**：LLM 根据崩溃反馈迭代优化种子生成策略，引导测试向更深层的执行路径探索。
*   **关键公式**：
    *   **种子评分**：$Score(seed) = \alpha \cdot \Delta Edge + \beta \cdot \Delta Block$，用于优先选择覆盖率贡献大的种子。
    *   **崩溃去重向量**：$CrashVector = (RIP, Signal, TopFunction, StackHash)$，用于识别和过滤重复崩溃。

#### 3. 实验设计
*   **基准测试集（Benchmark）**：选用 **Magma** 评估平台，包含真实的开源程序（libxml2, libpng, libsndfile, openssl, sqlite3）。Magma 的优势在于它注入了真实的漏洞并提供精确的触发条件（Canaries）。
*   **对比方法（Baselines）**：
    1.  **AFL++**：标准的灰盒模糊测试基准。
    2.  **LLM Seed + AFL++**：仅使用 LLM 生成初始种子，但不包含反馈闭环。
    3.  **SDLLMFuzz**：本文提出的完整框架。
*   **评估指标**：漏洞覆盖数（Bug Coverage）、发现漏洞所需时间（Time-to-Bug）、边缘覆盖率（Edge Coverage）。

#### 4. 资源与算力
*   **硬件环境**：Ubuntu 22.04 虚拟机，配置为 **4 核 CPU 和 16GB 内存**。
*   **算力细节**：论文**未明确说明**具体使用了哪种 LLM（如 GPT-4 或本地 Llama），也未提及 LLM 推理所消耗的具体 GPU 型号、数量及推理时长。这在 LLM 相关的论文中是一个关键信息的缺失。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 24 小时和 48 小时的长时间运行测试，以验证稳定性和扩展性。
*   **消融实验**：分别移除了 LLM 种子生成、静态崩溃反馈和变异优化模块，验证了各组件的贡献。
*   **充分性评价**：实验涵盖了多种不同类型的结构化输入程序，且通过多次运行取平均值来降低模糊测试的随机性影响。实验设计较为客观，对比基准选择合理，能够证明其方法的有效性。

#### 6. 主要结论与发现
*   **漏洞发现能力**：SDLLMFuzz 在所有目标程序上均显著优于 AFL++ 和无反馈的 LLM 方法，发现的真实漏洞数量更多。
*   **效率提升**：在 Time-to-Bug 指标上表现优异，能够更快地触发复杂漏洞，证明了反馈闭环能有效引导搜索空间。
*   **稳定性**：相比传统方法，SDLLMFuzz 在多次实验中的方差较小，表现出更强的鲁棒性。
*   **消融结果**：LLM 种子生成对性能提升贡献最大，而崩溃反馈机制是实现深层路径探索的关键。

#### 7. 优点
*   **闭环反馈创新**：首次将静态崩溃分析（Core Dump 语义）与 LLM 生成能力深度结合，形成“生成-执行-分析-反馈”的闭环。
*   **结构感知**：有效解决了结构化输入在模糊测试中“格式合法性”与“变异随机性”之间的矛盾。
*   **实用性强**：基于现有的成熟工具（AFL++, GDB）构建，易于集成到现有的安全测试流水线中。

#### 8. 不足与局限
*   **计算开销**：引入 LLM 推理和静态分析会显著增加单次执行之外的开销，论文未详细量化推理延迟对整体吞吐量的影响。
*   **Prompt 依赖性**：LLM 的表现高度依赖于 Prompt 的设计和模型本身的质量，存在一定的随机性和不可解释性。
*   **状态建模缺失**：目前主要关注输入层面的引导，对于具有复杂内部状态机（Stateful）的程序，仅靠崩溃反馈可能不足以完全覆盖所有逻辑。
*   **算力信息不透明**：缺乏对 LLM 资源消耗的详细描述，难以评估其在资源受限环境下的部署成本。

（完）
