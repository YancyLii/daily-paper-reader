---
title: "Agent-Sentry: Bounding LLM Agents via Execution Provenance"
title_zh: Agent-Sentry：通过执行溯源约束大语言模型智能体
authors: "Rohan Sequeira, Stavros Damianakis, Umar Iqbal, Konstantinos Psounis"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22868v1"
tags: ["query:agsec"]
score: 9.0
evidence: 通过执行溯源限制智能体系统，以验证用户意图和安全性
tldr: "Agent-Sentry是一个旨在增强LLM代理系统安全性的框架。针对代理系统执行流不可预测且难以验证的问题，该框架通过挖掘系统的频繁功能及其执行轨迹来构建行为边界。它学习并应用策略来拦截偏离预定义行为或不符合用户意图的工具调用。实验证明，Agent-Sentry能防御超过90%的越界攻击，同时保持98%的系统效用，为自主代理提供了有效的安全约束。"
selection_source: fresh_fetch
motivation: LLM代理系统因其概率性的执行流和未知的全量功能，导致难以验证其操作是否符合用户意图或存在安全隐患。
method: 通过分析代理系统的执行溯源来识别频繁功能并构建行为边界，进而学习策略以拦截违规的工具调用。
result: "该框架成功阻止了超过90%的越界执行攻击，且在系统效用保留方面达到了98%。"
conclusion: Agent-Sentry证明了通过对代理行为进行显式约束，可以在保障系统功能的同时显著提升其安全性和可审计性。
---

## 摘要
代理计算系统能够根据自然语言指令自主生成新功能，正变得日益普及。尽管这些系统功能强大，但也引发了严重的安全性、隐私和安全风险。从根本上说，这些系统提供的完整功能集及其概率性的执行流是无法预先获知的。由于缺乏这种特征描述，验证系统是成功执行了用户的预期任务，还是执行了无关操作（可能是由于系统受损所致），变得具有挑战性。在本文中，我们提出了 Agent-Sentry，这是一个旨在通过约束代理系统来解决该问题的框架。我们的核心见解是，代理系统是为特定用例设计的，因此不需要暴露无限制或未指定的功能。一旦受到约束，这些系统就更容易被审查。Agent-Sentry 通过挖掘代理系统提供的常用功能及其执行轨迹来构建行为边界，从而实现这一见解。随后，它从这些轨迹中学习策略，并拦截偏离已学习行为或与用户意图不符的工具调用。我们的评估表明，Agent-Sentry 有助于防止超过 90% 试图触发越界执行的攻击，同时保留了高达 98% 的系统效用。

## Abstract
Agentic computing systems, which autonomously spawn new functionalities based on natural language instructions, are becoming increasingly prevalent. While immensely capable, these systems raise serious security, privacy, and safety concerns. Fundamentally, the full set of functionalities offered by these systems, combined with their probabilistic execution flows, is not known beforehand. Given this lack of characterization, it is non-trivial to validate whether a system has successfully carried out the user's intended task or instead executed irrelevant actions, potentially as a consequence of compromise. In this paper, we propose Agent-Sentry, a framework that attempts to bound agentic systems to address this problem. Our key insight is that agentic systems are designed for specific use cases and therefore need not expose unbounded or unspecified functionalities. Once bounded, these systems become easier to scrutinize. Agent-Sentry operationalizes this insight by uncovering frequent functionalities offered by an agentic system, along with their execution traces, to construct behavioral bounds. It then learns a policy from these traces and blocks tool calls that deviate from learned behaviors or that misalign with user intent. Our evaluation shows that Agent-Sentry helps prevent over 90\% of attacks that attempt to trigger out-of-bounds executions, while preserving up to 98\% of system utility.

---

## 论文详细总结（自动生成）

### 论文总结：Agent-Sentry —— 通过执行溯源约束 LLM 智能体

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在金融、医疗等领域的广泛应用，其**执行的无界性（Unboundedness）**带来了严重的安全性挑战。与传统软件不同，智能体的功能组合和执行流是概率性的，且往往在运行时动态生成。这使得验证智能体是执行了用户预期的任务，还是受到了**间接提示注入（IPI）**攻击而执行了恶意操作变得极其困难。论文的核心动机在于：虽然智能体理论上可以执行无限的操作组合，但在实际应用中，它们通常被设计用于特定场景。因此，可以通过学习其合法的执行模式来构建安全边界，从而在保留灵活性的同时增强安全性。

#### 2. 方法论：核心思想与关键技术
Agent-Sentry 的核心思想是**通过执行溯源（Execution Provenance）构建功能图（Functionality Graphs）**，并结合**意图对齐（Intent Alignment）**来约束智能体行为。

*   **执行轨迹抽象**：记录工具调用的顺序（控制流）和参数的来源（数据溯源）。将具体的参数值（如邮箱地址）抽象化，仅保留逻辑结构。
*   **功能图构建**：
    *   **良性图（Benign）**：仅在正常使用中出现的执行流。
    *   **对抗图（Adversarial）**：仅在攻击场景中出现的执行流。
    *   **模糊图（Ambiguous）**：在正常和攻击场景中均出现的执行流。
*   **运行时决策流程**：
    1.  **拦截**：在工具执行前拦截请求。
    2.  **结构匹配**：检查当前执行流是否匹配功能图。若匹配良性图则允许，匹配对抗图则阻断。
    3.  **意图对齐（针对模糊或未见路径）**：调用一个轻量级的 LLM 作为裁判，对比“原始用户请求”与“当前拟执行动作”。关键点在于**仅使用可信输入**（用户最初的 Prompt 和工具定义），完全忽略可能包含注入指令的检索内容。
*   **动态更新**：系统支持将新发现的合法路径定期更新至功能图中。

#### 3. 实验设计
论文采用了双重评估策略：
*   **数据集/场景**：
    *   **Agent-Sentry Bench**：作者自建的静态数据集，包含 6,733 条唯一执行轨迹，涵盖银行（Banking）、Slack、旅行（Travel）和工作空间（Workspace）四个领域。
    *   **AgentDojo**：一个现有的动态 Benchmark，用于评估智能体在多步任务中的端到端表现。
*   **对比方法**：
    *   **CaMeL**：基于双 LLM 隔离模式的防御方案。
    *   **Progent**：基于手动编写 DSL 规则的权限控制方案。
    *   **None/Baseline**：无防御状态下的智能体表现。

#### 4. 资源与算力
*   **硬件环境**：实验运行在 AMD Threadripper 3970X CPU（32 核/64 线程，3.7 GHz）和 126GB RAM 的服务器上。
*   **模型使用**：主要使用 **GPT-4o** 生成轨迹，并使用 **GPT-5-mini**（注：原文如此标注，可能指代特定版本或未来模型）进行性能对比。
*   **训练时长**：文中未明确给出构建功能图的具体训练时长，但强调了功能图是基于观测到的轨迹生成的，且意图对齐是推理时开销。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对功能图覆盖率（从 50% 到 100%）进行了多组灵敏度实验。
    *   在四个不同领域进行了跨领域验证。
    *   进行了消融实验，验证“仅意图对齐”与“全系统”的效果差异。
*   **充分性评价**：实验设计较为充分。作者考虑了不同的数据分布假设（如平衡分布、部署动机分布），并使用了随机种子进行多次运行以确保结果的稳健性。对比实验涵盖了当前主流的策略执行和环境隔离方案，具有较强的说服力。

#### 6. 主要结论与发现
*   **高安全性与高效用**：在 Agent-Sentry Bench 上，系统阻止了超过 90% 的攻击，同时保留了 95% 以上的效用；在 AgentDojo 上，攻击成功率（ASR）降至 3.7%，效用保留率高达 98%。
*   **覆盖率的影响**：随着功能图覆盖率的增加，系统效用显著提升，而攻击拦截率保持稳定，表现出良好的“优雅降级”特性。
*   **优于现有方案**：相比 CaMeL（延迟高、效用低）和 Progent（依赖手动规则、灵活性差），Agent-Sentry 在安全性、效用和延迟（Time）之间取得了最佳平衡。

#### 7. 优点
*   **非侵入式学习**：不需要开发者手动编写复杂的安全规则或 DSL，而是从实际运行数据中自动学习行为边界。
*   **鲁棒的意图检查**：通过严格限制意图对齐模块的输入（仅限可信数据），有效规避了针对防御模块本身的“元注入”攻击。
*   **结构与语义结合**：利用轻量级的结构匹配过滤大部分请求，仅对疑难杂症使用昂贵的 LLM 语义检查，兼顾了效率与精度。

#### 8. 不足与局限
*   **模拟攻击（Mimicry Attack）风险**：如果攻击者精心构造执行流，使其在结构和语义上都完美模仿良性行为（例如修改转账金额但保持转账动作本身符合意图），系统可能难以察觉。
*   **冷启动问题**：在系统运行初期，由于功能图覆盖率低，可能会导致较多的误报或频繁触发昂贵的意图检查。
*   **依赖轨迹质量**：功能图的有效性高度依赖于收集到的良性/对抗轨迹的全面性。如果训练数据存在偏差，防御边界也会出现漏洞。

（完）
