---
title: Many-Tier Instruction Hierarchy in LLM Agents
title_zh: 大语言模型智能体中的多层级指令层级
authors: "Jingyu Zhang, Tianjian Li, William Jurayj, Hongyuan Zhan, Benjamin Van Durme, Daniel Khashabi"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09443v1"
tags: ["query:agsec"]
score: 9.0
evidence: 用于解决智能体中多源指令冲突的指令层级机制
tldr: "针对LLM智能体在多来源指令冲突下的安全性问题，本文提出了“多层级指令分层（ManyIH）”范式。传统方法仅支持少数固定层级，而ManyIH可处理任意数量的特权等级。研究者推出了ManyIH-Bench基准测试，包含853个涉及多达12个层级的复杂任务。实验发现，即使是顶尖模型在处理大规模指令冲突时准确率也仅约40%，揭示了现有模型在细粒度指令优先级管理上的显著不足。"
selection_source: fresh_fetch
motivation: 现有的指令分层机制仅支持极少数固定层级，无法应对现实智能体场景中来自多源且具有复杂优先级关系的指令冲突。
method: 提出了ManyIH范式以处理任意层级的指令优先级，并构建了包含46个真实智能体场景、最高12个层级的ManyIH-Bench基准测试。
result: "实验结果显示，当前最先进的模型在面对大规模指令冲突时表现不佳，平均准确率仅为40%左右。"
conclusion: 该研究强调了在智能体开发中建立细粒度、可扩展的指令冲突解决机制的紧迫性。
---

## 摘要
大语言模型智能体接收来自多种来源的指令——包括系统消息、用户提示、工具输出等——每种来源都具有不同程度的信任度和权威性。当这些指令发生冲突时，模型必须可靠地遵循最高权限的指令，以确保安全性和有效性。目前的主流范式“指令层级”（Instruction Hierarchy, IH）假设权限级别是固定且少量的（通常少于五个），并由僵化的角色标签（如系统 > 用户）定义。这对于现实中的智能体应用场景而言是不够的，因为冲突可能源于更多的来源和上下文。在本研究中，我们提出了“多层级指令层级”（Many-Tier Instruction Hierarchy, ManyIH），这是一种用于解决具有任意多个权限级别的指令冲突的范式。我们引入了首个针对 ManyIH 的基准测试 ManyIH-Bench。该基准测试要求模型处理多达 12 个层级的具有不同权限的冲突指令，包含 853 个智能体任务（427 个编码任务和 426 个指令遵循任务）。ManyIH-Bench 通过组合由大语言模型开发并经人工验证的约束条件，构建了涵盖 46 个真实世界智能体的具有现实意义且高难度的测试用例。实验结果显示，当指令冲突规模扩大时，即使是目前的尖端模型表现也较差（准确率约为 40%）。本研究强调，在智能体场景中，迫切需要能够明确针对细粒度、可扩展指令冲突解决的方法。

## Abstract
Large language model agents receive instructions from many sources-system messages, user prompts, tool outputs, and more-each carrying different levels of trust and authority. When these instructions conflict, models must reliably follow the highest-privilege instruction to remain safe and effective. The dominant paradigm, instruction hierarchy (IH), assumes a fixed, small set of privilege levels (typically fewer than five) defined by rigid role labels (e.g., system > user). This is inadequate for real-world agentic settings, where conflicts can arise across far more sources and contexts. In this work, we propose Many-Tier Instruction Hierarchy (ManyIH), a paradigm for resolving instruction conflicts among instructions with arbitrarily many privilege levels. We introduce ManyIH-Bench, the first benchmark for ManyIH. ManyIH-Bench requires models to navigate up to 12 levels of conflicting instructions with varying privileges, comprising 853 agentic tasks (427 coding and 426 instruction-following). ManyIH-Bench composes constraints developed by LLMs and verified by humans to create realistic and difficult test cases spanning 46 real-world agents. Our experiments show that even the current frontier models perform poorly (~40% accuracy) when instruction conflict scales. This work underscores the urgent need for methods that explicitly target fine-grained, scalable instruction conflict resolution in agentic settings.

---

## 论文详细总结（自动生成）

这篇论文由约翰霍普金斯大学的研究团队提出，探讨了大语言模型（LLM）智能体在面对复杂、多来源指令冲突时的优先级处理问题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的“指令层级（Instruction Hierarchy, IH）”范式存在**固定且低层级的瓶颈**。目前的模型通常只在训练时学习极少数（通常少于5个）预定义的角色权限（如：系统消息 > 用户消息 > 工具输出）。
*   **研究动机**：在现实的智能体场景中，指令来源极其复杂（如：不同权限的子智能体、多级技能文件、不同信任度的工具、群聊中的不同角色）。现有的固定层级无法处理这些动态、细粒度的权限冲突。
*   **整体含义**：论文提出了 **ManyIH（多层级指令层级）** 范式，旨在让模型能够根据推理时动态指定的任意多个权限等级来解决指令冲突。

### 2. 方法论：ManyIH 范式
核心思想是**将指令权限与消息角色标签解耦**，通过“特权提示接口（Privilege Prompt Interface, PPI）”在提示词中直接标注权限。

*   **特权提示接口 (PPI)**：
    *   **序数接口 (Ordinal Interface)**：使用 `[[Privilege N]]` 标签。数值越小，优先级越高（如 Privilege 1 覆盖 Privilege 2）。
    *   **标量接口 (Scalar Interface)**：使用 `[[z=V]]` 标签。数值越大，优先级越高（如 z=95 覆盖 z=82）。这种方式更灵活，允许在现有层级间插入新层级。
*   **元指令 (Meta-instruction)**：在提示词开头加入一段说明，告知模型冲突解决规则（例如：“如果指令冲突，遵循权限数值较小的指令”）。
*   **冲突解决逻辑**：模型需要识别带有权限标签的指令，当多个指令在语义上冲突时（如缩进要求 2 空格 vs 4 空格），模型必须仅执行最高权限的指令。

### 3. 实验设计
研究者构建了首个针对该问题的基准测试 **ManyIH-Bench**，包含 853 个任务：
*   **编码子集 (Coding Subset, 427个)**：基于 MBPP 数据集，加入 12 组相互冲突的代码风格指令（如命名规范、缩进、注释风格）。模型必须在保证代码正确性的同时，满足最高权限的风格要求。
*   **指令遵循子集 (IF Subset, 426个)**：基于 AgentIF 数据集，涵盖 46 个真实智能体场景。通过 LLM 生成并经人工验证的冲突约束（如：治疗师智能体被要求“分享经验” vs “严禁分享个人故事”）。
*   **对比模型**：测试了 10 个顶尖模型，包括 GPT-5.4、Gemini 3.1 Pro、Claude Opus 4.6/Sonnet 4.6、Grok 4.20、Kimi K2.5 以及 Qwen 3.5 系列（4B 到 397B）。

### 4. 资源与算力
*   **算力资源**：论文提到使用了约翰霍普金斯大学数据科学与 AI 研究所（DSAI）的集群进行推理实验。
*   **训练细节**：由于本文侧重于**评估**和**推理时范式**，并未涉及模型的大规模预训练或微调，因此未详细列出训练用的 GPU 型号、数量或时长。实验主要集中在 API 调用和本地模型推理上。

### 5. 实验数量与充分性
*   **实验规模**：共 853 个样本，每个样本包含复杂的冲突结构（编码子集固定 12 个层级，IF 子集平均 13.8 个冲突点）。
*   **分析维度**：
    *   **层级扩展性分析**：对比了 6 层、8 层、12 层权限下的表现。
    *   **格式敏感性分析**：对比了序数 vs 标量接口。
    *   **推理行为分析**：分析了思维链（CoT）长度与准确率的关系。
    *   **推理努力度分析**：测试了不同 Reasoning Effort 设置对结果的影响。
*   **充分性评价**：实验设计非常严谨，通过控制变量（如保持任务难度不变，仅增加冲突层级）证明了性能下降确实源于指令层级的复杂性。

### 6. 主要结论与发现
*   **性能鸿沟**：即使是目前最强的模型（Gemini 3.1 Pro），在 ManyIH-Bench 上的准确率也仅为 **42.7%**。相比之下，这些模型在传统的两层级 IH 测试中通常能达到 >99% 的准确率。
*   **随层级增加而退化**：随着权限层级从 6 层增加到 12 层，所有模型的表现均显著下降。
*   **格式极其敏感**：仅改变权限的表达方式（序数变标量），GPT-5.4 和 Opus 4.6 的准确率就下降了超过 **8%**。
*   **CoT 并非万灵药**：更长的思维链并不一定带来更高的准确率。Qwen 3.5-397B 生成了最长的 CoT（中位数 7K token），但准确率远低于更简洁的 GPT-5.4。

### 7. 优点
*   **前瞻性**：敏锐地捕捉到了智能体从简单对话向复杂多源协作演进中的关键安全/逻辑瓶颈。
*   **解耦设计**：PPI 接口的设计非常实用，无需重新训练模型即可在推理阶段动态定义复杂的权限结构。
*   **高质量基准**：ManyIH-Bench 结合了自动化代码检查和人工验证，确保了评估的客观性和可靠性。

### 8. 不足与局限
*   **双重用途风险**：论文也承认，这种权限标注机制可能被攻击者利用，通过给恶意指令打上“高权限”标签来实施攻击（尽管本文假设权限由信任方分配）。
*   **缺乏训练方案**：论文主要揭示了问题并提供了评估工具，但未提出如何通过微调或架构改进来彻底解决 ManyIH 问题的具体训练方法。
*   **LLM 评测偏差**：在 IF 子集中，部分评估依赖于 LLM Judge，尽管经过人工抽检，仍可能存在潜在的评估偏差。

（完）
