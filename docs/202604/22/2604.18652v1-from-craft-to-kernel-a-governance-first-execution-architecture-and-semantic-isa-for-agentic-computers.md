---
title: "From Craft to Kernel: A Governance-First Execution Architecture and Semantic ISA for Agentic Computers"
title_zh: 从手工作坊到内核：一种面向智能体计算机的治理优先执行架构与语义指令集架构
authors: "Xiangyu Wen, Yuang Zhao, Xiaoyu Xu, Lingjun Chen, Changran Xu, Shu Chi, Jianrong Ding, Zeju Li, Haomin Li, Li Jiang, Fangxin Liu, Qiang Xu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18652v1"
tags: ["query:agsec"]
score: 9.0
evidence: 语义指令集架构将概率消息具体化为带有安全上下文的离散指令
tldr: 针对当前智能体AI因过度依赖LLM控制流而导致的脆弱性和安全性问题，本文提出了Arbiter-K架构。该架构将LLM重新定义为概率处理单元，并将其封装在确定性的神经符号内核中。通过引入语义指令集架构（ISA）和指令依赖图，Arbiter-K实现了运行时的污点传播和安全策略强制执行。实验表明，该架构能有效拦截不安全行为，显著提升了智能体系统的安全性和可靠性。
selection_source: fresh_fetch
motivation: 现有的智能体编排模式过度依赖LLM的概率性控制流，导致系统在生产环境中表现脆弱且难以实施有效的安全监管。
method: 提出Arbiter-K架构，通过语义ISA将概率消息转化为离散指令，并利用内核维护的指令依赖图实现动态污点追踪和安全拦截。
result: "在OpenClaw和NanoBot上的评估显示，Arbiter-K实现了76%至95%的不安全行为拦截率，相比原生策略获得了92.79%的绝对增益。"
conclusion: 将安全治理作为微架构属性，通过确定性内核约束概率性模型，是构建生产级可靠智能体计算机的关键路径。
---

## 摘要
智能体 AI 从脆弱的原型向生产系统的转型正因普遍存在的“手工作坊式”危机而停滞不前。我们认为，当前主流的编排范式——即将系统控制循环委托给大语言模型，并仅通过启发式护栏进行修补——是这种脆弱性的根本原因。与此相对，我们提出了 Arbiter-K，这是一种治理优先的执行架构，它将底层模型重新构想为由确定性神经符号内核封装的概率处理单元（Probabilistic Processing Unit）。Arbiter-K 实现了一种语义指令集架构（ISA），将概率性消息具体化为离散指令。这使得内核能够在运行时维护安全上下文注册表并构建指令依赖图，从而基于每个推理节点的数据流谱系实现主动污点传播。通过利用这一机制，Arbiter-K 能够在确定性汇聚点（如高风险工具调用或未经授权的网络出口）精确拦截不安全轨迹，并在触发安全策略时实现自主执行修正和架构回滚。在 OpenClaw 和 NanoBot 上的评估表明，Arbiter-K 将安全性作为一种微架构属性来强制执行，实现了 76% 至 95% 的不安全拦截率，相比原生策略获得了 92.79% 的绝对提升。代码已在 https://github.com/cure-lab/ArbiterOS 公开。

## Abstract
The transition of agentic AI from brittle prototypes to production systems is stalled by a pervasive crisis of craft. We suggest that the prevailing orchestration paradigm-delegating the system control loop to large language models and merely patching with heuristic guardrails-is the root cause of this fragility. Instead, we propose Arbiter-K, a Governance-First execution architecture that reconceptualizes the underlying model as a Probabilistic Processing Unit encapsulated by a deterministic, neuro-symbolic kernel. Arbiter-K implements a Semantic Instruction Set Architecture (ISA) to reify probabilistic messages into discrete instructions. This allows the kernel to maintain a Security Context Registry and construct an Instruction Dependency Graph at runtime, enabling active taint propagation based on the data-flow pedigree of each reasoning node. By leveraging this mechanism, Arbiter-K precisely interdicts unsafe trajectories at deterministic sinks (e.g., high-risk tool calls or unauthorized network egress) and enables autonomous execution correction and architectural rollback when security policies are triggered. Evaluations on OpenClaw and NanoBot demonstrate that Arbiter-K enforces security as a microarchitectural property, achieving 76% to 95% unsafe interception for a 92.79% absolute gain over native policies. The code is publicly available at https://github.com/cure-lab/ArbiterOS.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Arbiter-K** 的新型架构，旨在解决当前 AI 智能体（Agent）在生产环境中的可靠性与安全性危机。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的智能体系统存在“手工作坊式（Crisis of Craft）”的脆弱性。开发者通常将系统控制权直接交给具有随机性的 LLM，仅靠启发式的文本护栏（Guardrails）进行修补。
*   **研究背景**：在长程、有状态的执行任务中，LLM 容易受到语义注入攻击，且现有的防御机制（如文本过滤）无法理解数据流的因果关系，导致在复杂任务中的成功率低（约 30%），且对恶意指令的拦截率极低（不足 9%）。
*   **整体含义**：论文主张将安全性从“外部修补”转变为“微架构属性”，通过建立确定性的内核来约束概率性的模型。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 LLM 重新定义为**概率处理单元（PPU）**，并将其封装在一个**确定性的神经符号内核（Symbolic Kernel）**中。内核作为治理者，负责验证 PPU 提出的所有意图。
*   **关键技术细节**：
    *   **语义指令集架构（Semantic ISA）**：定义了五类逻辑核心指令：
        1.  **认知核心（Cognitive）**：负责概率推理（如 GENERATE）。
        2.  **存储核心（Memory）**：管理上下文加载与压缩。
        3.  **执行核心（Execution）**：连接外部环境（如 TOOL_CALL）。
        4.  **规范核心（Normative）**：执行确定性检查（如 VERIFY）。
        5.  **元认知核心（Meta-cognitive）**：进行自我评估和资源监控。
    *   **神经符号污点追踪（Taint Tracking）**：内核构建**指令依赖图（IDG）**，将来自外部、用户或 PPU 的数据标记为“污点”。当污点数据试图流向“汇聚点（Sinks）”（如删除文件、发送邮件）时，内核会强制拦截。
    *   **符号策略引擎**：利用有限状态机（FSM）强制执行全局一致性策略（例如：禁止在概率生成后直接调用高危工具，必须经过验证步）。

### 3. 实验设计
*   **实验平台**：在 OpenClaw 和 NanoBot 两个智能体框架上实现。
*   **数据集/场景**：
    *   **不安全场景**：从 **AgentDojo** 和 **Agent-SafetyBench** 中提取了 1,914 个成功的攻击轨迹进行重放测试。
    *   **良性场景**：包含 312 个涵盖办公、邮件、文件管理等日常任务的良性案例。
*   **对比方法**：原生主机护栏（Native Host Policies） vs. Arbiter-K 增强架构。
*   **评估模型**：Claude 3.5/3.7 Sonnet、GPT-4o 等主流大模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现规模**：论文提到 Arbiter-K 原型系统由约 28,914 行 Python 代码组成，主要侧重于推理侧的架构封装而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了近 2,000 组不安全案例重放和数百组良性案例测试，涵盖了多种模型和两个不同的智能体宿主环境。
*   **充分性与客观性**：
    *   **充分**：包含了消融实验（针对不同策略层 O/R/U 的分析）和端到端拦截位置分析。
    *   **客观**：采用了“重放协议”以消除模型规划漂移的影响，确保了跨平台对比的公平性。
    *   **覆盖面**：对 Shell 指令集进行了 100% 的解析覆盖测试。

### 6. 主要结论与发现
*   **安全性显著提升**：Arbiter-K 实现了 **76% 至 95%** 的不安全行为拦截率，相比原生策略（拦截率常低于 9%）获得了 **92.79%** 的绝对增益。
*   **低误报率**：在显著提升安全性的同时，对良性操作的误拦截率保持在较低水平（NanoBot 上低于 6%）。
*   **上下文复用**：当检测到违规时，Arbiter-K 允许通过策略反馈进行“架构回滚”或修正，而非直接终止会话，平均可复用 60%-70% 的上下文 Token，减少了资源浪费。
*   **干预提前**：Arbiter-K 能在执行轨迹的中期（约 50% 进度处）识别风险，而原生护栏通常在末期（80% 处）才反应。

### 7. 优点
*   **架构创新**：首次提出了针对智能体计算机的语义 ISA，将模糊的文本交互转化为可审计的指令流。
*   **治理优先**：通过污点传播机制解决了“间接提示注入”等难以通过传统手段防御的问题。
*   **实用性强**：作为一种微内核设计，它可以适配不同的底层智能体框架，且不需要重新训练模型。

### 8. 不足与局限
*   **治理税（Governance Tax）**：虽然论文提到了可靠性预算，但多层验证（尤其是调用强模型作为 Judge 时）会带来额外的延迟和 Token 成本。
*   **策略依赖**：系统的有效性高度依赖于预定义的策略集和指令解析器的准确性，对于极其复杂或模糊的 Shell 管道命令可能存在解析偏差。
*   **应用限制**：目前主要针对文本/代码类工具调用，对于多模态环境下的复杂视觉反馈治理讨论较少。

（完）
