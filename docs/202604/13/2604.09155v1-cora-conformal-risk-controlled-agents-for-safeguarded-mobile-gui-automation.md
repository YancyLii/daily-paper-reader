---
title: "CORA: Conformal Risk-Controlled Agents for Safeguarded Mobile GUI Automation"
title_zh: CORA：用于安全移动端 GUI 自动化的符合性风险控制智能体
authors: "Yushi Feng, Junye Du, Qifan Wang, Zizhan Ma, Qian Niu, Yutaka Matsuo, Long Feng, Lequan Yu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09155v1"
tags: ["query:agsec"]
score: 9.0
evidence: 针对自主Agent的策略后动作前安全框架，通过风险控制实现安全动作执行
tldr: 针对移动端GUI智能体在自主操作中可能引发的隐私、财产等安全风险，本文提出了CORA框架。该框架利用保形风险控制技术，在动作执行前通过Guardian模型评估风险，并结合Diagnostician模型进行多模态诊断，为智能体提供具有统计学保障的安全边界。同时，引入Phone-Harm基准测试，证明了CORA在保障安全的同时，能有效平衡任务完成度与用户干扰。
selection_source: fresh_fetch
motivation: 现有的GUI智能体安全机制依赖提示工程或启发式方法，缺乏形式化验证和用户可调的统计安全保障。
method: 提出一种基于保形风险控制的框架，通过Guardian模型校准执行边界，并结合Diagnostician模型进行风险诊断与Goal-Lock机制防止攻击。
result: 在Phone-Harm基准测试中，CORA在安全、帮助性和用户干扰的权衡上达到了最优的帕累托前沿。
conclusion: CORA为自主GUI执行提供了一种实用且具有统计理论支撑的安全范式，显著提升了移动智能体的安全性与可靠性。
---

## 摘要
由视觉语言模型 (VLM) 驱动的图形用户界面 (GUI) 智能体正迅速从被动辅助转向自主运行。然而，这种不受限制的动作空间使用户面临严重且不可逆的经济、隐私或社交损害。现有的安全措施依赖于提示工程、脆弱的启发式方法以及将 VLM 作为评论者，缺乏形式化验证和用户可调的保证。我们提出了 CORA（符合性风险控制 GUI 智能体），这是一种策略后、动作前的安全框架，可为执行的有害动作提供统计保证。CORA 将安全性重新定义为选择性动作执行：我们训练了一个 Guardian（守护者）模型来估计每个建议步骤的动作条件风险。我们不直接对原始分数设置阈值，而是利用符合性风险控制（Conformal Risk Control）来校准满足用户指定风险预算的执行/放弃边界，并将被拒绝的动作路由到可训练的 Diagnostician（诊断者）模型，该模型对被拒绝的动作进行多模态推理，以推荐干预措施（例如确认、反思或中止），从而最大限度地减轻用户负担。目标锁定（Goal-Lock）机制将评估锚定在明确且冻结的用户意图上，以抵御视觉注入攻击。为了严格评估这一范式，我们引入了 Phone-Harm，这是一个在真实场景下具有步骤级损害标签的移动安全违规新基准。在 Phone-Harm 和公共基准上针对多种基线的实验验证了 CORA 改善了安全性-有用性-中断（safety-helpfulness-interruption）的帕累托前沿，为自主 GUI 执行提供了一种实用且具有统计依据的安全范式。代码和基准测试可在 cora-agent.github.io 获取。

## Abstract
Graphical user interface (GUI) agents powered by vision language models (VLMs) are rapidly moving from passive assistance to autonomous operation. However, this unrestricted action space exposes users to severe and irreversible financial, privacy or social harm. Existing safeguards rely on prompt engineering, brittle heuristics and VLM-as-critic lack formal verification and user-tunable guarantees. We propose CORA (COnformal Risk-controlled GUI Agent), a post-policy, pre-action safeguarding framework that provides statistical guarantees on harmful executed actions. CORA reformulates safety as selective action execution: we train a Guardian model to estimate action-conditional risk for each proposed step. Rather than thresholding raw scores, we leverage Conformal Risk Control to calibrate an execute/abstain boundary that satisfies a user-specified risk budget and route rejected actions to a trainable Diagnostician model, which performs multimodal reasoning over rejected actions to recommend interventions (e.g., confirm, reflect, or abort) to minimize user burden. A Goal-Lock mechanism anchors assessment to a clarified, frozen user intent to resist visual injection attacks. To rigorously evaluate this paradigm, we introduce Phone-Harm, a new benchmark of mobile safety violations with step-level harm labels under real-world settings. Experiments on Phone-Harm and public benchmarks against diverse baselines validate that CORA improves the safety--helpfulness--interruption Pareto frontier, offering a practical, statistically grounded safety paradigm for autonomous GUI execution. Code and benchmark are available at cora-agent.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《CORA: Conformal Risk-Controlled Agents for Safeguarded Mobile GUI Automation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着视觉语言模型（VLM）的发展，移动端 GUI 智能体正从简单的对话辅助转向自主操作（如转账、授权、发送消息）。
*   **核心问题**：自主性带来了巨大的安全风险，包括经济损失、隐私泄露和社会伤害。
*   **现有局限**：目前的防御手段（如提示工程、启发式规则或 VLM 充当评论者）存在三个缺陷：
    1.  **缺乏形式化验证**：无法提供统计学上的安全保证。
    2.  **不可调控**：用户无法根据需求灵活调整安全与效率的平衡。
    3.  **脆弱性**：容易受到视觉提示注入（Visual Prompt Injection）攻击或产生幻觉。
*   **论文目标**：提出一种名为 **CORA** 的框架，通过“符合性风险控制”技术，为智能体提供可量化、用户可调且具有统计保障的安全边界。

### 2. 方法论：核心思想与技术细节
CORA 将安全问题重新定义为**选择性动作执行（Selective Action Execution）**，其核心架构包含三个关键组件：

*   **Guardian（守护者 - 系统 1）**：
    *   一个轻量级的动作条件风险评估模型。它接收当前截图、用户目标、历史记录和建议动作，输出一个标量风险分数 $s_t$。
    *   **核心公式**：$s_t = R_\psi(g, o_t, h_t, \hat{a}_t)$。
*   **符合性风险控制（Conformal Risk Control, CRC）**：
    *   **核心思想**：利用一小组校准数据，计算出一个动态阈值 $\hat{\tau}$。只要风险分数 $s_t \leq \hat{\tau}$，动作才被执行。
    *   **统计保证**：该机制确保在测试集上，被执行的有害动作比例（Executed Harm Rate）被限制在用户预设的预算 $\alpha$ 之内。
*   **Diagnostician（诊断者 - 系统 2）**：
    *   当动作被 Guardian 拦截时，由一个更强大的生成式 VLM 进行多模态推理。
    *   它会生成诊断报告，并推荐干预措施：**确认（Confirm）**、**反思（Reflect）**或**中止（Abort）**，以减少不必要的干扰。
*   **Goal-Lock（目标锁定）机制**：
    *   强制冻结初始用户意图，将屏幕上的所有文本视为“不可信证据”，从而免疫通过修改屏幕内容诱导智能体偏离目标的注入攻击。

### 3. 实验设计
*   **新基准测试：Phone-Harm**：
    *   包含 **Harm-150**（150 个包含步骤级损害标签的有害任务）和 **Normal-150**（150 个纯良性任务）。
    *   涵盖 29 个常用 App，涉及误用（Misuse）、注入（Injection）和误行为（Misbehavior）三大威胁模型。
*   **公共基准**：MobileRisk（侧重注入攻击）和 AndroidWorld（侧重长程导航能力）。
*   **对比方法**：
    *   **基础策略**：GPT-5, Gemini-3, UI-TARS-1.5, AutoGLM。
    *   **防御基线**：提示词自检（Prompt-based）、启发式门控（Heuristic gate）、VLM 评论者（VLM-as-critic）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：
    *   Guardian 和 Diagnostician 均基于 AutoGLM-Phone-9B 进行微调。
    *   使用 LoRA 适配器（rank=16），训练 3 个 Epoch。
*   **推理延迟**：
    *   基础策略平均每步 20.52 秒。
    *   CORA 整体平均每步 21.37 秒（Guardian 增加的开销极小）。
    *   若触发 Diagnostician 诊断，该步延迟增加至 34.13 秒。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同的基准测试上进行了全面评估，涵盖了从安全性到任务成功率的多个维度。
*   **消融实验**：针对 CRC 机制、动作条件化、诊断者设计、目标锁定机制分别做了消融研究，验证了各组件的必要性。
*   **客观性与公平性**：
    *   采用了严格的“训练-校准-测试”拆分协议，确保评估数据不参与模型训练或阈值校准。
    *   引入了帕累托前沿（Pareto Frontier）分析，客观展示了安全、有用性与干扰频率之间的权衡。

### 6. 主要结论与发现
*   **安全保证**：CORA 成功将有害动作执行率控制在预设的 $\alpha$ 预算内，而基线方法往往在面对复杂视觉环境时失效。
*   **性能提升**：在 Phone-Harm 上，CORA 的目标达成率（GAR）达到 79.80%，显著优于 GPT-5 等保守策略（约 37.5%）。
*   **错误恢复**：在良性任务（AndroidWorld）中，CORA 不仅没有降低成功率，反而通过 Diagnostician 的“反思”建议，帮助智能体纠正了点击偏差，将成功率从 30% 提升至 40%。
*   **防御注入**：Goal-Lock 机制被证明能有效抵御 76% 以上的视觉注入攻击。

### 7. 优点
*   **理论严密**：首次将符合性风险控制（CRC）引入移动端 GUI 智能体领域，提供了统计学上的安全边界。
*   **用户友好**：提供了一个可调节的“安全旋钮”（$\alpha$），允许用户根据应用场景（如金融 vs 娱乐）自定义风险承受度。
*   **系统化设计**：结合了快速的风险评分（系统 1）和深度的语义诊断（系统 2），兼顾了效率与解释性。
*   **高质量基准**：Phone-Harm 填补了移动端智能体步骤级安全评估的空白。

### 8. 不足与局限
*   **校准集依赖**：CRC 的有效性依赖于校准数据与实际部署环境的分布一致性，尽管论文提出了加权校准方案，但在极端分布偏移下仍可能失效。
*   **推理延迟**：虽然 Guardian 很快，但 Diagnostician 的介入会显著增加单步延迟，可能影响实时交互体验。
*   **覆盖范围**：目前的实验主要集中在 Android 环境，尚未验证在 iOS 或桌面端复杂多窗口环境下的泛化能力。
*   **长期依赖**：对于需要极长步骤才能体现损害的任务，当前的步骤级评估可能存在滞后。

（完）
