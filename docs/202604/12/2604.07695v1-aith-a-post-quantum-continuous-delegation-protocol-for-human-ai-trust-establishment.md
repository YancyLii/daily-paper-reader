---
title: "AITH: A Post-Quantum Continuous Delegation Protocol for Human-AI Trust Establishment"
title_zh: AITH：一种用于人机信任建立的后量子持续授权协议
authors: Zhaoliang Chen
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07695v1"
tags: ["query:agsec"]
score: 8.0
evidence: 为自主AI智能体强制执行硬约束和速率限制的边界引擎
tldr: 针对AI代理在自主执行任务时缺乏有效信任管理的问题，本文提出AITH协议。该协议采用后量子加密技术（ML-DSA-87）实现持续授权，通过高效的边界引擎和撤销机制，解决了传统协议无法处理AI概率性行为的局限。经Tamarin形式化验证和大规模仿真，AITH在保证安全性的同时实现了极高的处理性能和自动化执行率，为人类与AI间的信任建立提供了新框架。
selection_source: fresh_fetch
motivation: 传统加密协议难以应对具有概率性行为且需持续运行的AI代理在动态信任边界下的授权、限制与撤销需求。
method: 提出一种包含后量子签名证书、高性能边界检查引擎、秒级撤销协议及SHA-256责任审计链的持续授权框架。
result: "该协议实现了每秒470万次的操作处理速度，并在10万次仿真中成功自主执行了79.5%的任务，同时有效拦截了14.4%的违规操作。"
conclusion: AITH为人类与AI代理之间的信任建立提供了一种安全、高效且具备后量子抗性的标准化授权与监管解决方案。
---

## 摘要
代表人类委托人自主运行的 AI 智能体的快速部署，已经超过了用于建立、界定和撤销人机信任关系的加密协议的发展速度。现有框架（如 TLS、OAuth 2.0、Macaroons）假设软件是确定性的，无法处理在可变信任边界内持续运行的概率性 AI 智能体。我们提出了 AITH（AI 信任握手），这是一种后量子持续授权协议。AITH 引入了：(1) 一种使用 ML-DSA-87（FIPS 204，NIST Level 5）进行单次签名的持续授权证书，以每秒 470 万次操作的亚微秒级边界检查取代了逐操作签名；(2) 一种六重检查边界引擎，在关键路径上以零加密开销强制执行硬约束、速率限制和人工干预触发器；(3) 一种基于推送的撤销协议，可在 1 秒内传播失效信息。三层 SHA-256 责任链提供了防篡改的审计日志记录。所有五个安全定理均在 Dolev-Yao 模型下通过 Tamarin Prover 进行了机器验证。我们通过五轮多模型对抗性审计验证了 AITH，解决了跨四个严重性层级的 12 个漏洞。对 100,000 次操作的模拟显示，79.5% 为自主执行，6.1% 升级至人工处理，14.4% 被拦截。

## Abstract
The rapid deployment of AI agents acting autonomously on behalf of human principals has outpaced the development of cryptographic protocols for establishing, bounding, and revoking human-AI trust relationships. Existing frameworks (TLS, OAuth 2.0, Macaroons) assume deterministic software and cannot address probabilistic AI agents operating continuously within variable trust boundaries.   We present AITH (AI Trust Handshake), a post-quantum continuous delegation protocol. AITH introduces: (1) a Continuous Delegation Certificate signed once with ML-DSA-87 (FIPS 204, NIST Level 5), replacing per-operation signing with sub-microsecond boundary checks at 4.7M ops/sec; (2) a six-check Boundary Engine enforcing hard constraints, rate limits, and escalation triggers with zero cryptographic overhead on the critical path; (3) a push-based Revocation Protocol propagating invalidation within one second. A three-tier SHA-256 Responsibility Chain provides tamper-evident audit logging. All five security theorems are machine-verified via Tamarin Prover under the Dolev-Yao model.   We validate AITH through five rounds of multi-model adversarial auditing, resolving 12 vulnerabilities across four severity layers. Simulation of 100,000 operations shows 79.5% autonomous execution, 6.1% human escalation, and 14.4% blocked.

---

## 论文详细总结（自动生成）

### 论文总结：AITH——用于人机信任建立的后量子持续授权协议

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体（AI Agents）开始自主执行金融交易、管理基础设施和谈判合同，现有的加密授权协议（如 TLS 1.3、OAuth 2.0、SPIFFE、Macaroons）显露出明显的局限性。这些协议是为**确定性软件**设计的，无法应对 AI 智能体的独特挑战：
*   **概率性行为**：AI 的决策具有不确定性，需要动态且细粒度的边界控制。
*   **持续自主性**：传统协议往往需要逐次授权，在高频操作下会产生巨大的计算开销。
*   **责任归属**：当 AI 造成损害时，缺乏加密可验证的证据链来界定人类与 AI 的责任。
*   **量子威胁**：现有协议多依赖传统非对称加密，难以抵御未来量子计算的攻击。

#### 2. 方法论：核心思想与关键技术
AITH 的核心思想是将“逐操作授权”转变为**“持续授权”（Continuous Delegation）**，类似于机器可读的“法律授权委托书”。

*   **持续授权证书 (Continuous Delegation Certificate)**：
    *   使用 **ML-DSA-87**（NIST FIPS 204 标准，Level 5 安全强度）进行单次签名。
    *   证书内嵌多维约束（金额限制、资产范围、时间窗口、速率上限等）。
*   **六重检查边界引擎 (Six-Check Boundary Engine)**：
    *   在操作执行的关键路径上，不进行复杂的加密运算，而是通过确定性的逻辑检查：1. 证书有效性；2. 授权等级；3. 边界约束；4. 速率限制；5. 异常检测；6. 人工干预触发。
*   **推送式撤销协议 (Push-Based Revocation)**：
    *   一旦人类发起撤销，信息会立即推送到所有注册的目标系统，确保在 1 秒内完成全球范围内的权限失效。
*   **三层责任链 (Three-Tier Responsibility Chain)**：
    *   利用 **SHA-256 哈希链**记录 AI 决策、人类确认和系统执行日志，形成不可篡改的审计证据。

#### 3. 实验设计
*   **场景模拟**：模拟了 1,000 名用户执行 100,000 次操作（包括查询、不同规模的交易、转账等）。
*   **Benchmark（基准测试）**：
    *   对比了 AITH 与传统协议（OAuth 2.0、Macaroons、TLS 1.3）在延迟和吞吐量上的表现。
    *   对比了并发研究（Google AP2、AIP、A-JWT、CA-MCPQ）的功能特性（如是否支持后量子、是否有边界引擎等）。
*   **形式化验证**：使用 **Tamarin Prover** 在 Dolev-Yao 威胁模型下对 5 个核心安全引理进行了机器验证。
*   **对抗性审计**：采用了一种新颖的“多模型红蓝对抗”方法，利用 Model $\alpha$ 和 $\beta$ 进行 5 轮攻防演练，并由 Model $\gamma$ 和 $\delta$ 进行压力测试。

#### 4. 资源与算力
*   **硬件环境**：实验在配备 **AMD Ryzen 9 7950X** CPU 的系统上运行。
*   **算力细节**：
    *   论文主要关注协议的推理和检查性能，而非模型训练，因此**未提及 GPU 算力或训练时长**。
    *   性能测试基于单线程执行，达到了 4.7M ops/sec 的吞吐量。
*   **软件实现**：使用 Python 编写参考实现（2,800+ 行代码），加密库模拟使用了 `liboqs`。

#### 5. 实验数量与充分性
*   **实验规模**：10 万次操作的仿真涵盖了从正常自主执行到违规拦截的各种分布（79.5% 允许，6.1% 升级，14.4% 拦截），样本量较为充分。
*   **审计深度**：通过 5 轮对抗审计发现了 12 个漏洞（从算法偏置到协议死锁），驱动协议从 v1 进化到 v5.1，显示了极高的鲁棒性测试深度。
*   **客观性**：通过 Tamarin Prover 的数学证明和多厂商 AI 模型的交叉验证，减少了单一开发者或单一模型的偏见，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能卓越**：AITH 的边界检查平均延迟仅为 **0.21 $\mu$s**，比逐次签名的方案快 500 倍以上。
*   **安全可靠**：通过了形式化验证，证明了证书不可伪造、边界不可逾越、撤销及时且责任链完整。
*   **范式有效性**：实验证明“单次签名 + 持续边界检查”的模式能够完美平衡 AI 的自主运行效率与人类的安全监管需求。
*   **人机协作**：发现人类直觉（如对延迟的敏感）与 AI 的分析深度在协议设计中具有互补性。

#### 7. 优点
*   **后量子安全性**：前瞻性地采用了 NIST 最新的 ML-DSA 标准。
*   **极低开销**：将加密成本分摊到证书颁发阶段，使日常操作几乎达到零加密开销。
*   **创新的验证方法**：利用多个前沿 AI 模型进行红蓝对抗审计，这为未来安全协议的开发提供了新范式。
*   **法律友好**：三层责任链的设计直接考虑了监管审计和法律取证的需求。

#### 8. 不足与局限
*   **约束完整性**：协议虽然能强制执行约束，但约束条件（Constraints）是否写得完善仍取决于人类委托人，存在“人为疏忽”风险。
*   **AI 审计盲点**：参与审计的 AI 模型可能存在训练数据重叠，导致它们对某些特定类型的逻辑漏洞产生共同的盲点。
*   **硬件安全缺失**：目前未考虑可信执行环境（TEE）的侧信道攻击或物理层面的安全保护。
*   **应用限制**：目标系统需要集成 AITH 的边界引擎和撤销监听器，对于完全封闭的旧系统存在集成难度。

（完）
