---
title: "Gradient-Controlled Decoding: A Safety Guardrail for LLMs with Dual-Anchor Steering"
title_zh: 梯度控制解码：一种基于双锚点引导的大语言模型安全护栏
authors: "Purva Chiniya, Kevin Scaria, Sagar Chaturvedi"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05179v1"
tags: ["query:agsec"]
score: 9.0
evidence: 梯度控制解码作为LLM输出的安全护栏
tldr: "本研究针对大语言模型易受越狱攻击且现有防御手段易误伤良性查询的问题，提出了一种名为梯度控制解码（GCD）的无需训练的防御框架。GCD通过引入“接受”和“拒绝”双锚点标记来收紧决策边界，显著降低误报率。在检测到风险时，它通过预设注入拒绝标记来确保首字安全。该方法在多个基准测试中表现优异，在保持高召回率的同时将误报率降低了52%，且延迟极低，具有良好的跨模型迁移性。"
selection_source: fresh_fetch
motivation: 现有的安全过滤方法决策边界脆弱，容易过度拒绝良性请求，且无法在解码阶段提供确定性的安全保证。
method: 提出双锚点转向机制，结合“Sure”和“Sorry”标记优化检测，并在识别到风险时强制注入拒绝标记以引导后续解码。
result: "在多个基准测试中，GCD比GradSafe降低了52%的误报率，并降低了10%的攻击成功率，且平均延迟增加不到20毫秒。"
conclusion: GCD是一种高效、无需训练且可迁移的安全护栏方案，在提升模型安全性的同时显著减少了过度拒绝现象。
---

## 摘要
大语言模型（LLMs）仍然容易受到越狱和直接提示词注入攻击的影响，然而最强大的防御过滤器经常会过度拒绝良性查询，从而降低用户体验。以往关于越狱和提示词注入检测的工作（如 GradSafe）使用单个“全部接受”锚点标记来检测不安全提示词，但其阈值较为脆弱，且无法确定性地保证在解码开始后不会输出有害内容。我们引入了梯度控制解码（GCD），这是一种无需训练的安全护栏，它结合了接受锚点标记（“Sure”）和拒绝锚点标记（“Sorry”），从而收紧了决策边界并显著降低了误报率。在缓解阶段，如果提示词被标记，GCD 会在自回归解码恢复之前预设注入一到两个拒绝标记（“Sorry, I can't...”），无论采用何种采样策略，都能保证首个标记的安全性。在 ToxicChat、XSTest-v2 和 AdvBench 数据集上，在召回率相当的情况下，GCD 相比 GradSafe 减少了 52% 的误报，相比最强的仅解码基线降低了高达 10% 的攻击成功率，在 V100 实例上平均增加的延迟低于 15-20 毫秒，可迁移至 LLaMA-2-7B、Mixtral-8x7B 和 Qwen-2-7B，且仅需 20 个演示模板。

## Abstract
Large language models (LLMs) remain susceptible to jailbreak and direct prompt-injection attacks, yet the strongest defensive filters frequently over-refuse benign queries and degrade user experience. Previous work on jailbreak and prompt injection detection such as GradSafe, detects unsafe prompts with a single "accept all" anchor token, but its threshold is brittle and it offers no deterministic guarantee that harmful content will not be emitted once decoding begins. We introduce Gradient-Controlled Decoding (GCD), a training-free guardrail that combines an acceptance anchor token ("Sure") and refusal anchor token ("Sorry") tightening the decision boundary and significantly lowering false positives. In the mitigation stage, if a prompt is flagged, GCD preset-injects one or two refusal tokens ("Sorry, I can't...") before autoregressive decoding resumes, guaranteeing first-token safety regardless of sampling strategy. On ToxicChat, XSTest-v2, and AdvBench, GCD reduces false positives by 52% vs. GradSafe at comparable recall, lowers attack success rate by up to 10% vs. the strongest decoding-only baseline, adds under 15-20 ms latency on an average on V100 instances, transfers to LLaMA-2-7B, Mixtral-8x7B, and Qwen-2-7B, and requires only 20 demonstration templates.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**梯度控制解码（Gradient-Controlled Decoding, GCD）**的创新方法，旨在为大语言模型（LLM）提供一个既能精准拦截攻击、又能减少“过度拒绝”的实时安全护栏。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的LLM安全防御手段存在两难境地：静态过滤器（如规则匹配）难以应对演进的攻击；微调（SFT/DPO）常导致模型过度保守（Over-refusal），误伤良性查询。
*   **现有技术缺陷**：之前的梯度检测方法（如 GradSafe）仅使用单一的“接受”锚点（如“Sure”），导致决策边界脆弱，且属于“仅检测”机制，无法在解码阶段提供确定性的安全保证，有害内容仍可能通过采样策略泄露。
*   **核心目标**：开发一种无需训练、低延迟、能显著降低误报率并提供确定性安全输出的防御框架。

### 2. 方法论
GCD 将安全防御分为两个阶段：
*   **双锚点梯度检测（Dual-Anchor Detection）**：
    *   **核心思想**：不仅计算提示词相对于“接受”标记（Sure）的梯度，还计算其相对于“拒绝”标记（Sorry）的梯度。
    *   **技术细节**：首先利用少量参考样本（20个模板）识别模型中的“安全敏感参数”切片。对于新输入的提示词，计算其在这些参数切片上产生的梯度，并与预设的有害参考梯度计算余弦相似度。
    *   **决策逻辑**：只有当提示词在“Sure”和“Sorry”两个锚点上的得分均超过各自阈值时，才判定为不安全。这种双重校验收紧了决策边界，大幅提升了识别精度。
*   **确定性缓解（Deterministic Mitigation）**：
    *   **核心思想**：一旦检测到风险，不再交由模型自由生成，而是强制干预解码过程。
    *   **技术细节**：在自回归解码开始前，预先向解码器注入一到两个拒绝标记（如“Sorry, I can’t...”）。这确保了无论采样策略（如 Temperature 或 Top-p）如何变化，模型输出的首个片段必然是安全的拒绝语，从而引导后续生成保持合规。

### 3. 实验设计
*   **数据集**：
    *   **ToxicChat**：真实世界的用户与AI对话，包含隐蔽的毒性。
    *   **XSTest-v2**：专门测试过度拒绝行为的基准。
    *   **AdvBench**：包含各种对抗性越狱攻击。
*   **对比方法（Benchmarks）**：
    *   **GradSafe**：单锚点梯度检测基线。
    *   **SCANS**：基于激活转向（Activation Steering）的方法。
    *   **Safe-Decoding**：现有的安全解码防御。
    *   **标准解码策略**：Greedy、Top-k、Top-p 采样。
*   **评估模型**：LLaMA-2-7B、Mixtral-8x7B、Qwen-2-7B 以及 Qwen3 系列和 Llama-3.2。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA V100 GPU** 实例上进行。
*   **算力开销**：该方法**无需训练或微调**。
*   **推理延迟**：在 V100 上，GCD 带来的平均延迟增加不到 **15-20 毫秒**（Time-To-First-Token, TTFT），对于实时交互应用几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流安全数据集，跨越了 3 个不同的模型家族及多种参数规模（从 1.7B 到 8x7B）。
*   **消融实验**：对参考模板的数量（n=2, 5, 10）进行了敏感性分析，并绘制了 Precision-Recall 曲线来验证阈值选择的科学性。
*   **客观性**：实验对比了多种最先进的（SOTA）防御方案，并同时关注了“攻击成功率（ASR）”和“误报率（FP）”这两个相互制约的指标，评估视角较为全面、客观。

### 6. 主要结论与发现
*   **误报率大幅下降**：在保持相同召回率的情况下，GCD 比 GradSafe 减少了 **52%** 的误报，有效解决了过度拒绝问题。
*   **安全性增强**：相比最强的仅解码基线，攻击成功率（ASR）降低了约 **10%-20%**。
*   **确定性保证**：通过预设标记注入，GCD 弥补了检测与生成之间的缝隙，提供了首字级别的安全保障。
*   **高效迁移**：仅需 20 个提示词模板即可在不同模型间迁移，无需针对新模型重新训练。

### 7. 优点
*   **即插即用**：无需昂贵的微调过程，适合快速部署。
*   **双锚点创新**：引入“拒绝锚点”极大地提高了区分良性/恶性提示词的能力，尤其是在处理语义模糊的查询时。
*   **低延迟**：梯度计算经过优化，对用户体验影响极小。
*   **鲁棒性**：对不同的解码采样参数具有不变性。

### 8. 不足与局限
*   **内存开销**：虽然延迟低，但计算梯度需要保留计算图或进行额外的反向传播步骤，相比纯文本过滤，对显存有一定额外要求。
*   **模板依赖**：虽然仅需 20 个模板，但这些模板的质量和覆盖面仍可能影响在极端生僻攻击下的表现。
*   **单轮限制**：目前主要针对单轮对话设计，在长文本或多轮复杂对话中的表现（如上下文注入攻击）尚需进一步验证。
*   **语言局限**：论文主要基于英文环境测试，跨语言的泛化能力有待观察。

（完）
