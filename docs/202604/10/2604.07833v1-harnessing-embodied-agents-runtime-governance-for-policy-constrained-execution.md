---
title: "Harnessing Embodied Agents: Runtime Governance for Policy-Constrained Execution"
title_zh: 驾驭具身智能体：受策略约束执行的运行时治理
authors: "Xue Qin, Simin Luan, John See, Cong Yang, Zhijun Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07833v1"
tags: ["query:agsec"]
score: 9.0
evidence: 具身智能体的运行时治理和策略约束执行
tldr: 具身智能体正从推理转向执行，但其运行时的可控性与安全性面临挑战。本文提出一种策略约束执行框架，将智能体的认知与执行监管分离，通过独立的运行时治理层实现策略检查、能力准入、监控及回滚。实验表明，该方法能有效拦截未授权行为并显著提升系统在异常情况下的恢复成功率，为具身智能体系统的安全设计提供了新准则。
selection_source: fresh_fetch
motivation: 现有的具身智能体安全逻辑通常嵌入在模型内部，导致执行控制难以标准化、审计和动态调整。
method: 提出一种将治理逻辑外部化的运行时框架，通过定义智能体、能力模块与治理层之间的控制边界，实现策略约束下的执行。
result: "实验显示该框架实现了96.2%的违规行为拦截率，并在运行时偏移下将不安全持续执行率从100%降至22.2%。"
conclusion: 运行时治理应被视为具身智能体系统的核心设计原则，通过解耦认知与监管来确保复杂环境下的执行安全。
---

## 摘要
具身智能体正从被动推理系统演变为与工具、机器人及物理环境交互的主动执行器。一旦被授予执行权限，核心挑战便在于如何在运行时保持动作的可治理性。现有方法将安全与恢复逻辑嵌入智能体循环内部，导致执行控制难以标准化、审计和适配。本文认为，具身智能不仅需要更强大的智能体，还需要更强大的运行时治理。我们提出了一个受策略约束的执行框架，将智能体认知与执行监督分离。治理功能被外部化到一个专用的运行时层，负责执行策略检查、能力准入、执行监控、回滚处理和人工干预。我们形式化了具身智能体、具身能力模块（ECMs）与运行时治理层之间的控制边界，并通过三个治理维度的 1000 次随机模拟试验进行了验证。结果显示，该框架拦截了 96.2% 的未授权动作，在运行时漂移情况下将不安全持续执行的比例从 100% 降低至 22.2%，并在完全符合策略的情况下实现了 91.4% 的恢复成功率，显著优于所有基准方法（p<0.001）。通过将运行时治理重新定义为一等系统问题，本文将受策略约束的执行定位为具身智能体系统的关键设计原则。

## Abstract
Embodied agents are evolving from passive reasoning systems into active executors that interact with tools, robots, and physical environments. Once granted execution authority, the central challenge becomes how to keep actions governable at runtime. Existing approaches embed safety and recovery logic inside the agent loop, making execution control difficult to standardize, audit, and adapt.   This paper argues that embodied intelligence requires not only stronger agents, but stronger runtime governance. We propose a framework for policy-constrained execution that separates agent cognition from execution oversight. Governance is externalized into a dedicated runtime layer performing policy checking, capability admission, execution monitoring, rollback handling, and human override.   We formalize the control boundary among the embodied agent, Embodied Capability Modules (ECMs), and runtime governance layer, and validate through 1000 randomized simulation trials across three governance dimensions. Results show 96.2% interception of unauthorized actions, reduction of unsafe continuation from 100% to 22.2% under runtime drift, and 91.4% recovery success with full policy compliance, substantially outperforming all baselines (p<0.001). By reframing runtime governance as a first-class systems problem, this paper positions policy-constrained execution as a key design principle for embodied agent systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Harnessing Embodied Agents: Runtime Governance for Policy-Constrained Execution》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着具身智能体（Embodied Agents）从单纯的对话推理转向物理世界的实际执行（操作机器人、工具等），系统面临一个根本性的挑战：**如何确保智能体的行为在运行时（Runtime）是可治理的？**
*   **研究动机**：现有的安全约束、恢复策略通常被硬编码在智能体的 Prompt 或控制器内部。这种“耦合”设计导致执行控制难以标准化、难以审计，且无法灵活适配不同的部署环境（如从模拟环境切换到真实人机协作环境）。
*   **核心问题**：论文旨在解决如何在不修改智能体模型的前提下，通过外部化的系统层级约束，实现受策略约束的执行（Policy-Constrained Execution）。

### 2. 论文提出的方法论
论文提出了一个**运行时治理框架（Runtime Governance Framework）**，其核心思想是**“认知与监管分离”**。

*   **核心实体**：
    1.  **具身智能体（Agent）**：负责高层规划和能力提议（Proposal）。
    2.  **能力包（Capability Package）**：封装了具体的技能（如抓取、导航）及其元数据（风险等级、回滚动作）。
    3.  **运行时治理层（Governance Layer）**：作为中介，决定执行是否被允许。
*   **关键技术组件（六大模块）**：
    *   **能力准入（Admission）**：检查智能体是否有权调用某项能力。
    *   **策略守卫（Policy Guard）**：根据环境 Profile（如“真实机器人”或“模拟器”）动态调整执行参数（如限速、限力）。
    *   **执行观察者（Watcher）**：实时监控执行状态，检测异常或策略违规。
    *   **恢复与回滚管理器**：当执行失败或违规时，触发预定义的恢复动作或状态回滚。
    *   **人工覆盖接口（Human Override）**：高风险动作需人工审批，且支持随时接管。
    *   **审计与遥测**：记录全生命周期的执行轨迹，用于事后追责和策略优化。
*   **逻辑流程**：执行被建模为：$E_t = G_{OV}(P_t, C_i, \Pi_t, \Gamma_t, \Omega_t)$。即最终执行（$E_t$）是治理函数对智能体意图（$P_t$）在特定策略（$\Pi_t$）和观察（$\Omega_t$）下的变换。

### 3. 实验设计
*   **实验场景**：使用 Gazebo Fortress 模拟器，搭载 UR5e 机械臂的移动机器人。
*   **任务类型**：导航（区域限制）、操纵（抓取易碎品）、长程复合任务（取送物）。
*   **环境 Profile**：定义了四种模式：模拟宽松（Sim-Relaxed）、真实受限（Real-Restricted）、人机共享（Human-Shared）、测试审计（Test-Audit）。
*   **对比基准（Baselines）**：
    1.  **直接执行（Direct Exec）**：无治理层。
    2.  **静态规则（Static Rule）**：仅有预执行检查，无运行时监控。
    3.  **能力内部安全（Cap.-Internal）**：安全逻辑分散在各个技能内部，缺乏统一治理。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究侧重于“运行时系统架构”而非模型训练，其实验主要在机器人模拟器（Gazebo）中进行逻辑验证和统计分析，对大规模深度学习算力的依赖相对较低。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **1000 次随机试验**（5 个随机种子 × 每组 200 次试验），涵盖了未授权动作拦截、运行时违规检测、恢复/回滚三个维度。
*   **充分性与客观性**：
    *   **消融实验**：逐一移除了准入、策略、观察者、恢复和人工模块，验证了各组件的独立贡献。
    *   **统计显著性**：使用了配对 t 检验（p < 0.001），证明了结果的统计学意义。
    *   **覆盖面**：实验涵盖了从权限缺失到物理碰撞、超时、人为干扰等多种违规注入，设计较为全面且客观。

### 6. 主要结论与发现
*   **拦截率高**：框架实现了 **96.2%** 的未授权动作拦截率，远高于静态规则（59.5%）。
*   **安全性提升**：在运行时发生偏移（如人进入工作区）时，不安全持续执行率从 100% 降至 **22.2%**。
*   **恢复能力强**：在完全符合策略的前提下，实现了 **91.4%** 的恢复成功率，且恢复延迟显著降低。
*   **人工干预有效**：人工覆盖接口成功拦截了 100% 未经批准的高风险请求。
*   **性能开销极低**：治理层的预执行延迟小于 **1.5 微秒**，远低于机器人控制周期，证明了其实用性。

### 7. 优点（亮点）
*   **架构解耦**：将“怎么做（智能体）”与“准不准做（治理层）”分离，使得同一套智能体逻辑可以无缝适配不同安全等级的环境。
*   **全生命周期治理**：不仅关注执行前的过滤，更强调执行中的监控（Watcher）和执行后的恢复（Recovery）。
*   **环境敏感性**：通过 Environment Profiles 实现了策略的动态切换，解决了具身智能中常见的“治理鸿沟”问题。

### 8. 不足与局限
*   **验证环境单一**：所有实验均在**模拟器**中完成，缺乏真实物理环境下的硬件变数验证（如传感器噪声对 Watcher 的影响）。
*   **观察者漏报风险**：运行时违规检测率（RVDR）为 61.3%，意味着仍有约 38% 的隐蔽违规未被察觉，这受限于观察者的灵敏度设置。
*   **适配性限制**：对于完全端到端（End-to-End）的连续动作策略（如直接输出力矩的扩散模型），该框架基于“能力包”的离散调用抽象可能难以直接应用，需要进一步重构。
*   **策略编写依赖**：治理的效果高度依赖于人工定义的策略质量，若策略定义模糊，可能导致过度拦截或安全漏洞。

（完）
