---
title: "The Consensus Trap: Rescuing Multi-Agent LLMs from Adversarial Majorities via Token-Level Collaboration"
title_zh: 共识陷阱：通过 Token 级协作将多智能体 LLM 从对抗性多数中解救出来
authors: "Jiayuan Liu, Shiyi Du, Weihua Du, Mingyu Guo, Vincent Conitzer"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17139v1"
tags: ["query:agsec"]
score: 9.0
evidence: 通过Token级协作防止多智能体系统中的对抗性破坏
tldr: 本研究揭示了多智能体大模型系统在响应级聚合（如多数投票）下的结构性漏洞：当受攻击的智能体占据多数时，系统会因盲目采纳错误结论而崩溃。为此，作者提出了令牌级轮询（RR）协作机制，通过让智能体在共享上下文中交替生成令牌，将静态投票转变为动态交织的逻辑链。理论证明和实验结果均显示，该机制能使诚实智能体在被恶意智能体包围的情况下依然实现逻辑修复，显著提升了系统在对抗环境下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统在面对恶意提示注入导致的局部多数错误时，由于响应级聚合无法识别中间逻辑缺陷而极易失效。
method: 提出令牌级轮询（RR）协作框架，通过在共享自回归上下文中交替生成令牌，实现智能体间的深度逻辑交织。
result: 实验证明，在恶意智能体占据多数的极端情况下，RR 机制仍能维持高准确率，而传统的多数投票法则会彻底崩溃。
conclusion: 令牌级协作将聚合过程从简单的线性求和提升为非线性算子作用，为构建鲁棒的多智能体推理系统提供了新路径。
---

## 摘要
多智能体大语言模型（LLM）架构日益依赖于响应级聚合（如多数投票，MAJ）来提升推理上限。然而，在开放环境中，智能体极易受到隐蔽的上下文损坏（如针对性提示注入）的影响。我们揭示了当前多智能体系统中的一个关键结构性漏洞：当受损智能体形成局部多数时，响应级聚合会失效。由于投票聚合的是已形成的结论，它无法察觉中间逻辑的缺陷。为了克服这一系统性限制，我们提出了 Token 级轮询（RR）协作，其中智能体在共享的自回归上下文中顺序交替生成。我们将这一过程形式化为离散时间动力系统，证明了 Token 级交替将聚合从脆弱的最终投票计数（线性求和）转变为动态交织的逻辑链（非线性算子积）。通过这一理论视角，我们证明了诚实模型的修复拉力可以压倒对抗性损坏，即使受损智能体占据多数。我们在多种推理基准上进行了详尽的实证评估，结果表明，虽然 MAJ 在受损智能体达到多数时会崩溃，但 RR 在远超这一临界阈值的情况下仍能保持稳健的准确性。

## Abstract
Multi-agent large language model (LLM) architectures increasingly rely on response-level aggregation, such as Majority Voting (MAJ), to raise reasoning ceilings. However, in open environments, agents are highly susceptible to stealthy contextual corruption, such as targeted prompt injections. We reveal a critical structural vulnerability in current multi-agent systems: response-level aggregation collapses when corrupted agents form a local majority. Because voting aggregates fully-formed conclusions, it is blind to flawed intermediate logic. To overcome this systematic limitation, we propose the Token-Level Round-Robin (RR) Collaboration, where agents sequentially interleave generation within a shared auto-regressive context. We formalize this process as a discrete-time dynamical system, proving that token-level interleaving transitions aggregation from a brittle counting of final votes (a linear sum) to a dynamic, interwoven chain of logic (a non-linear operator product). Through this theoretical lens, we prove that the honest model's restorative pull can overpower adversarial corruptions, even when corrupted agents form a majority. We conduct an exhaustive empirical evaluation across diverse reasoning benchmarks and demonstrate that while MAJ collapses when corrupted agents reach a majority, RR maintains robust accuracy well beyond this critical threshold.

---

## 论文详细总结（自动生成）

以下是对论文《The Consensus Trap: Rescuing Multi-Agent LLMs from Adversarial Majorities via Token-Level Collaboration》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前多智能体大语言模型（LLM）系统主要依赖“响应级聚合”（如多数投票 MAJ），这种机制存在一个**结构性漏洞**：一旦受损或被恶意诱导（如通过提示注入）的智能体占据局部多数（比例 $\rho > 0.5$），系统会陷入“共识陷阱”，盲目采纳错误的结论，因为投票机制对中间推理逻辑的错误是“失明”的。
*   **研究背景**：在开放环境中，智能体容易受到隐蔽的上下文操纵（如定向广告植入、政治虚假信息）。传统的独立采样假设在面对相关性攻击时失效，导致系统鲁棒性在对抗环境下迅速崩溃。

### 2. 论文提出的方法论
*   **核心思想**：将防御从宏观的“响应级”转向微观的“Token 级”。提出**Token 级轮询协作（RR）**，让多个智能体在一个共享的自回归上下文中交替生成内容。
*   **关键技术细节**：
    *   **轮询机制（Round-Robin）**：$N$ 个智能体顺序接力，每个智能体在当前共享上下文后生成固定长度 $K$ 的 Token 块（Chunk），然后强制停止并交给下一个智能体。
    *   **逻辑干预**：诚实智能体可以实时监测共享轨迹，在发现错误逻辑时立即介入并修正（例如生成“等等，之前的计算有误……”）。
    *   **RRMaj 机制**：为了进一步提升性能，并行运行 $M$ 条 RR 轨迹，并对最终结果进行多数投票。
*   **理论框架**：
    *   将自回归生成建模为**离散时间动力系统**。
    *   **真理吸引子（Truth Attractor）**：证明诚实智能体具有更强的“结构惯性”（基于线性表示假设），能产生修复拉力。
    *   **顺从性瓶颈（Sycophancy Bottleneck）**：证明受损智能体在面对已建立的正确逻辑上下文时，其偏离正确路径的能力会受到数学上的限制。

### 3. 实验设计
*   **数据集/场景**：涵盖了需要多步演绎的客观验证任务：
    *   数学推理：AQUA-RAT, GSM8K, MATH500。
    *   空间状态跟踪：Track-7。
    *   逻辑演绎：Logic-3, Logic-7（来自 BIG-bench Hard）。
*   **Benchmark 与对比方法**：
    *   **基准（Baseline）**：标准的多数投票（MAJ）。
    *   **威胁模型**：分为“中度建议性注入”（劝说式）和“强度指令性注入”（强制要求选择特定错误答案）。
    *   **对比维度**：改变受损智能体的比例 $\rho$（从 0% 到 80%）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **成本分析**：作者强调了该方法的**成本中性（Cost-Neutrality）**。由于 RRMaj 生成的总 Token 数与 MAJ 相同，主要的计算开销在于智能体切换时的上下文预填充（Pre-fill），而这在现代推理架构中是高度并行且高效的，因此总延迟和成本与传统方法基本持平。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 5 种主流模型架构（Llama-3.3-70B, Llama-4-Scout-17B, Mistral-Small-3.1-24B, Qwen2.5-32B, Qwen3-30B）。
    *   涵盖了从同构到异构（不同规模模型混合）的智能体集群。
    *   进行了详尽的消融实验，包括测试时计算缩放（M 从 1 到 40）、Token 块大小 $K$ 的影响、以及最后发言者身份的影响。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型、跨任务的对比，验证了该方法在极端对抗环境（如 4 个受损智能体对 1 个诚实智能体）下的有效性。

### 6. 论文的主要结论与发现
*   **打破 50% 限制**：传统 MAJ 在受损智能体过半时准确率趋近于零，而 RRMaj 在 $\rho=0.6$ 甚至 $\rho=0.8$ 时仍能保持较高的准确率（例如在 GSM8K 上实现 +69.9% 的恢复）。
*   **非线性增益**：Token 级协作将聚合从简单的线性求和转变为非线性算子积，使得诚实智能体的逻辑修复能力具有“以一敌多”的效果。
*   **能力不对称性**：弱小的诚实智能体（如 8B）能有效解救强大的受损智能体（如 70B），因为强大的模型在正确逻辑面前具有更强的自回归惯性。
*   **鲁棒性税（Robustness Tax）**：在完全无污染的环境下，RR 相比 MAJ 会有极轻微的性能下降（约 1-2%），这是由于频繁切换上下文导致的逻辑碎片化。

### 7. 优点
*   **理论严密**：利用 Lyapunov 稳定性理论和 Mesa-Optimization 框架为 LLM 的动态协作提供了坚实的数学基础。
*   **防御高效**：无需重新训练模型，仅通过推理侧的协议改变即可实现超强的对抗鲁棒性。
*   **成本友好**：在不增加解码计算量的前提下，解决了多智能体系统最核心的安全漏洞。

### 8. 不足与局限
*   **自适应攻击风险**：尚未测试能够感知共享上下文并刻意进行“逻辑破坏”的自适应对抗智能体。
*   **参数敏感性**：Token 块大小 $K$ 需要精细调优，过小会导致逻辑断裂，过大会给对抗者留下完整的错误生成空间。
*   **推理延迟**：虽然计算量持平，但频繁的上下文预填充在某些长文本场景下可能会增加首字延迟（TTFT）。
*   **任务局限**：目前主要集中在具有客观答案的推理任务，对于主观性较强的开放式生成任务效果尚待验证。

（完）
