---
title: Program Analysis Guided LLM Agent for Proof-of-Concept Generation
title_zh: 程序分析引导的用于概念验证生成的大语言模型智能体
authors: "Achintya Desai, Md Shafiuzzaman, Wenbo Guo, Tevfik Bultan"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07624v1"
tags: ["query:libfuz"]
score: 8.0
evidence: 程序分析引导的LLM用于漏洞复现和PoC生成
tldr: "针对软件漏洞PoC生成难题，本文提出PAGENT。该方法结合了轻量级静态分析引导和基于消毒器的动态分析反馈，构建了一个增强型LLM代理。通过融合程序分析技术，PAGENT克服了传统方法扩展性差及纯LLM方法成功率低的问题，在PoC生成任务上比现有最优方法提升了132%，显著提高了漏洞复现的自动化水平。"
selection_source: fresh_fetch
motivation: 传统的符号执行和模糊测试难以扩展，而现有LLM在自动生成漏洞PoC方面的成功率仍然较低。
method: 提出PAGENT框架，将轻量级静态分析规则与基于动态分析的覆盖率及剖析信息集成到LLM代理中。
result: "实验结果显示，PAGENT在PoC生成任务上的表现比之前最先进的代理方法提升了132%。"
conclusion: 结合程序分析引导的LLM代理能有效提升漏洞PoC生成的成功率和自动化程度。
---

## 摘要
软件开发人员经常收到漏洞报告，要求他们通过生成触发漏洞的概念验证（PoC）输入，以可靠的方式复现该漏洞。给定软件项目的源代码和潜在漏洞的特定代码位置，为给定漏洞自动生成 PoC 一直是一个具有挑战性的研究课题。符号执行和模糊测试技术在 PoC 生成方面需要专家指导和手动步骤，并面临可扩展性挑战。尽管大语言模型（LLM）的最新进展提高了自动化程度和可扩展性，但使用 LLM 生成 PoC 的成功率仍然较低。在本文中，我们提出了一种名为 PAGENT（程序分析引导的概念验证生成智能体）的新颖方法，该方法具有可扩展性，并且与之前的结果相比，显著提高了自动 PoC 生成的成功率。PAGENT 将用于提供静态分析指导的轻量级且基于规则的静态分析阶段，以及用于提供动态分析指导的基于 sanitizer 的分析和覆盖率信息，与 PoC 生成智能体集成在一起。我们的实验表明，这种混合方法在 PoC 生成任务上比之前表现最好的智能体方法性能提升了 132%。

## Abstract
Software developers frequently receive vulnerability reports that require them to reproduce the vulnerability in a reliable manner by generating a proof-of-concept (PoC) input that triggers it. Given the source code for a software project and a specific code location for a potential vulnerability, automatically generating a PoC for the given vulnerability has been a challenging research problem. Symbolic execution and fuzzing techniques require expert guidance and manual steps and face scalability challenges for PoC generation. Although recent advances in LLMs have increased the level of automation and scalability, the success rate of PoC generation with LLMs remains quite low. In this paper, we present a novel approach called Program Analysis Guided proof of concept generation agENT (PAGENT) that is scalable and significantly improves the success rate of automated PoC generation compared to prior results. PAGENT integrates lightweight and rule-based static analysis phases for providing static analysis guidance and sanitizer-based profiling and coverage information for providing dynamic analysis guidance with a PoC generation agent. Our experiments demonstrate that the resulting hybrid approach significantly outperforms the prior top-performing agentic approach by 132% for the PoC generation task.

---

## 论文详细总结（自动生成）

### 论文总结：PAGENT —— 程序分析引导的 PoC 生成智能体

#### 1. 核心问题与整体含义（研究动机和背景）
在软件安全领域，复现漏洞并生成可靠的概念验证（PoC）输入是漏洞修复、补丁验证和分类的关键步骤。然而，传统方法面临严峻挑战：
*   **传统工具局限性**：符号执行存在路径爆炸问题，模糊测试（Fuzzing）依赖高质量种子和专家编写的测试框架（Harness），且两者都难以在大规模复杂代码库中扩展。
*   **纯 LLM 方法不足**：虽然大语言模型（LLM）具备代码理解能力，但在处理复杂逻辑时容易产生“幻觉”，且缺乏对程序执行路径的精确感知，导致 PoC 生成的成功率较低。
*   **研究动机**：本文旨在结合程序分析（静态与动态）的精确性与 LLM 智能体的灵活性，提出一种可扩展且高成功率的自动化 PoC 生成框架 **PAGENT**。

#### 2. 方法论：核心思想与关键技术
PAGENT 采用混合架构，由三个核心组件构成：
*   **静态分析引导（SA Guidance）**：
    *   **轻量级分析**：通过函数签名分析（FSA）构建超近似调用图，识别从入口点（如 `main` 或 `LLVMFuzzerTestOneInput`）可达的函数，剔除无关代码以提高扩展性。
    *   **基于规则的分析**：使用 Datalog（Soufflé）编写漏洞模式规则（涵盖堆栈溢出、整数溢出等 12 种类型），生成包含污染路径、漏洞函数和断言模板的“漏洞报告”。
*   **PoC 生成智能体（PoC Generation Agent）**：
    *   基于 **CodeAct** 架构（使用 OpenHands 框架），智能体可以交互式地访问源代码、执行 Bash 命令和运行 Python 脚本。
    *   它将静态分析报告作为初始上下文，在沙盒环境中尝试构建候选 PoC。
*   **动态分析引导（DA Guidance）**：
    *   **反馈循环**：候选 PoC 在带有 Sanitizer（如 ASan, MSan, UBSan）的测试环境中运行。
    *   **覆盖率反馈**：如果 PoC 未触发崩溃，系统会提取行覆盖率、分支覆盖率和剖析信息（执行时间、入口点），反馈给智能体。智能体根据覆盖率数据定位逻辑偏差，迭代修正 PoC。

#### 3. 实验设计
*   **数据集**：使用 **ARVO** 数据集（源自 Cybergym），包含 10 个开源 C/C++ 项目（如 `binutils`, `gpac`, `selinux` 等），共 **203 个真实漏洞**。
*   **基准测试（Benchmark）**：
    *   **Cybergym 智能体**：包括基于 GPT-5、Claude-4、Sonnet-4 等顶级模型的智能体（即使这些基准模型拥有额外的堆栈轨迹信息）。
    *   **现有工具**：PoCGen（针对 NPM 漏洞设计的改进版）和 Faultline。
*   **对比模型**：主要测试了开源模型 **DeepSeek-V3.1** 和 **DeepSeek-V3.2**。

#### 4. 资源与算力
*   **硬件环境**：实验在配备 **Intel Core i9-13900K CPU** 和 **192 GB RAM** 的 Ubuntu 服务器上运行。
*   **算力说明**：论文未明确提及训练时长，因为 PAGENT 主要是基于现有的预训练 LLM（通过 API 调用）进行推理和交互，而非从头训练模型。
*   **成本优势**：论文强调使用 DeepSeek 模型通过 API 生成 PoC 的成本仅为 GPT-5 的 **1/33**（0.42美元 vs 14美元/百万 Token）。

#### 5. 实验数量与充分性
*   **实验规模**：针对 203 个漏洞实例进行了完整测试，涵盖了多种漏洞类型和复杂的工业级代码库。
*   **消融实验**：设计了“无引导”、“仅静态分析引导”和“仅动态分析引导”三组对比实验，验证了各组件的贡献。
*   **客观性**：实验对比了不同能力的 LLM，并分析了“独有 PoC 生成能力”和“补丁后漏洞检测”，实验设计较为全面、公平。

#### 6. 主要结论与发现
*   **性能飞跃**：PAGENT（基于 DeepSeek-3.2）达到了 **64.6%** 的成功率，比之前最强的 GPT-5 智能体基准提升了 **132%**。
*   **以弱胜强**：即使使用较弱的开源模型（DS-3.1），在程序分析引导下，其表现也优于未受引导的顶级闭源模型（如 GPT-5, Claude-4）。
*   **补丁后漏洞发现**：PAGENT 成功识别了 **32 个**在开发人员提交补丁后依然存在的漏洞（补丁不完整或新发现），检测能力是基准方法的 4 倍。
*   **迭代价值**：动态覆盖率反馈是智能体从错误路径回归正轨的关键。

#### 7. 优点与亮点
*   **混合驱动**：成功将传统的静态/动态分析工具（LLVM, Soufflé, Sanitizers）与现代 LLM 智能体深度集成。
*   **规则可扩展性**：基于 Datalog 的规则库易于扩展，可以快速适配新出现的漏洞模式。
*   **实用性强**：不依赖于漏洞报告中的文本描述或崩溃堆栈（这些在现实中往往缺失），仅需代码位置即可工作。
*   **高性价比**：证明了通过合理的系统设计，低成本的开源模型可以超越昂贵的闭源模型。

#### 8. 不足与局限
*   **规则依赖**：静态分析阶段依赖预定义的漏洞规则，对于逻辑漏洞或未建模的漏洞类型可能失效。
*   **数据泄露风险**：虽然作者进行了分析，但不能完全排除 LLM 在训练阶段见过部分开源项目 PoC 的可能性（尽管补丁后漏洞的发现缓解了这一疑虑）。
*   **语言限制**：目前仅支持 C/C++，对于 Java、Python 等其他语言需要重新开发静态分析插件。
*   **环境依赖**：需要能够成功编译并运行 Sanitizer 的构建环境，对于构建极其复杂的遗留系统可能存在部署困难。

（完）
