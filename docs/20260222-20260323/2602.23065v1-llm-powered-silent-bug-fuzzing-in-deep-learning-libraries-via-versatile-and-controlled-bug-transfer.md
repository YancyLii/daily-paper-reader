---
title: LLM-Powered Silent Bug Fuzzing in Deep Learning Libraries via Versatile and Controlled Bug Transfer
title_zh: 基于大语言模型驱动的深度学习库静默错误模糊测试：通过多功能且受控的错误转移
authors: "Kunpeng Zhang, Dongwei Xiao, Daoyuan Wu, Jiali Zhao, Yuanyi Lin, Tongtong Xu, Shaohua Wang, Shuai Wang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23065v1"
tags: ["query:libfuz"]
score: 9.0
evidence: 基于大模型的深度学习库模糊测试及API匹配
tldr: 针对深度学习库中难以检测的静默错误（Silent Bugs），本研究提出 TransFuzz。该工具利用大语言模型（LLM）从历史漏洞报告中提取上下文相关的错误模式，通过功能嵌入匹配相似 API，并将已知漏洞的触发逻辑和预测器（Oracle）迁移到目标 API。实验表明，TransFuzz 在 PyTorch、TensorFlow 和 MindSpore 中发现了 79 个新漏洞，显著提升了静默错误的检测能力。
selection_source: fresh_fetch
motivation: 现有的深度学习库模糊测试工具难以检测不导致崩溃但结果错误的静默漏洞，且缺乏有效的测试程序和预测器。
method: 利用 LLM 提取历史漏洞模式，通过功能嵌入匹配相似 API，并自动合成带有定制预测器的测试用例，同时引入自验证模块确保迁移可靠性。
result: 在 PyTorch、TensorFlow 和 MindSpore 中成功发现了 79 个此前未知的漏洞，其中 12 个被确认为 CVE。
conclusion: 该研究证明了利用 LLM 进行跨 API 的漏洞模式迁移是检测深度学习库静默错误的有效且通用的方法。
---

## 摘要
深度学习（DL）库广泛应用于关键应用中，即使是细微的静默错误也可能导致严重后果。虽然现有的深度学习模糊测试技术在检测崩溃方面取得了进展，但由于缺乏有效的测试程序和相应的测试预言机（oracles），它们在检测静默错误方面本质上存在困难。基于历史错误报告包含丰富的、未被充分利用的静默错误信息这一观察，我们利用大语言模型（LLM）进行多功能且受控的错误转移，以进行静默错误模糊测试。具体而言，我们的方法使用 LLM 从历史问题中提取上下文感知的错误模式，使用基于功能的嵌入匹配语义相关的应用程序编程接口（API），并合成带有定制预言机的测试用例。这通过将高风险上下文和预言机设计从已知的有缺陷 API 转移到功能相似的目标 API，实现了对静默错误的主动检测。为了确保上下文感知错误转移的可靠性，我们引入了一个由 LLM 驱动的自验证模块，系统地评估每个转移的错误实例的有效性。我们在名为 TransFuzz 的工具中实现了这一方法，并在三个主流深度学习库（PyTorch、TensorFlow 和 MindSpore）上对其进行了评估。TransFuzz 成功发现了 10 种错误类型中的 79 个此前未知的错误（其中 12 个被确认为通用漏洞披露 (CVE)），证明了其在迁移深度学习库错误发现能力方面的有效性和通用性。

## Abstract
Deep learning (DL) libraries are widely used in critical applications, where even subtle silent bugs can lead to serious consequences. While existing DL fuzzing techniques have made progress in detecting crashes, they inherently struggle to detect silent bugs due to the lack of effective test programs and corresponding oracles.   Building on the observation that historical bug reports contain rich, underutilized information about silent bugs, we leverage large language models (LLMs) to perform versatile yet controlled bug transfer for silent bug fuzzing. Specifically, our approach uses LLMs to extract context-aware bug patterns from historical issues, match semantically related Application Programming Interfaces (APIs) using functionality-based embeddings, and synthesize test cases with customized oracles. This enables proactive detection of silent bugs by transferring high-risk contexts and oracle designs from known buggy APIs to functionally similar target APIs. To ensure the reliability of our context-aware bug transfer, we introduce an LLM-powered self-validation module that systematically evaluates the validity of each transferred bug instance. We implement this methodology in a tool named TransFuzz and evaluate it on three mainstream DL libraries: PyTorch, TensorFlow, and MindSpore. TransFuzz successfully discovers 79 previously unknown bugs (12 confirmed as Common Vulnerabilities and Exposures (CVEs)) in 10 bug types, demonstrating its effectiveness and generalizability in migrating DL library bug discovery capabilities.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TransFuzz** 的工具，旨在利用大语言模型（LLM）解决深度学习（DL）库中极其隐蔽的“静默错误”（Silent Bugs）检测难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：深度学习库（如 PyTorch）中的**静默错误**（即程序不崩溃但输出错误结果、性能下降或梯度异常）比崩溃类错误更难检测。
*   **研究动机**：
    *   **缺乏通用预言机（Oracle）**：崩溃可以通过程序中断识别，但静默错误需要针对具体 API 语义设计复杂的检查逻辑。
    *   **现有工具局限**：目前的模糊测试工具（如 TitanFuzz, DFUZZ）大多依赖通用的 CPU-GPU 差异检测，仅能覆盖约 33% 的静默错误类型。
    *   **历史信息的价值**：作者发现历史漏洞报告中蕴含了丰富的触发上下文和预言机设计模式，且功能相似的 API 往往存在类似的漏洞模式。

### 2. 方法论：转移-验证（Transfer-then-Verify）框架
TransFuzz 的核心思想是将已知漏洞从“源 API”迁移到“功能相似的目标 API”。
*   **上下文感知错误模式提取**：利用 LLM（如 o3-mini）从 GitHub 的历史 Issue 和 PR 中提取三要素：漏洞 API、触发上下文（输入条件）、预言机设计（如何判断出错）。
*   **基于功能的 API 匹配**：不依赖语法相似性，而是通过 LLM 生成 API 的功能描述并计算向量嵌入（Embedding），在库中检索语义最接近的候选 API。
*   **上下文感知错误转移**：使用 LLM 将提取的漏洞模式适配到目标 API，自动合成包含特定触发逻辑和定制化预言机的测试程序。
*   **LLM 驱动的自验证模块**：为了降低误报，引入了多步验证逻辑：
    1.  **插桩监控**：记录运行时变量状态。
    2.  **症状相似性分析**：对比目标程序与原始漏洞的 IR 模式是否一致。
    3.  **预言机正确性校验**：反向假设目标 API 无误，检查预言机是否会因环境噪声误报。
    4.  **基于准则的漏洞验证**：提取原始漏洞的判定逻辑，对目标程序进行最终裁决。

### 3. 实验设计
*   **实验对象**：PyTorch (v2.6/2.7), TensorFlow (v2.18), MindSpore (v2.3)。
*   **数据集**：从 PyTorch 收集了 7,466 个历史 Issue 作为漏洞模式来源。
*   **Benchmark 与对比方法**：
    *   对比了 6 种最先进的工具：FreeFuzz, DeepREL, ACETest, TitanFuzz, DFUZZ, Pathfinder。
    *   涵盖了 9 种静默错误类型（如 Eager vs Compiled 差异、梯度错误、性能退化等）。

### 4. 资源与算力
*   **硬件环境**：NVIDIA RTX A6000 GPU, 256 GB RAM, Ubuntu 20.04。
*   **模型使用**：o3-mini（提取模式）、GPT-4o mini（API 匹配与测试生成）、GPT-4.1 mini（自验证）。
*   **成本开销**：在 PyTorch 上运行完整流程的总花费约为 **89.07 美元**。

### 5. 实验数量与充分性
*   **实验规模**：生成了超过 35,000 个测试程序，覆盖了 2,421 个 PyTorch API。
*   **消融实验**：
    *   测试了不同窗口大小（Window Size）对漏洞发现效率的影响。
    *   验证了 LLM 自验证模块中重复实验次数（Repeated Time）对稳定性的贡献。
    *   对比了多种 LLM（DeepSeek R1/V3, Qwen-plus, Doubao 等）在验证任务上的表现。
*   **充分性评价**：实验设计非常全面，不仅验证了静默错误，还展示了跨框架（PyTorch 模式迁移至 TF/MindSpore）检测崩溃错误的能力，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **漏洞发现**：共发现 **79 个新漏洞**，其中 31 个为静默错误，48 个为崩溃。
*   **安全影响**：已有 **12 个漏洞被分配了 CVE 编号**，部分漏洞在代码中潜伏超过 3 年。
*   **检测能力**：TransFuzz 覆盖了全部 9 种静默错误类型，而现有工具组合仅能覆盖 3 种。
*   **准确率**：自验证模块将静默错误检测的精度提升至 **71.42%**，远高于纯 LLM 判断的 30%。

### 7. 优点与亮点
*   **首创性**：这是首个系统性利用 LLM 进行深度学习库静默错误迁移测试的工作。
*   **定制化预言机**：突破了传统模糊测试依赖通用预言机的限制，能够根据 API 语义动态生成检查逻辑。
*   **反馈驱动**：引入了反馈机制，一旦发现新漏洞，会自动探索其相似 API，提高了测试的针对性。
*   **自验证机制**：通过 IR 模式匹配和逻辑推理，有效解决了 LLM 在复杂代码分析中的幻觉问题。

### 8. 不足与局限
*   **误报风险**：尽管有自验证，仍存在约 28% 的误报，部分涉及复杂的编译器优化（如数值微差），LLM 难以完全区分正常优化与功能漏洞。
*   **依赖历史数据**：该方法的效果高度依赖于历史漏洞报告的质量和数量，对于全新的、无先例的漏洞类型可能失效。
*   **跨框架静默错误迁移难**：目前跨框架迁移主要成功于崩溃检测，由于不同框架间语义差异巨大，静默错误的跨框架迁移仍具挑战。

（完）
