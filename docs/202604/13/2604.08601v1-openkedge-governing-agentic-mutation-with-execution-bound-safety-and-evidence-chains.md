---
title: "OpenKedge: Governing Agentic Mutation with Execution-Bound Safety and Evidence Chains"
title_zh: OpenKedge：通过执行绑定安全与证据链治理智能体状态变更
authors: "Jun He, Deying Yu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.08601v1"
tags: ["query:agsec"]
score: 9.0
evidence: 通过执行绑定安全和策略约束治理智能体变动
tldr: 针对自主AI代理在API架构中直接执行状态变更带来的安全风险，本文提出OpenKedge协议。该协议将变更定义为受控过程，通过声明式意图提案、执行合约及临时身份，将安全保障从反应式过滤转为预防式强制执行。同时引入IEEC证据链实现全流程加密审计，为大规模代理系统的安全运行提供了确定性治理框架。
selection_source: fresh_fetch
motivation: 现有的以API为中心的架构在处理概率性AI代理的执行请求时，缺乏足够的上下文协调和预防性安全保障。
method: 提出一种将意图提案转化为受限执行合约的协议，并利用加密证据链记录从意图到结果的完整谱系以实现确定性审计。
result: 在多代理冲突和云基础设施场景中，该协议成功实现了确定性的意图仲裁和安全边界限制，同时保持了高吞吐量。
conclusion: OpenKedge通过将变更过程可验证化和确定性化，为构建安全、可审计的大规模自主代理系统提供了核心治理机制。
---

## 摘要
自主 AI 智能体的兴起暴露了以 API 为中心的架构中的一个根本缺陷：概率性系统在缺乏足够上下文、协调或安全保障的情况下直接执行状态变更。我们引入了 OpenKedge，这是一种将状态变更重新定义为受治理过程而非 API 调用直接结果的协议。OpenKedge 要求执行者提交声明式意图提案，并在执行前根据确定性推导的系统状态、时间信号和策略约束对其进行评估。获批的意图被编译为执行合约，严格限制允许的操作、资源范围和时间，并通过临时的、面向任务的身份进行强制执行。这将安全性从反应式过滤转变为预防性的、受执行约束的强制执行。至关重要的是，OpenKedge 引入了“意图到执行证据链”（IEEC），通过密码学方式将意图、上下文、策略决策、执行边界和结果链接成统一的溯源体系。这将状态变更转化为一个可验证且可重构的过程，实现了确定性的可审计性以及对系统行为的推理。我们在多智能体冲突场景和云基础设施变更中对 OpenKedge 进行了评估。结果表明，该协议在保持高吞吐量的同时，能够确定性地仲裁竞争意图并限制不安全的执行，为大规模安全运行智能体系统奠定了原则性基础。

## Abstract
The rise of autonomous AI agents exposes a fundamental flaw in API-centric architectures: probabilistic systems directly execute state mutations without sufficient context, coordination, or safety guarantees. We introduce OpenKedge, a protocol that redefines mutation as a governed process rather than an immediate consequence of API invocation. OpenKedge requires actors to submit declarative intent proposals, which are evaluated against deterministically derived system state, temporal signals, and policy constraints prior to execution. Approved intents are compiled into execution contracts that strictly bound permitted actions, resource scope, and time, and are enforced via ephemeral, task-oriented identities. This shifts safety from reactive filtering to preventative, execution-bound enforcement. Crucially, OpenKedge introduces an Intent-to-Execution Evidence Chain (IEEC), which cryptographically links intent, context, policy decisions, execution bounds, and outcomes into a unified lineage. This transforms mutation into a verifiable and reconstructable process, enabling deterministic auditability and reasoning about system behavior. We evaluate OpenKedge across multi-agent conflict scenarios and cloud infrastructure mutations. Results show that the protocol deterministically arbitrates competing intents and cages unsafe execution while maintaining high throughput, establishing a principled foundation for safely operating agentic systems at scale.

---

## 论文详细总结（自动生成）

### 论文总结：OpenKedge 智能体治理协议

#### 1. 核心问题与研究动机
*   **核心问题**：传统的以 API 为中心的架构是“被动且盲目”的。它们假设调用者（通常是确定性的程序）是正确且具备完整上下文的。然而，当自主 AI 智能体（Agent）作为调用者时，其“概率性”特征会导致幻觉、上下文缺失或多智能体冲突，从而触发灾难性的系统故障（如误删生产数据库）。
*   **研究背景**：随着 Agent 深入 DevOps、云基础设施等生产环境，API 无法识别语义意图和全局依赖。现有的安全手段多为反应式的过滤（如正则匹配），缺乏预防性的、与执行绑定的治理机制。

#### 2. 方法论：核心思想与技术细节
OpenKedge 将状态变更（Mutation）从“直接执行 API”重定义为“受控的治理过程”。其核心流程如下：
*   **意图提案（Intent Proposal）**：智能体不再直接调用 API，而是提交声明式的意图，描述“想要达到的结果”。
*   **上下文评估与策略治理**：系统根据实时全局状态（如流量、依赖图）和确定性策略（使用 Cedar 策略语言）评估提案。
    *   **冲突仲裁公式**：使用优先级评分 $Priority(p_i) = \alpha \cdot Authority(a_i) + \beta \cdot Trust(a_i)$，结合时间近度（Recency）来解决多智能体间的竞争冲突。
*   **执行合约（Execution Contracts）**：获批的意图被编译为合约 $C = (a, R, T)$，明确限定允许的操作（$a$）、资源范围（$R$）和有效期（$T$）。
*   **任务导向身份（Task-Oriented Identity）**：通过动态生成的临时凭证（如 AWS STS 令牌）强制执行合约。即使智能体逻辑出错，其权限也被物理限制在合约范围内。
*   **意图到执行证据链（IEEC）**：引入加密链接的事件流，记录从意图、上下文、决策到执行结果的完整谱系，确保系统状态变更可追溯、可重构。

#### 3. 实验设计
*   **实验场景**：
    1.  **多智能体冲突场景**：模拟人类操作员与多个 AI 智能体并发修改共享资源，测试权限、信任分和时间戳对冲突的仲裁能力。
    2.  **云基础设施（AWS）场景**：模拟 Agent 试图删除“看似闲置”但实际有下游依赖的 EC2 实例，以及在高峰期进行不安全的缩容操作。
*   **Benchmark/对比对象**：对比了传统 API 架构、工作流引擎、ReAct 模式及 AARM 等运行时控制框架。
*   **评估指标**：冲突仲裁准确性、执行边界安全性（Blast Radius）、系统确定性及性能开销。

#### 4. 资源与算力
*   **硬件环境**：核心评估引擎运行在 AWS EC2 `m5.2xlarge` 实例（8 vCPUs, 32 GiB RAM），存储使用 Amazon RDS PostgreSQL（`db.m5.2xlarge`）配合 `gp3` EBS 卷。
*   **算力说明**：文中未提及 GPU 训练，因为该研究侧重于治理协议和系统架构，而非模型本身的训练。实验重点在于策略评估延迟（平均 11ms）和系统吞吐量。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 10,000 次异步提案的并发负载测试，以验证算法的确定性。
*   **充分性评价**：实验涵盖了典型的 Agent 失败模式（幻觉、陈旧上下文、权限溢出）。通过高并发负载测试证明了系统在 3,200 次变更/秒的压力下仍能保持低延迟（99分位延迟 < 30ms），实验设计较为客观且具有工业参考价值。

#### 6. 主要结论与发现
*   **确定性治理**：OpenKedge 能有效仲裁多智能体冲突，防止因 Agent 幻觉导致的非预期状态变更。
*   **安全边界**：通过临时身份强制执行合约，成功将 Agent 的故障影响范围（Blast Radius）限制在预设边界内。
*   **可审计性**：IEEC 证据链使得“为什么执行”和“如何执行”变得透明，解决了 AI 决策的黑盒问题。
*   **高性能**：协议在提供强安全保障的同时，能够支撑大规模企业级应用的吞吐需求。

#### 7. 优点
*   **范式转移**：从“执行 API”转向“治理意图”，从根本上解决了概率性 Agent 与确定性基础设施之间的不匹配。
*   **执行绑定安全**：不仅在决策层拦截，还通过密码学身份在执行层进行物理隔离，提供了极强的安全保障。
*   **IEEC 创新**：将审计从被动的日志记录提升为主动的、可验证的决策溯源。

#### 8. 不足与局限
*   **策略编写依赖**：目前高度依赖人工编写的确定性策略（如 Cedar 规则），对人类的前瞻性要求较高。
*   **治理核心的信任假设**：协议假设治理层（Policy Engine, IEEC）本身运行在可信计算基（TCB）上，若治理层被攻破，系统将失效。
*   **动态适应性**：目前尚不支持根据历史事件自动学习或推导安全策略，缺乏一定的自适应进化能力。

（完）
