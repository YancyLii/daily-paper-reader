---
title: "If you're waiting for a sign... that might not be it! Mitigating Trust Boundary Confusion from Visual Injections on Vision-Language Agentic Systems"
title_zh: 如果你在等待一个信号……那可能不是它！缓解视觉-语言智能体系统中视觉注入导致的信任边界混淆
authors: "Jiamin Chang, Minhui Xue, Ruoxi Sun, Shuchao Pang, Salil S. Kanhere, Hammond Pearce"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19844v1"
tags: ["query:agsec"]
score: 9.0
evidence: 缓解多模态智能体中覆盖用户意图的视觉注入攻击
tldr: 本研究探讨了具身视觉语言智能体（VLAS）在面对环境信号时的“信任边界混淆”问题，即智能体难以区分合法的环境指令（如红绿灯）与恶意的视觉注入攻击。作者构建了双重意图数据集和评估框架，系统评估了7种主流模型，发现它们普遍存在安全隐患。为此，提出了一种分离感知与决策的多智能体防御框架，在保持对合法信号响应的同时，显著增强了对视觉注入攻击的抵御能力。
selection_source: fresh_fetch
motivation: 具身智能体在利用环境视觉信号进行推理时，容易受到恶意视觉注入的干扰，产生信任边界混淆并导致安全风险。
method: 开发了双重意图评估框架，并提出一种通过分离感知与决策逻辑来动态评估视觉输入可靠性的多智能体防御方案。
result: 评估显示现有模型在处理视觉注入时表现不佳，而新防御框架在降低误导行为的同时保留了对正确信号的响应。
conclusion: 采用感知与决策分离的架构是解决视觉语言智能体信任边界混淆、提升系统鲁棒性的有效途径。
---

## 摘要
由大型视觉-语言模型（LVLM）驱动的具身视觉-语言智能体系统（VLAS）的最新进展，使 AI 系统能够感知现实场景并进行推理。在此背景下，交通信号灯等环境信号是关键的带内信号，能够且应当影响智能体的行为。然而，类似的信号也可能被精心设计为误导性的视觉注入，从而覆盖用户意图并带来安全风险。这种双重性带来了一个根本性的挑战：智能体必须对合法的环境线索做出反应，同时对误导性线索保持鲁棒。我们将这种矛盾称为“信任边界混淆”。为了研究这一行为，我们设计了一个双重意图数据集和评估框架，结果表明当前的基于 LVLM 的智能体无法可靠地平衡这种权衡，表现为要么忽略有用信号，要么遵循有害信号。我们在多种具身设置下，针对基于结构和基于噪声的视觉注入，系统地评估了 7 个 LVLM 智能体。为了应对这些漏洞，我们提出了一个多智能体防御框架，通过将感知与决策分离来动态评估视觉输入的可靠性。我们的方法在保留正确响应的同时显著减少了误导性行为，并提供了对抗扰动下的鲁棒性保证。评估框架的代码和产物已在 https://anonymous.4open.science/r/Visual-Prompt-Inject 公开。

## Abstract
Recent advances in embodied Vision-Language Agentic Systems (VLAS), powered by large vision-language models (LVLMs), enable AI systems to perceive and reason over real-world scenes. Within this context, environmental signals such as traffic lights are essential in-band signals that can and should influence agent behavior. However, similar signals could also be crafted to operate as misleading visual injections, overriding user intent and posing security risks. This duality creates a fundamental challenge: agents must respond to legitimate environmental cues while remaining robust to misleading ones. We refer to this tension as trust boundary confusion. To study this behavior, we design a dual-intent dataset and evaluation framework, through which we show that current LVLM-based agents fail to reliably balance this trade-off, either ignoring useful signals or following harmful ones. We systematically evaluate 7 LVLM agents across multiple embodied settings under both structure-based and noise-based visual injections. To address these vulnerabilities, we propose a multi-agent defense framework that separates perception from decision-making to dynamically assess the reliability of visual inputs. Our approach significantly reduces misleading behaviors while preserving correct responses and provides robustness guarantees under adversarial perturbations. The code of the evaluation framework and artifacts are made available at https://anonymous.4open.science/r/Visual-Prompt-Inject.

---

## 论文详细总结（自动生成）

这是一份关于论文《If you're waiting for a sign... that might not be it! Mitigating Trust Boundary Confusion from Visual Injections on Vision-Language Agentic Systems》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：论文探讨了具身视觉-语言智能体系统（VLAS）中的**“信任边界混淆”（Trust Boundary Confusion）**现象。
*   **研究动机**：VLAS（如自动驾驶、机器人）需要同时处理来自用户的指令（受信任渠道）和来自环境的视觉信号（如交通标志、警告标签）。然而，环境中的视觉信号既可能是合法的安全约束（Helpful），也可能是恶意的视觉注入攻击（Misleading）。当前的智能体难以区分这两者，导致要么盲目遵循恶意指令（Fail-Open），要么忽略关键的安全警告（Fail-Closed）。

### 2. 方法论
论文提出了一种**解耦的环境感知多智能体防御框架**，旨在动态划定信任边界。其核心思想是将感知与决策分离，分为三个功能平面：
*   **信号平面（观察智能体 - Observation Agent）**：作为一个纯粹的视觉语义解析器，提取图像中所有潜在的文字或符号信号（$I_v$），而不判断其意图。这解决了智能体对视觉模态的“感知懒惰”问题。
*   **信任平面（判断智能体 - Judgment Agent）**：核心认知层。它接收提取的视觉信号和用户指令，利用上下文学习（ICL）和领域安全规范，判定该信号是合法的环境约束还是恶意的任务劫持。
*   **执行平面（LVLM 智能体 - Execution Agent）**：根据判断结果生成最终行动计划。若信号合法，则优先遵循环境约束；若信号误导，则显式忽略并执行用户原始指令。
*   **攻击技术细节**：
    *   **结构化注入**：通过添加“忽略之前指令”、“警告”、“任务已完成”等语义前缀来增强视觉注入的效果。
    *   **噪声化注入**：使用投影梯度下降（PGD）算法，在 $\ell_\infty$ 约束下优化像素级扰动，通过组合对抗损失（$L_{adv}$）、不可感知损失（$L_{imp}$）和鲁棒性损失（$L_{rob}$）来强制模型产生特定输出。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像编辑（数字域）**：基于 InstructPix2Pix 构建了 250 个测试案例，涉及版权警告（合法）和不安全编辑（误导）。
    *   **具身操控（物理域）**：基于多模态情境安全（MSS）基准构建了 100 个案例，涉及设备故障警告（合法）和危险动作诱导（误导）。
*   **对比方法（Benchmark）**：
    *   **评估模型**：包括 GPT-4o、GPT-5 (预研版)、Gemini-1.5-Pro、Claude-3.5-Sonnet 以及开源模型 Qwen2.5-VL-7B、InternVL3-8B、DeepSeek-VL-7B。
    *   **防御对比**：对比了传统的 OCR 增强、基于 LVLM 的过滤、以及图像净化（JPEG 压缩、位深削减）等方法。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU 型号和总训练时长。
*   **实现细节**：对于闭源模型使用 API 调用；对于开源模型（<8B 参数），在本地环境进行推理。在进行噪声化注入实验时，使用了 PGD 优化（2000 步迭代），这通常需要单张或多张消费级/企业级 GPU（如 A100 或 RTX 3090/4090）支持，但具体配置未标明。

### 5. 实验数量与充分性
*   **实验规模**：系统评估了 7 种主流 LVLM，涵盖了数字图像处理和物理具身操控两大领域。
*   **消融实验**：对多智能体框架的各个组件（仅观察、仅判断、观察+判断）进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分且客观。作者不仅在模拟器中测试，还通过 JetCobot 机械臂、自动驾驶街景和无人机应急着陆三个真实/仿真案例验证了“仿真到现实”（Sim-to-Real）的可迁移性。

### 6. 主要结论与发现
*   **模态懒惰（Modality Laziness）**：较小的模型往往忽略视觉中的文字，盲目遵循文本指令；而感知能力越强的模型（如 GPT-4o），越容易受到视觉注入的影响。
*   **信任边界脆弱**：在无防御情况下，恶意视觉注入能显著诱导智能体偏离用户意图，尤其是在具身操控场景下。
*   **防御有效性**：提出的多智能体框架将误导性注入的成功率降低至 **3% 以下**，同时保留了 **95% 以上** 的合法安全信号响应率，远优于传统的二元过滤器。
*   **白盒攻击上限**：通过梯度优化的噪声注入在白盒条件下几乎 100% 成功，但跨模型迁移性较差。

### 7. 优点与亮点
*   **概念创新**：首次形式化定义了 VLAS 中的“信任边界混淆”问题，并区分了“有用”与“误导”信号。
*   **架构解耦**：多智能体防御框架不依赖于特定模型的重新训练，具有很强的模型无关性和可扩展性。
*   **双重保证**：既能防御结构化的语义攻击，也能通过集成净化技术抵御噪声扰动。

### 8. 不足与局限
*   **推理延迟**：由于需要三次连续的智能体调用（观察、判断、执行），系统延迟较高，可能难以满足自动驾驶等高实时性场景的需求。
*   **模型规模限制**：开源模型的评估局限于 8B 以下参数，未测试更大规模开源模型（如 Llama-3-V 70B）的表现。
*   **自适应攻击风险**：未来的攻击者可能会专门针对“判断智能体”的逻辑进行对抗性设计，论文对此类高级自适应攻击的探讨较少。

（完）
