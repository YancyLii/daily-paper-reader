---
title: "SafeAgent: A Runtime Protection Architecture for Agentic Systems"
title_zh: SafeAgent：一种针对智能体系统的运行时保护架构
authors: "Hailin Liu, Eugene Ilyushin, Jie Ni, Min Zhu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17562v1"
tags: ["query:agsec"]
score: 9.0
evidence: 用于Agent安全和执行治理的运行时安全架构
tldr: 针对大语言模型智能体易受提示注入攻击且传统过滤方法不足的问题，本文提出SafeAgent运行时安全架构。该架构将智能体安全视为状态化决策问题，通过运行时控制器和上下文感知决策核心，对交互轨迹进行动态治理。实验证明，SafeAgent在ASB和InjecAgent基准测试中显著提升了鲁棒性，同时保持了良好的任务性能，为智能体系统提供了更可靠的保护。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17562v1/fig-001.webp\", \"caption\": \"Fig. 4. Execution flow of the SafeAgent Core.\", \"page\": 7, \"index\": 1, \"width\": 1073, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.17562v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the SafeAgent architecture.\", \"page\": 4, \"index\": 2, \"width\": 1072, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.17562v1/fig-003.webp\", \"caption\": \"Fig. 3. Flow control sequence in the agent lifecycle.\", \"page\": 5, \"index\": 3, \"width\": 524, \"height\": 644}]"
motivation: 传统的输入输出过滤难以应对多步工作流和持久上下文中传播的复杂提示注入攻击。
method: 提出一种由运行时控制器和上下文感知决策核心组成的架构，通过风险编码、效用评估和策略仲裁等算子实现状态化安全治理。
result: 在Agent Security Bench和InjecAgent上的实验显示，该方法在显著提升安全性的同时，维持了与基准相当的良性任务表现。
conclusion: SafeAgent通过将安全治理与语义风险推理分离，为构建鲁棒且高效的智能体系统提供了一种有效的运行时保护方案。
---

## 摘要
大语言模型（LLM）智能体容易受到提示注入攻击的影响，这些攻击会通过多步工作流、工具交互和持久上下文进行传播，使得仅靠输入输出过滤不足以提供可靠的保护。本文提出了 SafeAgent，这是一种运行时安全架构，它将智能体安全视为演化交互轨迹上的有状态决策问题。所提出的设计通过两个协调组件将执行治理与语义风险推理分离：一个是在智能体循环周围调解动作的运行时控制器，另一个是在持久会话状态上运行的上下文感知决策核心。该核心被形式化为一种上下文感知的高级机器智能，并通过风险编码、效用成本评估、后果建模、策略仲裁和状态同步等算子进行实例化。在 Agent Security Bench (ASB) 和 InjecAgent 上的实验表明，SafeAgent 在保持竞争力的良性任务性能的同时，相比基准方法和文本级护栏（guardrail）方法，一致地提高了鲁棒性。消融研究进一步表明，恢复置信度和策略权重决定了不同的安全-效用运行点。

## Abstract
Large language model (LLM) agents are vulnerable to prompt-injection attacks that propagate through multi-step workflows, tool interactions, and persistent context, making input-output filtering alone insufficient for reliable protection. This paper presents SafeAgent, a runtime security architecture that treats agent safety as a stateful decision problem over evolving interaction trajectories. The proposed design separates execution governance from semantic risk reasoning through two coordinated components: a runtime controller that mediates actions around the agent loop and a context-aware decision core that operates over persistent session state. The core is formalized as a context-aware advanced machine intelligence and instantiated through operators for risk encoding, utility-cost evaluation, consequence modeling, policy arbitration, and state synchronization. Experiments on Agent Security Bench (ASB) and InjecAgent show that SafeAgent consistently improves robustness over baseline and text-level guardrail methods while maintaining competitive benign-task performance. Ablation studies further show that recovery confidence and policy weighting determine distinct safety-utility operating points.

---

## 论文详细总结（自动生成）

以下是对论文《SafeAgent: A Runtime Protection Architecture for Agentic Systems》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）被部署为能够规划、调用工具并与外部环境交互的智能体（Agents），其面临的安全威胁也从单纯的文本生成转向了操作性风险。
*   **核心问题**：传统的安全防御（如输入输出过滤）通常是单次、无状态的，难以应对在多步工作流中通过工具输出、检索文档或记忆传播的**间接提示注入（Indirect Prompt Injection）**。攻击者可以利用这些渠道逐步渗透，诱导智能体执行未经授权的工具调用或数据泄露。
*   **整体含义**：论文提出将智能体安全视为一个**有状态的运行时决策问题**，而非简单的文本过滤，旨在通过一种解耦的架构在保证任务效用的同时，实现对复杂攻击轨迹的动态拦截。

### 2. 论文提出的方法论
SafeAgent 架构由两个核心组件组成，实现了执行治理与语义推理的分离：
*   **运行时控制器（Runtime Controller）**：
    *   **职责**：作为智能体与环境之间的中介，管理会话上下文（Session Context），拦截并规范化所有工具调用。
    *   **关键技术**：提供上下文覆盖（Override）、重新规划（Replanning）、状态回滚（Rollback）和人工审批等恢复机制。
*   **决策核心（SafeAgent Core）**：
    *   **核心思想**：将其形式化为“上下文感知的高级机器智能（AMI）”，通过内部模拟和预测来评估风险。
    *   **关键算子**：
        1.  **风险编码（Risk Encoding）**：使用多个专家编码器识别混淆、触发器激活、策略违规等维度。
        2.  **优势-成本建模（Advantage-Cost Modeling）**：评估不同干预策略（如拒绝、修复）的即时效用与成本。
        3.  **世界模型模拟（World Model Simulation）**：在潜在空间模拟动作后果，预测未来轨迹。
        4.  **策略仲裁（Policy Arbitration）**：根据任务需求（安全优先或任务优先）平衡竞争目标。
        5.  **状态同步（State Synchronization）**：维护多时间尺度的风险状态（即时、任务级、长周期）。

### 3. 实验设计
*   **数据集/场景**：
    *   **Agent Security Bench (ASB)**：包含 51 个任务和 40 种攻击工具，共 2040 个评估案例，涵盖直接注入（DPI）、间接注入（IPI）和记忆中毒（MP）。
    *   **InjecAgent**：专注于工具集成智能体的注入攻击，包含 510 个直接伤害任务和 544 个多阶段数据窃取任务。
*   **对比方法（Baselines）**：
    *   **No Defense**：无防御的基准智能体。
    *   **Llama Guard**：基于 LLM 的输入输出护栏模型。
    *   **LLM Guard**：基于分类器（如 RoBERTa）的文本级扫描工具。
*   **评估指标**：攻击成功率（ASR）、良性任务性能（PNA）、归一化奖励保留（NRP，综合衡量安全与效用）。

### 4. 资源与算力
*   **模型使用**：智能体后端使用 **DeepSeek-V3.2**；SafeAgent Core 使用 **gpt-oss-safeguard-20b** 模型进行风险推理。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。但提到该架构引入了非平凡的计算开销（Latency），这是未来的优化方向。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流安全基准上进行了大规模测试（总计超过 3000 个测试案例），并针对“恢复置信度”和“策略权重”进行了详细的消融实验。
*   **充分性与客观性**：实验涵盖了多种攻击向量（直接、间接、记忆中毒）和多阶段攻击场景（数据提取+传输），对比了当前工业界常用的护栏方法。通过 NRP 指标客观地展示了安全增强往往伴随着一定的性能损失（Safety-Utility Trade-off），实验设计较为全面且公平。

### 6. 论文的主要结论与发现
*   **防御有效性**：SafeAgent 在所有攻击类型下均显著降低了 ASR。在 InjecAgent 的多阶段数据窃取攻击中，SafeAgent 将成功率降至 **0%**，远优于 Llama Guard 等方法。
*   **状态化的必要性**：实验证明纯文本过滤（如 LLM Guard）在应对通过工具交互传播的攻击时效果有限，而 SafeAgent 的有状态决策能有效拦截演化中的攻击轨迹。
*   **参数敏感性**：消融实验发现，**覆盖置信度**决定了系统是倾向于“修复”还是“拒绝”；**策略权重**则决定了系统在安全与任务完成度之间的运行平衡点。

### 7. 优点
*   **架构解耦**：将执行层的“硬约束”与推理层的“软决策”分离，增强了系统的灵活性和可扩展性。
*   **恢复机制丰富**：不仅是简单的拦截，还提供了回滚、重写参数、重新规划等多种恢复手段，提升了系统的鲁棒性。
*   **理论严谨**：将安全问题形式化为 AMI 决策问题，为智能体安全研究提供了新的理论视角。

### 8. 不足与局限
*   **计算开销**：由于在每个执行步骤都需要调用额外的推理模型（Core），会显著增加系统的响应延迟和推理成本。
*   **应用限制**：目前主要针对 ReAct 模式的智能体，对于更复杂的异步或多智能体协作场景的适用性尚待验证。
*   **偏差风险**：决策核心本身依赖于另一个 LLM（如 gpt-oss-safeguard），如果该模型本身存在偏见或被绕过，防御系统可能会失效。
*   **新兴威胁**：论文提到对于“技能注入（Skill Injection）”等新兴威胁类别的研究尚不充分。

（完）
