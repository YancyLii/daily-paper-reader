---
title: "Stop Fixating on Prompts: Reasoning Hijacking and Constraint Tightening for Red-Teaming LLM Agents"
title_zh: 摆脱对提示词的执着：针对大语言模型智能体红队测试的推理劫持与约束收紧
authors: "Yanxu Mao, Peipei Liu, Tiehan Cui, Congying Liu, Mingzhe Xing, Datao You"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05549v1"
tags: ["query:agsec"]
score: 9.0
evidence: 操纵智能体推理轨迹与约束收紧
tldr: 随着大语言模型智能体的广泛应用，其安全性日益重要。现有红队测试主要依赖修改提示词，存在适应性差等局限。本文提出 JailAgent 框架，完全避免修改用户提示词，而是通过推理劫持和约束收紧来操纵智能体的推理轨迹和记忆检索。该框架包含触发器提取、推理劫持和约束收紧三个阶段，在跨模型和跨场景测试中表现优异，为评估智能体安全性提供了新视角。
selection_source: fresh_fetch
motivation: 现有的红队测试方法过度依赖提示词修改，导致其在处理新数据时缺乏适应性且可能损害智能体性能。
method: 提出 JailAgent 框架，通过触发器提取、推理劫持和约束收紧三个阶段，在不改变提示词的情况下隐式操纵智能体的推理过程。
result: 实验证明 JailAgent 在多种模型和复杂应用场景下均展现出卓越的攻击性能和跨环境适应性。
conclusion: 该研究表明，通过操纵推理轨迹而非仅仅修改提示词，可以更有效地揭示 LLM 智能体的潜在安全漏洞。
---

## 摘要
随着基于大语言模型（LLM）的智能体在各个领域的广泛应用，其复杂性引入了新的安全威胁。现有的红队测试方法大多依赖于修改用户提示词，这缺乏对新数据的适应性，并可能影响智能体的性能。为了应对这一挑战，本文提出了 JailAgent 框架，该框架完全避免了对用户提示词的修改。具体而言，它通过三个关键阶段隐式地操纵智能体的推理轨迹和记忆检索：触发器提取、推理劫持和约束收紧。通过精确的触发器识别、实时自适应机制和优化的目标函数，JailAgent 在跨模型和跨场景环境中表现出了卓越的性能。

## Abstract
With the widespread application of LLM-based agents across various domains, their complexity has introduced new security threats. Existing red-team methods mostly rely on modifying user prompts, which lack adaptability to new data and may impact the agent's performance. To address the challenge, this paper proposes the JailAgent framework, which completely avoids modifying the user prompt. Specifically, it implicitly manipulates the agent's reasoning trajectory and memory retrieval with three key stages: Trigger Extraction, Reasoning Hijacking, and Constraint Tightening. Through precise trigger identification, real-time adaptive mechanisms, and an optimized objective function, JailAgent demonstrates outstanding performance in cross-model and cross-scenario environments.

---

## 论文详细总结（自动生成）

### 论文总结：《Stop Fixating on Prompts: Reasoning Hijacking and Constraint Tightening for Red-Teaming LLM Agents》

#### 1. 论文的核心问题与整体含义
*   **研究动机**：随着基于大语言模型（LLM）的智能体（Agent）在临床决策、视频分析等高价值领域广泛应用，其安全性成为焦点。
*   **核心问题**：现有的红队测试（Red-Teaming）方法过度依赖于**显式修改用户提示词（Prompt）**。这种方式存在三大缺陷：
    1.  **适应性差**：难以泛化到新数据或不同格式。
    2.  **易被检测**：修改后的 Prompt 往往带有明显的攻击特征，容易被防御机制拦截。
    3.  **性能损耗**：强行改变 Prompt 可能破坏智能体完成原始任务的能力，导致攻击不隐蔽。
*   **整体含义**：本文提出 **JailAgent** 框架，主张“摆脱对提示词的执着”，通过在不改变用户输入的前提下，隐式地操纵智能体的**推理轨迹**和**记忆检索**来实现越狱攻击。

#### 2. 方法论
JailAgent 采用三阶段流水线设计：
*   **触发器提取 (Trigger Extraction)**：
    *   利用 BERT 的 WordPiece 分词与 spaCy 的语法解析进行对齐。
    *   通过**粗到细**的策略：先在短语层面进行掩码测试，再在 Token 层面利用 **Log-probability 变化**和**步进式 KL 散度（Step-wise KL）**量化每个 Token 对模型输出的贡献度，从而识别高贡献触发器。
*   **推理劫持 (Reasoning Hijacking)**：
    *   **Data Factory**：自动合成“上下文-毒响应-良性响应”的三元组训练数据。
    *   **Reranker 机制**：使用冻结的编码器（SentenceTransformer）和可学习的轻量级 MLP 评分头。通过 **Pairwise Ranking Loss** 实时微调评分头，使智能体在生成多个候选答案时，倾向于选择被劫持的、偏离正常逻辑的响应。
*   **约束收紧 (Constraint Tightening)**：
    *   设计了四种联合优化损失函数来约束触发器的语义空间特征：
        1.  **Particularity Loss**：确保触发器在语义上远离良性数据簇。
        2.  **Clustering Loss**：增强触发器内部 Token 的紧凑性，提升稳定性。
        3.  **Separability Loss**：优化检索命中率，确保毒样本易被检索。
        4.  **Margin Loss**：扩大毒样本与良性样本之间的决策边界。

#### 3. 实验设计
*   **智能体类型与场景**：
    *   **VideoAgent**：长视频理解（数据集：EgoSchema, NExT-QA）。
    *   **ReAct-UALA**：协调推理与行动（数据集：HotpotQA, StrategyQA, MMLU）。
    *   **EHRAgent**：电子健康档案复杂推理（数据集：MIMIC-III, eICU, TREQS）。
*   **基准模型 (Backbones)**：涵盖 7 种核心 LLM，包括 GPT-3.5-turbo, GPT-4o, GPT-5, Llama-3.1-70B, Claude-3.5-haiku, Gemini-3.0-pro, ERNIE-3.5。
*   **对比方法 (Baselines)**：
    *   **PAIR**：基于自动迭代生成的提示词优化方法。
    *   **AgentPoison**：针对知识库的隐蔽投毒攻击。
    *   **BadChain**：注入恶意推理步骤的后门攻击。
*   **评估指标**：ASR-R（检索成功率）、ASR-L（日志分析成功率）、ASR-H（人工专家验证成功率）、ACC/EM/SR（原始任务性能指标）、CR（任务完成率）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：文中提供了 **TCPS（每次成功攻击的时间成本）** 分析。结果显示 JailAgent 的效率极高，例如在 StrategyQA 任务上，其耗时仅为 PAIR 的 15.3% 左右，显著优于其他基准方法。

#### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，涵盖了 3 类智能体架构、7 种主流 LLM 核心、8 个跨领域数据集。
*   **消融实验**：针对触发器提取的指标（$\Delta L$ 和 $KL$）、四种约束损失函数、数据工厂规模、候选答案数量等均进行了消融实验。
*   **客观性**：引入了 **ASR-H（人工审核）**，每条执行日志由至少 5 名专家独立评审并投票，有效弥补了自动化评估可能存在的偏差。整体实验设计客观、公平，覆盖了从攻击效果到系统鲁棒性的多个维度。

#### 6. 主要结论与发现
*   **攻击效能**：JailAgent 在所有测试场景下的越狱成功率（ASR）均显著超过现有方法。
*   **隐蔽性与无损性**：JailAgent 在实施攻击时，智能体完成原始任务的能力（ACC/EM）几乎没有下降，证明了其极高的隐蔽性。
*   **跨模型鲁棒性**：通过约束收紧优化的触发器在不同 LLM 核心之间表现出极强的迁移能力。
*   **防御抵抗力**：在面对 Perplexity Filter 和 RA-LLM 等防御机制时，JailAgent 依然保持了较高的攻击成功率。

#### 7. 优点
*   **范式创新**：跳出了“修改 Prompt”的传统思维，转向操纵内部推理轨迹，更符合智能体系统的攻击面特征。
*   **实时自适应**：Reranker 的实时微调机制使其能够根据当前上下文动态调整攻击策略。
*   **数学约束严谨**：通过四种损失函数对语义空间进行精细化建模，提升了触发器的稳定性和区分度。

#### 8. 不足与局限
*   **白盒/准白盒依赖**：触发器提取阶段需要访问影子模型的 Log-probabilities，在完全黑盒（无概率输出）的 API 环境下可能受限。
*   **计算延迟**：虽然比迭代优化 Prompt 快，但实时微调 Reranker 评分头仍会给大规模实时交互系统带来额外的计算开销。
*   **动态场景挑战**：实验主要基于静态 Benchmark，对于环境高度动态变化（如多智能体实时博弈）的场景，其表现尚待验证。

（完）
