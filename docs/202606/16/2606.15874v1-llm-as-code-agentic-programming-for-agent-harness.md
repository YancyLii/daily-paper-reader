---
title: LLM-as-Code Agentic Programming for Agent Harness
title_zh: 面向 Agent Harness 的 LLM-as-Code 智能体编程
authors: "Junjia Qi, Zichuan Fu, Jingtong Gao, Wenlin Zhang, Hanyu Yan, Xian Wu, Xiangyu Zhao"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15874v1"
tags: ["query:agsec"]
score: 9.0
evidence: 提出token爆炸和控制流幻觉源于让LLM作为调度器，通过Agentic编程控制流程
tldr: 将LLM作为编排器会导致令牌爆炸、控制流幻觉等架构性缺陷。Agentic Programming让程序主导控制流，LLM作为可调用代码组件，仅在推理生成时激活。调用树构建DAG上下文，每个调用的上下文长度由调用深度决定。计算机使用代理的案例表明，该方法大幅增强了长期视觉操作序列的稳定性，为构建可靠LLM代理提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15874v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15874v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1561, \"height\": 445, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15874v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15874v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 634, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15874v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 891, \"height\": 558, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15874v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 288, \"label\": \"Table\"}]"
motivation: LLM代理以模型为编排器导致控制幻觉和上下文膨胀等架构缺陷。
method: 提出Agentic Programming，将控制流交还程序，LLM作为可调用代码，上下文以调用树DAG组织，长度由深度决定。
result: 计算机使用代理案例表明该方法大幅提升了长期视觉操作序列的稳定性。
conclusion: Agentic Programming通过程序控制流解决了LLM代理的根本可靠性问题，为构建稳定代理系统提供了有效范式。
---

## 摘要
每个主流 LLM 智能体框架都将 LLM 赋予编排者的角色；模型决定下一步做什么、何时调用工具以及何时停止。我们认为，令牌爆炸、控制流幻觉和不可靠的完成并非实现缺陷，而是将循环、分支和顺序执行等确定性工作分配给概率系统所导致的架构性后果。更好的提示或更强的模型无法保证 LLM 智能体的可靠性。因此，我们提出智能体编程（Agentic Programming），其中程序控制所有控制流，而 LLM 本身作为程序的一部分，是一个自适应组件，我们称之为 LLM-as-Code，并仅在任务需要推理或生成时才调用。在每次调用中，模型保持完全的灵活性，但无法改变程序的执行路径。通过程序控制，LLM 的上下文由执行历史的调用树构建，并形成有向无环图（DAG）。每次调用的上下文长度由其调用深度决定，而不是由步骤累积决定。一项关于计算机使用智能体的案例研究表明，该设计是实用的，而不仅仅是理论立场，显著提高了长视觉操作序列的稳定性。

## Abstract
Every major LLM agent framework gives the LLM the role of orchestrator; the model decides what to do next, when to call tools, and when to stop. We argue that token explosion, control-flow hallucination, and unreliable completion are not implementation bugs but architectural consequences of assigning the deterministic work of looping, branching, and sequencing to a probabilistic system. A better prompt or a stronger model cannot guarantee the reliability of the LLM agent. We therefore propose Agentic Programming, in which the program governs all control flow, and the LLM is itself part of it, an adaptive component we call LLM-as-Code and invoke only where a task calls for reasoning or generation. Within each call the model keeps full flexibility, but it cannot alter the program's execution path. With control in the program, the LLM's context is built from the execution history's call tree and forms a directed acyclic graph (DAG). Each call's context length is then determined by its call depth rather than by accumulation over steps. A case study of computer-use agents shows that the design is practical, not just a theoretical stance, substantially improving the stability of long visual operation sequences.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心批评对象**：当前主流 LLM 智能体框架（如 ReAct、AutoGen、OpenHands、MetaGPT）普遍将 LLM 置于“编排者”角色，由模型决定下一步工具调用、控制流分支与终止时机。
- **根本缺陷诊断**：论文认为，令牌爆炸、控制流幻觉、不可靠的完成并非工程实现 bug，而是架构层面的“类别错误”：将确定性工作（循环、分支、排序、变量绑定）分配给概率系统，必然导致失败复合化。
- **动机与主张**：更好的提示或更强的模型无法从根源上保证可靠性。因此，应当将控制流从模型中剥离，交由程序负责，LLM 仅作为被调用的自适应推理组件。

## 2. 论文提出的方法论：核心思想与关键技术细节

- **核心理念——Agentic Programming（智能体编程）**：程序拥有全部控制流，LLM 化身“LLM-as-Code”，作为程序的一部分，仅在需要推理或生成的步骤被调用。模型在每次调用内保持完全灵活性，但无法改变程序的执行路径。
- **四大支柱**：
  - **代码驱动的工作流**：确定性步骤（循环、分支、排序）由编译型代码强制执行，LLM 调用只在需要推理的叶子节点发生。柔性通过递归调用展开，可靠性由每一层的代码保证。
  - **DAG 结构的上下文**：执行历史不再是平铺对话日志，而是函数调用与推理节点的有向无环图（DAG）。每个调用的上下文长度由其调用深度决定（`*O*(depth)`），已返回的子树折叠为摘要，避免了 `*O*(steps)` 的线性膨胀与长程衰减。
  - **多智能体协作**：每路智能体即一个普通函数，可并行执行，结果通过调用图汇合，协作无需引入额外的编排模型，与现有 DAG 上下文无缝继承。
  - **自我编程进化**：产生新技能或改进现有技能的 LLM 调用结果，一旦通过测试即被固化为持久代码，成为确定性执行的一部分，而非每次运行时再被采样读取的文本。
- **技术实现要点**：LLM 调用点通过 Python 装饰器封装，签名和文档字符串驱动提示模板，普通函数的返回与并发展开都是原生编程语言语义。

## 3. 实验设计：数据集 / 场景、基准与方法对比

- **主要实验场景**：计算机使用（GUI 自动化）与自主研究管道两个生产级 agent，背后共用同一套 Agentic Programming 架构。
- **基准数据集**：OSWorld 基准测试，评估开放任务下的多模态计算机操作能力。
- **对比方法**（OSWorld leaderboard 公开数据）：
  - 智能体框架：Agent S3 w/ Opus 4.5、Agent S3 w/ GPT-5、OpenAPA w/ gemini-3.1-pro 等。
  - 通用模型：Claude Sonnet 4.6、Kimi K2.6 等。
  - 专业模型：Holo3-35B-A3B。
- **实验配置**：
  - 基线方法通常分配 100 步预算。
  - 本方法在 **15 步** 内完成，控制流由代码掌握，避免步数浪费。
- **附录 E 中的分领域结果**：涵盖 Chrome、多应用混合（Multi-Apps）、OS 综合等子项。

## 4. 资源与算力

- 论文中未明确提供 GPU 型号、数量、训练时长等算力资源细节。
- 文中提及“参考实现”（附录 C）以及两个基于该范式的生产级 agent，但实验评价侧重于成功率与步数，不涉及大规模模型训练或微调。
- 算力消耗主要集中在推理调用（LLM 推理），而非训练。

## 5. 实验数量与充分性

- **主要实验结果**：集中在 OSWorld 基准的整体成功率、分领域成功率和所需步骤数量。
- **对比对象**：覆盖主流 agent 框架、通用模型和专用模型，以公开 Leaderboard 数据进行横向对比。
- **消融实验**：论文未提供系统的消融研究（如分别移除四项设计支柱的效果对比）。
- **充分性与公平性**：
  - 作为概念验证，在真实计算机使用场景中取得了领先结果，且步骤预算远低于对手，具有一定说服力。
  - 但实验只围绕一个基准展开，未在更多元的长程任务（例如软件工程、代码审查、多步规划等）上评估，结论的普适性尚待检验。
  - 对比条件中，步骤预算和底层模型等不完全对齐（本方法 15 步 vs 对手 100 步），论文强调这正是控制流优化带来的优势，但仍属于“体系性差异”而非单一变量对比。

## 6. 论文的主要结论与发现

- **架构归因**：LLM 智能体的长期稳定性失败是确定性控制被概率化采样的必然结果，模型能力提升不能消除复合失败的风险。
- **范式替代有效**：Agentic Programming 通过代码掌握流程，将 LLM 降级为自适应调用组件，从根本上消除了控制流幻觉与上下文膨胀。
- **DAG 上下文管理的价值**：调用图的历史表达方式使上下文长度与调用深度绑定，解决了令牌爆炸和长程记忆衰减。
- **智能体协作的简并**：多智能体协作自然地成为函数并发调用，不再需要复杂的多模型编排逻辑。
- **实际性能**：在 OSWorld 基准上，仅用 15 步就达到 86.8% 的成功率，超越所有需要 100 步的竞争方法。

## 7. 优点：方法或实验设计上的亮点

- **根本性解决思路**：不依赖提示工程或更强的模型，而是通过架构转换根除控制流幻觉与上下文膨胀，思路新颖且逻辑自洽。
- **调用图（DAG）上下文**：将执行历史结构化，实现了上下文按需组装、子树压缩释放，同时保留完整图用于复盘调试，比平摊式日志有质的提升。
- **零额外开销的并发**：多智能体协作直接复用编程语言的并发模型，无需引入新的协调协议或共享记忆模型。
- **进化成果固化成代码**：改进一旦通过测试即成为确定性代码，区别于传统反射机制中依赖模型记忆文本的方式，保证了经验的有效复用。
- **与现有代码工程无缝结合**：LLM 调用封装为普通函数，可单元测试，故障定位到具体函数返回值，继承了传统软件工程的可靠性实践。

## 8. 不足与局限

- **实验广度有限**：仅聚焦于计算机使用（OSWorld）一个基准，对软件工程、代码生成、长程工具使用等通用 agent 场景缺少验证。
- **缺乏消融与控制变量实验**：虽然总体优势明显，但无法量化四项设计支柱各自贡献，也未排除底层模型和步数预算对结果的影响。
- **对无结构化任务适用性存疑**：论文承认完全开放式探索（如无明确阶段模型的头脑风暴）可能仍适合 LLM 编排，Agentic Programming 的边界有待明确。
- **实现细节公开不足**：未完整开源的参考实现、运行时骨架等使外部难以复现和扩展，评估也缺少关于错误类型分布、资源消耗等精细化数据。
- **泛化风险**：成功案例建立在“工作流具有已知结构”的前提上，若任务结构本身高度不确定或频繁变化，代码维护成本可能上升，灵活性不及模型编排。

（完）
