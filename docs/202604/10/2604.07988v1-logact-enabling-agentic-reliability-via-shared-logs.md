---
title: "LogAct: Enabling Agentic Reliability via Shared Logs"
title_zh: LogAct：通过共享日志实现智能体可靠性
authors: "Mahesh Balakrishnan, Ashwin Bharambe, Davide Testuggine, David Geraghty, David Mao, Vidhya Venkat, Ilya Mironov, Rithesh Baradi, Gayathri Aiyer, Victoria Dudin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07988v1"
tags: ["query:agsec"]
score: 9.0
evidence: 在共享日志中通过可插拔投票器在执行前停止 Agent 动作
tldr: "LogAct 是一种旨在提升大模型智能体生产环境可靠性的新框架。它将智能体抽象为基于共享日志的状态机，使动作在执行前可见并可被拦截，从而实现一致性故障恢复。通过智能体自省，LogAct 支持语义化的健康检查和性能优化。实验表明，该方法能高效恢复故障、优化 Token 使用，并在仅损失 3% 性能的情况下拦截所有违规动作。"
selection_source: fresh_fetch
motivation: 针对大模型智能体在异步和故障频发的生产环境中难以保证执行可靠性及安全性的挑战。
method: 提出 LogAct 抽象架构，将智能体解构为操作共享日志的状态机，引入可插拔投票机制拦截动作并支持基于日志的一致性恢复。
result: 实验证明 LogAct 能实现高效故障恢复和自我调试，在显著降低违规行为的同时保持了极高的任务成功率。
conclusion: LogAct 通过共享日志和自省机制，为构建可控、可靠且高效的智能体系统提供了一种有效的架构方案。
---

## 摘要
智能体（Agents）是由大语言模型（LLM）驱动的组件，能够以强大且任意的方式改变环境。由于异步和故障，在生产环境中确保智能体执行的可靠性具有挑战性。在本文中，我们提出了一种名为 LogAct 的新抽象，其中每个智能体都是一个运行在共享日志上的解构状态机。在 LogAct 中，智能体动作在执行前在共享日志中可见；可以通过可插拔、解耦的投票器在执行前拦截；并在智能体或环境发生故障时实现一致性恢复。LogAct 实现了智能体内省（introspection），允许智能体利用 LLM 推理分析自身的执行历史，进而实现恢复、健康检查和优化的语义变体。在评估中，LogAct 智能体能够高效且正确地从故障中恢复；调试自身性能；优化集群中的 token 使用；并在代表性基准测试中，仅以 3% 的良性效用下降为代价，拦截了目标模型的所有非预期动作。

## Abstract
Agents are LLM-driven components that can mutate environments in powerful, arbitrary ways. Extracting guarantees for the execution of agents in production environments can be challenging due to asynchrony and failures. In this paper, we propose a new abstraction called LogAct, where each agent is a deconstructed state machine playing a shared log. In LogAct, agentic actions are visible in the shared log before they are executed; can be stopped prior to execution by pluggable, decoupled voters; and recovered consistently in the case of agent or environment failure. LogAct enables agentic introspection, allowing the agent to analyze its own execution history using LLM inference, which in turn enables semantic variants of recovery, health check, and optimization. In our evaluation, LogAct agents recover efficiently and correctly from failures; debug their own performance; optimize token usage in swarms; and stop all unwanted actions for a target model on a representative benchmark with just a 3% drop in benign utility.

---

## 论文详细总结（自动生成）

这篇论文由 Meta 公司的研究团队撰写，提出了一种名为 **LogAct** 的新架构，旨在解决大模型智能体（Agents）在生产环境中面临的安全性、可靠性和可审计性挑战。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：智能体（如 CodeAct）通过“观察-推理-动作”循环操作环境，具有极强的能力（如执行任意代码）。但在生产环境中，它们面临三大难题：
    *   **安全性**：难以阻止智能体执行破坏性动作（如删除生产数据库）。
    *   **故障恢复**：智能体或环境崩溃时，系统可能处于中间状态，传统的检查点（Checkpoint）或预写日志（WAL）难以处理非结构化的任意代码动作。
    *   **可审计性**：事后难以追踪智能体动作的意图和具体执行过程。
*   **核心目标**：通过借鉴分布式系统中的“共享日志（Shared Log）”概念，为智能体提供一种强保证的执行框架。

### 2. 论文提出的方法论
*   **核心思想**：将智能体重新定义为**在共享日志上运行的状态机**。智能体的所有意图、投票、决策和结果都必须先持久化到日志（称为 **AgentBus**）中。
*   **关键技术细节**：
    *   **解构状态机**：将智能体逻辑拆分为四个解耦组件：
        1.  **Driver（驱动器）**：负责 LLM 推理，生成“意图（Intention）”。
        2.  **Voter（投票器）**：可插拔组件（基于规则或 LLM），审查意图的安全性。
        3.  **Decider（决策器）**：根据投票结果决定是否提交（Commit）或中止（Abort）。
        4.  **Executor（执行器）**：在沙箱中执行已提交的动作。
    *   **AgentBus（共享日志）**：一个强类型的、追加式的持久化日志，支持按类型访问控制。
    *   **智能体内省（Agentic Introspection）**：智能体可以读取自己的执行历史日志，利用 LLM 进行“语义化恢复”。例如，崩溃后通过分析日志确定哪些步骤已完成，并生成补偿代码。
*   **算法流程**：推理 -> 投票 -> 决策 -> 执行。每一步的状态转换都由日志条目驱动。

### 3. 实验设计
*   **数据集与场景**：
    *   **AgentDojo 基準测试**：用于评估针对提示注入（Prompt Injection）攻击的防御能力和良性任务的成功率（Utility）。
    *   **文件校验和任务（Checksum Task）**：模拟长耗时任务中的崩溃恢复和性能优化。
    *   **代码类型标注集群（Swarm）**：评估多智能体协作效率。
*   **对比方法**：
    *   对比了 FrontierModel（顶级模型）和 TargetModel（旧版模型）在无防御、仅规则投票、以及双重投票（规则+LLM）模式下的表现。
    *   对比了不同后端（内存、SQLite、分布式数据库 AnonDB）的性能开销。

### 4. 资源与算力
*   **硬件环境**：实验在 x86 Linux 服务器上运行。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于系统架构而非模型训练，它主要调用了远程推理接口（如 FrontierModel 和 TargetModel）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 AgentDojo 上进行了完整的安全防御实验，涵盖了攻击成功率（ASR）和良性效用的权衡。
    *   展示了故障恢复的端到端案例，包括语义化修复将执行速度提升 290 倍的实例。
    *   进行了 6 个智能体组成的集群实验，验证了 Token 节省和效率提升。
*   **充分性评价**：实验设计较为全面，涵盖了安全、可靠性、性能开销和多智能体协作四个维度。对比实验客观地展示了防御机制带来的延迟和 Token 开销。

### 6. 论文的主要结论与发现
*   **安全性**：通过“规则+LLM”的双重投票机制，LogAct 成功拦截了目标模型在 AgentDojo 中的所有违规动作，而良性任务成功率仅下降 3%。
*   **故障恢复**：利用“内省”机制，智能体能自动诊断崩溃原因并优化低效代码，实现比盲目重试更高效的语义恢复。
*   **效率提升**：在智能体集群中，通过一名“主管智能体”内省所有成员的日志，可减少 41% 的 Token 消耗并提升 17% 的工作量。
*   **低开销**：日志记录的延迟远小于 LLM 推理延迟，在生产环境中具有可行性。

### 7. 优点
*   **架构创新**：首次将分布式系统中的共享日志与 LLM 智能体深度结合，解决了智能体“黑盒执行”的问题。
*   **解耦设计**：投票器和执行器的分离使得安全策略可以动态插拔和升级，无需停机。
*   **语义恢复**：超越了传统的字节级恢复，利用 LLM 的推理能力处理复杂的环境一致性问题。

### 8. 不足与局限
*   **推理依赖**：内省和语义恢复高度依赖 LLM 的推理能力，如果模型本身发生严重幻觉，恢复过程可能失效。
*   **延迟增加**：虽然日志开销小，但引入额外的投票步骤（尤其是 LLM 投票）会显著增加端到端响应时间（实验显示增加约 82%）。
*   **多智能体复杂性**：论文主要关注单智能体可靠性，虽然提及了集群优化，但尚未深入探讨多智能体并发冲突和复杂的分布式事务处理。
*   **应用限制**：对于极高性能要求的实时场景，这种多阶段确认的架构可能过于沉重。

（完）
