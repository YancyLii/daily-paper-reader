---
title: "Distributed Predictive Control Barrier Functions: Towards Scalable Safety Certification in Modular Multi-Agent Systems"
title_zh: 分布式预测控制屏障函数：迈向模块化多智能体系统中的可扩展安全认证
authors: "Jonas Ohnemus, Alexandre Didier, Ahmed Aboudonia, Andrea Carron, Melanie N. Zeilinger"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29560v1"
tags: ["query:agsec"]
score: 8.0
evidence: 多智能体系统的安全认证与控制屏障函数
tldr: 针对具有动态拓扑的多智能体系统，本文提出了一种分布式预测控制屏障函数（D-PCBF）框架。该方法结合了结构化控制屏障函数（s-CBF）与预测优化，为分布式控制提供正式的安全保障。其核心贡献在于支持智能体的即插即用，并能在网络拓扑变化导致暂时不安全时确保安全恢复，通过赛车车队实验验证了其可扩展性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的分布式学习控制在面对不可预见扰动和动态网络拓扑变化时，缺乏形式化的安全保障。
method: 提出结构化控制屏障函数（s-CBF）并将其集成到分布式预测控制框架中，利用模型预测实现可恢复的安全控制。
result: 成功开发出一种支持智能体灵活加入或退出的即插即用协议，并在微型赛车车队实验中验证了其安全恢复能力。
conclusion: 该研究为模块化多智能体系统提供了一种可扩展且具备形式化安全认证的分布式控制方案。
---

## 摘要
我们考虑具有分布式控制架构且网络拓扑可能发生变化的安全关键型多智能体系统。虽然基于学习的分布式控制能够实现可扩展性和高性能，但在面对不可预见的扰动和不安全的网络拓扑变化时，缺乏正式的安全保证可能会导致系统故障。为了应对这一挑战，我们引入了结构化控制屏障函数 (s-CBFs) 作为一种多智能体安全框架。s-CBFs 被增强为分布式预测控制屏障函数 (D-PCBF)，这是一种基于预测和优化的安全层，利用模型预测来确保在任何时候都具有可恢复的安全性。所提出的方法实现了一种宽松但正式的即插即用协议，允许智能体加入或离开网络，同时在网络拓扑变化需要临时不安全行为时确保安全恢复。我们通过小型赛车车队的仿真和实时实验验证了该方案。

## Abstract
We consider safety-critical multi-agent systems with distributed control architectures and potentially varying network topologies. While learning-based distributed control enables scalability and high performance, a lack of formal safety guarantees in the face of unforeseen disturbances and unsafe network topology changes may lead to system failure. To address this challenge, we introduce structured control barrier functions (s-CBFs) as a multi-agent safety framework. The s-CBFs are augmented to a distributed predictive control barrier function (D-PCBF), a predictive, optimization-based safety layer that uses model predictions to guarantee recoverable safety at all times. The proposed approach enables a permissive yet formal plug-and-play protocol, allowing agents to join or leave the network while ensuring safety recovery if a change in network topology requires temporarily unsafe behavior. We validate the formulation through simulations and real-time experiments of a miniature race-car platoon.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**分布式预测控制屏障函数（D-PCBF）**的框架，旨在解决模块化多智能体系统在动态环境和拓扑变化下的安全保障问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着自动驾驶车队、无人机群等大规模多智能体系统的发展，传统的集中式控制因计算量巨大而难以扩展。虽然基于学习的分布式控制（如多智能体强化学习）性能优异，但缺乏形式化的安全保证。
*   **核心问题**：现有的安全过滤技术（如控制屏障函数 CBF）在面对不可预见的扰动或网络拓扑变化（如智能体加入/退出）时，往往会导致优化问题无解（不可行性），从而引发系统崩溃。
*   **研究动机**：开发一种既能保证分布式扩展性，又能在系统暂时进入不安全状态后实现“安全恢复”（Recoverable Safety）的机制，并支持“即插即用”的模块化操作。

### 2. 方法论
该论文提出了一套结合控制理论与分布式优化的综合方案：
*   **结构化控制屏障函数 (s-CBFs)**：这是核心创新点。不同于传统的局部 CBF 要求每个智能体严格保持安全，s-CBF 允许单个智能体在必要时暂时违反安全约束（通过松弛项 $\beta$），只要整个网络的全局安全函数保持下降趋势。这允许智能体之间为了全局安全进行“协作式牺牲”。
*   **分布式预测控制屏障函数 (D-PCBF)**：将 s-CBF 嵌入到预测优化框架中。通过在预测时域内引入松弛变量（Slacks），D-PCBF 实际上扩大了安全集的范围。即使初始状态不安全，优化算法也能引导系统收敛回安全集。
*   **即插即用 (PnP) 协议**：设计了一套基于证书的准入机制。当新智能体请求加入或现有智能体离开时，系统会检查“违规证书”（确保临时违规在容许范围内）和“恢复证书”（确保系统能回到安全状态），从而决定是否接受拓扑更改。
*   **分布式合成**：针对线性系统，利用线性矩阵不等式（LMI）和交替方向乘子法（ADMM）实现了 s-CBF 参数的分布式离线合成。

### 3. 实验设计
*   **实验场景**：车辆编队控制（Platooning），关注纵向动力学（距离和速度控制）。
*   **对比基准 (Benchmark)**：
    *   **集中式求解器**：对比分布式算法与集中式求解（如 MOSEK）在计算时间上的差异。
    *   **传统安全过滤器**：对比在极端情况下（如高速切入），传统方法因不可行性导致失败，而 D-PCBF 如何实现安全恢复。
*   **实验阶段**：
    1.  **仿真验证**：测试了从 5 个到 40 个智能体的扩展性，验证计算时间随智能体数量的变化。
    2.  **硬件实验**：使用 ETH Zurich 的 Chronos/CRS 微型赛车平台，在真实物理环境下测试高速加入（Join）和退出（Leave）操作。

### 4. 资源与算力
*   **软件工具**：使用了 MOSEK（集中式求解）、HPIPM（硬件实时求解）以及基于 Python 的自定义 ADMM 分布式求解器。
*   **算力说明**：文中**未明确提及具体的 GPU 型号或训练时长**。由于该方法主要基于模型预测控制（MPC）和凸优化，其核心计算压力在于 CPU 上的实时优化求解，而非深度学习的大规模并行训练。硬件实验在 100Hz 的频率下运行，显示了极高的实时性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真部分涵盖了多达 40 个智能体的场景，足以证明其**可扩展性**。
    *   硬件实验展示了多次“即插即用”循环，包括高速接近、切入、退出及再次切入。
*   **充分性评价**：实验设计较为充分。不仅有理论证明，还有从数值仿真到物理硬件的完整链路验证。特别是硬件实验中的“端点预警”场景（End-of-queue scenario），真实模拟了交通管理中的极端危险情况，增强了说服力。

### 6. 主要结论与发现
*   **可扩展性**：分布式 D-PCBF 的理想并行运行时间几乎不随智能体数量增加而增长，远优于集中式方法。
*   **安全恢复能力**：在初始距离过近（不安全）或执行高速 PnP 操作时，D-PCBF 能够通过临时违反约束并最终引导车辆回到安全间距，避免了碰撞。
*   **模块化支持**：提出的 PnP 协议能够在线认证拓扑变化，无需系统停机或预先调整到特定稳态。

### 7. 优点
*   **理论与实践结合**：不仅提供了严谨的数学证明（保证前向不变性和收敛性），还通过真实赛车实验验证了鲁棒性。
*   **灵活性**：s-CBF 的松弛机制解决了分布式系统中常见的因耦合导致的优化不可行问题。
*   **即插即用**：为动态变化的多智能体网络提供了一种形式化的安全准入标准。

### 8. 不足与局限
*   **系统模型限制**：s-CBF 的自动合成目前主要针对**线性系统**。对于高度非线性的动力学，合成过程可能非常复杂或具有保守性。
*   **通信依赖**：虽然是分布式，但 PnP 协议中的“恢复证书”仍需要一定程度的全局信息交换（如全局求和），在极端通信延迟或丢包环境下的表现未被深入探讨。
*   **扰动处理**：虽然提到了鲁棒性，但实验主要假设模型已知。在存在剧烈外部扰动或模型失配时，安全边界的收缩可能导致性能下降。

（完）
