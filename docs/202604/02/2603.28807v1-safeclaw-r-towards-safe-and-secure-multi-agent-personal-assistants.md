---
title: "SafeClaw-R: Towards Safe and Secure Multi-Agent Personal Assistants"
title_zh: SafeClaw-R：迈向安全可靠的多智能体个人助手
authors: "Haoyu Wang, Zibo Xiao, Yedi Zhang, Christopher M. Poskitt, Jun Sun"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.28807v1"
tags: ["query:agsec"]
score: 9.0
evidence: 多智能体系统中安全风险的实时执行保护
tldr: 针对基于大语言模型的多智能体系统（MAS）在处理个人数据时面临的安全风险（如推理错误和提示词注入），本文提出了 SafeClaw-R 框架。该框架通过在执行图上实施系统级安全约束，在动作执行前进行中介处理，并系统性地增强技能安全性。实验表明，SafeClaw-R 在 Google Workspace、第三方技能和代码执行等场景中表现优异，显著提升了自主 MAS 的运行时安全性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统框架在处理跨平台任务时存在严重的安全性与隐私泄露风险，且现有的防护手段缺乏实时强制执行能力。
method: 提出 SafeClaw-R 框架，通过在执行图中引入系统级安全不变式，在动作执行前进行中介拦截，并对技能进行安全性增强。
result: "在 Google Workspace 场景中达到 95.2% 的准确率，并能检测出 97.8% 的恶意第三方技能模式以及 100% 的对抗性代码执行。"
conclusion: SafeClaw-R 为自主多智能体系统提供了一种实用的运行时安全增强方案，有效平衡了自动化效率与系统安全性。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）正通过自主执行复杂的跨平台任务来改变个人生产力。诸如 OpenClaw 之类的框架展示了与个人数据和服务集成的本地部署智能体的潜力，但这种自主性引入了重大的安全（safety）和保障（security）风险。LLM 推理失败导致的意外操作可能造成不可逆的损害，而提示词注入攻击可能会窃取凭据或破坏系统。我们的分析显示，OpenClaw 内置技能中有 36.4% 构成了高风险或关键风险。现有的方法，包括静态护栏（static guardrails）和 LLM-as-a-Judge，在 MAS 环境中缺乏可靠的实时执行能力和一致的权威性。为了解决这一问题，我们提出了 SafeClaw-R，这是一个通过确保在执行前对操作进行调解，从而将安全性作为执行图上的系统级不变性进行强制执行的框架，并系统地使用安全副本增强技能。我们在三个代表性领域评估了 SafeClaw-R：生产力平台、第三方技能生态系统和代码执行环境。SafeClaw-R 在 Google Workspace 场景中实现了 95.2% 的准确率，显著优于正则表达式基准（61.6%），检测到了 97.8% 的恶意第三方技能模式，并在我们的对抗性代码执行基准测试中实现了 100% 的检测准确率。这些结果表明，SafeClaw-R 为自主 MAS 实现了实用的运行时强制执行。

## Abstract
LLM-based multi-agent systems (MASs) are transforming personal productivity by autonomously executing complex, cross-platform tasks. Frameworks such as OpenClaw demonstrate the potential of locally deployed agents integrated with personal data and services, but this autonomy introduces significant safety and security risks. Unintended actions from LLM reasoning failures can cause irreversible harm, while prompt injection attacks may exfiltrate credentials or compromise the system. Our analysis shows that 36.4% of OpenClaw's built-in skills pose high or critical risks. Existing approaches, including static guardrails and LLM-as-a-Judge, lack reliable real-time enforcement and consistent authority in MAS settings. To address this, we propose SafeClaw-R, a framework that enforces safety as a system-level invariant over the execution graph by ensuring that actions are mediated prior to execution, and systematically augments skills with safe counterparts. We evaluate SafeClaw-R across three representative domains: productivity platforms, third-party skill ecosystems, and code execution environments. SafeClaw-R achieves 95.2% accuracy in Google Workspace scenarios, significantly outperforming regex baselines (61.6%), detects 97.8% of malicious third-party skill patterns, and achieves 100% detection accuracy in our adversarial code execution benchmark. These results demonstrate that SafeClaw-R enables practical runtime enforcement for autonomous MASs.

---

## 论文详细总结（自动生成）

### SafeClaw-R：迈向安全可靠的多智能体个人助手 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着基于大语言模型（LLM）的多智能体系统（MAS）如 OpenClaw 的兴起，智能体已能自主处理跨平台的复杂任务（如管理邮件、日程和文档）。然而，这种高度自主性带来了严重的**安全（Safety）与保障（Security）风险**：
*   **推理失败：** LLM 的幻觉或逻辑错误可能导致不可逆的破坏性操作（如误删整个收件箱）。
*   **恶意攻击：** 提示词注入攻击可能导致凭据泄露或系统受损。
*   **现有手段不足：** 静态护栏（Static Guardrails）缺乏灵活性，而“LLM-as-a-Judge”通常是事后评估，无法实现实时的运行时强制执行。
*   **风险现状：** 研究发现 OpenClaw 内置技能中有 36.4% 属于高危或关键风险级别。

#### 2. 方法论：核心思想与技术细节
论文提出了 **SafeClaw-R** 框架，其核心思想是将安全性作为执行图（Execution Graph）上的**系统级不变性（System-level Invariant）**。
*   **图不变性（Graph Invariant）：** 在多智能体执行图中，规定每一个功能节点（Functional Node）必须前置一个对应的**强制执行节点（Enforcement Node）**。只有经过强制执行节点的验证，功能节点才能运行。
*   **节点抽象（Trigger, Task, Resources）：**
    *   **Trigger（触发器）：** 定义节点激活的条件（如检测到 PII 敏感信息）。
    *   **Task（任务）：** 具体的安全逻辑（如评估风险并决定允许、拦截或要求用户确认）。
    *   **Resources（资源）：** 节点可调用的操作接口（如 `stop` 停止执行、`user_confirm` 请求确认、`log_event` 记录日志）。
*   **安全技能工厂（Safe Skill Factory）：** 一个自动化的六阶段流水线（威胁建模 -> 规范编写 -> 测试生成 -> 基准评估 -> 根因分析 -> 迭代优化），用于为现有技能生成对应的“安全副本”。
*   **SafeSkillHub：** 建立社区驱动的生态系统，共享和持续改进经过验证的安全规范。

#### 3. 实验设计
研究在三个代表性领域进行了评估：
*   **Google Workspace：** 涵盖 Gmail、日历、云端硬盘等 20 个风险场景（如 OAuth 令牌窃取、批量文件删除）。
*   **第三方技能：** 使用 *MaliciousAgentSkills* 数据集，包含 49 个带有 632 个漏洞模式的恶意技能。
*   **原生代码执行：** 构建了包含 20 个种子脚本（良性与恶意各半）的基准，并通过 9 种语义保持变异算子生成了 2020 个测试用例。
*   **对比基准（Baseline）：** 主要对比了基于正则表达式（Regex）的传统过滤方法。

#### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析：** SafeClaw-R 侧重于运行时框架的逻辑构建和推理阶段的策略执行，而非从头训练大模型。其核心开销在于推理时的额外 LLM 调用。

#### 5. 实验数量与充分性
*   **实验规模：** 
    *   Google Workspace 场景测试了 2000 个案例。
    *   代码执行场景测试了 2020 个案例。
    *   第三方技能涵盖了 13 种主要的威胁模式。
*   **充分性评价：** 实验设计较为充分且具有针对性。通过引入自然语言变体、对抗性边缘案例和语义保持变异，验证了系统在面对模糊输入和复杂攻击时的鲁棒性。实验不仅关注检出率，还评估了误报率（Usability），体现了客观性。

#### 6. 主要结论与发现
*   **高效性：** SafeClaw-R 在 Google Workspace 场景中达到 95.2% 的准确率，远超 Regex 基准（61.6%）。
*   **强鲁棒性：** 在代码执行检测中达到 100% 的准确率，且不受代码混淆或语义保持变异的影响。
*   **安全性：** 成功检测到 97.8% 的恶意第三方技能模式。
*   **可用性平衡：** 在 Google Workspace 中误报率仅为 3.4%，在代码执行中为 0 误报，证明了安全与效率的平衡。

#### 7. 优点（亮点）
*   **系统级强制执行：** 改变了以往“靠提示词约束”的软性防御，通过图结构强制介入执行流。
*   **自动化闭环：** 提出的“安全技能工厂”能够自动化生成安全规范，降低了人工维护成本。
*   **语义理解能力：** 相比正则匹配，能更好地处理自然语言中的社会工程学攻击和语境歧义。
*   **社区化愿景：** 通过 SafeSkillHub 推动安全标准的标准化和共享。

#### 8. 不足与局限
*   **决策模糊性：** 在某些场景（如“邮件轰炸”）中，拦截（Block）与人工审查（Review）的界限仍存在歧义，导致部分准确率下降。
*   **潜在延迟：** 增加强制执行节点意味着每个动作前都有额外的 LLM 推理开销，论文未深入探讨其对实时交互延迟的具体影响。
*   **语义逃逸风险：** 虽然在现有变异下表现良好，但更高级的语义逃逸技术（如动态属性访问、多文件协同攻击）仍可能构成威胁。
*   **数据集偏差：** 第三方技能评估缺乏良性技能库的对比，无法全面衡量该领域的误报风险。

（完）
