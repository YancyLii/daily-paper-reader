---
title: "Temporal UI State Inconsistency in Desktop GUI Agents: Formalizing and Defending Against TOCTOU Attacks on Computer-Use Agents"
title_zh: 桌面 GUI 智能体中的时序 UI 状态不一致性：针对计算机使用智能体的 TOCTOU 攻击的形式化与防御
authors: Wenpeng Xu
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18860v1"
tags: ["query:agsec"]
score: 7.5
evidence: 通过状态验证防御针对GUI智能体的TOCTOU攻击
tldr: 本研究揭示了桌面GUI智能体在“观察-动作”循环中存在的时序不一致漏洞，即TOCTOU攻击。由于智能体处理截图到执行动作之间存在平均6.51秒的延迟，攻击者可在此窗口期篡改UI状态。本文形式化定义了视觉原子性违规，并提出了三种攻击原语。为应对此威胁，设计了预执行UI状态验证（PUSV）防御机制，通过多层验证在动作执行前即时核对UI状态，有效拦截了大部分攻击并验证了分层防御的必要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18860v1/fig-001.webp\", \"caption\": \"Figure 5: PUSV architecture: three layers applied sequentially before each action dispatch. Each layer targets a distinct attack surface.\", \"page\": 6, \"index\": 1, \"width\": 416, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.18860v1/fig-002.webp\", \"caption\": \"Table 4: Primitive C no-defense results (45 trials, 3 tasks × 15 trials).\", \"page\": 5, \"index\": 2, \"width\": 481, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.18860v1/fig-003.webp\", \"caption\": \"Table 3: Primitive B no-defense results (Claude 4.6, 45 trials, 3 tasks × 15 trials).\", \"page\": 5, \"index\": 3, \"width\": 481, \"height\": 281}]"
motivation: 针对GUI智能体在截图观察与动作执行之间存在的显著时间延迟，探索并形式化其面临的TOCTOU安全威胁。
method: 提出了预执行UI状态验证（PUSV）防御框架，结合像素级相似度、全局截图差异和窗口快照对比进行三层实时验证。
result: "PUSV在针对通知劫持和窗口焦点操纵的测试中达到了100%的拦截率，且误报率为零，系统开销极低。"
conclusion: 研究证明了分层防御在应对UI状态不一致方面的有效性，同时也指出了针对无视觉特征攻击（如DOM注入）仍需更深层的系统级防御。
---

## 摘要
通过“截图-点击”循环控制桌面计算机的 GUI 智能体引入了一类新型漏洞：观察到动作之间的间隙（在真实 OSWorld 工作负载上平均为 6.51 秒）产生了一个“检查时间-使用时间”（TOCTOU）窗口，在此期间，非特权攻击者可以操纵 UI 状态。我们将此形式化为“视觉原子性违反”（Visual Atomicity Violation），并刻画了三种具体的攻击原语：(A) 通知叠加层劫持，(B) 窗口焦点操纵，以及 (C) Web DOM 注入。原语 B 是 Android 动作重绑定（Action Rebinding）在桌面端最接近的对应物，它在观察时无需任何视觉证据即可实现 100% 的动作重定向成功率。我们提出了执行前 UI 状态验证（PUSV），这是一种轻量级的三层防御机制，在每次动作分发前立即重新验证 UI 状态：包括点击目标处的掩码像素 SSIM（L1）、全局截图差异（L2a）以及 X Window 快照差异（L2b）。PUSV 在 180 次对抗性试验（135 次原语 A + 45 次原语 B）中实现了 100% 的动作拦截率，且误报率为零，开销小于 0.1 秒。针对原语 C（零视觉足迹的 DOM 注入），PUSV 暴露了一个结构性盲点（拦截率约为 0%），这为未来的 OS+DOM 深度防御架构提供了动力。没有任何单一的 PUSV 层能实现全面覆盖；不同的原语需要不同的检测信号，这验证了分层设计的有效性。

## Abstract
GUI agents that control desktop computers via screenshot-and-click loops introduce a new class of vulnerability: the observation-to-action gap (mean 6.51 s on real OSWorld workloads) creates a Time-Of-Check, Time-Of-Use (TOCTOU) window during which an unprivileged attacker can manipulate the UI state. We formalize this as a Visual Atomicity Violation and characterize three concrete attack primitives: (A) Notification Overlay Hijack, (B) Window Focus Manipulation, and (C) Web DOM Injection. Primitive B, the closest desktop analog to Android Action Rebinding, achieves 100% action-redirection success rate with zero visual evidence at the observation time. We propose Pre-execution UI State Verification (PUSV), a lightweight three-layer defense that re-verifies the UI state immediately before each action dispatch: masked pixel SSIM at the click target (L1), global screenshot diff (L2a), and X Window snapshot diff (L2b). PUSV achieves 100% Action Interception Rate across 180 adversarial trials (135 Primitive A + 45 Primitive B) with zero false positives and < 0.1 s overhead. Against Primitive C (zero-visual-footprint DOM injection), PUSV reveals a structural blind spot (~0% AIR), motivating future OS+DOM defense-in-depth architectures. No single PUSV layer alone achieves full coverage; different primitives require different detection signals, validating the layered design.