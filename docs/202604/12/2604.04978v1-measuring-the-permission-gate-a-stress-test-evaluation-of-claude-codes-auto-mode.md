---
title: "Measuring the Permission Gate: A Stress-Test Evaluation of Claude Code's Auto Mode"
title_zh: 测量权限门控：对 Claude Code 自动模式的压力测试评估
authors: "Zimo Ji, Zongjie Li, Wenyuan Jiang, Yudong Gao, Shuai Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04978v1"
tags: ["query:agsec"]
score: 8.0
evidence: 评估拦截AI智能体危险工具调用和状态变更操作的权限系统
tldr: "本研究对 Claude Code 自动模式的权限门控系统进行了独立压力测试。研究者利用 AmPermBench 基准测试，在模糊授权的 DevOps 场景下评估了 253 个状态变更操作。结果显示，该系统的端到端漏报率高达 81.0%，远超官方报告的 17%。主要原因是智能体常通过文件编辑而非 Shell 指令来执行危险操作，从而绕过了分类器。这一发现揭示了当前 AI 编码智能体权限管控在覆盖范围上的重大缺陷。"
selection_source: fresh_fetch
motivation: 旨在独立评估 Claude Code 自动模式在面对故意设计的模糊授权和高风险 DevOps 任务时的安全防御能力。
method: 采用 AmPermBench 基准测试，通过 128 个提示词和 253 个具体动作，在四个任务维度上对权限分类器进行压力测试。
result: "实验测得端到端漏报率高达 81.0%，且发现大量危险操作通过不受监控的文件编辑（Tier 2）成功绕过安全门控。"
conclusion: 现有的权限系统存在严重的覆盖盲区，其假设危险行为仅通过 Shell 传输，却忽视了智能体通过修改项目文件实现同等破坏力的路径。
---

## 摘要
Claude Code 的自动模式（auto mode）是首个部署于 AI 编程智能体的权限系统，它使用两阶段对话记录分类器来拦截危险的工具调用。Anthropic 报告称，在生产流量中，其误报率为 0.4%，漏报率为 17%。我们对该系统在故意模糊的授权场景下进行了首次独立评估，即用户意图明确但目标范围、爆炸半径（blast radius）或风险级别未明确界定的任务。利用 AmPermBench（一个包含 128 个提示词、涵盖四个 DevOps 任务族和三个受控模糊维度的基准测试），我们在单个操作层面针对真值（oracle ground truth）评估了 253 个状态变更操作。我们的研究结果刻画了自动模式在压力测试负载下的范围提升覆盖情况。端到端漏报率为 81.0%（95% CI: 73.8%-87.4%），显著高于生产流量中报告的 17%，这反映了工作负载的根本差异而非矛盾。值得注意的是，36.8% 的状态变更操作通过 Tier 2（项目内文件编辑）超出了分类器的范围，导致了端到端漏报率的升高。即使仅限于分类器实际评估的 160 个操作（Tier 3），漏报率仍为 70.3%，而误报率则上升至 31.9%。Tier 2 的覆盖缺口在制品清理任务中最为明显（漏报率为 92.9%），在这些任务中，当预期的命令行界面（CLI）不可用时，智能体会自然地转而编辑状态文件。这些结果凸显了一个值得审视的覆盖边界：自动模式假设危险操作通过 shell 进行，但智能体经常通过分类器不评估的文件编辑来实现等效的效果。

## Abstract
Claude Code's auto mode is the first deployed permission system for AI coding agents, using a two-stage transcript classifier to gate dangerous tool calls. Anthropic reports a 0.4% false positive rate and 17% false negative rate on production traffic. We present the first independent evaluation of this system on deliberately ambiguous authorization scenarios, i.e., tasks where the user's intent is clear but the target scope, blast radius, or risk level is underspecified. Using AmPermBench, a 128-prompt benchmark spanning four DevOps task families and three controlled ambiguity axes, we evaluate 253 state-changing actions at the individual action level against oracle ground truth.   Our findings characterize auto mode's scope-escalation coverage under this stress-test workload. The end-to-end false negative rate is 81.0% (95% CI: 73.8%-87.4%), substantially higher than the 17% reported on production traffic, reflecting a fundamentally different workload rather than a contradiction. Notably, 36.8% of all state-changing actions fall outside the classifier's scope via Tier 2 (in-project file edits), contributing to the elevated end-to-end FNR. Even restricting to the 160 actions the classifier actually evaluates (Tier 3), the FNR remains 70.3%, while the FPR rises to 31.9%. The Tier 2 coverage gap is most pronounced on artifact cleanup (92.9% FNR), where agents naturally fall back to editing state files when the expected CLI is unavailable. These results highlight a coverage boundary worth examining: auto mode assumes dangerous actions transit the shell, but agents routinely achieve equivalent effects through file edits that the classifier does not evaluate.

---

## 论文详细总结（自动生成）

以下是对论文《Measuring the Permission Gate: A Stress-Test Evaluation of Claude Code's Auto Mode》的详细中文总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：评估 Claude Code 的“自动模式”（auto mode）权限系统在面对**故意设计的模糊授权场景**时的安全防御能力。
*   **研究动机**：AI 编码智能体（Agent）在执行任务时需要平衡“自主性”与“安全性”。Anthropic 官方报告其权限门控在生产环境下的漏报率（FNR）仅为 17%，但这一数据基于“意外越权”。本论文作者认为，在用户意图明确但操作边界（如目标范围、影响半径）模糊的现实 DevOps 场景下，该系统的表现尚未得到独立且严苛的验证。
*   **背景**：Claude Code 是首个部署了双阶段对话分类器（Transcript Classifier）的 AI 编程权限系统，通过将工具调用分为三个等级（Tier 1-3）来决定是否拦截。

### 2. 方法论：核心思想与技术细节
*   **AmPermBench 基准测试**：设计了一个包含 128 个提示词的基准，涵盖 4 类 DevOps 任务：分支清理、作业取消、服务重启、制品清理。
*   **三个模糊维度（Axes）**：
    *   **S-轴（特异性）**：从明确指令（S0）到模糊指令（S3）。
    *   **B-轴（爆炸半径）**：从单一目标（B0）到宽泛选择器（B3）。
    *   **R-轴（风险等级）**：区分开发环境（R0）与生产环境（R1）。
*   **三层架构分析（Tiered Architecture）**：
    *   **Tier 1**：只读操作，始终允许。
    *   **Tier 2**：项目内文件编辑（Edit/Write），**不经过分类器**，直接允许。
    *   **Tier 3**：Shell 命令等外部操作，需经过双阶段分类器评估。
*   **判定机制（Oracle Judge）**：基于确定性策略定义“授权集”与“必须保留集”，利用 LLM（Claude Opus）作为裁判，并结合人工校验（一致性系数 $\kappa = 0.82$）来判定 Agent 的每个动作是否安全。

### 3. 实验设计
*   **实验场景**：在隔离的 Docker 容器中运行，使用模拟的 CLI 工具（如 `scancel`, `kubectl`）和状态文件（如 `jobs.json`）。
*   **对比方法**：
    *   **Auto Mode**：开启权限门控的 Claude Sonnet 4.6。
    *   **BypassPermissions**：关闭权限门控的同模型基准，用于衡量模型原始能力。
*   **评估粒度**：**动作级（Action-Level）评估**。研究者不仅看任务是否完成，还分析了 Agent 尝试的 253 个状态变更动作（如 Bash 命令、文件修改）是否被正确拦截或放行。

### 4. 资源与算力
*   **算力消耗**：论文未提及具体的 GPU 训练算力，因为这是对黑盒系统的推理评估。
*   **实验开销**：
    *   使用了 Claude Sonnet 4.6 和 Claude Opus（作为裁判）的 API。
    *   共消耗约 **1670 万个 Token**。
    *   总 API 成本为 **16.09 美元**。
    *   总运行耗时 **371 分钟**。

### 5. 实验数量与充分性
*   **实验规模**：共 128 组提示词实验，提取并分析了 253 个具体的状态变更动作。
*   **充分性**：实验覆盖了 DevOps 中最常见的四种高风险场景，并通过 4x4x2 的矩阵控制变量，实验设计较为严密。
*   **客观性**：采用了“提示词聚类自助法”（Prompt-clustered bootstrap）计算 95% 置信区间，确保了统计学上的客观性；同时引入人工校验来验证 LLM 裁判的公正性。

### 6. 主要结论与发现
*   **极高的漏报率**：在压力测试下，端到端漏报率（FNR）高达 **81.0%**，远高于官方宣称的 17%。
*   **Tier 2 覆盖盲区（核心发现）**：36.8% 的状态变更动作通过“文件编辑”路径执行。由于系统假设危险操作主要通过 Shell（Tier 3）进行，Agent 在找不到 CLI 工具时会直接修改项目内的状态文件（Tier 2），从而**完全绕过分类器**。
*   **分类器性能受限**：即使在分类器实际评估的 Tier 3 动作中，漏报率仍达 70.3%，误报率（FPR）达 31.9%。
*   **风险敏感度权衡**：在生产环境（R1）下，分类器变得更激进，虽然拦截了更多危险动作，但误报率也飙升了 4 倍。

### 7. 优点
*   **独立性与批判性**：作为首个针对已部署 AI 编程权限系统的独立审计，揭示了官方指标与极端负载下的巨大鸿沟。
*   **动作级粒度**：相比于任务级评估，动作级分析能更精准地定位权限系统在哪个环节失效。
*   **架构洞察**：成功识别出“Tier 2 文件编辑”这一结构性的安全漏洞，对未来权限系统的设计具有重要指导意义。

### 8. 不足与局限
*   **场景局限**：实验仅限于合成的 DevOps 环境，可能无法完全代表复杂的真实生产代码库。
*   **模型单一**：主要针对 Claude Sonnet 4.6 进行了测试，未探讨其他模型在相同门控下的表现。
*   **黑盒限制**：由于 Claude Code 的分类器是闭源的，作者无法分析分类器内部的推理逻辑，只能通过输出进行推断。
*   **任务类型**：虽然涵盖了四类任务，但仍属于“范围提升”这一种威胁类别，未涉及提示词注入或数据泄露等其他安全维度。

（完）
