---
title: "LLM Safety From Within: Detecting Harmful Content with Internal Representations"
title_zh: LLM 的内在安全：利用内部表示检测有害内容
authors: "Difan Jiao, Yilun Liu, Ye Yuan, Zhenwei Tang, Linfeng Du, Haolun Wu, Ashton Anderson"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18519v1"
tags: ["query:agsec"]
score: 9.0
evidence: 利用LLM内部表示检测有害内容
tldr: 针对现有大模型安全防护模型仅依赖顶层表示而忽略内部层丰富安全特征的问题，本文提出SIREN。这是一种轻量级防护模型，通过线性探测识别内部安全神经元，并利用自适应层加权策略构建有害内容检测器。SIREN在多个基准测试中超越了现有SOTA模型，且参数量减少250倍，具备更强的泛化能力、实时检测性能和推理效率。
selection_source: fresh_fetch
motivation: 现有的安全防护模型主要利用模型顶层输出，忽略了LLM内部层中蕴含的丰富安全相关特征。
method: 提出SIREN模型，通过线性探测识别内部安全神经元，并采用自适应层加权策略融合这些特征，在不修改原模型的情况下实现有害性检测。
result: SIREN在多个基准测试上显著优于现有开源防护模型，且可训练参数量仅为后者的1/250，同时支持实时流式检测。
conclusion: 研究证明LLM内部状态是构建高效、高性能有害内容检测器的重要基础，为大模型安全治理提供了新思路。
---

## 摘要
防护模型被广泛用于检测用户提示词和 LLM 回复中的有害内容。然而，最先进的防护模型仅依赖于末层表示，忽略了分布在内部层中丰富的安全相关特征。我们提出了 SIREN，一种利用这些内部特征的轻量级防护模型。通过线性探测识别安全神经元，并利用自适应层加权策略将其组合，SIREN 在不修改底层模型的情况下，从 LLM 内部构建了一个有害性检测器。我们的全面评估表明，SIREN 在多个基准测试中显著优于最先进的开源防护模型，同时使用的可训练参数减少了 250 倍。此外，SIREN 对未见过的基准测试表现出卓越的泛化能力，能够自然地实现实时流式检测，并与生成式防护模型相比显著提高了推理效率。总之，我们的结果强调了 LLM 内部状态是实现实用、高性能有害性检测的一个极具前景的基础。

## Abstract
Guard models are widely used to detect harmful content in user prompts and LLM responses. However, state-of-the-art guard models rely solely on terminal-layer representations and overlook the rich safety-relevant features distributed across internal layers. We present SIREN, a lightweight guard model that harnesses these internal features. By identifying safety neurons via linear probing and combining them through an adaptive layer-weighted strategy, SIREN builds a harmfulness detector from LLM internals without modifying the underlying model. Our comprehensive evaluation shows that SIREN substantially outperforms state-of-the-art open-source guard models across multiple benchmarks while using 250 times fewer trainable parameters. Moreover, SIREN exhibits superior generalization to unseen benchmarks, naturally enables real-time streaming detection, and significantly improves inference efficiency compared to generative guard models. Overall, our results highlight LLM internal states as a promising foundation for practical, high-performance harmfulness detection.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SIREN**（Safeguard with Internal REpresentatioN）的轻量级大语言模型（LLM）安全防护框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 安全防护模型（如 Llama Guard）通常将有害性检测视为一个“生成式分类”任务，仅依赖模型的**末层（Terminal Layer）**输出。
*   **研究动机**：
    *   **特征遗忘**：LLM 的内部层（Internal Layers）实际上编码了丰富的安全相关语义特征，仅用末层表示会忽略这些关键信息。
    *   **效率低下**：生成式防护模型需要自回归解码，推理成本高且延迟大。
    *   **一致性差**：现有的防护模型在不同数据集上的判定标准（Precision-Recall）波动较大。
*   **整体含义**：通过挖掘 LLM 内部的“安全神经元”，可以在不修改原模型参数的情况下，构建一个更高效、更精准、泛化能力更强的插件式安全检测器。

### 2. 方法论
SIREN 采用两阶段框架，无需修改底层 LLM 权重：
*   **第一阶段：安全神经元识别（Safety Neuron Identification）**
    *   **特征提取**：从 LLM 的每一层提取残差流（Residual Streams）或前馈网络（FFN）的激活值。
    *   **线性探测**：在每一层独立训练线性分类器（Linear Probes），并引入 **L1 正则化**。
    *   **神经元筛选**：根据线性探测权重的绝对值大小对神经元进行排序，选择对有害性分类贡献最大的稀疏子集（即“安全神经元”）。
*   **第二阶段：自适应神经元聚合（Adaptive Neuron Aggregation）**
    *   **层权重计算**：根据各层线性探测器在验证集上的 F1 分数，计算该层的自适应权重 $\alpha_l$。
    *   **特征融合**：将各层筛选出的安全神经元按权重加权并拼接，形成跨层的丰富表示。
    *   **分类器训练**：在聚合特征之上训练一个轻量级的多层感知机（MLP）作为最终的有害性判定头。

### 3. 实验设计
*   **基准模型（Backbones）**：为了公平对比，SIREN 使用了与 SOTA 防护模型相同的底座，包括 Qwen3 (0.6B, 4B) 和 Llama3 (1B, 8B)。
*   **对比方法**：LlamaGuard3 (1B, 8B) 和 Qwen3Guard (0.6B, 4B)。
*   **数据集**：
    *   **训练/评估集**：使用了 7 个主流安全基准，包括 ToxicChat, OpenAIModeration, Aegis, Aegis2.0, WildGuard, SafeRLHF 和 BeaverTails，涵盖提示词和回复的有害性检测。
    *   **泛化性测试**：使用 **Think** 基准（包含推理链 Reasoning Traces），测试模型对未见分布数据的处理能力。
*   **场景**：包括离线序列检测和**实时流式检测（Streaming Detection）**。

### 4. 资源与算力
*   **训练效率**：SIREN 极其轻量。以 Qwen3-4B 为底座时，SIREN 仅引入了 **14M** 个可训练参数，比全量微调的防护模型少 **250 倍**。
*   **算力消耗**：在 **A100 GPU** 上，训练 SIREN 仅需约 **6 个小时**。
*   **推理效率**：由于 SIREN 只需要一次前向传播（Single Forward Pass）即可得出结果，无需自回归生成，其计算量（FLOPs）仅为生成式防护模型的约 **1/4**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种不同规模的底座模型上进行了测试，覆盖了 7 个训练数据集和 1 个极具挑战性的泛化测试集。
*   **消融实验**：详细探讨了神经元筛选阈值 $\eta$、层聚合策略（均匀 vs 自适应）以及正则化强度 $C$ 的影响。
*   **客观性与公平性**：
    *   **底座对齐**：通过在完全相同的底座模型上对比 SIREN 和官方 Guard 模型，排除了底座能力差异带来的干扰。
    *   **指标全面**：不仅对比了 F1 分数，还分析了 Precision-Recall 曲线的一致性。
    *   **实验充分性**：实验设计涵盖了从性能、泛化性到效率的多个维度，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能超越**：SIREN 在所有基准测试中均显著优于对应的生成式 Guard 模型（例如在 Llama3.2-1B 上提升了 15%）。
*   **内部编码**：实验证实 LLM 的**中间层**（而非末层）包含最强的安全相关信号，中间层的线性探测性能甚至接近微调后的 Guard 模型。
*   **泛化能力强**：在处理复杂的推理链（Reasoning Traces）时，SIREN 的表现远超基准模型，证明其捕捉的是通用的安全概念而非表面模式。
*   **流式检测**：SIREN 天然支持流式检测，能够随着 Token 的生成实时更新有害性评分，且在检测延迟和准确率上表现优异。

### 7. 优点
*   **极度轻量**：参数量极小，训练和推理成本极低，适合工业化大规模部署。
*   **即插即用**：无需修改原始 LLM 权重，不影响模型原有能力。
*   **策略一致性**：相比 Guard 模型，SIREN 在不同数据集上的判定边界更稳定，减少了过度防御或防御不足的情况。
*   **可解释性**：通过识别特定的“安全神经元”，为理解 LLM 内部的安全机制提供了工具。

### 8. 不足与局限
*   **线性假设依赖**：方法基于“线性表示假设”，如果某些复杂的有害概念在内部是以非线性方式编码的，该方法可能失效。
*   **分类粒度**：目前主要集中在二分类（有害 vs 安全），对于多分类（如区分暴力、色情、仇恨言论等细分类别）的扩展虽有潜力但尚未在文中深入验证。
*   **底座依赖**：SIREN 的表现仍受限于底座模型内部表示的质量，如果底座模型本身在预训练阶段完全未接触过相关安全概念，SIREN 也难以提取有效特征。

（完）
