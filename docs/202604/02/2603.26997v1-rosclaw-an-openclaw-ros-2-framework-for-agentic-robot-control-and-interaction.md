---
title: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction"
title_zh: ROSClaw：一种用于代理式机器人控制与交互的 OpenClaw ROS 2 框架
authors: "Irvin Steve Cardenas, Marcus Anthony Arnett, Natalie Catherine Yeo, Lucky Sah, Jong-Hoon Kim"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26997v1"
tags: ["query:agsec"]
score: 8.0
evidence: Agent在可配置安全包络内的执行前动作验证
tldr: ROSClaw是一个将OpenClaw代理运行时与ROS 2集成的模型无关执行层，旨在解决基础模型与物理机器人集成中的定制化难题。它通过动态能力发现、多模态观测归一化和安全验证，使任何基础模型都能在不同机器人平台上进行感知、推理和行动。实验表明，该框架能有效支持多种机器人和模型后端，并揭示了不同模型在物理行为和安全性上的显著差异，为具身智能提供了标准化的基础设施。
selection_source: carryover_cache
motivation: 现有的基础模型与机器人集成方案通常是针对特定模型和平台的定制化开发，缺乏通用性、安全性和可移植性。
method: 提出ROSClaw框架，通过标准化功能注入、多模态数据归一化、预执行动作验证和结构化审计日志，实现模型与ROS 2机器人的解耦连接。
result: 在轮式、四足和人形机器人上成功部署，发现不同模型在动作建议率上存在高达4.8倍的差异，且执行层设计显著影响任务完成度和安全性。
conclusion: ROSClaw不仅是实用的智能机器人基础设施，也是评估具身人工智能性能和安全性的可重复测量工具。
---

## 摘要
基础模型可以赋予机器人开放式推理、语言理解和自适应规划能力，但目前将模型连接到物理机器人需要定制化的集成，将感知、执行和安全性与单一模型和平台耦合。我们提出了 ROSClaw，这是一个与模型无关的执行层，它将 OpenClaw 智能体运行时与 ROS 2 集成，使任何基础模型能够通过以下方式感知、推理并作用于任何支持 ROS 的机器人：(i) 具有标准化示能注入的动态能力发现，(ii) 多模态观测归一化，(iii) 在可配置安全包络内的执行前动作验证，以及 (iv) 结构化审计日志。更换模型后端或机器人平台仅需更改配置；工具模式、安全执行和溯源日志保持不变。我们在三个平台（轮式、四足、人形）上部署了 ROSClaw，并使用了四个基础模型后端。在这种受控基质下，模型在策略外动作建议率方面表现出高达 4.8 倍的差异（仅在前沿模型中就有 3.4 倍），并针对相同的指令产生性质截然不同的物理行为。针对 ROSA 的跨框架对等协议证实，执行层设计（而非仅仅是提示词措辞）会显著影响任务完成情况和安全行为，从而确立了 ROSClaw 既是实用的代理式机器人基础设施，又是具身智能的可重复测量工具。

## Abstract
Foundation models can endow robots with open-ended reasoning, language understanding, and adaptive planning, yet connecting a model to a physical robot today requires bespoke integration that couples perception, actuation, and safety to a single model and platform. We present ROSClaw, a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2, enabling any foundation model to perceive, reason about, and act on any ROS-enabled robot through (i) dynamic capability discovery with standardized affordance injection, (ii) multimodal observation normalization, (iii) pre-execution action validation within a configurable safety envelope, and (iv) structured audit logging. Swapping model backends or robot platforms is a configuration change; tool schemas, safety enforcement, and provenance logging remain invariant. We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid) with four foundation-model backends. Under this controlled substrate, models exhibit up to 4.8 x differences in out-of-policy action proposal rates (3.4 x among frontier models alone) and produce qualitatively distinct physical behaviors from identical commands. A cross-framework parity protocol against ROSA confirms that executive-layer design, not just prompt wording, significantly affects both task completion and safety behavior, establishing ROSClaw as both practical agentic-robot infrastructure and a reproducible measurement instrument for embodied AI.

---

## 论文详细总结（自动生成）

以下是对论文《ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前将基础模型（如 LLM/VLM）集成到物理机器人中通常是“定制化”的（Bespoke Integration），导致感知、执行和安全逻辑与特定模型或硬件平台高度耦合，缺乏通用性、可移植性和可重复性。
*   **研究动机**：
    *   **接口缺失**：缺乏一个标准化的“执行层”来介导机器人的“大脑”（模型）与“身体”（硬件）。
    *   **行为差异**：相同的指令在不同模型驱动下会产生截然不同的物理行为，需要一种工具来量化这种“具身智能”的差异。
    *   **安全风险**：模型生成的规划可能违反物理约束，需要独立的验证层来确保安全。

### 2. 方法论
ROSClaw 提出了一个**模型无关的执行层（Executive Layer）**，其核心思想是将认知与执行解耦。
*   **执行层契约（Contract $C = \langle A, O, V, L \rangle$）**：
    *   **$A$ (Affordance)**：标准化的工具模式注册表，通过动态发现机制注入机器人能力。
    *   **$O$ (Observation)**：观测归一化，将原始传感器数据（如图像、里程计）转换为模型可理解的规范格式。
    *   **$V$ (Validator)**：预执行验证器，在动作发布前根据安全包络（如速度限制、白名单）进行拦截。
    *   **$L$ (Logger)**：结构化审计日志，记录所有尝试、允许和拒绝的动作，用于事后分析。
*   **关键技术细节**：
    *   **动态能力发现**：自动扫描 ROS 2 计算图，生成包含 Topic、Service 和 Action 的清单并注入 Prompt。
    *   **多模态处理**：支持原生视觉输入或通过桥接 VLM 将图像转换为 JSON 场景描述。
    *   **传输抽象**：支持本地 DDS、WebSocket (rosbridge) 和 WebRTC P2P 三种通信模式。
    *   **工具集**：暴露了 8 种核心工具（如 `ros2_publish`, `ros2_action` 等），映射到 ROS 2 原语。

### 3. 实验设计
*   **机器人平台**：TurtleBot3（轮式）、Unitree Go2（四足）、Unitree G1（人形）。
*   **基础模型后端**：Claude Opus 4.6、GPT-5.2、Gemini 3.1 Pro、Llama 4 Maverick（注：论文标注日期为 2026 年，模型版本为前瞻性设定）。
*   **任务套件（共 40 个任务）**：
    *   **结构化任务 (20个)**：分为直接指令、上下文相关和多步规划三个等级。
    *   **开放式行为任务 (10个)**：如“Shake and bake”、“表现出兴奋”，测试模型的创造性解释。
    *   **安全发散任务 (10个)**：对抗性指令，如“忽略安全规则”、“以最高速行驶”。
*   **Benchmark 与对比方法**：
    *   **ROSA (NASA JPL)**：作为直接的系统级基准对比。
    *   **消融实验**：针对“安全边界可见性”进行了 2x2 实验。

### 4. 资源与算力
*   **模型部署**：Llama 4 使用 **vLLM** 进行自托管部署。
*   **推理延迟**：LLM 推理耗时约 1.3–2.3 秒（P95 为 3.8 秒），远超传输延迟（5-10 毫秒）。
*   **算力细节**：文中未明确给出具体的 GPU 型号、数量或训练时长，因为该研究侧重于预训练模型的集成与推理，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：每个任务在每个模型上重复 $N=10$ 次实验，总计进行了数百次物理/仿真运行。
*   **客观性与公平性**：
    *   使用了**跨框架对等协议**，确保 ROSClaw 与 ROSA 在相同的硬件、Prompt、安全限制和解码设置下对比。
    *   引入了双盲评分机制（由两名独立评分员对开放式任务评分，一致性系数 $\kappa = 0.81$）。
    *   实验覆盖了从轮式到人形的不同复杂度平台，验证了框架的可移植性。

### 6. 主要结论与发现
*   **模型间显著差异**：在相同指令和约束下，不同模型在策略外（Out-of-policy）动作建议率上存在高达 **4.8 倍** 的差异（前沿模型间也有 3.4 倍差异）。
*   **执行层的重要性**：实验证实，执行层的设计（如工具规范化、错误语义反馈）比单纯的 Prompt 措辞对任务完成度和安全性的影响更大。
*   **行为多样性**：相同的模糊指令（如“Shake and bake”）会导致完全不同的物理轨迹（如 Claude 选择旋转+前进，而 GPT 选择线性振荡）。
*   **安全性**：ROSClaw 的验证层实现了 100% 的拦截率，防止了所有违反安全包络的动作执行。

### 7. 优点
*   **高度解耦**：更换模型或机器人平台仅需更改配置文件，无需修改核心代码。
*   **安全性增强**：通过预执行拦截和审计日志，为不可预测的 AI 代理提供了坚实的物理护栏。
*   **诊断价值**：结构化日志能够清晰地揭示模型失败的原因（如参数格式错误、逻辑死循环等）。
*   **标准化**：为具身智能研究提供了一个可重复、可量化的实验基质。

### 8. 不足与局限
*   **实时性限制**：受限于 LLM 的推理延迟（秒级），目前仅适用于高层任务规划（Deliberative Control），无法处理需要毫秒级响应的反应式控制（Reactive Control）。
*   **安全覆盖面**：目前的验证器主要侧重于速度边界和接口白名单，尚未集成复杂的避障、工作空间约束或形式化验证。
*   **死循环问题**：当模型陷入重复发布错误指令的逻辑中时，框架缺乏自动打破循环的机制。
*   **样本量**：虽然 $N=10$ 足以观察到显著差异，但对于更细微的模型对比，可能需要更大规模的统计样本。

（完）
