---
title: "Guardrails in Logit Space: Safety Token Regularization for LLM Alignment"
title_zh: Logit 空间中的护栏：用于大语言模型对齐的安全 Token 正则化
authors: "Thong Bach, Truyen Tran"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17210v1"
tags: ["query:agsec"]
score: 9.0
evidence: 通过在训练期间约束相关Logit来实现安全Token正则化
tldr: 针对大模型微调中安全性退化的问题，本文提出安全令牌正则化（STR）。该方法通过约束拒绝回复中关键令牌的 Logit 空间，在微调时保留安全属性。STR 计算开销极低，兼容 LoRA，在维持任务性能的同时显著提升了安全性与训练稳定性，为持续安全对齐提供了高效方案。
selection_source: fresh_fetch
motivation: 现有安全对齐多在预训练阶段，导致模型在下游领域微调时容易因行为偏移而丧失安全性。
method: 识别对齐模型拒绝回复中的关键令牌，并在微调过程中对这些令牌的 Logit 空间施加正则化约束。
result: 实验证明该方法在保持任务效能的同时，安全性可比肩最先进技术，且具有极低的计算开销。
conclusion: STR 是一种实用且易于部署的策略，能有效解决微调过程中的安全性退化并增强训练稳定性。
---

## 摘要
在新领域对对齐良好的大语言模型（LLMs）进行微调，往往会降低其安全对齐性能，即使使用的是良性数据集。现有的安全对齐技术主要集中在预训练阶段，使得微调后的模型容易受到行为偏移的影响。在这项工作中，我们引入了安全 Token 正则化（STR），这是一种旨在微调过程中保持安全特性的轻量级方法。我们的方法从对齐良好的模型的拒绝模板中识别出显著 Token，并在训练期间约束其相关的 Logit，从而防止关键安全行为的丧失。与强化学习或偏好优化方法不同，STR 仅需极少的额外计算，并能与 LoRA 等参数高效微调技术无缝集成。全面的实验表明，我们的方法在保持特定任务效用的同时，实现了与最先进方法相当的安全性能，且实现开销极小。此外，我们还表明，除了安全方面的考虑外，安全 Token 正则化还增强了训练稳定性和整体性能。这项工作为微调后的 LLM 的持续安全对齐提供了一种实用且易于部署的策略。

## Abstract
Fine-tuning well-aligned large language models (LLMs) on new domains often degrades their safety alignment, even when using benign datasets. Existing safety alignment techniques primarily focus on pretraining, leaving fine-tuned models vulnerable to behavioral shifts. In this work, we introduce safety token regularization (STR), a lightweight method designed to preserve safety properties during fine-tuning. Our approach identifies salient tokens from rejection templates of well-aligned models and constrains their associated logits during training, preventing the loss of critical safety behaviors. Unlike reinforcement learning or preference optimization methods, STR requires minimal additional computation and seamlessly integrates with parameter-efficient fine-tuning techniques such as LoRA. Comprehensive experiments demonstrate that our approach achieves safety performance on par with state-of-the-art methods, while preserving task-specific utility and requiring minimal implementation overhead. Furthermore, we show that safety token regularization enhances training stability and overall performance beyond safety considerations alone. This work offers a practical and readily deployable strategy for continual safety alignment in fine-tuned LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Guardrails in Logit Space: Safety Token Regularization for LLM Alignment》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）在预训练阶段通过 RLHF 等方法建立了安全对齐，但在针对特定下游任务进行微调（Fine-tuning）时，即使是使用良性数据，模型原有的安全边界也极易发生退化或被“抹除”。
*   **核心问题**：如何在不显著增加计算开销、不损害下游任务性能的前提下，在微调过程中持久地保留模型预训练时的安全属性？

### 2. 方法论
*   **核心思想**：提出**安全 Token 正则化（Safety Token Regularization, STR）**。该方法认为对齐良好的模型在面对违规请求时，其 Logit 空间中特定“安全令牌”（如“I”, “cannot”）的概率分布具有特征性。通过在微调时约束这些特定 Token 的 Logit 输出，使其偏离预训练模型的程度最小化，从而锁定安全行为。
*   **关键技术细节**：
    1.  **识别安全 Token**：分析对齐模型的拒绝回复模板（如 "I am sorry, I cannot..."），提取高频核心词（实验中使用了 `{I, cannot, can't}`）。
    2.  **损失函数设计**：总损失由两部分组成：
        *   **交叉熵损失 ($L_{CE}$)**：确保模型学习下游任务。
        *   **正则化损失 ($L_F$)**：计算微调模型与原始预训练模型在“安全 Token”位置上的 Logit 差异（采用平方损失/Frobenius 范数）。
    3.  **公式**：$L = L_{CE} + \lambda L_F$，其中 $\lambda$ 是调节安全约束强度的超参数。
*   **算法流程**：在每次训练迭代中，同时获取冻结的基座模型和可训练的 PEFT 模型（如 LoRA）的 Logit，仅针对预设的安全 Token 计算差异并反向传播。

### 3. 实验设计
*   **模型架构**：LLaMA-2-7b-chat 和 LLaMA-3-8b-instruct。
*   **数据集/场景**：
    *   **Alpaca**：良性指令遵循数据，评估通用微调下的安全性保持。
    *   **PureBad**：包含 100 条极具危害性的提示词，作为极端压力测试（对抗性微调）。
    *   **Commonsense-15k**：评估模型在常识推理任务中的效用（Utility）。
*   **对比方法（Benchmark）**：
    *   基础 PEFT 方法：LoRA, DoRA, PiSSA。
    *   安全增强方法：InferAligner (IA), Vaccine (Vac), Safe LoRA, SaLoRA。
*   **评估指标**：有害响应率（HRR）、攻击成功率（ASR）、任务平均准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **效率分析**：文中提供了运行时间对比（Table 6）。在相同设置下，STR 的迭代速度比标准 LoRA 慢约 1.34 倍，但比 DoRA 快 1.25 倍，证明了其轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从良性到极度有害的数据环境，测试了不同的 LoRA Rank（16 到 256），并进行了持续学习（Continual Learning）场景下的测试。
*   **充分性**：实验设计较为全面，不仅关注安全性，还深入探讨了 Rank 增加对稳定性的影响。
*   **客观性**：使用了自动化的安全分类器（Llama Guard）进行评估，减少了人工标注的主观偏差。

### 6. 主要结论与发现
*   **安全性保持**：STR 在 LLaMA-2 上达到了 SOTA 性能，甚至在纯有害数据微调下也能将有害响应率降至 0%。
*   **效用增强**：令人意外的是，STR 在高 Rank 设置下显著提升了微调的稳定性。当标准 LoRA 和 DoRA 性能大幅波动时，STR 仍能保持高准确率。
*   **正则化效应**：即使是随机选择 Token 进行正则化也能产生一定的安全保护效果，但针对性的“安全 Token”效果最佳。
*   **低成本**：无需额外的偏好模型或复杂的对抗训练，易于部署。

### 7. 优点
*   **极简高效**：实现代码改动极小，不需要复杂的强化学习流程。
*   **稳定性**：解决了 PEFT 方法在高容量（高 Rank）微调时容易出现的训练不稳定问题。
*   **兼容性强**：可以无缝集成到现有的 LoRA 等参数高效微调框架中。

### 8. 不足与局限
*   **继承限制**：该方法本质上是让模型“模仿”预训练时的拒绝行为，因此模型无法通过此方法学习到超出预训练范围的新安全准则。
*   **Token 依赖**：安全 Token 的选择基于简单的拒绝模板，对于不含这些特定词汇的隐蔽性有害回复（如反讽、伪装）可能防御力有限。
*   **任务偏差**：虽然实验显示效用良好，但在某些极度需要改变输出风格的任务中，强行约束 Logit 可能会产生潜在的负面干扰。

（完）
