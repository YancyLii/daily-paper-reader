---
title: "No Bit Left Behind: Using Brute-Force Lifting to Achieve Fully Static Binary Recompilation"
title_zh: 不遗漏任何比特：使用暴力提升实现全静态二进制重编译
authors: "Tianjiao Huang, Po-An Chen, Nick Baron, Michael Franz"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.16423v1"
tags: ["query:ai-infra-sec"]
score: 6.0
evidence: 二进制重编译可在编译器级别回溯添加安全缓解措施
tldr: 静态二进制重编译因计算分支目标难以静态确定，常依赖目标端运行时回退机制。本文提出全静态暴力提升技术：将每个字节偏移均视为潜在分支目标，构造包含所有可行控制流的超集CFG，并把不可解析计算分支转化为查表操作。原型实现从x86-64二进制到LLVM IR的完整提升，无需任何代码/数据区分启发式，并成功交叉编译至AArch64。该工作消除了对目标运行时翻译支持的依赖，为遗留二进制安全加固和跨ISA移植提供了全自动方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.16423v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1395, \"height\": 825, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16423v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1350, \"height\": 1894, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16423v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1426, \"height\": 675, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.16423v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 852, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16423v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1038, \"height\": 686, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16423v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 757, \"height\": 630, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16423v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 841, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16423v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 868, \"height\": 704, \"label\": \"Table\"}]"
motivation: 静态二进制重编译的核心障碍是计算分支目标往往无法静态确定，导致现有系统必须携带运行时回退机制，增加复杂性和目标端负担。
method: 本文采用全静态暴力提升策略，把二进制中每个字节偏移看作潜在分支目标，生成保守的超集CFG，并将不可解析计算分支缩减为查表操作。
result: 原型实现了x86-64二进制到LLVM IR的全静态提升，无需代码/数据启发式，并成功交叉编译至AArch64。
conclusion: 该工作证明全静态二进制重编译可行，消除了目标端运行时翻译开销，可复用LLVM后端实现跨ISA迁移，为自动化安全加固提供新途径。
---

## 摘要
二进制重编译是一种直接操作可执行代码的技术。它有望自动化两个重要任务：为遗留二进制程序加装安全缓解措施，以及将二进制程序跨指令集架构（ISA）迁移。然而，目前尚无完全自动化系统能够可靠地将任意二进制可执行文件提升至编译器中间表示（IR，如 LLVM IR），或能够完全静态且可靠地将非平凡二进制可执行文件从一种 ISA 翻译到另一种 ISA。主要底层问题是，静态恢复程序的控制流图（CFG）通常是不可能的：计算型分支可以跳转到不实际运行程序就无法确定的目标。现有系统诉诸于运行时回退机制，要求二进制翻译机制的相当一部分伴随已翻译程序在目标机器上运行。本文提出一种完全静态的、全程序二进制提升系统，在目标机器上不需要任何运行时翻译支持。我们不是试图区分代码与数据，而是将每个字节偏移都视为潜在分支目标，以暴力方式提升整个二进制，构造一个保守包含所有可行控制流的超集 CFG。静态无法解析的计算型分支由此被简化为在分发表中进行查找，该表指向相应的已翻译控制流路径。我们已将这一方法实现为一个原型二进制重编译器，从 x86-64 二进制到 LLVM IR，不需要任何代码/数据启发式。我们通过完全静态地交叉编译到 AArch64 来验证它，该过程通过复用现有的 LLVM 后端而不做任何修改实现。

## Abstract
Binary recompilation is a technique for operating directly on executable code. It promises to automate two important tasks: retrofitting security mitigations onto legacy binaries, and migrating binaries across instruction set architectures (ISAs). Yet today, there is no fully automated system that can reliably lift arbitrary binary executables to a compiler intermediate representation (IR) such as LLVM IR, or that can fully statically and reliably translate non-trivial binary executables from one ISA to another. The main underlying problem is that recovering a program's control flow graph (CFG) statically is impossible in general: computed branches can jump to targets that cannot be determined without actually running the program. Existing systems resort to runtime fallback mechanisms, requiring a significant portion of the binary translation machinery to accompany the translated program on the target machine.   This article presents a fully static, whole-program binary lifting system requiring no runtime translation support on the target. Rather than attempting to distinguish code from data, we treat every byte offset as a potential branch target and lift the entire binary in a brute-force manner, constructing a superset CFG that conservatively contains all feasible control flows. Statically unresolvable computed branches are thereby reduced to lookups in a dispatch table that points to the corresponding translated control flow path. We have implemented this approach as a prototype binary recompiler from x86-64 binaries to LLVM IR, requiring no code/data heuristics. We validate it with a fully static cross-compilation to AArch64, achieved by reusing existing LLVM backends with no modification.