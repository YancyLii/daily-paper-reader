---
title: Policy-Invisible Violations in LLM-Based Agents
title_zh: 基于 LLM 智能体中的策略不可见违规
authors: "Jie Wu, Ming Gong"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12177v1"
tags: ["query:agsec"]
score: 8.0
evidence: LLM智能体的安全控制与策略违规检测
tldr: 本文研究了LLM智能体中一种新型失效模式——策略不可见违规，即智能体因缺乏必要的背景事实而执行了违反组织策略的操作。研究者推出了PhantomPolicy基准测试，并提出了Sentinel框架，该框架利用反事实图模拟技术，将智能体动作映射为知识图谱的变更并验证其合规性，为解决复杂环境下的智能体安全问题提供了新思路。
selection_source: fresh_fetch
motivation: LLM智能体在决策时常因关键背景信息缺失，导致其执行看似合理实则违反组织策略的操作。
method: 提出了PhantomPolicy基准测试，并开发了Sentinel框架，通过在知识图谱上进行反事实模拟执行来验证动作的合规性。
result: "Sentinel在实验中达到了93.0%的准确率，显著优于仅基于内容的DLP基准（68.8%），且保持了极高的精确度。"
conclusion: 通过将策略相关的世界状态引入强制执行层，可以有效识别并拦截传统方法难以发现的策略不可见违规行为。
---

## 摘要
基于 LLM 的智能体可以执行语法有效、用户授权且语义恰当的操作，但仍可能违反组织策略，因为进行正确策略判断所需的事实信息在决策时是隐藏的。我们将这种失效模式称为“策略不可见违规”（policy-invisible violations）：即合规性取决于实体属性、上下文状态或会话历史，而这些信息在智能体的可见上下文中是缺失的。我们提出了 PhantomPolicy，这是一个涵盖八个违规类别的基准测试，包含平衡的违规案例和安全对照案例，其中所有工具响应仅包含纯业务数据，而不包含策略元数据。我们人工审查了由五种前沿模型生成的全部 600 条模型轨迹，并使用经人工审核的轨迹标签对其进行评估。相对于原始的案例级标注，人工审查修改了 32 个标签（5.3%），证实了进行轨迹级人工审查的必要性。为了展示在有利条件下基于世界状态的执行机制所能达到的效果，我们引入了 Sentinel，这是一个基于反事实图模拟的执行框架。Sentinel 将智能体的每个动作视为对组织知识图谱的拟议变更，通过推测执行来具现化动作后的世界状态，并验证图结构的不变量，从而做出“允许/拦截/澄清”的决策。在针对人工审核的轨迹标签进行的测试中，Sentinel 的表现显著优于仅基于内容的 DLP 基准（准确率从 68.8% 提升至 93.0%），同时保持了高精确率，尽管在某些违规类别上仍有改进空间。这些结果表明，一旦将与策略相关的世界状态提供给执行层，就能实现显著的性能提升。

## Abstract
LLM-based agents can execute actions that are syntactically valid, user-sanctioned, and semantically appropriate, yet still violate organizational policy because the facts needed for correct policy judgment are hidden at decision time. We call this failure mode policy-invisible violations: cases in which compliance depends on entity attributes, contextual state, or session history absent from the agent's visible context. We present PhantomPolicy, a benchmark spanning eight violation categories with balanced violation and safe-control cases, in which all tool responses contain clean business data without policy metadata. We manually review all 600 model traces produced by five frontier models and evaluate them using human-reviewed trace labels. Manual review changes 32 labels (5.3%) relative to the original case-level annotations, confirming the need for trace-level human review. To demonstrate what world-state-grounded enforcement can achieve under favorable conditions, we introduce Sentinel, an enforcement framework based on counterfactual graph simulation. Sentinel treats every agent action as a proposed mutation to an organizational knowledge graph, performs speculative execution to materialize the post-action world state, and verifies graph-structural invariants to decide Allow/Block/Clarify. Against human-reviewed trace labels, Sentinel substantially outperforms a content-only DLP baseline (68.8% vs. 93.0% accuracy) while maintaining high precision, though it still leaves room for improvement on certain violation categories. These results demonstrate what becomes achievable once policy-relevant world state is made available to the enforcement layer.

---

## 论文详细总结（自动生成）

这篇论文对基于大语言模型（LLM）的智能体在组织环境中的安全性进行了深入探讨，以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文定义了一种新型失效模式——**策略不可见违规（Policy-Invisible Violations）**。在这种模式下，智能体执行的操作在语法上正确、经过用户授权且语义合理，但由于进行正确策略判断所需的关键事实（如实体属性、上下文状态或会话历史）在智能体的可见上下文中是缺失的，导致其违反了组织策略。
*   **研究背景**：随着 LLM 智能体从简单的问答转向工具调用（如发送邮件、共享文件），它们经常在复杂的企业环境里操作。现有的安全研究多关注对抗性攻击（如越狱），而忽略了这种由于“信息不对称”导致的非对抗性违规。

### 2. 论文提出的方法论：Sentinel 框架
论文提出了 **Sentinel** 强制执行框架，其核心思想是**反事实图模拟（Counterfactual Graph Simulation）**。
*   **核心思想**：不直接让 LLM 判断合规性，而是将智能体的每个动作视为对组织知识图谱（Knowledge Graph）的拟议变更，通过模拟执行来验证结果状态是否违反预设的不变量。
*   **关键技术流程（五阶段流水线）**：
    1.  **翻译（Translate）**：将工具调用（如 `send_email`）转换为一组拟议的图变更（Mutation），如增加“数据流向”边。
    2.  **分叉（Fork）**：对当前世界状态图进行 $O(1)$ 的写时复制（Copy-on-write）快照。
    3.  **变更（Mutate）**：在快照图上应用拟议的变更。
    4.  **检查（Check）**：验证 7 个声明式图不变量（如：接收者是否活跃、是否跨越上下文边界、信息流是否符合权限等）。
    5.  **决策（Decide）**：根据不变量检查结果决定“允许（Allow）”、“拦截（Block）”或“澄清（Clarify）”。
*   **算法特性**：验证复杂度为 $O(|M|)$（$M$ 为变更集大小），与基础图的大小无关，确保了极高的运行效率。

### 3. 实验设计
*   **数据集/场景（PhantomPolicy 基准测试）**：
    *   包含 8 个违规类别（如：上下文边界违规、过度分享、受限受众、会话累积泄露等）。
    *   共 120 个案例（60 个违规案例，60 个平衡的安全对照案例）。
    *   **设计原则**：工具响应中不包含任何策略元数据（如文件名不含“机密”字样），迫使系统必须依赖外部世界模型。
*   **对比方法**：
    *   **基础模型（Baseline）**：五种前沿模型（GPT-5.4, GPT-5 mini, GPT-5.4 nano, Claude Sonnet 4.6, Claude Opus 4.6）。
    *   **策略入提示词（Policy-in-prompt）**：在系统提示词中加入高层策略规则。
    *   **内容 DLP 基准**：仅基于正则表达式和关键词匹配的传统数据泄露防护（DLP）方法。
*   **评估协议**：人工审查了所有 600 条模型轨迹，并以人工审核后的轨迹标签作为金标准。

### 4. 资源与算力
*   **算力消耗**：论文未提及具体的 GPU 训练时长，因为主要评估的是现成的 API 模型。
*   **API 成本**：记录了单次完整测试的 API 费用（如 GPT-5.4 约 1.21 美元，Claude Opus 4.6 约 4.60 美元）。
*   **Sentinel 性能**：在普通 CPU 上运行，验证延迟小于 1 毫秒，远低于 LLM 的推理时间。

### 5. 实验数量与充分性
*   **实验规模**：对 5 种模型在 120 个案例下进行了测试，共生成并人工审查了 600 条轨迹。
*   **消融实验**：进行了**覆盖率退化实验**（模拟知识图谱信息缺失时对召回率的影响）和**不变量消融实验**。
*   **充分性评价**：实验设计非常严谨。通过人工审查修正了 5.3% 的原始标注错误，确保了评估的客观性。消融实验深入探讨了“世界模型完整性”与“安全效果”之间的量化关系，具有很强的说服力。

### 6. 论文的主要结论与发现
*   **违规普遍性**：在缺乏隐藏策略状态的情况下，所有前沿模型在风险案例中的违规率高达 90%–98%。
*   **Sentinel 的优越性**：Sentinel 达到了 93.0% 的准确率，远超内容 DLP 基准（68.8%），且保持了极高的精确率（False Positive 极少）。
*   **瓶颈不在推理**：实验证明，失效的主要原因不是模型推理能力不足，而是**决策所需的事实状态在上下文中缺失**。
*   **提示词工程的局限**：将策略放入提示词虽能降低违规率，但效果极不稳定（不同模型表现差异巨大），且无法处理实体级的事实缺失。

### 7. 优点：亮点与创新
*   **问题定义新颖**：首次系统化定义了“策略不可见违规”这一智能体特有的安全挑战。
*   **架构解耦**：提出了将“任务推理”与“策略强制执行”在架构上分离的原则，符合安全工程中的权限分离原则。
*   **形式化保证**：Sentinel 框架具有形式化的完备性、组合性和极高的计算效率。
*   **高质量基准**：PhantomPolicy 填补了智能体在非对抗性环境下合规性评估的空白。

### 8. 不足与局限
*   **领域局限**：目前仅在企业办公（邮件、文件分享）领域进行了验证，尚未扩展到医疗、金融等其他垂直领域。
*   **对世界模型的依赖**：Sentinel 的效果高度依赖于底层知识图谱的完整性和准确性。如果企业无法维护一个高质量的实体关系图，该框架的效果会打折扣。
*   **文本输出监控缺失**：目前 Sentinel 主要拦截工具调用，如果模型在直接回复的文本中泄露机密信息（而非通过工具），目前尚无法完全覆盖。
*   **基准规模**：120 个案例虽然质量高，但作为统计样本规模仍相对较小。

（完）
