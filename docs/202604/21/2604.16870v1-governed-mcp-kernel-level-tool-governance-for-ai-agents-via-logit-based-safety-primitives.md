---
title: "Governed MCP: Kernel-Level Tool Governance for AI Agents via Logit-Based Safety Primitives"
title_zh: 受控 MCP：通过基于 Logit 的安全原语实现 AI 智能体的内核级工具治理
authors: Daeyeon Son
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16870v1"
tags: ["query:agsec"]
score: 9.0
evidence: 基于Logit安全原语的AI Agent内核级工具治理
tldr: 针对AI智能体通过MCP协议调用工具时安全防护易被绕过的问题，本文提出Governed MCP。这是一种在Anima OS内核中实现的工具治理网关，核心采用ProbeLogits语义安全原语。该方案通过六层过滤流水线（含模式验证、语义检查、策略匹配等）对工具调用进行强制审计。实验显示，该内核级防护能有效杜绝用户态绕过，且ProbeLogits层使安全检测F1分数提升了0.446，显著增强了智能体操作的安全性。
selection_source: fresh_fetch
motivation: 当前AI智能体的工具调用安全防护完全处于用户态，极易被简单脚本绕过，缺乏类似操作系统内核级的硬性安全约束。
method: 在Rust开发的Anima OS内核中构建六层治理网关，利用基于Logit的语义检查原语ProbeLogits对每个MCP调用进行实时拦截与审计。
result: 非推理层开销仅为65.3微秒，而ProbeLogits层的加入将MCP基准测试的F1分数从0.327提升至0.773，成功防御了所有用户态绕过攻击。
conclusion: 将工具治理下沉至内核态并结合语义逻辑检查，是解决AI智能体越权操作、构建高安全性AI操作系统的有效途径。
---

## 摘要
AI 智能体越来越多地通过模型上下文协议 (MCP) 调用外部工具（文件系统、网络、API）。这些工具调用是智能体的系统调用 (syscall)——即对共享状态具有副作用的特权操作——然而，当前的安全执行完全位于用户空间，一段 10 行的脚本即可将其绕过。我提出了 Governed MCP，这是一个构建在基于 Logit 的安全原语（ProbeLogits，配套论文：arXiv:2604.11943）之上的内核驻留工具治理网关。该网关通过一个 6 层流水线干预每一次 MCP 工具调用：模式验证、信任层级检查、速率限制、对抗性预过滤、ProbeLogits 门控（核心语义检查）以及宪法策略匹配，并配有 Blake3 哈希审计链。我在 Anima OS 中实现了 Governed MCP，这是一个使用约 86,000 行 Rust 代码编写的裸机 x86_64 操作系统。五个非推理层为每次调用增加了 65.3 微秒的开销；ProbeLogits 在 7B Q4_0 模型上增加了 65 毫秒（每个 token 类别的语义决策）。在包含 101 个提示词的 MCP 领域基准测试中进行的 4 配置消融实验表明，移除 ProbeLogits 层会导致 F1 分数从 0.773 骤降至 0.327（Delta F1 = -0.446）——仅靠手动规则防火墙是不够的。运行时中所有 15 个 WASM 到系统的宿主函数都通过该网关路由（实现了对 WASM ABI 表面的完全调解；该声明的范围和注意事项见第 4.6 节）；对于内核驻留门控而言，能够击败现有护栏库的 10 行代码用户空间绕过在结构上是不可能的。

## Abstract
AI agents increasingly call external tools (file system, network, APIs) through the Model Context Protocol (MCP). These tool calls are the agent's syscalls -- privileged operations with side effects on shared state -- yet today's safety enforcement lives entirely in userspace, where a 10-line script can bypass it. I propose Governed MCP, a kernel-resident tool governance gateway built on a logit-based safety primitive (ProbeLogits, companion paper: arXiv:2604.11943). The gateway interposes on every MCP tool call in a 6-layer pipeline: schema validation, trust tier check, rate limit, adversarial pre-filter, ProbeLogits gate (the load-bearing semantic check), and constitutional policy match, with a Blake3-hashed audit chain.   I implement Governed MCP in Anima OS, a bare-metal x86_64 OS in approximately 86,000 lines of Rust. The five non-inference layers add 65.3 microseconds of overhead per call; ProbeLogits adds 65 ms (per-token-class semantic decision) on 7B Q4_0. A 4-config ablation on a 101-prompt MCP-domain benchmark shows that removing the ProbeLogits layer collapses F1 from 0.773 to 0.327 (Delta F1 = -0.446) -- hand-rule firewalling alone is insufficient. All 15 WASM-to-system host functions in the runtime route through the gateway (complete mediation of the WASM ABI surface; the scope and caveats of this claim are stated in Section 4.6); a 10-LoC userspace bypass that defeats existing guardrail libraries is structurally impossible against the kernel-resident gate.

---

## 论文详细总结（自动生成）

### 论文总结：Governed MCP —— 基于内核级 Logit 安全原语的 AI 智能体工具治理

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体（Agents）通过模型上下文协议（MCP）自主调用外部工具（如文件系统、网络 API）的情况日益增多，这些“工具调用”实质上已等同于操作系统的“系统调用（Syscalls）”。然而，现有的安全防护方案（如 NeMo Guardrails、AGT 等）大多作为用户态库（Python/JS）运行。
*   **核心痛点**：由于防护层与智能体处于同一特权级和地址空间，智能体可以通过跳过导入、修改函数符号或篡改返回结果等方式轻松绕过防护（论文展示了仅需 10 行代码即可绕过现有护栏）。
*   **研究动机**：将工具治理下沉至操作系统内核（Kernel），在特权边界处建立不可逾越的“参考监视器（Reference Monitor）”，确保所有工具调用必须经过语义安全检查。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Governed MCP**，一个集成在 **Anima OS**（作者自研的 Rust 裸机操作系统）内核中的治理网关。
*   **核心思想**：利用 WASM 沙箱隔离智能体，并将所有工具调用路径强制路由至内核态的六层过滤流水线。
*   **六层流水线流程**：
    1.  **L1 模式验证**：检查 JSON-RPC 格式及参数是否符合工具定义的 JSON Schema。
    2.  **L2 信任层级检查**：根据智能体来源（系统级、原生 AI、经典应用等）匹配工具白名单。
    3.  **L3 速率限制**：基于令牌桶算法防止拒绝服务攻击或病态循环。
    4.  **L4 对抗性预过滤**：使用正则 DFA 快速拦截已知的提示词注入模式。
    5.  **L5 ProbeLogits 门控（核心）**：调用内核推理引擎，读取模型在“安全/危险”词元位置的 Logit 值，计算语义安全得分。
    6.  **L6 宪法策略匹配**：应用 12 条预设的“宪法”原则（如禁止读取敏感路径）。
*   **关键技术细节**：
    *   **ProbeLogits**：无需生成完整文本，仅通过一次前向传播读取 Logit，大幅降低延迟。
    *   **完全调解（Complete Mediation）**：通过 WASM ABI 强制拦截所有 15 个宿主函数。
    *   **故障闭锁（Fail-Closed）**：若推理引擎不可用，默认拒绝所有工具调用。
    *   **审计链**：使用 Blake3 哈希将所有决策链接成防篡改的审计日志。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Custom-101**：包含 101 个 MCP 工具调用提示词（37 个危险，64 个良性），覆盖文件系统、网络、内存、注入攻击等 18 个领域。
    *   **Custom-260**：260 个操作系统操作提示词，用于验证 OS 动作的安全性。
*   **Benchmark 与对比方法**：
    *   主要对比了“完整流水线”与“移除 ProbeLogits 语义层”的性能。
    *   在讨论中对比了用户态方案（NeMo Guardrails）和独立分类模型（Llama Guard 3）的延迟与安全性。

#### 4. 资源与算力
*   **硬件平台**：AMD Ryzen 9800X3D (8 核, 16 线程), 60GB DDR5-6000 内存。
*   **推理模型**：主要使用 Qwen2.5-7B-Instruct (Q4_0 量化)，同时也验证了 Llama 3 8B 和 Mistral 7B。
*   **算力说明**：实验在**裸机 CPU** 上运行，未利用 GPU。ProbeLogits 在 7B 模型上的推理延迟约为 65ms。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 101 个样本的基准测试、260 个样本的动作验证、4 组消融实验以及阈值（Alpha）扫描实验。
*   **充分性评价**：实验涵盖了性能开销、分类准确率（F1/精确率/召回率）和结构化绕过测试。虽然样本量（101/260）在统计学上不算巨大，但覆盖了关键的攻击向量。
*   **客观性**：作者诚实地指出了数据集为自标（Author-labeled）的局限性，并提供了详细的混淆矩阵和置信区间。

#### 6. 主要结论与发现
*   **内核级治理的必要性**：内核驻留网关在结构上杜绝了用户态绕过攻击。
*   **语义检查是支柱**：移除 ProbeLogits 层后，F1 分数从 **0.773** 骤降至 **0.327**（$\Delta F1 = -0.446$），证明仅靠手动规则（防火墙）无法应对复杂的语义攻击。
*   **性能可行性**：非推理层开销极低（65.3μs），总延迟（约 66ms）远低于传统的全文本生成检查（通常需 1-5 秒）。
*   **完全调解验证**：通过测试套件验证了所有 123 条从智能体到系统的路径均被成功拦截。

#### 7. 优点
*   **架构创新**：首次将 AI 语义安全检查下沉到操作系统内核特权级，解决了“谁来监管监管者”的特权边界问题。
*   **高效性**：利用 Logit 原语而非文本生成，使实时内核级治理成为可能。
*   **安全性设计**：引入了故障闭锁、防篡改审计链和 KV 缓存快照恢复，体现了严谨的安全工程思维。
*   **开源贡献**：提供了约 8.6 万行 Rust 代码的裸机 OS 实现。

#### 8. 不足与局限
*   **数据集偏差**：主要依赖作者自标的数据集，缺乏大规模社区公认的 MCP 安全基准测试。
*   **硬件限制**：目前仅支持 CPU 推理，在高并发场景下可能存在锁竞争和吞吐量瓶颈（需等待 GPU/NPU 加速支持）。
*   **防御范围**：尚未完全解决间接提示词注入（即工具返回的恶意结果）的实时探测（列为未来工作）。
*   **模型依赖**：治理效果高度依赖于底层模型的质量，若模型本身被投毒，内核防护也会失效。

（完）
