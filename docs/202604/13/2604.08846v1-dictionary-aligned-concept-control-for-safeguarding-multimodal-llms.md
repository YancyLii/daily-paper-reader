---
title: Dictionary-Aligned Concept Control for Safeguarding Multimodal LLMs
title_zh: 字典对齐的概念控制：为多模态大语言模型提供安全保障
authors: "Jinqi Luo, Jinyu Yang, Tal Neiman, Lei Fan, Bing Yin, Son Tran, Mubarak Shah, René Vidal"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08846v1"
tags: ["query:agsec"]
score: 8.0
evidence: 保护多模态大模型免受恶意查询和不安全响应的影响
tldr: 针对多模态大模型（MLLM）易受恶意攻击的问题，本文提出DACO框架。该框架通过构建包含1.5万个概念的DACO-400K数据集，并结合稀疏自编码器（SAE）技术，实现了对模型激活值的细粒度控制。DACO不仅能有效拦截不安全响应，还能在不重新训练模型的情况下保持其通用能力，在多个主流模型和安全基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的MLLM安全防御方法在应对演化攻击时灵活性不足，且难以在不影响通用性能的情况下实现精确的概念干预。
method: 提出DACO框架，利用大规模多模态概念字典初始化稀疏自编码器（SAE），通过稀疏编码对推理时的模型激活进行细粒度引导。
result: 在QwenVL、LLaVA等模型上的实验表明，DACO在MM-SafetyBench等安全基准上显著提升了安全性，同时保留了模型的通用任务处理能力。
conclusion: DACO证明了基于字典对齐的激活引导是一种高效、灵活且可解释的MLLM安全防护方案。
---

## 摘要
多模态大语言模型（MLLMs）已被证明容易受到恶意查询的影响，从而诱发不安全的响应。近期研究采用提示工程、响应分类或微调来提升 MLLM 的安全性。然而，这些方法通常难以应对不断演变的恶意模式，可能需要重新运行查询，或需要大量的计算资源。在推理阶段引导冻结模型的激活值，近期已成为一种灵活且有效的解决方案。然而，现有的 MLLM 引导方法通常只能处理有限的安全相关概念，或者难以在不影响其他概念的情况下调整特定概念。为了应对这些挑战，我们提出了字典对齐的概念控制（DACO），这是一个利用精心策划的概念字典和稀疏自编码器（SAE）对 MLLM 激活进行细粒度控制的框架。首先，我们通过检索超过 40 万个“描述-图像”刺激，并将其激活值总结为概念方向，策划了一个包含 15,000 个多模态概念的字典。我们将该数据集命名为 DACO-400K。其次，我们展示了该策划字典可以通过稀疏编码来干预激活。第三，我们提出了一种新的引导方法，利用我们的字典来初始化 SAE 的训练，并自动标注 SAE 原子的语义，以保障 MLLM 的安全。在多个 MLLM（如 QwenVL、LLaVA、InternVL）以及多个安全基准测试（如 MM-SafetyBench、JailBreakV）上的实验表明，DACO 在保持通用能力的同时，显著提升了 MLLM 的安全性。

## Abstract
Multimodal Large Language Models (MLLMs) have been shown to be vulnerable to malicious queries that can elicit unsafe responses. Recent work uses prompt engineering, response classification, or finetuning to improve MLLM safety. Nevertheless, such approaches are often ineffective against evolving malicious patterns, may require rerunning the query, or demand heavy computational resources. Steering the activations of a frozen model at inference time has recently emerged as a flexible and effective solution. However, existing steering methods for MLLMs typically handle only a narrow set of safety-related concepts or struggle to adjust specific concepts without affecting others. To address these challenges, we introduce Dictionary-Aligned Concept Control (DACO), a framework that utilizes a curated concept dictionary and a Sparse Autoencoder (SAE) to provide granular control over MLLM activations. First, we curate a dictionary of 15,000 multimodal concepts by retrieving over 400,000 caption-image stimuli and summarizing their activations into concept directions. We name the dataset DACO-400K. Second, we show that the curated dictionary can be used to intervene activations via sparse coding. Third, we propose a new steering approach that uses our dictionary to initialize the training of an SAE and automatically annotate the semantics of the SAE atoms for safeguarding MLLMs. Experiments on multiple MLLMs (e.g., QwenVL, LLaVA, InternVL) across safety benchmarks (e.g., MM-SafetyBench, JailBreakV) show that DACO significantly improves MLLM safety while maintaining general-purpose capabilities.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DACO (Dictionary-Aligned Concept Control)** 的框架，旨在通过干预模型内部激活值来增强多模态大语言模型（MLLM）的安全性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大模型（如 QwenVL, LLaVA）容易受到恶意文本查询或对抗性图像输入的攻击（即“越狱攻击”），从而产生有害、暴力或违反政策的内容。
*   **研究动机**：
    *   **现有方法局限**：提示工程（Prompting）容易被绕过；响应过滤（Filtering）计算开销大；微调（Finetuning）成本高且难以应对新出现的攻击模式。
    *   **表示引导（Representation Steering）的潜力**：在推理阶段直接调整模型内部激活值更灵活，但现有方法通常只覆盖极少数安全概念，且缺乏语义解释性。

### 2. 方法论：DACO 框架
DACO 的核心思想是结合**大规模概念字典**与**稀疏自编码器（SAE）**，实现对模型激活值的精确、可解释控制。
*   **概念字典构建（DACO-400K）**：
    *   从 WordNet 提取 1.5 万个概念。
    *   利用 CLIP 在 CC-3M 数据集中检索超过 40 万个“图像-描述”对作为刺激源。
    *   通过“对比表示读取”（Contrastive Representation Reading）计算每个概念在模型层中的向量方向。
*   **多模态斜投影（MOP）**：
    *   利用弹性网络（Elastic Net）稀疏求解器，将输入查询的激活值分解为字典中各概念的线性组合。
    *   直接减去被标记为“不安全”的概念成分。
*   **字典对齐的 SAE 训练与自动标注**：
    *   **初始化**：使用预先构建的概念字典向量初始化 SAE 的解码器权重，加速收敛并提升特征质量。
    *   **自动标注**：通过计算 SAE 原子（Atoms）与字典中已知安全/不安全概念向量的余弦相似度，自动为 SAE 特征贴上语义标签。
    *   **推理干预**：在推理时，实时监测激活值，抑制（置零）不安全原子的系数，增强（放大）安全原子的系数。

### 3. 实验设计
*   **目标模型**：Qwen2.5-VL-7B-Instruct, LLaVA1.6-Mistral-7B, InternVL3.5-8B-Instruct。
*   **安全基准（Safety Benchmarks）**：MM-SafetyBench, JailBreakV-28K。
*   **通用能力基准（Utility Benchmarks）**：MMMU（多学科理解）, MM-Vet（综合能力）, MOSSBench（过度拒绝测试）, 以及困惑度（PPL）和流畅度分析。
*   **对比方法**：无干预（No Steering）、安全提示（Prompting）、激活叠加（ActAdd）、以及 PaCE/MOP 等基准引导方法。
*   **评测指标**：防御成功率（由 RoBERTa-SafeEdit 和 Qwen3Guard 作为裁判）、模型准确率、生成时间开销。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到了 SAE 训练使用了 Adam 优化器，Batch Size 为 1024，总步数为 30,000 步。
*   **推理开销**：详细记录了时间成本，DACO 相比原始模型增加了约 **14.62%** 的单 Token 生成时间，这在实时应用中属于可接受范围。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种主流开源 MLLM 架构。
    *   在 2 个大规模安全基准上进行了测试，涉及十几个安全类别（如非法活动、仇恨言论、隐私侵犯等）。
    *   进行了详尽的**消融实验**，验证了字典初始化、不同 SAE 变体（L1 vs TopK）、标注阈值 $\eta$ 和增强强度 $\gamma$ 的影响。
*   **客观性**：使用了多种第三方“裁判模型”进行安全评估，并专门测试了“过度拒绝”（Over-refusal），确保模型在变安全的同时不会变得“谨小慎微”而无法回答正常问题。实验设计较为全面、客观。

### 6. 主要结论与发现
*   **显著提升安全性**：DACO 在所有测试模型和基准上均取得了最佳的防御效果，显著优于提示工程和简单的激活叠加。
*   **保持通用能力**：在 MMMU 等任务中，DACO 的性能下降极小，远低于其他强力干预手段。
*   **高效性**：通过 SAE 进行干预比复杂的稀疏编码求解（MOP）快得多，适合部署。
*   **可解释性**：通过 SAE 原子的激活情况，可以直观看到模型在处理恶意查询时哪些“有害概念”被激活了。

### 7. 优点与亮点
*   **大规模多模态字典**：DACO-400K 是目前已知针对 MLLM 激活控制最大的概念数据集，填补了该领域的空白。
*   **自动标注技术**：解决了 SAE 特征难以解释的痛点，无需人工标注即可识别数万个特征的语义。
*   **训练与推理结合**：利用先验知识（字典）引导无监督学习（SAE），既保证了特征的表达力，又保证了语义的准确性。

### 8. 不足与局限
*   **推理延迟**：虽然比部分方法快，但 14% 的额外延迟对于极高性能要求的场景仍有影响。
*   **字典依赖**：防御效果受限于初始字典的覆盖范围，如果攻击涉及字典中未包含的极冷门概念，效果可能打折扣。
*   **潜在风险**：论文提到这种技术如果被恶意利用，也可能被用来“反向引导”模型，使其更容易产生有害内容（即移除安全概念）。

（完）
