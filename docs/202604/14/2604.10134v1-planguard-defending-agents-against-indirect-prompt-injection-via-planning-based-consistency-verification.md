---
title: "PlanGuard: Defending Agents against Indirect Prompt Injection via Planning-based Consistency Verification"
title_zh: PlanGuard：通过基于规划的一致性验证防御智能体免受间接提示注入攻击
authors: "Guangyu Gong, Zizhuang Deng"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10134v1"
tags: ["query:agsec"]
score: 9.0
evidence: 通过动作验证防御Agent免受间接提示注入攻击
tldr: "PlanGuard 是一种无需训练的防御框架，旨在保护 LLM 智能体免受间接提示注入（IPI）攻击。该方法基于上下文隔离原则，通过独立的规划器仅依据用户指令生成参考动作集，并结合层次化验证机制（硬约束与意图验证器）对实际执行的工具调用进行一致性检查。实验表明，它能将攻击成功率降至 0%，同时保持极低的误报率，具有极强的通用性。"
selection_source: fresh_fetch
motivation: 现有的间接提示注入防御方法多集中于预处理阶段，忽视了对模型实际执行行为的实时监控。
method: 引入隔离的规划器生成参考动作，并利用层次化验证机制对工具调用的合法性及参数意图进行双重校验。
result: "在 InjecAgent 基准测试中，PlanGuard 将攻击成功率从 72.8% 降低至 0%，且误报率仅为 1.49%。"
conclusion: PlanGuard 为 LLM 智能体提供了一种模型无关、高效且兼容性强的防御方案，有效解决了恶意指令劫持问题。
---

## 摘要
大语言模型（LLM）智能体正越来越多地集成到关键系统中，利用外部工具与现实世界进行交互。然而，这种能力使其面临间接提示注入（IPI）的风险，攻击者在检索到的内容中嵌入恶意指令，以操纵智能体执行未经授权或非预期的操作。现有的防御措施主要集中在预处理阶段，忽略了对模型实际行为的监控。在本文中，我们提出了 PlanGuard，这是一个基于上下文隔离（Context Isolation）原则的无需训练的防御框架。与以往的方法不同，PlanGuard 引入了一个隔离的规划器（Planner），该规划器仅根据用户指令生成一组有效的参考动作。此外，我们设计了一种分层验证机制，首先通过严格的硬约束来阻止未经授权的工具调用，随后利用意图验证器（Intent Verifier）来验证参数偏差是良性的格式差异还是恶意的劫持。在 InjecAgent 基准测试上的实验表明，PlanGuard 有效地抵御了这些攻击，将攻击成功率（ASR）从 72.8% 降低到 0%，同时保持了 1.49% 的可接受误报率。此外，我们的方法具有模型无关性且高度兼容。

## Abstract
Large Language Model (LLM) agents are increasingly integrated into critical systems, leveraging external tools to interact with the real world. However, this capability exposes them to Indirect Prompt Injection (IPI), where attackers embed malicious instructions into retrieved content to manipulate the agent into executing unauthorized or unintended actions. Existing defenses predominantly focus on the pre-processing stage, neglecting the monitoring of the model's actual behavior. In this paper, we propose PlanGuard, a training-free defense framework based on the principle of Context Isolation. Unlike prior methods, PlanGuard introduces an isolated Planner that generates a reference set of valid actions derived solely from user instructions. In addition, we design a Hierarchical Verification Mechanism that first enforces strict hard constraints to block unauthorized tool invocations, and subsequently employs an Intent Verifier to validate whether parameter deviations are benign formatting variances or malicious hijacking. Experiments on the InjecAgent benchmark demonstrate that PlanGuard effectively neutralizes these attacks, reducing the Attack Success Rate (ASR) from 72.8% to 0%, while maintaining an acceptable False Positive Rate of 1.49%. Furthermore, our method is model-agnostic and highly compatible.

---

## 论文详细总结（自动生成）

这是一份关于论文《PlanGuard: Defending Agents against Indirect Prompt Injection via Planning-based Consistency Verification》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 智能体（Agent）在集成外部工具时，容易受到**间接提示注入（IPI）**攻击。攻击者将恶意指令嵌入网页、邮件等外部内容中，利用 LLM 无法区分“用户指令”与“外部数据”的架构缺陷（即**上下文混合**问题），诱导智能体执行未经授权的操作（如转账、删除文件或窃取数据）。
*   **研究背景**：现有的防御手段（如分类器检测、困惑度过滤、指令微调等）要么泛化性差，要么训练成本高，且往往在执行层缺乏有效的行为监控。

### 2. 方法论：核心思想与技术细节
PlanGuard 的核心思想是**上下文隔离（Context Isolation）**，通过将“规划”与“执行”解耦来确保安全。
*   **隔离规划器（Isolated Planner）**：当接收到用户指令时，一个完全隔离的 LLM 规划器（不接触任何外部检索数据）仅根据原始指令生成一份“参考动作集”（Sref）。由于该规划器看不见攻击载荷，其生成的计划被视为绝对可信的基准。
*   **层次化验证机制（Hierarchical Verification）**：
    *   **阶段 I：硬规则匹配**。将智能体实际生成的动作与参考集进行字符串匹配。如果工具名称不符，直接判定为“功能劫持”并拦截。
    *   **阶段 II：意图验证器（Intent Verifier）**。针对工具正确但参数不一致的情况（可能是由于 LLM 生成的随机性导致的良性格式差异），调用另一个 LLM 结合用户意图和智能体的推理过程（Thought）进行语义判断，识别是“良性变体”还是“参数劫持”。
*   **算法流程**：生成参考集 -> 捕获智能体动作 -> 硬规则过滤 -> 语义意图校验 -> 最终执行或拦截。

### 3. 实验设计
*   **数据集与场景**：使用 **InjecAgent** 基准测试集，包含 1,054 个测试用例，涉及 17 种用户工具和 62 种攻击工具。
*   **攻击类别**：
    *   **直接伤害（DH）**：如未经授权的资金转账、解锁智能锁。
    *   **数据窃取（DS）**：如将私人笔记发送给攻击者。
*   **对比方法**：
    1.  **Vanilla Agent**：无防御的原始智能体（为了严谨测试，加入了合规诱导提示词以绕过模型内置安全对齐）。
    2.  **Stage-I Only**：仅使用硬规则匹配的消融版本。
    3.  **PlanGuard**：完整的两阶段防御系统。
*   **基座模型**：使用 **DeepSeek-V3.2** 作为智能体、规划器和验证器的核心模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **原因分析**：由于 PlanGuard 是一个**无需训练（Training-free）**的框架，其核心开销在于推理阶段的额外 LLM 调用（规划器和验证器各一次），而非大规模模型训练，因此对算力的需求主要体现在推理延迟和 Token 消耗上。

### 5. 实验数量与充分性
*   **实验规模**：在 1,054 个标准化用例上进行了测试，涵盖了 DH 和 DS 两个核心安全维度。
*   **充分性评价**：
    *   **充分性**：实验包含了消融实验（验证阶段 II 的必要性），并针对 LLM 的随机性进行了误报率（FPR）分析，实验设计较为完整。
    *   **客观性**：通过引入“合规诱导提示词”强制模型执行外部指令，排除了模型自身安全对齐带来的干扰，从而更客观地衡量了防御框架本身的有效性。

### 6. 主要结论与发现
*   **极高的安全性**：PlanGuard 在 DH 和 DS 场景下均实现了 **0.0% 的攻击成功率（ASR）**，相比之下，无防御状态下的 ASR 高达 72.8%。
*   **兼顾实用性**：单纯的硬规则匹配会导致极高的误报率（约 27%-38%），而引入阶段 II 的意图验证后，**总误报率（FPR）降至 1.49%**。
*   **架构优势**：证明了通过“架构约束”而非“概率对齐”来解决 IPI 攻击是更稳健的路径。

### 7. 优点（亮点）
*   **确定性防御**：利用输入隔离确保了规划阶段的绝对纯净，从根源上切断了攻击路径。
*   **无需训练**：作为一种即插即用的防御插件，它不依赖特定的模型微调，具有极强的模型无关性和兼容性。
*   **语义容错**：通过意图验证器解决了 LLM 输出不一致导致的误报问题，平衡了安全与用户体验。

### 8. 不足与局限
*   **推理开销**：每次工具调用都需要额外的 LLM 推理步骤，会增加响应延迟和运营成本。
*   **上下文依赖的参数验证难题**：当用户指令本身高度依赖外部信息（例如“支付邮件里的账单”）时，由于规划器看不到邮件内容，它无法验证账单金额等具体参数的准确性，存在“信息不对称”导致的验证盲区。
*   **自适应攻击风险**：虽然论文讨论了抗性，但如果攻击者能精准操纵智能体的推理逻辑（Thought）来欺骗验证器，理论上仍存在被绕过的风险。

（完）
