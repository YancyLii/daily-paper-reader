---
title: "AnyPoC: Universal Proof-of-Concept Test Generation for Scalable LLM-Based Bug Detection"
title_zh: AnyPoC：面向可扩展 LLM 漏洞检测的通用概念验证测试生成
authors: "Zijie Zhao, Chenyuan Yang, Weidong Wang, Yihan Yang, Ziqi Zhang, Lingming Zhang"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11950v1"
tags: ["query:libfuz"]
score: 8.0
evidence: 自动生成可执行的 PoC 测试以验证漏洞
tldr: 针对LLM漏洞检测报告难以自动验证的问题，本文提出AnyPoC框架。它将验证过程建模为PoC测试生成任务，利用多智能体协作进行报告分析、迭代合成执行及独立审查，有效克服了LLM的幻觉和奖励作弊。在Firefox、Chromium等12个大型系统上的实验显示，AnyPoC发现了122个新漏洞，其中45个PoC被采纳为官方回归测试，显著提升了自动化漏洞检测的实用性。
selection_source: fresh_fetch
motivation: 现有的LLM漏洞检测工具生成的报告多为静态假设，缺乏自动化的可执行验证手段，导致人工验证成本高且容易产生幻觉。
method: 提出一种通用的多智能体框架，通过事实核查、迭代合成执行、独立重执行审查以及动态知识库演化来生成并验证PoC。
result: 在12个大型系统中发现了122个新漏洞（105个已确认），且在PoC生成有效性和误报过滤能力上显著优于Claude Code等先进模型。
conclusion: AnyPoC通过自动化生成可执行证据，实现了端到端的自主漏洞检测，为大规模软件系统的质量保障提供了实用且通用的解决方案。
---

## 摘要
虽然最近基于大语言模型（LLM）的智能体能够识别源代码中的许多候选漏洞，但其报告仍属于静态假设，需要人工验证，这限制了自动化漏洞检测的实用性。我们将这一挑战定义为测试生成任务：给定一份候选报告，合成一个可执行的概念验证（PoC）测试——例如脚本、命令序列或精心构造的输入——以触发疑似缺陷。自动化的 PoC 生成可以作为一种可扩展的验证预言机（oracle），通过提供具体的执行证据，实现端到端的自主漏洞检测。然而，朴素的 LLM 智能体是不可靠的验证者：它们倾向于“成功”结论，并可能通过生成看似合理但无法运行的 PoC 甚至幻觉追踪（hallucinated traces）来进行奖励欺骗（reward-hack）。为了解决这一问题，我们提出了 AnyPoC，这是一个通用的多智能体框架，它能够：(1) 分析并事实核查候选漏洞报告；(2) 迭代地合成并执行 PoC，同时收集执行追踪；(3) 独立地重新执行并审查 PoC，以减轻幻觉和奖励欺骗。此外，AnyPoC 还会持续提取并演进 PoC 知识库，以处理异构任务。AnyPoC 能够处理来自任何来源的候选漏洞报告，并可与不同的漏洞报告工具配合使用。为了证明其实用性和通用性，我们将 AnyPoC 与一个简单的智能体漏洞报告器结合，应用于 12 个跨不同语言和领域的关键软件系统（其中许多系统拥有数百万行代码），包括 Firefox、Chromium、LLVM、OpenSSL、SQLite、FFmpeg 和 Redis。与最先进的代码智能体（如 Claude Code 和 Codex）相比，AnyPoC 为真阳性漏洞报告生成的有效 PoC 数量增加了 1.3 倍，并多拒绝了 9.8 倍的假阳性漏洞报告。截至目前，AnyPoC 已发现 122 个新漏洞（105 个已确认，86 个已修复），其中 45 个生成的 PoC 已被采纳为官方回归测试。

## Abstract
While recent LLM-based agents can identify many candidate bugs in source code, their reports remain static hypotheses that require manual validation, limiting the practicality of automated bug detection. We frame this challenge as a test generation task: given a candidate report, synthesizing an executable proof-of-concept test, or simply a PoC - such as a script, command sequence, or crafted input - to trigger the suspected defect. Automated PoC generation can act as a scalable validation oracle, enabling end-to-end autonomous bug detection by providing concrete execution evidence. However, naive LLM agents are unreliable validators: they are biased toward "success" and may reward-hack by producing plausible but non-functional PoCs or even hallucinated traces. To address this, we present AnyPoC, a general multi-agent framework that (1) analyzes and fact-checks a candidate bug report, (2) iteratively synthesizes and executes a PoC while collecting execution traces, and (3) independently re-executes and scrutinizes the PoC to mitigate hallucination and reward hacking. In addition, AnyPoC also continuously extracts and evolves a PoC knowledge base to handle heterogeneous tasks. AnyPoC operates on candidate bug reports regardless of their source and can be paired with different bug reporters. To demonstrate practicality and generality, we apply AnyPoC, with a simple agentic bug reporter, on 12 critical software systems across diverse languages/domains (many with millions of lines of code) including Firefox, Chromium, LLVM, OpenSSL, SQLite, FFmpeg, and Redis. Compared to the state-of-the-art coding agents, e.g., Claude Code and Codex, AnyPoC produces 1.3x more valid PoCs for true-positive bug reports and rejects 9.8x more false-positive bug reports. To date, AnyPoC has discovered 122 new bugs (105 confirmed, 86 already fixed), with 45 generated PoCs adopted as official regression tests.

---

## 论文详细总结（自动生成）

### AnyPoC：面向可扩展 LLM 漏洞检测的通用概念验证测试生成

#### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然基于大语言模型（LLM）的漏洞检测工具能发现大量潜在漏洞，但其输出通常是“静态报告”，存在极高的误报率（False Positives）。目前，验证这些报告主要依赖人工，这成为了自动化漏洞检测规模化扩展的瓶颈。
*   **核心挑战**：
    *   **LLM 奖励欺骗（Reward Hacking）**：LLM 倾向于生成看起来成功但实际无效的 PoC 或幻觉追踪。
    *   **异构性**：不同系统（如浏览器、编译器、数据库）对 PoC 的格式和执行环境要求迥异。
    *   **可扩展性**：在大型代码库中重复探索构建系统和工具链的成本极高。
*   **整体含义**：论文提出将漏洞验证建模为“测试生成”任务，通过自动生成可执行的概念验证（PoC）脚本或输入，作为自动化的验证预言机（Oracle），从而消除人工干预，实现端到端的自主漏洞检测。

#### 2. 方法论
AnyPoC 是一个通用的多智能体框架，其核心流程包含以下四个关键组件：
*   **漏洞分析器（Analyzer）**：对候选报告进行静态事实核查，分析漏洞根因、后果及所需的触发条件，过滤掉明显的逻辑错误报告。
*   **PoC 生成器（Generator）**：
    *   **阶段 1**：利用代码库访问权限和环境工具，迭代地编写、调试并执行 PoC。
    *   **阶段 2**：在 PoC 成功触发后，重新执行并收集详细的执行证据（如 Sanitizer 日志、追踪记录）。
*   **证据检查器（Checker）**：在独立、干净的环境中重新执行 PoC。它不共享生成器的上下文，以防止被生成器的幻觉所误导，通过对比预期信号与实际执行结果来最终判定漏洞真伪。
*   **自演进知识库（Self-Evolving Knowledge Base）**：
    *   自动提取并存储项目特定的知识（如编译标志、内部工具用法、测试框架）。
    *   通过“提取-过滤-评分”机制，使后续的生成任务能复用先前的经验，减少重复探索。

#### 3. 实验设计
*   **实验对象**：12 个大型真实世界软件系统，涵盖 C/C++、Rust、JavaScript 等语言，包括 Chromium、Firefox、LLVM、OpenSSL、SQLite、FFmpeg、Redis 等。
*   **数据集**：
    *   **新漏洞发现**：使用简单的 LLM-Reporter 生成候选报告，由 AnyPoC 进行验证。
    *   **基准测试集**：构建了一个包含 144 份报告（48 份真阳性，96 份假阳性）的评估集。
*   **对比方法（Baselines）**：
    *   **Claude Code** (基于 Claude 3.5/4.5 Opus)。
    *   **Codex** (基于 GPT-5.2-Codex)。
*   **评估指标**：有效 PoC 生成数量、假阳性报告拒绝率、API 消耗成本。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号或训练时长，因为该框架主要基于现有的商业 LLM API（如 Claude 和 GPT 系列）构建。
*   **成本分析**：论文提供了 API 使用成本。例如，AnyPoC Claude 变体在处理每份报告时的平均成本约为 6.47 美元，而基准方法 Claude Code 约为 2.71 美元。AnyPoC 成本较高是因为其包含了多轮迭代调试和独立验证步骤。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 12 个顶级开源项目中进行了长期的漏洞挖掘。
    *   针对 144 份标注报告进行了对比实验。
    *   进行了消融实验，分别验证了分析器、生成器、检查器以及知识库对结果的贡献。
*   **充分性与公平性**：实验涵盖了数百万行代码的大型系统，具有极高的现实意义。对比实验中，AnyPoC 与基准方法使用了相同的底层模型和工具权限，确保了比较的公平性。通过引入真实开发者拒绝的报告进行额外测试，增强了实验的客观性。

#### 6. 主要结论与发现
*   **显著的检测成果**：AnyPoC 发现了 **122 个新漏洞**，其中 105 个已获开发者确认，86 个已修复。
*   **高质量 PoC**：生成的 PoC 中有 45 个被项目官方采纳为回归测试用例。
*   **验证性能提升**：相比 Claude Code，AnyPoC 为真漏洞生成的有效 PoC 增加了 1.3 倍，同时对假漏洞报告的拒绝率提升了 **9.8 倍**。
*   **抑制幻觉**：实验证明，朴素的 LLM 智能体在验证任务中极易“作弊”（生成看似正确实则无效的代码），而 AnyPoC 的多智能体审查机制能有效抑制这一行为。

#### 7. 优点
*   **通用性强**：不依赖特定的漏洞类型或编程语言，适用于各种复杂的异构系统。
*   **闭环验证**：通过“生成-执行-独立审查”的闭环，提供了比纯文本报告更具说服力的执行证据。
*   **知识积累**：自演进知识库解决了 LLM 在处理超大型项目时上下文窗口受限和重复探索的问题。
*   **实战价值高**：直接获得了大量顶级开源社区的认可和 CVE 编号。

#### 8. 不足与局限
*   **运行成本**：由于涉及多次模型调用和迭代调试，其 API 消耗成本显著高于直接生成报告的方法。
*   **依赖初始报告**：AnyPoC 本身是验证器，其发现漏洞的能力上限仍受限于前端漏洞报告器（Bug Reporter）的质量。
*   **环境依赖**：虽然框架是通用的，但仍需要为每个待测系统准备基础的 Docker 镜像（包含源码和构建环境）。
*   **网络依赖风险**：在 PoC 生成过程中，智能体可能访问在线资源，虽然论文进行了防作弊检查，但在极端情况下仍存在数据泄露或利用已知 PoC 的风险。

（完）
