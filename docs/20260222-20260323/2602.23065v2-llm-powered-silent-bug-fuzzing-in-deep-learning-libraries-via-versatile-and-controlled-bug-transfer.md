---
title: LLM-Powered Silent Bug Fuzzing in Deep Learning Libraries via Versatile and Controlled Bug Transfer
title_zh: 基于大语言模型驱动的深度学习库静默漏洞模糊测试：通过多功能且受控的漏洞迁移
authors: "Kunpeng Zhang, Dongwei Xiao, Daoyuan Wu, Shuai Wang, Jiali Zhao, Yuanyi Lin, Tongtong Xu, Shaohua Wang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23065v2"
tags: ["query:libfuz"]
score: 9.0
evidence: 大模型驱动的深度学习库模糊测试与API匹配
tldr: 针对深度学习库中难以检测的静默错误（Silent Bugs），本文提出了一种基于大语言模型（LLM）的模糊测试工具TransFuzz。该工具通过挖掘历史漏洞报告中的模式，利用LLM将高风险上下文和测试先验知识迁移到功能相似的API中，并自动生成带有定制化Oracle的测试用例。实验表明，TransFuzz在PyTorch、TensorFlow和MindSpore中发现了79个新漏洞，显著提升了静默错误的检测能力。
selection_source: fresh_fetch
motivation: 现有的深度学习库模糊测试技术难以检测不触发崩溃但结果错误的静默漏洞，且缺乏有效的测试程序和Oracle。
method: 利用LLM从历史漏洞中提取模式，通过功能嵌入匹配相似API，并结合自验证模块实现受控的漏洞模式迁移与测试生成。
result: 在三大主流深度学习框架中成功发现了79个此前未知的漏洞，其中12个被确认为CVE。
conclusion: 该研究证明了利用LLM进行跨API的漏洞模式迁移是检测深度学习库静默错误的有效途径。
---

## 摘要
深度学习（DL）库广泛应用于关键应用中，即使是细微的静默漏洞也可能导致严重后果。尽管现有的深度学习模糊测试技术在检测崩溃方面取得了进展，但由于缺乏有效的测试程序和相应的测试预言机（oracles），它们在检测静默漏洞方面仍面临固有挑战。基于历史漏洞报告包含丰富的、未被充分利用的静默漏洞信息这一观察，我们利用大语言模型（LLM）进行多功能且受控的漏洞迁移，以实现静默漏洞模糊测试。具体而言，我们的方法使用 LLM 从历史问题中提取上下文感知的漏洞模式，利用基于功能的嵌入匹配语义相关的应用程序编程接口（API），并合成带有定制预言机的测试用例。通过将高风险上下文和预言机设计从已知的有漏洞 API 迁移到功能相似的目标 API，该方法能够主动检测静默漏洞。为了确保上下文感知漏洞迁移的可靠性，我们引入了一个由 LLM 驱动的自验证模块，系统地评估每个迁移漏洞实例的有效性。我们在名为 TransFuzz 的工具中实现了这一方法，并在三个主流深度学习库（PyTorch、TensorFlow 和 MindSpore）上进行了评估。TransFuzz 成功发现了 10 种漏洞类型中的 79 个此前未知的漏洞（其中 12 个被确认为通用漏洞披露 (CVE)），证明了其在迁移深度学习库漏洞发现能力方面的有效性和通用性。

## Abstract
Deep learning (DL) libraries are widely used in critical applications, where even subtle silent bugs can lead to serious consequences. While existing DL fuzzing techniques have made progress in detecting crashes, they inherently struggle to detect silent bugs due to the lack of effective test programs and corresponding oracles.   Building on the observation that historical bug reports contain rich, underutilized information about silent bugs, we leverage large language models (LLMs) to perform versatile yet controlled bug transfer for silent bug fuzzing. Specifically, our approach uses LLMs to extract context-aware bug patterns from historical issues, match semantically related Application Programming Interfaces (APIs) using functionality-based embeddings, and synthesize test cases with customized oracles. This enables proactive detection of silent bugs by transferring high-risk contexts and oracle designs from known buggy APIs to functionally similar target APIs. To ensure the reliability of our context-aware bug transfer, we introduce an LLM-powered self-validation module that systematically evaluates the validity of each transferred bug instance. We implement this methodology in a tool named TransFuzz and evaluate it on three mainstream DL libraries: PyTorch, TensorFlow, and MindSpore. TransFuzz successfully discovers 79 previously unknown bugs (12 confirmed as Common Vulnerabilities and Exposures (CVEs)) in 10 bug types, demonstrating its effectiveness and generalizability in migrating DL library bug discovery capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-Powered Silent Bug Fuzzing in Deep Learning Libraries via Versatile and Controlled Bug Transfer》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：深度学习（DL）库中的**静默漏洞（Silent Bugs）**检测。这类漏洞（如计算结果错误、梯度异常、性能下降）不会导致程序崩溃，因此传统的基于崩溃（Crash）或简单差异测试的模糊测试工具难以捕捉。
*   **研究动机**：
    *   **缺乏 Oracle**：静默漏洞没有通用的判定标准（Oracle），其正确性高度依赖于 API 语义和调用上下文。
    *   **测试空间巨大**：API 组合、输入数据和触发条件的组合爆炸，使得盲目搜索效率极低。
    *   **历史数据未被利用**：作者观察到，历史漏洞报告中蕴含了丰富的“高风险模式”，这些模式在功能相似的 API 中往往会重复出现。

### 2. 方法论：TransFuzz 框架
核心思想是**“迁移后验证”（Transfer-then-Verify）**，利用 LLM 的推理能力实现受控的漏洞模式迁移。
*   **漏洞模式提取**：使用 LLM（如 o3-mini）从 GitHub 的历史 Issue 和 PR 中提取“上下文感知漏洞模式”，包含：漏洞 API、触发上下文（输入/环境）和 Oracle 设计（判定逻辑）。
*   **功能性 API 匹配**：不依赖语法相似性，而是利用 LLM 生成 API 的功能描述，通过 Embedding 向量化并计算余弦相似度，识别出可能存在类似漏洞的“目标 API”。
*   **漏洞迁移驱动的模糊测试**：
    *   **反馈驱动选择**：优先测试相似度高的 API，若发现漏洞则进一步探索其邻近 API。
    *   **测试合成**：利用 LLM 将源漏洞的逻辑适配到目标 API，自动生成触发代码和定制化的断言（Oracle）。
*   **LLM 驱动的自验证**：
    *   **程序插桩**：通过 AST 变换在测试代码中插入监控点，获取运行时变量日志。
    *   **多重过滤**：LLM 结合日志进行“症状相似性分析”、“Oracle 正确性校验”和“基于准则的漏洞判定”，以剔除因 LLM 幻觉产生的误报。

### 3. 实验设计
*   **实验对象**：三大主流框架 PyTorch、TensorFlow 和 MindSpore。
*   **数据集**：从 PyTorch 官方仓库筛选出的 7,466 条历史 Issue 作为漏洞模式源。
*   **对比方法（Baselines）**：
    *   通用 API 模糊测试工具：FreeFuzz, DeepREL, ACETest。
    *   基于 LLM 的工具：TitanFuzz, DFUZZ。
    *   专用工具：Pathfinder（针对内核函数）。
*   **评估指标**：发现的新漏洞数量、漏洞类型覆盖度、精度（Precision）、召回率（Recall）以及跨框架迁移能力。

### 4. 资源与算力
*   **硬件环境**：配备 256 GB RAM 的服务器，单张 **NVIDIA RTX A6000 GPU**，操作系统为 Ubuntu 20.04。
*   **模型使用**：
    *   模式提取：o3-mini。
    *   API 描述与测试生成：GPT-4o mini（兼顾成本与效率）。
    *   自验证：GPT-4.1 mini（强化推理）。
*   **成本开销**：在 PyTorch 上的完整测试流程总花费约为 **89.07 美元**。

### 5. 实验数量与充分性
*   **实验规模**：针对 PyTorch 生成了 **35,909 个测试程序**，覆盖了 **2,421 个 API**。
*   **消融实验**：
    *   测试了不同窗口大小（Window Size）对漏洞发现效率的影响。
    *   验证了 LLM 自验证模块中“重复检查次数”对稳定性的贡献。
    *   对比了多种主流 LLM（DeepSeek R1/V3, Qwen, Doubao 等）在自验证任务上的表现。
*   **充分性评价**：实验设计较为全面，不仅涵盖了新漏洞的发现，还通过消融实验验证了每个核心组件的必要性，并进行了跨框架的泛化性测试，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **漏洞发现**：共发现 **79 个此前未知的漏洞**，其中 31 个为静默漏洞，48 个为崩溃漏洞。
*   **安全贡献**：已有 **12 个漏洞被分配了 CVE 编号**，多数评级为中危（Medium）。
*   **静默漏洞特性**：发现的静默漏洞中，74% 已存在超过 3 年，96% 无法被传统的 CPU-GPU 差异测试检出，证明了定制化 Oracle 的必要性。
*   **有效性**：TransFuzz 在静默漏洞检测类型上覆盖了全部 9 种分类，远超现有工具（仅覆盖约 33%）。

### 7. 优点（亮点）
*   **首创性**：首次系统性地提出利用 LLM 进行跨 API 的静默漏洞模式迁移。
*   **语义深度**：通过功能嵌入（Functionality-based Embedding）实现了深层语义匹配，而非表面的函数名相似。
*   **闭环验证**：引入了基于运行时日志的自验证机制，有效解决了 LLM 在自动化测试中易产生高误报的顽疾。

### 8. 不足与局限
*   **误报残留**：尽管有自验证，仍存在约 28.5% 的误报率，主要源于 API 文档不完整导致 LLM 对“预期行为”理解偏差。
*   **复杂逻辑瓶颈**：对于涉及编译器内部复杂优化（如浮点数精度微差）的场景，LLM 仍难以完全替代人类专家的深度分析。
*   **跨框架静默迁移挑战**：由于不同框架（如 PyTorch vs TensorFlow）的底层实现差异巨大，跨框架迁移目前在崩溃检测上效果较好，但在极度依赖语义的静默漏洞上仍有提升空间。

（完）
