---
title: LLM-Guided Safety Agent for Edge Robotics with an ISO-Compliant Perception-Compute-Control Architecture
authors: "Xu Huang, Ruofan Zhang, Lu Cheng, Yuefeng Song, Xu Huang, Huayu Zhang, Sheng Yin, Anyang Liang, Chen Qian, Yin Zhou, Xiaoyun Yuan, Yuan Cheng"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20193v1"
tags: ["query:agsec"]
score: 8.0
evidence: 机器人自主智能体的安全控制策略
tldr: 本研究针对人机交互中AI感知的概率性与工业安全标准确定性要求之间的矛盾，提出了一种大语言模型（LLM）引导的边缘机器人安全代理。该系统采用符合ISO标准的感知-计算-控制架构，利用LLM将自然语言安全规约转化为可执行谓词，并通过对称双模块冗余设计在双RK3588硬件上实现。实验证明，该方案能在低成本边缘设备上满足ISO 13849 Cat 3和PL d安全等级，为安全关键型具身智能的实际部署提供了有效路径。
selection_source: carryover_cache
motivation: 工业标准要求确定性行为，而AI感知具有概率性，导致人机交互中的功能安全难以保障。
method: 提出一种由大语言模型引导的安全代理，通过双模块冗余架构将自然语言安全法规转化为可执行谓词。
result: 在双RK3588平台上实现了低延迟的感知-计算-控制闭环，达到了ISO 13849 Category 3和PL d安全等级。
conclusion: 该研究为在成本受限的边缘硬件上部署符合工业安全标准的具身智能提供了可行路径。
---

## Abstract
Ensuring functional safety in human-robot interaction is challenging because AI perception is inherently probabilistic, whereas industrial standards require deterministic behavior. We present an LLM-guided safety agent for edge robotics, built on an ISO-compliant low-latency perception-compute-control architecture. Our method translates natural-language safety regulations into executable predicates and deploys them through a redundant heterogeneous edge runtime. For fault-tolerant closed-loop execution under edge constraints, we adopt a symmetric dual-modular redundancy design with parallel independent execution for low-latency perception, computation, and control. We prototype the system on a dual-RK3588 platform and evaluate it in representative human-robot interaction scenarios. The results demonstrate a practical edge implementation path toward ISO 13849 Category 3 and PL d using cost-effective hardware, supporting practical deployment of safety-critical embodied AI.

---

## 论文详细总结（自动生成）

以下是对论文《LLM-Guided Safety Agent for Edge Robotics with an ISO-Compliant Perception-Compute-Control Architecture》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着工业5.0的发展，人机协作（HRC）变得日益频繁。传统的机器人安全协议（如ISO/TS 15066）通常采用简单的“距离监测”，将工作空间视为二进制区域，缺乏语义理解能力（例如无法区分授权操作员与入侵者）。
*   **核心痛点**：
    1.  **语义瓶颈**：传统传感器无法识别复杂的危险行为或目标身份。
    2.  **确定性冲突**：AI感知本质上是概率性的（Probabilistic），而工业安全标准（如ISO 13849）要求行为必须是确定性的（Deterministic）。
    3.  **边缘约束**：在低成本边缘硬件上，软件延迟的不确定性（长尾延迟）难以满足严格的安全时间预算。
*   **论文目标**：提出一种由大语言模型（LLM）引导的安全代理，通过符合ISO标准的感知-计算-控制（PPC）架构，在边缘设备上实现高语义、高可靠的安全保障。

### 2. 方法论
论文提出了一套从自然语言规约到硬件执行的顶层设计框架：
*   **多维安全语义定义**：将安全从一维距离扩展到三维：空间安全保护（物理边界）、异常目标识别（身份验证）、异常行为识别（意图预测）。
*   **LLM引导的安全形式化**：
    *   **核心思想**：利用LLM作为“安全专家”，解析自然语言形式的工业标准（如ISO 13849-1）。
    *   **技术细节**：通过特定提示词工程（Prompt Engineering），LLM提取关键变量（最大速度 $v_{max}$、系统延迟 $T_{stop}$、制动距离 $d_{brake}$），并将其转化为数学谓词（Predicate），如：$S_{safe}(t) \iff d(t) \geq v_{max} \cdot T_{stop} + d_{offset}$。
*   **感知-计算-控制（PPC）架构**：
    *   **空间对齐**：将AI检测到的像素坐标投影至3D机器人坐标系，划分停止区、警告区和安全区。
    *   **时间对齐**：对感知、推理、后处理全链路进行“最坏情况执行时间”（WCET）分析，确保总延迟受限。
*   **冗余硬件与可靠执行**：
    *   **对称双模块冗余（DMR）**：采用两块瑞芯微 RK3588 SoC 构成双通道。
    *   **并行独立执行（PIE）**：两个节点独立处理视觉流并输出控制信号，通过UART心跳和ADC（模数转换器）硬件探测进行交叉监控。
    *   **自愈机制**：一旦检测到单点故障（如NPU死机或电压异常），系统立即触发安全状态并尝试自动重启故障节点。

### 3. 实验设计
*   **实验场景**：设置了三种代表性的人机交互场景：场景1（标准工作空间，单工人）、场景2（部分遮挡环境）、场景3（多目标复杂布局）。
*   **数据集**：
    *   **基础任务**：使用自定义的人机交互（HRI）数据集验证空间保护。
    *   **高级语义任务**：使用 IPAD（工业过程异常检测）、HR-STC、HR-Ubnormal、HR-Avenue 等公开数据集评估异常目标和行为识别的准确性。
*   **Benchmark 与对比**：
    *   主要指标为**端到端信号延迟（$T_{stop}$）**及其标准差，用于衡量确定性。
    *   使用 **AUC（曲线下面积）** 评估异常检测任务的性能。
    *   对比了不同场景下的 WCET 表现，验证系统在峰值负载下的稳定性。

### 4. 资源与算力
*   **硬件平台**：原型系统基于双 **Rockchip RK3588** SoC。每个 SoC 包含 8 核 CPU 和 **6 TOPS** 算力的 NPU。
*   **软件优化**：使用 **RKNN-Toolkit2** 进行模型量化（INT8）和 NPU 加速，代码采用 C++17 编写以确保执行的确定性。
*   **算力说明**：论文未提及 LLM 的具体训练算力（LLM 主要在设计阶段作为离线工具使用），边缘端侧重于轻量化模型的推理性能。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **10,000 次** 推理循环的性能分析，以获取统计学意义上的 WCET 数据。
*   **故障注入实验**：针对心跳丢失、NPU 死机、电源跌落、传感器故障等 4 类典型故障进行了注入测试，记录了检测时间（$T_{det}$）和恢复时间（$T_{rec}$）。
*   **充分性评价**：实验设计较为全面，不仅覆盖了实时性能分析，还通过故障注入验证了工业标准要求的“单点故障不失效”原则。实验结果客观地展示了系统在处理多目标时的延迟波动。

### 6. 主要结论与发现
*   **实时性**：系统平均延迟约为 35-40ms，WCET 严格控制在 60ms 以内，能够满足工业安全制动的实时性要求。
*   **可靠性**：通过 ADC 硬件探测，NPU 死机的检测延迟低至 **2.04ms**，远快于传统的软件看门狗。
*   **合规性**：该架构为在低成本边缘硬件上实现 **ISO 13849 Category 3** 和 **Performance Level d (PL d)** 提供了一条可行的技术路径。

### 7. 优点
*   **语义增强**：通过 LLM 将抽象的法律法规转化为可执行的机器逻辑，解决了 AI 感知与安全标准之间的语义断层。
*   **确定性保障**：通过 WCET 分析和 LIFO 队列管理，有效抑制了 AI 推理中的随机延迟抖动。
*   **硬件鲁棒性**：DMR + PIE 设计实现了真正的硬件级冗余和自愈，适合安全关键型应用。

### 8. 不足与局限
*   **LLM 实时性限制**：目前 LLM 仅用于设计阶段的静态规约生成，无法在运行时根据复杂环境动态调整安全谓词。
*   **边缘算力瓶颈**：高级语义任务（如复杂行为识别）的 AUC 在边缘端仍有提升空间，部分高负载任务目前仍需主机端支持。
*   **环境适应性**：实验主要在受控的实验室场景下完成，对于极端工业环境（如强电磁干扰、粉尘遮挡）的鲁棒性尚待进一步验证。

（完）
