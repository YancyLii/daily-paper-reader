---
title: "AnchorKV: Safety-Aware KV Cache Compression via Soft Penalty with a Refusal Anchor"
title_zh: AnchorKV：通过拒绝锚点的软惩罚实现安全感知的KV缓存压缩
authors: "Ning Ni, Yingjie Lao"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17872v1"
tags: ["query:agsec"]
score: 9.0
evidence: 安全感知的KV缓存压缩，利用拒绝锚点保持安全对齐以防御越狱攻击。
tldr: 大型语言模型推理中，KV缓存成为内存瓶颈，现有压缩方法虽通过保留注意力相关标记降低成本，却面临越狱攻击失效或激进驱逐导致安全对齐恶化的风险。AnchorKV提出基于安全锚的压缩增强技术，离线利用表征工程方法在层特定键投影空间构建拒绝方向，并在推理时依据软惩罚规则调整标记保留分数，使其远离有害提示方向。实验证明，该方法仅以极少性能损失，便实现安全对齐的显著提升，且当惩罚系数为零时完全还原原始压缩器行为。AnchorKV可即插即用于各类压缩器，为安全高效的LLM部署提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17872v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1377, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1112, \"height\": 822, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1360, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1093, \"height\": 680, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17872v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1087, \"height\": 651, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17872v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1629, \"height\": 558, \"label\": \"Table\"}]"
motivation: 现有KV缓存压缩方法无法抵御越狱攻击，且在激进驱逐下安全对齐严重退化，缺乏安全考虑。
method: 构建离线安全锚，利用差异均值表征工程在层特定键投影空间计算拒绝方向，并施加软惩罚调整标记保留分数。
result: 以极小性能代价实现安全对齐显著提升，惩罚归零时还原原始压缩器效果。
conclusion: AnchorKV作为一种即插即用的安全增强模块，可广泛应用于各类KV缓存压缩器，以低开销实现高效安全防御。
---

## 摘要
大型语言模型（LLM）在生成推理和长上下文任务上优于早期架构，但其庞大的规模在内存使用、能耗和设备端部署方面带来了显著挑战。由于扩展预训练语言模型能提升下游能力，键值（KV）缓存成为了主要的推理瓶颈。最近的KV缓存压缩方法通过仅保留与注意力相关的令牌子集来降低这一成本。然而，尽管这些方法在良性工作负载中保持了准确性，其压缩策略要么未能抵御越狱攻击，要么在激进驱逐下削弱了安全对齐。我们提出AnchorKV，这是一种对KV缓存压缩的即插即用改进，通过使令牌保留分数偏离与有害提示相关的键空间方向来产生偏置。AnchorKV通过将均值差异表示工程方法适配到KV缓存所用的特定层键投影空间，构建了一个离线安全锚点。基于此锚点，软惩罚令牌选择规则以少量效用为代价，大幅改善了安全对齐，并在惩罚为零时退化为原始压缩器。

## Abstract
Large language models (LLMs) outperform earlier architectures on generative inference and long-context tasks, but their large size introduces significant challenges in memory usage, energy cost, and on-device deployment. Since scaling pre-trained language models improves downstream capability \cite{zhao2023survey}, the key-value (KV) cache becomes a dominant inference bottleneck. Recent KV cache compression methods \cite{jo2025fastkv,li2024snapkv,zhou2024dynamickv} reduce this cost by retaining only a subset of attention-relevant tokens. However, while these approaches preserve accuracy on benign workloads, their compression policies either fail to defend against jailbreak attacks \cite{jiang2024robustkv} or degrade safety alignment under aggressive eviction.   We propose AnchorKV, a drop-in modification to KV cache compression that biases token retention scores away from directions in key space associated with harmful prompts. AnchorKV constructs an offline safety anchor by adapting a difference-of-means representation engineering approach \cite{arditi2024refusal,zou2023representation} to the layer-specific key projection space used in KV caching. Based on this anchor, a soft penalty token selection rule trades a small amount of utility for substantially improved safety alignment, while reducing to the original compressor when the penalty is zero.

---

## 论文详细总结（自动生成）

### 论文：AnchorKV: 基于拒绝锚点的安全感知KV缓存压缩

#### 1. 核心问题与整体含义

本文聚焦于**大型语言模型（LLM）推理时KV缓存压缩与模型安全对齐之间存在矛盾、但鲜有研究**的关键问题。

-   **研究背景与动机**：
    -   **KV缓存成为瓶颈**：LLM的上下文窗口不断扩展，导致键值（KV）缓存在推理时成为内存和延迟的主要瓶颈。现有压缩方法（如FastKV）通过仅保留注意力相关的令牌子集，有效降低了成本。
    -   **压缩技术存在安全隐患**：现有压缩方法的设计仅以“任务准确性”为目标，忽略了安全对齐。作者通过初步实验发现一个关键现象：**激进的令牌驱逐不仅无法防御越狱攻击，反而会严重侵蚀模型的安全对齐**。
        -   **现象1：拒绝证据稀释**。在注意力贪心策略下，越狱攻击的诱导框架可能获得高注意力分数，而有害查询令牌反而被驱逐，导致下游层缺乏足够信息触发拒绝机制。
        -   **现象2：模板模式退化**。当有害内容令牌被丢弃，而通用的无害骨架令牌被保留时，模型会退化为套用安全模板，给出与具体查询无关的“万能拒绝”回复。
    -   **研究缺口**：现有的防御方法（如RobustKV）基于注意力排序，无法与目标为压缩缓存的方法兼容，且其判别信号可能被对抗性攻击操纵。缺乏一种能与激进压缩操作共存、且信号独立于注意力排序的安全保护机制。

**整体含义**：本文旨在解决LLM高效推理中的一个被忽视的安全隐患，提出一种既能维持压缩收益、又能有效保持模型安全对齐的新范式。

#### 2. 方法论

本文提出**AnchorKV**，一种对现有KV缓存压缩器的**即插即用式**安全增强模块。其核心思想是在令牌保留决策中引入一个独立于注意力、基于几何表征的**安全偏置**（即软惩罚）。

**核心思想**：

-   利用表征工程（Representation Engineering）的研究发现，在模型的内部激活空间中存在一个代表“拒绝”的线性方向。
-   AnchorKV将此方向从残差流适配到KV缓存压缩所操作的**层特定键投影空间（K-Space）**，构建一个“有害锚点”。
-   在推理时，根据令牌的键状态在锚点方向上的投影得分，对原本的注意力重要性分数施加**软惩罚**，从而降低有害令牌被保留的概率。

**关键技术细节与算法流程**：

论文将方法分为离线构建和在线推理两个阶段。

-   **阶段一：离线锚点构建 (Alg. 1)**
    -   **目标**：在指定的驱逐层 `ℓ*` 的键投影空间中，计算出一个代表有害内容的线性方向 `vh` 和一个校准阈值 `θ`。
    -   **步骤**：
        1.  **数据准备**：收集两组长度匹配的对比提示集——有害提示（`Dtr_h`，来自AdvBench）和无害提示（`Dtr_s`，由Deepseek-V3通过意图反转生成）。
        2.  **键提取**：将所有提示输入冻结的LLM，通过前向钩子捕获在指定驱逐层 `ℓ*` 的每头键投影 `K(ℓ*)(p)`。
        3.  **计算方向**：对每个提示的所有令牌键进行均值池化，得到提示级别的表征 `ϕ(p)`。然后，计算两类提示均值之差，得到有害锚点方向 `vh`（公式 2）。
        4.  **层选择与阈值校准**：
            -   **层选择 (`ℓ*`)**：在候选层上重复上述步骤，并在一组验证集上评估其区分有害/无害的AUROC，选择AUROC最高的层作为操作层。
            -   **阈值校准 (`θ`)**：在验证集上，设定 `θ` 为有害类和无害类提示得分中位数的中点（公式 4）。**此步骤至关重要**，因为研究发现所有令牌的投影得分均为负值，存在共享偏移；若不校准阈值，将系统性地应用不足惩罚。

-   **阶段二：在线软惩罚选择 (Alg. 2)**
    -   **目标**：在每次触发预填充（prefill）时，修改FastKV的令牌选择规则，融入安全惩罚。
    -   **计算流程**：
        1.  **计算原始重要性分数**：获取FastKV基础的每令牌注意力重要性得分 `at`。
        2.  **计算有害得分**：对每个令牌 `t`，计算其键向量在单位化锚点 `cvh` 上的投影，并对所有头求平均，得到原始有害倾向 `ht`（公式 3）。
        3.  **应用阈值与安全保护**：
            -   `h+_t = max(ht - θ, 0)`：将原始得分校准到阈值以上。
            -   **接收域保护（Sink Protection）**：前 `S` 个令牌（默认4）因其异常统计属性被强制豁免惩罚。
            -   **重要性豁免（Importance Immunity）**：注意力得分 `at` 最高的 `α` 比例（默认30%）令牌被豁免惩罚，以防止惩罚破坏提示的内容主干。
        4.  **生成最终保留分数**（公式 5）：`score_t = at - λ · ā · fh+_t`。其中 `λ` 是惩罚强度（安全率），`ā` 是原始得分的均值（用于归一化），`fh+_t` 是经过上述安全保护后的最终有害强度。
        5.  **令牌选择**：根据最终的 `score_t` 进行top-k选择，决定哪些令牌的KV对被传播到后续层。

**关键设计属性**：

-   **连续性**：当 `λ=0` 时，`score_t = at`，AnchorKV在比特级别精确还原为未修改的FastKV基线，方便进行严格的消融实验。
-   **正交性**：安全信号（`fh+_t`）与基础压缩器的注意力信号（`at`）来源独立，互不干扰。

#### 3. 实验设计

实验主要围绕安全性和实用性两个维度展开评估。

-   **核心实验场景与数据集**：
    -   **安全性评估**：使用**越狱攻击成功率 (ASR)** 作为指标。在 `AdvBench` 数据集上，使用 `AdvPrompter` 方法生成对抗性提示进行攻击。
    -   **实用性评估**：使用 **`LongBench`** 基准测试套件（包含16个子任务）来衡量模型在良性长上下文任务上的理解能力，以验证安全增强是否损害了下游任务性能。
-   **目标与攻击模型**：
    -   **目标模型**：`Llama-3.1-8B-Instruct`（所有压缩和安全操作均在此模型上进行）。
    -   **攻击者模型**：主要使用 `Mistral-NeMo-Instruct-2407` 进行跨模型迁移攻击，也包含基于 `Llama-3.1-8B-Instruct` 的白盒自攻击作为参考。
-   **对比方法**：
    -   **`FullKV`**：无任何压缩，作为效用和安全性的上界参考。
    -   **`FastKV`**：未修改的KV缓存压缩基线方法，作为AnchorKV的主要比较对象。
    -   **`AnchorKV`（本文方法）**：在多个安全率（`λ`）下的表现，用于描绘安全-效用的权衡曲线。

#### 4. 资源与算力

**论文未明确说明实验所使用的具体算力资源**，如GPU型号、数量或总训练/推理时长。方法本身的离线锚点构建过程仅需在少量对比提示上推理冻结模型，计算开销极小；在线阶段增加的 `einsum` 操作，其计算复杂度 `O(BHLd)` 与单头注意力计算相当，相对完整模型前向传播而言开销不大。但实验整体花费的算力无法从文中得知。

#### 5. 实验数量与充分性

论文设计了多组、系统性且相互印证的实验，整体实验充分、客观且公平。

-   **核心机制验证**：
    -   进行**层选择扫描**（在8个候选层上），并报告每个层的AUROC和Cohen's d指标，为选择最优操作层提供了依据。
    -   在独立测试集上报告了**锚点区分有害/无害内容的能力**（AUROC=0.996），其分布和ROC曲线清晰有力，验证了核心假设。
-   **安全性与效用的双维度扫参**：
    -   在**8个不同惩罚强度（`λ`）** 下，分别测量越狱ASR和LongBench平均得分。这清晰揭示了**“有效区”、“快速生效区”和“反转区”三个性能区间**，并找到了安全-效用的帕累托最优点。实验设计全面，结论可靠。
    -   **比特级等价性验证**：在 `λ=0` 时，通过哈希输出验证了其与FastKV基线比特精确等价，实现了严格的消融实验。
    -   **细粒度效用分析**：Table 1 展示了LongBench全部16个子任务的得分，而非仅报告平均值，展示了效用损失的异质性，分析透明度高。
-   **对比基线公平**：将AnchorKV（带惩罚）与FastKV（无惩罚）和FullKV（无压缩）进行直接对比，公平地量化了压缩本身和引入安全惩罚各自带来的性能代价。

#### 6. 主要结论与发现

-   **核心发现**：通过在KV缓存压缩器的驱逐层键空间中引入一个离线构建的、代表“拒绝”的线性方向，并以软惩罚的形式干预令牌保留决策，可以在极小性能代价下，显著恢复并保持压缩后模型的安全对齐。
-   **量化结果**：
    -   **安全性大幅提升**：在安全有效的 `λ` 范围内（如 `λ=10`），AnchorKV将越狱ASR从FastKV基线的0.68**降低至0.13**，相对降幅达81%。
    -   **效用损失极小**：在取得最佳安全效果的 `λ=10` 处，LongBench平均分仅下降1.07分，而单纯的压缩（FullKV→FastKV）带来的损失就有1.59分。即，**额外引入安全机制的代价，小于压缩本身带来的性能损失**。
-   **关键现象**：发现了安全强度与性能之间的**非单调“反转”关系**。过高的惩罚强度（如 `λ=300`）会破坏内容主干，导致模型性能崩溃、安全防御失效，ASR反而升至0.78，超过了无保护的基线。

#### 7. 优点

-   **问题新颖且实际**：首次深入研究了现代KV缓存压缩技术对LLM安全对齐的直接影响，指出并命名了“拒绝证据稀释”和“模板模式退化”两种具体失败模式。
-   **方法设计巧妙**：
    -   **空间匹配**：将表征工程中的“拒绝方向”从残差流精确适配到KV压缩的实际操作空间（键投影空间），避免了投影误差。
    -   **即插即用与可扩展**：作为一个模块，可无缝集成到FastKV等基于重要性分数的压缩器上，无需修改模型权重或架构。
    -   **连续可调与精确还原**：惩罚强度 `λ` 连续可调，且在 `λ=0` 时比特精确还原为原始方法，为学术研究和工程实践提供了极大便利。
-   **实验设计严谨全面**：
    -   **自适应阈值校准**：发现并处理了键空间的共享负偏移问题，避免了惩罚失效。
    -   **设计结构性保护措施**：“接收域保护”和“重要性豁免”有效防止了安全机制损害模型的基本功能。
    -   **安全-效用权衡剖析清晰**：通过扫参实验揭示了非单调关系，对未来的工作具有警醒意义。

#### 8. 不足与局限

-   **表征能力的单一性**：该方法基于“单一线性拒绝方向”的假设。虽然实验证明非常有效，但安全对齐可能涉及更复杂（如非线性或多子空间）的表征，单一方向可能无法捕捉全貌。
-   **评估的泛化性有限**：
    -   **安全隐患**：评估仅限于一种攻击方法（AdvPrompter）和一种目标模型（Llama-3.1-8B-Instruct），其在不同攻击、不同模型架构和规模下的效果有待验证。
    -   **数据分布偏差**：锚点的构建和验证完全基于AdvBench衍生的同分布数据，其在分布外（OOD）的良性或恶意提示上的判别能力尚不清楚，存在被针对性攻击绕过的风险。
-   **应用局限性**：该方法是为基于令牌重要性排序的驱逐式压缩器（如FastKV）设计的，不适用于所有类型的KV缓存压缩技术。

（完）
