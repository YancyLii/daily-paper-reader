---
title: "ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection"
title_zh: ClawGuard：一种针对间接提示注入的工具增强型大语言模型智能体运行时安全框架
authors: "Wei Zhao, Zhe Li, Peixin Zhang, Jun Sun"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11790v1"
tags: ["query:agsec"]
score: 9.0
evidence: 针对工具增强型智能体在工具调用边界的运行时安全框架
tldr: 针对工具增强型LLM智能体易受间接提示注入攻击的问题，本文提出ClawGuard运行时安全框架。该框架通过在工具调用边界强制执行用户确认的规则集，将不可靠的对齐防御转变为确定性的审计机制。它能从用户目标中自动推导访问约束，有效拦截来自网页、MCP服务器和技能文件的恶意指令，在不修改模型或架构的前提下提供稳健保护。
selection_source: fresh_fetch
motivation: 工具返回的恶意内容（间接提示注入）会误导智能体执行非授权操作，现有的基于模型对齐的防御手段不够可靠。
method: 在工具调用前根据用户目标自动生成并强制执行确定性的访问控制规则，从而在恶意指令产生实际影响前将其拦截。
result: 在AgentDojo等多个基准测试和主流模型上的实验表明，ClawGuard在不损害智能体效能的情况下实现了对注入攻击的稳健防御。
conclusion: 确定性的工具调用边界强制执行是构建安全智能体AI系统的有效防御机制，且无需模型微调或架构更改。
---

## 摘要
工具增强型大语言模型（LLM）智能体在自动化复杂的、多步骤的现实任务方面展现出了卓越的能力，但仍容易受到间接提示注入的影响。攻击者利用这一弱点，在工具返回的内容中嵌入恶意指令，而智能体直接将这些内容作为可信的观测结果纳入其对话历史中。这种漏洞主要体现在三个攻击渠道：Web 和本地内容注入、MCP 服务器注入以及技能文件注入。为了应对这些漏洞，我们引入了 ClawGuard，这是一种新型的运行时安全框架，它在每个工具调用边界强制执行用户确认的规则集，将不可靠的、依赖对齐的防御转化为一种确定性的、可审计的机制，在产生任何现实影响之前拦截对抗性工具调用。通过在任何外部工具调用之前，从用户陈述的目标中自动推导出特定任务的访问约束，ClawGuard 在无需修改模型或更改基础设施的情况下，阻断了所有三种注入路径。在 AgentDojo、SkillInject 和 MCPSafeBench 上针对五种最先进语言模型的实验表明，ClawGuard 在不损害智能体效用的情况下，实现了针对间接提示注入的稳健防护。这项工作确立了确定性工具调用边界强制执行作为安全智能 AI 系统的一种有效防御机制，既不需要针对安全性的微调，也不需要架构修改。代码已在 https://github.com/Claw-Guard/ClawGuard 公开。

## Abstract
Tool-augmented Large Language Model (LLM) agents have demonstrated impressive capabilities in automating complex, multi-step real-world tasks, yet remain vulnerable to indirect prompt injection. Adversaries exploit this weakness by embedding malicious instructions within tool-returned content, which agents directly incorporate into their conversation history as trusted observations. This vulnerability manifests across three primary attack channels: web and local content injection, MCP server injection, and skill file injection. To address these vulnerabilities, we introduce \textsc{ClawGuard}, a novel runtime security framework that enforces a user-confirmed rule set at every tool-call boundary, transforming unreliable alignment-dependent defense into a deterministic, auditable mechanism that intercepts adversarial tool calls before any real-world effect is produced. By automatically deriving task-specific access constraints from the user's stated objective prior to any external tool invocation, \textsc{ClawGuard} blocks all three injection pathways without model modification or infrastructure change. Experiments across five state-of-the-art language models on AgentDojo, SkillInject, and MCPSafeBench demonstrate that \textsc{ClawGuard} achieves robust protection against indirect prompt injection without compromising agent utility. This work establishes deterministic tool-call boundary enforcement as an effective defense mechanism for secure agentic AI systems, requiring neither safety-specific fine-tuning nor architectural modification. Code is publicly available at https://github.com/Claw-Guard/ClawGuard.

---

## 论文详细总结（自动生成）

以下是对论文《ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：工具增强型大语言模型（LLM）智能体（如 AutoGPT、LangChain 框架下的智能体）通过调用外部工具（搜索、执行代码、访问数据库）来完成复杂任务。
*   **核心问题**：**间接提示注入（Indirect Prompt Injection, IPI）**。攻击者在工具返回的内容（如网页、文档、MCP 服务器响应）中嵌入恶意指令。由于智能体通常将工具输出视为可信观测并直接纳入对话历史，这些恶意指令会诱导智能体执行未经授权的操作（如数据外泄、删除文件、非法转账）。
*   **研究动机**：现有的防御手段存在局限性：模型级对齐（如 RLHF）仍可被绕过；协议级分离需要各厂商协作；架构级强制执行则往往牺牲了灵活性或需要专家手动编写规则。

### 2. 论文提出的方法论：ClawGuard 框架
ClawGuard 是一种**运行时安全框架**，其核心思想是在**工具调用边界**（Tool-call Boundary）建立确定性的防御层。

*   **核心组件**：
    1.  **内容净化器 (Content Sanitizer)**：利用模式库自动识别并脱敏工具参数和返回结果中的敏感信息（如 API 密钥、密码）。
    2.  **规则评估器 (Rule Evaluator)**：将工具调用与规则集 $R$ 进行比对。规则集由基础规则 $R_{base}$（系统级安全不变式）和任务特定规则 $R_{task}$ 组成。评估范围涵盖命令执行、本地文件路径和外部网络目标。
    3.  **技能检查员 (Skill Inspector)**：在首次执行新技能前，通过 LLM 辅助风险分析并强制要求用户确认。
    4.  **审批机制 (Approval Mechanism)**：对于评估结果为“模糊（ambiguous）”的调用，将其推送到审批队列由用户手动决策。
*   **关键技术：上下文感知规则归纳 (Context-Aware Rule Induction)**：
    *   在智能体调用任何外部工具之前，ClawGuard 会根据用户的初始指令，利用 LLM 自动推导出该任务所需的访问约束（白名单/黑名单）。
    *   这种方式确保了规则集是在环境尚未被对抗性内容污染时生成的，保证了规则的纯净性。

### 3. 实验设计
*   **数据集/基准测试**：
    1.  **AgentDojo**：包含 10 个环境和 16 个攻击场景，侧重于数据外泄和非法操作。
    2.  **SkillInject**：针对技能文件注入的攻击，包含 84 个攻击案例。
    3.  **MCPSafeBench**：针对模型上下文协议（MCP）服务器的真实攻击场景，涵盖金融、搜索等领域。
*   **对比对象**：五种最先进的开源/闭源模型作为智能体骨干：**DeepSeek-V3.2、GLM-5、Kimi-K2.5、MiniMax-M2.5、Qwen3.5-397B**。
*   **评估指标**：防御成功率 (DSR)、攻击成功率 (ASR)、任务完成率 (CR)、拒绝率 (RR) 和隐式抵抗率 (IRR)。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：ClawGuard 属于运行时中间件框架，主要依赖于推理过程中的规则匹配和少量的 LLM 辅助判断（如规则归纳和技能检查），不需要对骨干模型进行大规模微调，因此其算力需求主要体现在推理延迟而非训练资源上。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试上进行了广泛测试，每个模型在不同基准下处理了 84 到 215 个不等的任务实例。
*   **充分性**：实验覆盖了目前已知的三大主要注入渠道（Web/本地内容、MCP 服务器、技能文件），并使用了多种主流 LLM 骨干，证明了框架的通用性。
*   **客观性**：通过 RR/IRR 的分解，客观展示了 ClawGuard 如何将模型模糊的“隐式抵抗”转化为可审计的“显式拒绝”。

### 6. 论文的主要结论与发现
*   **防御有效性**：ClawGuard 显著降低了所有测试模型在所有基准测试中的 ASR。例如，在 AgentDojo 上实现了 0% 的 ASR（100% 防御成功）。
*   **确定性优势**：相比于依赖模型概率性的安全对齐，ClawGuard 提供的确定性边界强制执行更加稳健，能够拦截那些绕过模型安全检查的复杂攻击。
*   **可用性保持**：在大幅提升安全性的同时，智能体的任务完成率（CR）保持在竞争水平，证明了规则归纳的精准性。

### 7. 优点
*   **模型无关性**：无需微调模型，适用于闭源 API 部署。
*   **自动化程度高**：通过规则归纳减少了人工编写安全策略的负担。
*   **防御深度**：结合了内容脱敏、静态规则匹配和动态用户审批，形成了多层防御体系。
*   **可审计性**：所有拦截行为均记录在审计日志中，便于事后分析。

### 8. 不足与局限
*   **用户交互压力**：审批机制和规则确认需要用户参与，在复杂或高频任务中可能导致用户疲劳。
*   **语义攻击挑战**：对于不涉及工具调用、仅通过生成误导性文本内容（Content-misleading）的攻击，ClawGuard 的拦截效果有限。
*   **规则归纳依赖模型能力**：如果骨干模型本身指令遵循能力极弱，生成的 $R_{task}$ 可能会过严（导致任务失败）或过宽（导致安全漏洞）。
*   **实验配置限制**：论文中部分实验使用的是“基础规则（Basic Rules）”配置，尚未完全展示“上下文感知规则归纳”在所有基准测试中的全量潜力。

（完）
