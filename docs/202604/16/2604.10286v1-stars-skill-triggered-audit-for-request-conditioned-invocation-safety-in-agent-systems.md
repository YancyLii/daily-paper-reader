---
title: "STARS: Skill-Triggered Audit for Request-Conditioned Invocation Safety in Agent Systems"
title_zh: STARS：智能体系统中基于请求条件的调用安全的技能触发审计
authors: "Guijia Zhang, Shu Yang, Xilin Gong, Di Wang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10286v1"
tags: ["query:agsec"]
score: 9.0
evidence: 针对请求条件的调用安全进行技能触发审计
tldr: 针对自主智能体在调用外部技能时的安全风险，本文提出了STARS框架。传统的静态审计无法捕捉运行时上下文相关的风险，STARS通过结合静态能力先验、请求触发的调用风险模型及校准风险融合策略，实现了对技能调用的动态风险评估。在新建的SIA-Bench基准测试中，该方法在应对间接提示注入攻击等方面表现优于静态基准，为智能体系统提供了一层关键的运行时安全分诊机制。
selection_source: fresh_fetch
motivation: 传统的静态技能审计无法识别特定用户请求和运行时上下文中的动态安全风险。
method: 提出STARS框架，通过融合静态能力先验、请求触发的风险模型和校准融合策略来进行连续风险估计。
result: 在SIA-Bench基准测试中，该方法在处理间接提示注入攻击时的风险预测准确率显著优于静态基准。
conclusion: 请求触发的审计最适合作为调用时的风险评分和分诊层，而非静态筛选的替代品。
---

## 摘要
自主语言模型智能体越来越多地依赖可安装的技能和工具来完成用户任务。静态技能审计可以在部署前暴露能力表面，但无法确定在当前用户请求和运行时上下文下，特定的调用是否不安全。因此，我们将技能调用审计作为一个连续风险估计问题进行研究：给定用户请求、候选技能和运行时上下文，预测一个分值，以便在实施硬性干预之前支持排序和分流。我们提出了 STARS，它结合了静态能力先验、基于请求条件的调用风险模型以及校准后的风险融合策略。为了评估这一设置，我们构建了 SIA-Bench，这是一个包含 3,000 条调用记录的基准测试，具有分组安全划分、谱系元数据、运行时上下文、规范动作标签以及衍生的连续风险目标。在间接提示注入攻击的留出划分上，校准融合达到了 0.439 的高风险 AUPRC，优于上下文评分器的 0.405 和最强静态基线的 0.380，而上下文评分器在预期校准误差为 0.289 的情况下保持了更好的校准性。在锁定的分布内测试划分上，增益较小，且静态先验仍然有用。因此，得出的结论更为具体：基于请求条件的审计作为调用时的风险评分和分流层最有价值，而不是作为静态筛选的替代方案。代码可在 https://github.com/123zgj123/STARS 获取。

## 速览
**TLDR**：针对自主语言模型代理在调用外部技能时的安全风险，本文提出了STARS框架。该框架将技能调用审计视为持续风险评估问题，结合了静态能力先验、基于请求的调用风险模型和校准风险融合策略。通过在新建的SIA-Bench基准测试上评估，STARS在处理间接提示注入攻击等动态风险方面表现优异，证明了请求触发式审计作为调用时风险分流层的有效性。 \
**Motivation**：传统的静态技能审计无法识别特定用户请求和运行时上下文下的动态调用风险。 \
**Method**：提出STARS框架，结合静态能力先验、请求触发的风险模型及校准风险融合策略进行持续风险评估。 \
**Result**：在间接提示注入攻击的测试中，STARS的校准融合策略在AUPRC指标上显著优于静态基准和单一上下文模型。 \
**Conclusion**：基于请求的审计最适合作为调用时的风险评分与分流层，而非静态筛选的替代方案。

---

## Abstract
Autonomous language-model agents increasingly rely on installable skills and tools to complete user tasks. Static skill auditing can expose capability surface before deployment, but it cannot determine whether a particular invocation is unsafe under the current user request and runtime context. We therefore study skill invocation auditing as a continuous-risk estimation problem: given a user request, candidate skill, and runtime context, predict a score that supports ranking and triage before a hard intervention is applied. We introduce STARS, which combines a static capability prior, a request-conditioned invocation risk model, and a calibrated risk-fusion policy. To evaluate this setting, we construct SIA-Bench, a benchmark of 3,000 invocation records with group-safe splits, lineage metadata, runtime context, canonical action labels, and derived continuous-risk targets. On a held-out split of indirect prompt injection attacks, calibrated fusion reaches 0.439 high-risk AUPRC, improving over 0.405 for the contextual scorer and 0.380 for the strongest static baseline, while the contextual scorer remains better calibrated with 0.289 expected calibration error. On the locked in-distribution test split, gains are smaller and static priors remain useful. The resulting claim is therefore narrower: request-conditioned auditing is most valuable as an invocation-time risk-scoring and triage layer rather than as a replacement for static screening. Code is available at https://github.com/123zgj123/STARS.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STARS** 的框架，旨在解决大语言模型（LLM）智能体在调用外部工具（技能）时的动态安全审计问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着智能体（Agent）能力的增强，它们频繁调用外部工具（如执行代码、访问文件）。现有的安全防御多侧重于**静态审计**（部署前检查工具权限和元数据），但无法识别**运行时风险**。
*   **核心矛盾**：一个权限很高的工具（如文件读取）本身可能是合法的，但在被恶意指令（如间接提示注入 IPI）触发去读取敏感文件并外传时，该“调用”就是不安全的。
*   **核心问题**：论文区分了“能力风险”（工具能做什么）和“激活风险”（在当前上下文中该调用是否危险），并将技能调用审计建模为一个**连续风险估计问题**。

### 2. 方法论：STARS 框架
STARS 包含三个核心阶段，将静态先验与动态上下文相结合：
*   **阶段 A：静态能力先验 ($R_{static}$)**：在不考虑上下文的情况下，根据工具的权限、语义关键词和来源可靠性计算基础风险分。
*   **阶段 B：请求触发的调用风险模型 ($R_{trigger}$)**：这是核心动态层，通过五个维度的信号进行评分：
    1.  **意图分析**：请求文本中是否存在破坏性意图。
    2.  **参数敏感性**：调用参数是否涉及敏感对象或路径。
    3.  **来源风险**：先前工具输出内容的置信度。
    4.  **轨迹状态**：执行历史中的累积风险。
    5.  **污染传播**：追踪受污染的数据流是否进入高风险槽位（如代码执行）。
    *   **计算逻辑**：通过规则评分器将文本基础分与经过门控处理的上下文增益相结合。
*   **阶段 C：校准与风险融合**：使用验证集的分位数对静态和动态分数进行归一化，并通过凸组合（Convex Fusion）生成最终的连续风险分 $[0, 1]$。该分数可进一步映射为 `ALLOW`（允许）、`ESCALATE`（人工介入）或 `BLOCK`（拦截）。

### 3. 实验设计
*   **基准测试 (SIA-Bench)**：论文构建了一个专门针对调用审计的基准，包含 **3,000 条调用记录**。
    *   **数据组成**：涵盖良性请求、直接恶意请求、数据外泄、工具选择劫持、能力滥用及**间接提示注入 (IPI)**。
    *   **划分方式**：训练集 (1600)、验证集 (500)、锁定分布内测试集 (450) 和留出的 OOD 测试集 (450，专门存放 IPI 攻击)。
*   **对比方法**：
    *   **静态基线**：无审计、静态先验、静态黑名单、Agent-Audit（开源静态扫描器）。
    *   **动态变体**：仅文本审计、上下文审计（STARS 核心）、校准融合策略（STARS 全体）。
*   **评估指标**：高风险 AUPRC（曲线下面积）、Recall@10%（固定预算下的召回率）、Spearman 相关系数、ECE（预期校准误差）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于 STARS 的核心评分器（Stage B）在文中被描述为“基于规则的评分器（rule-based scorer）”，且主要涉及文本特征提取和逻辑判断，其计算开销可能远低于端到端训练的大模型，因此对重型算力的依赖较小。

### 5. 实验数量与充分性
*   **实验规模**：基于 3,000 条样本进行了多轮测试，包括对不同攻击家族的细分评估。
*   **消融实验**：对 Stage B 的五个上下文信号进行了消融研究，验证了各信号的互补性。
*   **敏感性分析**：针对风险目标值的构建比例（决策权重 vs 启发式权重）进行了敏感性测试。
*   **充分性评价**：实验设计较为全面，特别是通过“留出 IPI 攻击”作为 OOD 测试，客观地验证了模型在面对未知威胁时的泛化能力。

### 6. 主要结论与发现
*   **动态审计的必要性**：在处理间接提示注入（IPI）等复杂攻击时，校准融合模型（Fusion）的 AUPRC 达到 0.439，显著优于最强静态基线（0.380）。
*   **静态先验的价值**：在分布内（熟悉）的任务中，静态先验依然非常有效，动态审计的增益相对较小。
*   **分层防御理念**：基于请求条件的审计不应取代静态筛选，而应作为调用时的“分诊层”，决定一个具有风险能力的工具在当前时刻是否应该被允许执行。
*   **排序与校准的权衡**：增强动态触发信号能提高风险识别的排序质量，但可能会损害分数的校准性（ECE 升高）。

### 7. 优点（亮点）
*   **任务定义新颖**：将 Agent 安全从简单的“提示词过滤”提升到了“调用行为审计”，更符合实际部署场景。
*   **区分风险类型**：明确区分了能力风险与激活风险，为防御提供了更清晰的理论框架。
*   **基准质量高**：SIA-Bench 提供了丰富的运行时上下文和谱系元数据，填补了该领域调用级数据集的空白。

### 8. 不足与局限
*   **规则依赖**：Stage B 的评分逻辑在很大程度上依赖于预定义的规则和权重，可能在面对极度灵活的 LLM 攻击变体时存在滞后性。
*   **算力细节缺失**：虽然方法可能较轻量，但缺乏推理延迟和资源消耗的具体数据，难以评估其在实时系统中的性能开销。
*   **应用限制**：目前主要针对技能调用，对于 Agent 内部推理逻辑的安全性（如思维链攻击）覆盖较少。

（完）
