---
title: "CASCADE: A Cascaded Hybrid Defense Architecture for Prompt Injection Detection in MCP-Based Systems"
title_zh: CASCADE：一种用于基于 MCP 系统中提示词注入检测的级联混合防御架构
authors: "İpek Abasıkeleş Turgut, Edip Gümüş"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17125v1"
tags: ["query:agsec"]
score: 9.0
evidence: 智能体系统中针对提示注入和工具投毒的级联防御架构
tldr: 针对基于模型上下文协议（MCP）的LLM应用中存在的提示词注入和工具中毒等安全威胁，本文提出了CASCADE防御架构。该架构采用三层级联机制，结合正则过滤、语义分析及输出模式匹配，实现了完全本地化的防御。实验表明，CASCADE在保持极低误报率的同时，能有效检测数据泄露和提示词注入攻击，为MCP系统提供了高效且隐私安全的保护方案。
selection_source: fresh_fetch
motivation: 现有LLM防御方案存在高误报率、依赖外部API或需白盒访问等局限，且难以应对MCP协议带来的工具中毒等新攻击面。
method: 提出一种三层级联架构，依次通过正则与熵分析预过滤、BGE嵌入与Llama3语义分析、以及基于模式的输出过滤进行防御。
result: "系统实现了95.85%的精确率和6.06%的低误报率，在数据泄露和提示词注入检测方面表现尤为出色。"
conclusion: CASCADE证明了本地化级联架构能在不依赖外部API的情况下有效提升MCP系统的安全性，是兼顾隐私与防御性能的有效方案。
---

## 摘要
模型上下文协议 (Model Context Protocol, MCP) 是 LLM 应用中定义和调用外部工具的一种被快速采用的标准。MCP 的多层架构除了传统的提示词注入外，还引入了诸如工具投毒 (tool poisoning) 等新的攻击面。现有的防御系统存在误报率高、依赖 API 或需要白盒访问权限等局限性。在本研究中，我们提出了 CASCADE，一种针对基于 MCP 系统的三层级联防御架构：(i) 第一层使用正则表达式、短语权重和熵分析进行快速预过滤；(ii) 第二层通过 BGE 嵌入进行语义分析，并配备了 Ollama Llama3 回退机制；(iii) 第三层应用基于模式的输出过滤。在包含 5,000 个样本的数据集上的评估结果显示，准确率为 95.85%，误报率为 6.06%，召回率为 61.05%，F1 分数为 74.59%。对分为 6 个层级的 31 种攻击类型的分析表明，数据外泄 (91.5%) 和提示词注入 (84.2%) 的检测率较高，而语义攻击 (52.5%) 和工具投毒 (59.9%) 类别仍有改进空间。与现有解决方案相比，CASCADE 的一个关键优势是其完全本地化运行，无需外部 API 调用。

## Abstract
Model Context Protocol (MCP) is a rapidly adopted standard for defining and invoking external tools in LLM applications. The multi-layered architecture of MCP introduces new attack surfaces such as tool poisoning, in addition to traditional prompt injection. Existing defense systems suffer from limitations including high false positive rates, API dependency, or white-box access requirements. In this study, we propose CASCADE, a three-tiered cascaded defense architecture for MCP-based systems: (i) Layer 1 performs fast pre-filtering using regex, phrase weighting, and entropy analysis; (ii) Layer 2 conducts semantic analysis via BGE embedding with an Ollama Llama3 fallback mechanism; (iii) Layer 3 applies pattern-based output filtering. Evaluation on a dataset of 5,000 samples yielded 95.85% precision, 6.06% false positive rate, 61.05% recall, and 74.59% F1-score. Analysis across 31 attack types categorized into 6 tiers revealed high detection rates for data exfiltration (91.5%) and prompt injection (84.2%), while semantic attack (52.5%) and tool poisoning (59.9%) categories showed potential for improvement. A key advantage of CASCADE over existing solutions is its fully local operation, requiring no external API calls

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CASCADE** 的级联混合防御架构，专门用于保护基于模型上下文协议（Model Context Protocol, MCP）的大语言模型（LLM）系统免受提示词注入和工具投毒攻击。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着 MCP 协议的快速普及，LLM 能够更方便地调用外部工具。然而，这种多层架构引入了新的攻击面，如**工具投毒（Tool Poisoning）**和**间接注入**。
*   **核心问题**：现有的防御方案存在三大痛点：
    1.  **误报率（FPR）极高**：部分方案误报率高达 90% 以上，无法实际应用。
    2.  **隐私风险**：依赖外部 API（如 OpenAI）进行检测，导致敏感数据外泄。
    3.  **访问限制**：部分高性能方案需要白盒访问模型内部参数，不适用于闭源 API 模型。
*   **研究意义**：CASCADE 旨在提供一种完全本地化、低误报、且能应对 MCP 特有威胁的防御机制。

### 2. 方法论
CASCADE 采用了**三层级联（Cascaded）**的防御逻辑，核心思想是“先快后慢、先粗后精”：
*   **L1：预过滤层（Pre-Filter）**
    *   **技术细节**：执行 Unicode 规范化、同形文字转换和多种编码（Base64, ROT13 等）解码。
    *   **检测手段**：使用 100 多个正则表达式，涵盖直接注入、工具滥用、数据外泄等 7 个类别，并结合熵分析识别加密载荷。
*   **L2：语义评判层（Semantic Judge）**
    *   **核心思想**：采用“嵌入优先，LLM 回退”策略。
    *   **流程**：首先使用 **BGE 嵌入模型**将输入向量化，计算与已知攻击模式的相似度。若结果模糊，则调用本地运行的 **Llama3 (8B)** 模型进行深度语义推理。
    *   **输出**：提供三级决策（允许 ALLOW / 人工审查 REVIEW / 拦截 BLOCK）。
*   **L3：输出过滤层（Output Filter）**
    *   **功能**：监控 MCP 主机生成的响应，识别敏感信息（API 密钥、凭据）和异常熵值，防止数据通过工具调用被窃取。

### 3. 实验设计
*   **数据集**：构建了一个包含 **5,000 个样本**的综合数据集（1,521 个良性，3,479 个恶意）。数据源自 GitHub Adversarial、VulnerableMCP、OWASP API Security 等真实世界来源，而非纯合成数据。
*   **对比基准**：论文对比了现有的 MCP 防御系统（如 MCPShield, MCP Guardian, MINDGUARD 等），重点对比了误报率和本地化能力。
*   **评估维度**：针对 31 种攻击类型（分为 6 个层级）进行了详细的召回率分析。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Apple M3 Max 芯片（14 核）**和 **36 GB RAM** 的 MacBook Pro 上完成。
*   **软件环境**：使用 Python 3.14，本地 LLM 采用 **Ollama** 部署的 **Llama3-8B (Q4_0 量化版)**，占用约 4.7 GB 显存。
*   **训练时长**：文中未提及模型训练时长，因为主要使用的是预训练模型（BGE 和 Llama3）进行推理检测。

### 5. 实验数量与充分性
*   **实验规模**：对 5,000 个样本进行了全量测试，并根据 6 个攻击层级（Tier 1-6）进行了细分统计。
*   **充分性评价**：实验设计较为客观，涵盖了从精确率到误报率的多种指标。通过对 31 种细分攻击类型的分析，展示了系统在不同场景下的真实表现。然而，缺乏对不同规模 LLM（如 70B 模型）作为回退机制的消融对比。

### 6. 主要结论与发现
*   **高性能与低误报**：CASCADE 实现了 **95.85% 的精确率**和仅 **6.06% 的误报率**，远优于此前文献中 90% 以上的误报水平。
*   **分类表现**：对**数据外泄 (91.5%)** 和**提示词注入 (84.2%)** 的检测效果极佳；但对**语义攻击 (52.5%)** 和**工具投毒 (59.9%)** 的检测仍有提升空间。
*   **本地化可行性**：证明了使用轻量级嵌入模型配合本地 8B 参数模型，可以在不牺牲隐私的前提下提供有效的安全防护。

### 7. 优点
*   **隐私保护**：完全本地化运行，不依赖外部 API，适合处理敏感业务。
*   **实用性强**：三级输出机制（允许/审查/拦截）比传统的二元分类更符合实际运维需求。
*   **架构高效**：级联设计平衡了检测速度（L1 正则）和深度分析（L2 语义）。

### 8. 不足与局限
*   **语义检测瓶颈**：对于隐藏在自然语言中的复杂语义攻击，召回率（52.5%）偏低。
*   **语言局限性**：数据集以英语为主，未测试多语言环境下的防御表现。
*   **静态评估**：实验基于静态数据集，未在动态、多轮对话的实时 MCP 环境中验证其性能。
*   **边缘案例失效**：对于权限提升、授权绕过等 9 种特定攻击类型的检测率为 0%，说明架构对逻辑类漏洞防御不足。

（完）
