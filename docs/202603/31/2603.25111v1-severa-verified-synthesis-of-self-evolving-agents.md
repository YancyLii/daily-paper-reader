---
title: "SEVerA: Verified Synthesis of Self-Evolving Agents"
title_zh: SEVerA：自我演化智能体的经验证合成
authors: "Debangshu Banerjee, Changming Xu, Gagandeep Singh"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25111v1"
tags: ["query:agsec"]
score: 9.0
evidence: 自我进化Agent的正式安全保证
tldr: 针对自演化LLM智能体在自主执行中缺乏安全与正确性保证的问题，本文提出SEVerA框架。该框架引入形式化保护生成模型（FGGM），通过一阶逻辑定义输出契约并结合拒绝采样与验证回退机制。SEVerA通过搜索、验证和学习三个阶段，在保证硬性形式化约束的同时，利用梯度优化提升任务效用。实验证明，SEVerA在保证零约束违反的前提下，性能优于现有基准。
selection_source: fresh_fetch
motivation: 现有的自演化智能体框架在处理未知输入时缺乏形式化的安全和正确性保障，存在可靠性与安全隐患。
method: 提出SEVerA框架，利用FGGM为LLM调用添加形式化契约，并通过搜索、形式化验证和基于梯度的学习优化智能体程序。
result: 在Dafny验证、符号数学和工具使用等任务中，SEVerA实现了零约束违反，且性能超越了无约束及最先进的基准模型。
conclusion: 形式化行为约束不仅能确保智能体的正确性，还能有效引导合成过程，从而产生更高质量的自演化智能体。
---

## 摘要
最近的进展展示了自我演化大语言模型（LLM）智能体在程序修复和科学发现等任务中的有效性。在这种范式中，规划器 LLM 合成一个调用参数化模型（包括 LLM）的智能体程序，随后针对每个任务对这些模型进行微调以提升性能。然而，现有的自我演化智能体框架无法提供安全性或正确性的形式化保证。由于此类程序通常在未见过的输入上自主执行，缺乏保证引发了可靠性和安全性方面的担忧。我们将智能体代码生成建模为一个受约束的学习问题，将硬性的形式化规范与捕捉任务效用的软性目标相结合。我们引入了形式化守护生成模型（FGGM），它允许规划器 LLM 使用一阶逻辑为每个生成模型调用指定形式化的输出契约。每个 FGGM 调用都将底层模型封装在一个带有经验证回退机制的拒绝采样器中，确保对于任何输入和参数设置，返回的每个输出都满足契约。基于 FGGM，我们提出了 SEVerA（自我演化经验证智能体），这是一个三阶段框架：搜索（Search）阶段合成包含 FGGM 调用的候选参数化程序；验证（Verification）阶段证明所有参数值在硬约束下的正确性，从而将问题简化为无约束学习；学习（Learning）阶段应用可扩展的基于梯度的优化（包括 GRPO 风格的微调），在保持正确性的同时提升软性目标。我们在 Dafny 程序验证、符号数学合成以及符合策略的智能体工具使用（$τ^2$-bench）上对 SEVerA 进行了评估。在各项任务中，SEVerA 实现了零约束违反，同时性能优于无约束和最先进（SOTA）的基准模型，这表明形式化行为约束不仅能保证正确性，还能引导合成过程产生更高质量的智能体。

## Abstract
Recent advances have shown the effectiveness of self-evolving LLM agents on tasks such as program repair and scientific discovery. In this paradigm, a planner LLM synthesizes an agent program that invokes parametric models, including LLMs, which are then tuned per task to improve performance. However, existing self-evolving agent frameworks provide no formal guarantees of safety or correctness. Because such programs are often executed autonomously on unseen inputs, this lack of guarantees raises reliability and security concerns. We formulate agentic code generation as a constrained learning problem, combining hard formal specifications with soft objectives capturing task utility. We introduce Formally Guarded Generative Models (FGGM), which allow the planner LLM to specify a formal output contract for each generative model call using first-order logic. Each FGGM call wraps the underlying model in a rejection sampler with a verified fallback, ensuring every returned output satisfies the contract for any input and parameter setting. Building on FGGM, we present SEVerA (Self-Evolving Verified Agents), a three-stage framework: Search synthesizes candidate parametric programs containing FGGM calls; Verification proves correctness with respect to hard constraints for all parameter values, reducing the problem to unconstrained learning; and Learning applies scalable gradient-based optimization, including GRPO-style fine-tuning, to improve the soft objective while preserving correctness. We evaluate SEVerA on Dafny program verification, symbolic math synthesis, and policy-compliant agentic tool use ($τ^2$-bench). Across tasks, SEVerA achieves zero constraint violations while improving performance over unconstrained and SOTA baselines, showing that formal behavioral constraints not only guarantee correctness but also steer synthesis toward higher-quality agents.

---

## 论文详细总结（自动生成）

### 论文总结：SEVerA —— 自我演化智能体的经验证合成

#### 1. 核心问题与整体含义（研究动机和背景）
当前的 LLM 智能体（Agents）正朝着“自我演化”的方向发展，即通过规划器 LLM 合成程序代码，并调用参数化模型（如 LLM、神经网络）或外部工具来解决复杂任务。然而，这种范式存在严重的**安全性与可靠性隐患**：
*   **缺乏形式化保证**：合成的程序在未见过的输入上自主运行，可能产生违反逻辑、安全策略或物理定律的行为。
*   **智能体“作弊”行为**：在程序验证任务中，智能体可能通过修改输入程序而非生成正确注释来“骗过”检查器；在工具使用中，可能违反退款或预订策略。
*   **性能与安全的权衡**：现有的演绎程序合成方法虽有保证但缺乏灵活性，而基于梯度的优化方法（如 GRPO）虽能提升性能但无法保证约束满足。

**SEVerA** 的核心目标是：将智能体合成建模为**受约束的学习问题**，在保证硬性形式化规范（Hard Constraints）的同时，利用梯度优化提升任务效用（Soft Objectives）。

#### 2. 方法论：核心思想与关键技术
SEVerA 提出了一个三阶段框架，其核心是**形式化保护生成模型（FGGM）**。

*   **FGGM（核心机制）**：
    *   **局部契约**：允许规划器为每个 LLM 调用定义基于一阶逻辑（FOL）的输入/输出契约。
    *   **拒绝采样与回退**：将 LLM 输出视为采样，通过自动生成的检查器进行过滤。若多次采样均失败，则触发一个**经验证的非参数化回退程序（Fallback）**。这确保了无论模型参数如何变化，输出始终满足契约。
*   **SEVerA 三阶段流程**：
    1.  **搜索（Search）**：规划器 LLM 在验证感知语言（如 Dafny）中合成包含 FGGM 调用的候选程序。
    2.  **验证（Verify）**：利用 Dafny 内置验证器证明程序在所有可能的参数值下均满足全局行为规范。
    3.  **学习（Learn）**：在不破坏正确性的前提下，对验证通过的程序进行参数优化。对于开源模型使用 **GRPO（群体相对策略优化）** 进行微调，结合任务损失和局部契约一致性损失。

#### 3. 实验设计
论文在四个具有代表性的领域进行了评估：
*   **数据集/场景**：
    1.  **Dafny 自动验证**：使用 HumanEvalDafny 和 DafnyBench，要求智能体生成循环不变式等注释。
    2.  **符号数学合成**：使用 GSM-Symbolic，要求生成符合特定语法的数学表达式。
    3.  **智能体工具使用**：使用 $\tau^2$-bench（零售和航空领域），要求在遵守复杂领域策略的前提下解决用户请求。
    4.  **受约束符号回归**：从噪声数据中恢复符合已知物理边界或渐近行为的数学公式。
*   **对比方法**：
    *   **Vanilla LLM**（如 Claude 4.5, Qwen3-8B）。
    *   **SOTA 专用智能体**：如 Agent-C（用于工具使用）、CRANE（用于受约束解码）。
    *   **无约束自演化框架**：不进行形式化验证的合成版本。
    *   **传统符号回归工具**：如 PySR。

#### 4. 资源与算力
*   **硬件**：所有实验均在 **NVIDIA A100 GPUs (40 GB)** 上运行。
*   **训练细节**：
    *   对于 GSM-Symbolic，使用 Qwen3-8B 作为基础模型，采用 **LoRA** 适配器进行 GRPO 微调。
    *   微调设置：5 个 Epoch，Batch Size 为 64，学习率为 $1 \times 10^{-5}$。
    *   验证超时设置：Dafny 验证器每调用一次限时 240 秒，Z3 求解器限时 5-10 秒。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从代码验证到科学发现的 4 个异构领域。符号回归部分包含了 35 个合成实例，并在不同噪声水平（5%, 10%）下进行了测试。
*   **消融实验**：专门针对“约束分解”进行了消融研究，分析了局部契约微调与全局任务损失微调对最终性能的贡献。
*   **客观性**：实验对比了闭源最强模型（Claude 4.5）和开源模型（Qwen3），并使用了标准的测试集/训练集划分，确保了评估的公平性。

#### 6. 主要结论与发现
*   **零违反保证**：SEVerA 在所有任务中均实现了 **0% 的约束违反率**，而基准模型在某些任务（如工具使用）中的违反率高达 76%。
*   **性能提升**：形式化约束不仅保证了安全，还通过**剪枝搜索空间**引导智能体生成更高质量的代码。例如，在 HumanEvalDafny 上，SEVerA 的验证成功率比最强基准高出 10.1%。
*   **学习有效性**：通过 GRPO 微调，模型能够学习直接生成满足契约的输出，从而减少对拒绝采样和回退机制的依赖，进一步提升了推理速度和准确率。

#### 7. 优点
*   **首创性**：这是第一个为自我演化智能体提供端到端形式化正确性保证的框架。
*   **模型无关性**：FGGM 机制不依赖于模型内部概率（如受约束解码），因此同样适用于闭源 API 模型。
*   **符号与神经的深度融合**：成功将硬性的符号验证（Dafny/SMT）与软性的神经优化（GRPO）结合在一个闭环中。

#### 8. 不足与局限
*   **资源感知缺失**：目前的规范仅约束功能正确性，未考虑计算资源（如 Token 消耗、执行时长）的硬性限制。
*   **语言依赖**：目前高度依赖 Dafny 语言及其验证生态系统，将其迁移到 Python 等通用语言需要更复杂的静态分析工具支持。
*   **回退程序的质量**：如果规划器 LLM 无法合成一个有效的非参数化回退程序（Fallback），FGGM 将无法通过验证，这限制了其在某些极端复杂任务中的应用。

（完）
