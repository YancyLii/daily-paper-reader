---
title: Systematic API Testing Through Model Checking and Executable Contracts
title_zh: 通过模型检测与可执行契约实现的系统化 API 测试
authors: "Ana Ribeiro, Margarida Mamede, Carla Ferreira"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08633v1"
tags: ["query:libfuz"]
score: 9.0
evidence: 自动生成有效的有状态API调用序列
tldr: 本研究针对黑盒API测试中缺乏行为语义导致的状态序列生成难和预言机失效问题，提出了IcePick框架。该框架利用TLA+对API状态演化建模，通过TLC模型检查器穷举可达状态，并结合覆盖引导的广度优先搜索生成测试序列。此外，引入Glacier语言定义可执行语义契约，增强了行为验证能力。实验表明，IcePick能实现完全的状态覆盖并发现复杂交互中的故障。
selection_source: fresh_fetch
motivation: 现有的黑盒API测试缺乏行为语义，难以生成有效的有状态调用序列并进行深层逻辑验证。
method: 结合TLA+建模与TLC模型检查技术，通过覆盖引导的图遍历生成测试序列，并利用Glacier契约语言实现自动化行为验证。
result: 在EvoMaster基准测试中，IcePick实现了完全的状态空间覆盖，并成功检测出多操作交互中的逻辑故障。
conclusion: IcePick为关键API系统提供了一种具有强覆盖保证、可复现且可扩展的系统化测试方案。
---

## 摘要
API 的自动化黑盒测试通常依赖于定义了可用操作和数据模式的接口规范，但这些规范提供的行为语义非常有限甚至完全缺失。这种语义鸿沟放大了测试断言（test-oracle）问题，并限制了有效的有状态调用序列的生成。我们介绍了 IcePick，这是一个利用模型检测实现 API 测试系统化状态空间覆盖的框架。IcePick 使用 TLA+ 对 API 状态演化进行形式化建模，利用 TLC 模型检测器穷举探索可达状态，并生成可证明覆盖行为模型的测试序列。为了缓解状态空间爆炸并改进序列提取，我们引入了一种针对 TLC 状态空间图的覆盖引导广度优先遍历方法。为了解决 HTTP 状态码之外的断言局限性，我们提出了 Glacier，这是一种一阶逻辑契约语言，它通过可执行语义契约丰富了 API 规范，从而在测试执行期间实现自动化的行为验证。我们在 EvoMaster 基准系统上对 IcePick 进行了评估，结果表明，模型检测引导的探索实现了完全的状态覆盖，并揭示了多操作交互中的故障。我们还分析了可扩展性，以刻画其实际限制和适用性要求。总的来说，IcePick 为关键的基于 API 的系统提供了具有强覆盖保证的可重现测试套件。

## Abstract
Automated black-box testing of APIs typically relies on interface specifications that define available operations and data schemas, but offer limited or no behavioural semantics. This semantic gap amplifies the test-oracle problem and limits the generation of effective, stateful call sequences.   We introduce IcePick, a framework that achieves systematic state-space coverage for API testing by leveraging model checking. IcePick uses TLA+ to formally model API state evolution, employs the TLC model checker to exhaustively explore reachable states, and generates test sequences that provably cover the behavioural model. To mitigate state-space explosion and improve sequence extraction, we introduce a coverage-guided breadth-first traversal of the TLC state-space graph. To address oracle limitations beyond HTTP status codes, we propose Glacier, a first-order logic contract language that enriches API specifications with executable semantic contracts, enabling automated behavioural verification during test execution.   We evaluate IcePick on EvoMaster Benchmark systems, demonstrating that model-checking-guided exploration achieves complete state coverage and reveals faults in multi-operation interactions. We also analyse scalability to characterise practical limits and applicability requirements. Overall, IcePick provides reproducible test suites with strong coverage guarantees for critical API-based systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **IcePick** 的框架，旨在通过形式化方法解决 RESTful API 自动化测试中的状态序列生成和测试断言（Oracle）难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 API 黑盒测试主要依赖 OpenAPI 规范（OAS），但 OAS 仅定义了接口结构（如 URL、参数类型），缺乏**行为语义**。
*   **研究动机**：
    *   **测试断言难题**：现有工具多依赖 HTTP 状态码（如 5xx）作为错误判断标准，无法捕捉深层的逻辑错误。
    *   **状态探索不足**：难以生成能够触发复杂业务逻辑的、有状态的操作序列（如“先创建、再更新、最后删除”的特定组合）。
    *   **语义鸿沟**：API 规范与实际系统行为之间存在断层，导致测试覆盖率受限。

### 2. 方法论
IcePick 框架结合了模型检测（Model Checking）和可执行契约技术，分为两个阶段：
*   **阶段 1：规范预处理与模型生成**
    *   **Glacier 契约语言**：提出了一种基于一阶逻辑的领域特定语言，用于编写 API 的前置条件、后置条件和全局不变性（Invariants）。
    *   **自动化契约推断**：提供工具根据 CRUD 语义从 OAS 自动生成基础契约，并允许人工扩展复杂逻辑。
    *   **TLA+ 建模**：将 API 行为抽象为 TLA+ 形式化模型，利用 **TLC 模型检查器** 穷举生成状态空间图（SSG）。
    *   **覆盖引导的 BFS 算法**：开发了一种基于广度优先搜索的算法，从 SSG 中提取最短的调用序列，确保实现 100% 的状态覆盖和转换覆盖。
*   **阶段 2：系统化测试执行**
    *   **状态模拟器**：在测试执行期间维护一个抽象状态，跟踪资源的存在性。
    *   **动态断言验证**：在每个 API 调用前后，实时执行 Glacier 契约检查，将结果分类为 OK、WARN（警告）、ERR（错误）或 NOT_TESTED。

### 3. 实验设计
*   **实验对象**：
    *   **Tournaments App**：一个自定义的复杂锦标赛管理系统。
    *   **EvoMaster Benchmark (EMB)**：包含 Swagger-Petstore、E-Commerce API 和 Features-Service 等标准基准系统。
*   **对比与评估维度**：
    *   **可扩展性**：分析 TLC 生成状态空间图的时间与模型规模的关系。
    *   **故障检测能力**：通过在 Tournaments 系统中注入 3 种典型逻辑故障（如引用完整性违规）来验证 IcePick 的有效性。
    *   **契约效果**：对比自动推断契约与人工扩展契约在发现故障上的差异。

### 4. 资源与算力
*   **硬件环境**：使用了 4 台相同的 Debian 服务器，每台配置为：
    *   **CPU**：2 颗 AMD EPYC 7343 处理器（共 32 核心，64 线程）。
    *   **内存**：128 GiB DDR4 RAM。
    *   **存储**：450 GB SSD。
*   **网络**：2 × 10 Gbps 网络连接。
*   **运行时间**：实验记录了从几秒到 16 小时不等的模型检测时间，具体取决于状态空间的复杂度。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个系统进行了 17 种不同规模的模型配置（从 3 个模型值到 12 个模型值）。
*   **充分性评价**：
    *   **客观性**：使用了学术界公认的 EvoMaster 基准测试集，具有较好的代表性。
    *   **覆盖面**：实验不仅验证了故障检测，还深入探讨了模型检测在 API 测试中的性能瓶颈（状态爆炸问题）。
    *   **局限性**：由于高质量开源 API 规范较少，实验仅深入分析了 3-4 个系统，样本量尚有提升空间。

### 6. 主要结论与发现
*   **状态覆盖**：IcePick 能够实现 100% 的模型状态覆盖，生成的测试序列比随机探索更具针对性。
*   **故障发现**：成功检测出所有注入的逻辑故障，特别是那些仅通过 HTTP 状态码无法识别的“静默”错误（如删除操作未更新关联集合）。
*   **REST 合规性依赖**：发现 IcePick 的有效性高度依赖于 API 对 REST 原则的遵守程度。对于不规范的 API（如 Features-Service），框架会因无法推断语义而受阻。
*   **性能界限**：对于中等规模模型（约 5-21 GB 的状态图），生成时间在一小时内；超过此规模会面临严重的内存和时间挑战。

### 7. 优点
*   **形式化保证**：利用 TLA+ 提供了数学上的覆盖保证，测试过程可重现。
*   **强大的断言机制**：Glacier 语言解决了黑盒测试中“预言机”缺失的问题，能够验证复杂的业务一致性。
*   **自动化程度高**：支持从 OAS 自动推断基础契约，降低了形式化方法的使用门槛。

### 8. 不足与局限
*   **人工建模成本**：虽然契约可推断，但编写高质量的 TLA+ 抽象模型仍需要形式化方法专家参与。
*   **状态爆炸风险**：随着 API 资源和操作数量增加，模型检测的计算开销呈指数级增长。
*   **对非规范 API 脆弱**：如果 API 不遵循标准的 HTTP 语义（如所有请求都返回 200 OK），IcePick 的推断机制将失效。
*   **实验范围**：目前仅在少数几个系统上验证，缺乏在大规模工业级微服务集群中的应用数据。

（完）
